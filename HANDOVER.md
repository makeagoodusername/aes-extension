# AES — Handover

This is the live current-state doc for the **AirlineSim Enhancement Suite (AES)** Chrome extension. A fresh session should be able to read this top-to-bottom and pick up cleanly. For long-form reference (algorithms, formulas, derivations), see `MANUAL.md`.

- **Project:** AirlineSim Enhancement Suite (AES) — Chrome MV3 extension, vanilla JS + jQuery (no build step).
- **What it is:** quality-of-life toolkit for AirlineSim, the browser airline-management game. Reads game pages via content scripts, scrapes data into `chrome.storage.local`, surfaces panels with scoring/filtering/automation. Read-mostly today; no writes back to AS.
- **Repo root:** `/Users/jihwan/Downloads/AES.v0.6.9-beta/`
- **Manifest version:** 0.6.9-beta (`manifest.json` → `version_name`).
- **Upstream author:** Zoe Bijl (https://github.com/ZoeBijl/airlinesim-enhancement-suite/). User is maintaining/extending a fork.
- **Git user:** `makeagoodusername`.

---

## 1 · Current state

### Auto-Pricing Tier 3.2 — live single-route writes + circuit breaker — shipped this session
**Surfaces in:** Route Assistant panel · Auto-Pricing settings expander + per-route apply modal (`/app/com/scheduling*`). **Files:** `modules/route-assistant/settings-store.js`, `modules/route-assistant/pricing-applier.js`, `modules/route-assistant/panel.js`. Tech-debt sweep folded in: `modules/route-assistant/schedule-page-scraper.js` (renamed from `ticket-price-scraper.js`), `modules/used-aircraft-scanner/deal-metrics.js`, `modules/used-aircraft-scanner/type-family-map.js`, `content_dashboard.js`, `manifest.json`.

Flips Auto-Pricing from rehearsal-only (3.1) to producing real POSTs to AS's markets-page form, with a circuit breaker mirroring `ors-scraper.js`'s 429/503 pattern, a one-click Undo toast on success, and the per-route + global cooldown enforced from the apply log.

**Default flip + dry-run toggle.** `settings.routeAssistant.pricing.apply.dryRunOnly` default flips `true → false`. Existing users keep their persisted value via the deep-merge in `_mergePricing`. The Auto-Pricing Tier 3 expander gains a new "Dry-run only" checkbox above the "Apply enabled" kill switch — flipping it re-arms the dry-run gate without touching the kill switch. Both gates remain independent (semantic split: kill switch vs slice gate); a real POST still requires `apply.enabled === true && apply.dryRunOnly === false`. `_getPricingApplier` threads the dry-run state into the applier's instance flag, which overrides any `{dryRun: false}` opt the modal might pass — same belt-and-braces convention 3.1 shipped.

**Circuit breaker.** New settings under `pricing.apply`: `circuitBreakerThreshold: 3`, `circuitBreakerCooldownMs: 600000` (10 min), `circuitBreakerTrippedAt: null`, `circuitBreakerHaltReason: null`. Applier gains `_handleRateLimit(envelope, where, status)` (called from both the GET and POST paths when AS responds 429 or 503) which increments `_consecutiveErrors`, fires `onBreakerTrip(reason, trippedAt)` when the threshold is hit, and writes a `code: "rateLimit"` failure entry to the apply log so the audit trail captures every rejection. Counterpart `_resetBreakerCounter()` runs from `_completeAsVerified` and `_completeAsPostedUnverified` and fires `onBreakerReset` — first successful write clears trippedAt. New Step-0 cooldown gate at the head of `apply()` short-circuits to an aborted record (`code: "breakerCooldown"`) when `Date.now() − trippedAt < cooldownMs`; dry-run is exempt since it's a pure GET. Panel persists the trip state via `_persistPricingBreakerTrip` / `_persistPricingBreakerReset` (RouteAssistantSettings.save round-trips), so every panel instance + the next page load honour the cooldown until it expires or the user clicks Reset.

**Breaker banner.** When `circuitBreakerTrippedAt` is set and the cooldown hasn't elapsed, the Tier 3 expander renders a red-tinted banner above the scope chooser with "Circuit breaker tripped" + the halt reason + "~N min remaining" + a Reset breaker button gated on a confirm() prompt. Reset clears trippedAt + haltReason, persists, and re-renders. The banner takes precedence over the existing rationale paragraph so a user opening the expander on a cooled extension immediately sees why Apply is short-circuiting.

**Undo toast.** `renderResult` in the per-route apply modal now branches on `(result.status === "verified" || "posted") && Object.keys(result.prevPrices).length > 0` to fire a 6-second `RouteAssistantToast.success` carrying an Undo action. Click → re-runs `applier.apply(hub, dest, result.prevPrices, {...})` with `source: "undo"`, verifies, toasts the revert outcome. Re-uses the existing applier instance from the original click (closured into the renderer) so the breaker counter + cooldown context survive the round-trip. The previously-shipped `_undoableSave` primitive isn't reused here because pricing applies aren't a single-write semantic — `prevPrices` already round-trips through the `result` envelope and the applier is the canonical revert path.

**Cooldown threading.** `_openPricingApplyModal`'s Apply path now awaits `fetchLastApplyAt() → log.getLastSuccessAt(hub, dest)` before calling `collectArgs(false, lastApplyAt)`; the applier's `preflight({lastApplyAt, cooldownMinPerRoute})` already had the `cooldownActive` blocker logic in 3.1 — slice 3.2 just wires the threading. Dry-run path passes `null` (cooldown only gates real writes).

**Modal + rationale copy de-tiered.** Removed every "Tier 3.1" / "Tier 3.2" string from the panel — the user-facing copy now reads "Dry-run only" / "LIVE writes ENABLED" / "Live writes disabled" with action-oriented wording ("Turn off the dry-run gate plus turn on Apply enabled below to commit real writes"). The Apply button's disabled-state title splits into two cases: dry-run-on vs apply-enabled-off, each with the specific gate the user needs to flip.

**Rate-limit toasts.** `renderResult` adds two new toast branches before the existing failed/aborted/dry-run/success cases: `error.code === "rateLimit"` (warns "Pricing apply halted: HTTP 429 ×N in a row · cooling M min" when `breakerTripped`, errors "rateLimit (HTTP 429, N× in a row)" otherwise), `error.code === "breakerCooldown"` (warns "Pricing apply skipped — circuit breaker cooldown (N min remaining)").

**Tech-debt sweep folded in.** Five long-standing items from §9's Tech-debt list closed in the same diff because they touched the same files as Tier 3.2 (single open + edit, no extra context cost):
- **`ticket-price-scraper.js → schedule-page-scraper.js`** — file rename + class rename (`RouteAssistantTicketPriceScraper → RouteAssistantSchedulePageScraper`) + manifest update + every panel.js call site. Storage prefix `routeAssistant:ticketPrice:` left as-is per HANDOVER §10's stable-key invariant — the rename is class/file only.
- **Family-card grid override-walk** — `TypeFamilyMap.familyList(overrides)` now folds `block.typeFamilyOverrides` into the matching family card so user-redirected types appear in their actual family rather than the grid's "Custom" card. Truly unknown labels still land in Custom.
- **`routeAssistant:topRoutes` freshness on dashboard** — `displayUsedAircraftScanner()` installs a singleton `chrome.storage.onChanged` listener (250ms debounced — RA writes both global + per-hub keys per render) that re-pulls `loadDealContext(server)` and re-renders results. Previous listener removed on each remount via `window._aesTopRoutesListener`.
- **Break-even daily-hours assumption** — `MarketScanDealMetrics._blockHoursFor(row)` picks from `BLOCK_HOURS_BY_CATEGORY` (commuter/turboprop/regional 8h, narrowbody 12h, widebody 14h) keyed off `row.familyCategory`. Falls back to the 10h `DAILY_BLOCK_HOURS` constant when category is missing.
- All four are crossed out in §9's Tech-debt section.

**Verification.** Manual smoke test (no automated harness): reload extension; open the Auto-Pricing expander; confirm the dry-run-only checkbox + Apply-enabled toggle render and persist; right-click a route row → "Apply price…"; in dry-run mode confirm the modal runs the full preflight + body preview + audit log entry without a Network POST. With both gates flipped, the Apply button POSTs the markets-page form, parses verifyOk, fires the Undo toast, and the apply log records `status: "verified"`. Force a 429 by repeated rapid clicks (3 in a row) — breaker trips, banner appears, subsequent Apply clicks short-circuit to `breakerCooldown`, Reset clears the trip. The schedule-page-scraper bulk-sync + Eq/Dep/Wk columns continue to populate (rename had no semantic change). Untouched: existing Tier 3.1 dry-run path, ORS Sandbox bridge to apply modal, sandbox-backtest-store hook (still gates on `sandboxProjected !== null && status ∈ {verified, posted}` per the cohesion-pass slice).

### Track 4 — Slot Optimizer (auto-scheduler preset tweaks) — slices 4a, 4b, 4c, 4d, 4e shipped this session
**Surfaces in:** per-aircraft Flight Plan page (`/app/fleets/aircraft/<id>/0`) — pure module, no UI; the only user-visible footprint is a 🔧 glyph in the existing preset picker dropdown for tweaked variants. **Files:** `modules/aircraft-flight-plan/auto-scheduler/slot-optimizer.js` (NEW), `modules/schedule-management/presets-store.js` (extended), `modules/aircraft-flight-plan/wave-applier.js` (4-line surgical patch), `manifest.json`, `CLAUDE/handover-fragments/AUTO-4a.md` … `AUTO-4e.md` (NEW × 5).

Track 4 is the slot optimizer: take a wave preset whose windows may not match this hub's demand peaks, produce a tweaked preset where wave start times have been shifted to fit the hub's demand profile, and feed the tweaked preset into Track 3's allocator. Slices 4a-4e land sequentially.

**Slice 4a — demand profile per hour.** New `SlotOptimizer.demandProfile({candidates, hubIata})` returns a length-24 histogram `[{hour, demandWeight}, ...]` indexed 0..23 by local hub hour. Each candidate contributes `(paxScore + 0.5 × cargoScore) × weeklyFlights` (weeklyFlights defaults to 1 when missing); the weight is distributed by per-route `frequency` / `hourMask` arrays when present (defensive — checked on the candidate, on `cand._raw`, and on `cand._scoredRow`), with a flat fallback `weight / 24` otherwise. Today's `FlightsFromStore` scrape (`content_flightsFrom.js:560-583`) doesn't expose hour-level frequencies so production runs always go through the flat path; the bucket-aware branch is wired so a future scraper enrichment activates per-hour distribution without touching this module. `?aes-debug` console smoke tests (5 assertions) verify empty input, single-candidate flat formula, default `weeklyFlights`, per-bucket distribution, and bad-entry resilience. Manifest registration sits AFTER the Track 3 auto-scheduler files (`grid-state.js` / `objective.js` / `allocator.js`) since slice 4d depends on them, BEFORE `wave-applier.js`. Tier-gate posture: this slice is pure helper + `?aes-debug` smoke; the production `optimize()` entrypoint is null until 4e ships and will return `{skipped: true, reason: "autoScheduler disabled"}` while `settings.aircraftFlightPlan.autoScheduler.enabled !== true`.

**Slice 4b — slot proposal heuristic.** New `SlotOptimizer.proposeAdjustments({preset, demandProfile})` returns up to 16 `{waves, source, deltaDescription}` proposals via three heuristics: **shift** slides each wave's departure window by ±30/60/90/120 min toward the nearest demand peak (peak chosen by `weight − 0.5×distance` so we prefer heavy nearby peaks over distant heavier ones); **split** halves any wave whose ≥1h-wide window contains a demand valley (defined as an interior hour <0.5× the edge hours' average) into two narrower waves with halved composition; **merge** combines two adjacent thin (<60min) waves whose centres both sit within ±2h of the same hour-bucket peak. `MAX_SHIFT_MIN` capped at ±120min per the plan's "don't blow up the user's intent" guardrail. Helpers `_waveCentreMin`, `_nearestPeakHour`, `_shiftWaveBy`, `_splitWaveAcrossValley`, `_mergeWavesIfStraddlingPeak`, `_halveComposition`, `_addCompositions` are all private — only `proposeAdjustments` is exposed. Verified outside the browser via a Node stub against synthetic demand profiles: a mistuned 14:00 wave + a peak at 17:00 yields 4 shift proposals targeting 17:00; a wave already on the peak yields 0 shifts; a 4-hour wave straddling a clear valley yields 1 split; two adjacent thin waves both within ±2h of a shared peak yield 1 merge. `?aes-debug` smoke tests cover shape, cap, source enum, peak-targeting, empty-preset bail, and the on-peak no-shift case.

**Slice 4c — turnaround-feasibility filter.** New `SlotOptimizer.filterFeasible(proposals, {basePreset, candidates, selectedSpec})` composes each proposal's waves into a synthetic preset (deep-cloning `basePreset.factors` so per-proposal mutations don't leak), runs `ScheduleBuilder.validatePreset()` to hard-drop structural breaks (`presetInvalid` from things like sub-`minTransferMinutes` connection gaps), then runs `ScheduleBuilder.build(routes)` against the OOR-pre-filtered candidate set to surface per-leg `rangeExceeded`/`shortfall`/`routeUnplaced`/`slotViolation` warnings. Each survivor accrues a weighted feasibility score (presetInvalid=10, rangeExceeded=4, shortfall=2, others=1) attached to a fresh `_meta` block. The worst quartile by score is then culled (`Math.ceil(N × 0.75)` kept, minimum 1). Verified via a Node stub loading the real `ScheduleFactors` + `ScheduleBuilder`: 2 valid proposals → 2 survivors with feasibilityScore 1; an invalid proposal (5min gap) → 0 survivors; 4 → 3 after cull. `?aes-debug` smoke tests assert each path. The OOR pre-filter avoids redundant `rangeExceeded` warnings on routes EVERY proposal would fail — only timing-sensitive warnings then drive the score, which is what we want.

**Slice 4d — scoring + selection.** New `SlotOptimizer.selectBest(proposals, {candidates, spec, basePreset, afpCtx, budget})` runs Track 3's `AesAfpAutoScheduler.run` against each surviving proposal and picks the highest-scoring winner. Track 3's allocator resolves the preset via `SchedulePresets.load()` keyed by `o.presetId` with no direct `preset` override hook, so 4d takes the cleanest available path: persist each synthetic preset under a `__aes-auto-tmp-<source>-<ts>` name via `SchedulePresets.create`, call the allocator with the temp's id, then `remove` in `finally` whether the run threw or succeeded. The picker dropdown (4e) will filter out names starting with `__aes-auto-tmp-` so users never see orphans even if a tab dies mid-run. Each run dispatches with `persist: false` so `AesAfpActiveDraftStore` stays untouched until 4e's save path. Sort: descending by `metadata.totalScore`; tie-breaker on `_meta.feasibilityScore` (lower wins) then proposal source enum (shift > split > merge — reflects the plan's "minimal disturbance" preference). Returns `{winner, winnerRun: {build, totalScore, ...}, runs}`. `budget: null` is passed straight through; Track 3's allocator already calls `_fallbackBudget(settings)` when missing, so 4d ships **without blocking on Track 2's `MaintenanceBudget`** as the plan's coordination table requires. Bail-paths covered by `?aes-debug` smoke (empty input → null winner; function-existence). Happy-path is verified end-to-end through slice 4e's `optimize()` against the live page; in-file Node verification of the per-proposal allocator dispatch isn't tractable without the full Track 3 module graph (`AesAfpRouteCandidates`, `AesAfpSpecResolver`, fuel-burn estimator, etc.) all mounted.

### Track 5 — Auto-scheduler preview panel + Apply-all batch pipeline — slices 5a, 5b, 5c, 5d, 5e shipped this session
**Surfaces in:** per-aircraft Flight Plan page (`/app/fleets/aircraft/<id>/0`) — new `auto-preview` slot in the wide host scaffold, rendered between the toolbar and the candidate table. **Files:** `modules/aircraft-flight-plan/auto-scheduler/preview-panel.js` (NEW), `modules/aircraft-flight-plan/auto-scheduler/apply-batch.js` (NEW), `modules/aircraft-flight-plan/auto-scheduler/auto-apply-log.js` (NEW), `modules/aircraft-flight-plan/host.js`, `modules/aircraft-flight-plan/form-driver.js`, `background.js`, `manifest.json`, `CLAUDE/handover-fragments/AUTO-5a.md` (NEW), `CLAUDE/handover-fragments/AUTO-5b.md` (NEW), `CLAUDE/handover-fragments/AUTO-5c.md` (NEW), `CLAUDE/handover-fragments/AUTO-5d.md` (NEW), `CLAUDE/handover-fragments/AUTO-5e.md` (NEW).

Track 5 is the user-facing surface for the auto-scheduler: render the Track 3 `Build` as a Gantt + per-leg overlay list, then (in slices 5b-5e) commit the whole proposal through the existing background-tab fillAndSubmit pipeline as a single Apply-all batch. Slice 5a ships render-only.

**Slice 5e — audit log + retry queue.** NEW `auto-apply-log.js` (~260 LOC) — `class AesAfpAutoApplyLogClass` exported as singleton `window.AesAfpAutoApplyLog`. Same convention as `audit-log.js`'s per-AFP-aircraft store: dual-store ring buffer with global timeline `aircraftFlightPlan:autoApplyLog` (cap 200) + per-aircraft ring `aircraftFlightPlan:autoApplyLog:<server>:<aircraftId>` (cap 80). Single `chrome.storage.local.set` per `add(record)` keeps both stores in sync (HANDOVER §10 single-writer invariant). `_cleanRecord` normalises IATAs to uppercase, clamps strings, drops nulls. Statuses: `queued | started | ok | failed | aborted | done | error | queue-dismissed`. Public API: `add` / `getRecent` / `getForAircraft` / `getRetryQueue` / `dismissRetryQueue` / `clear`. `getRetryQueue(server, aircraftId)` returns failed entries from the LATEST batch on this aircraft with three exclusions: failed seqs that have a later "ok" entry anywhere in the ring (succeeded on retry or via single-leg apply), all entries when the latest batch carries a `queue-dismissed` marker (user clicked Dismiss; entries stay in the ring for audit history), and duplicate seqs within the same batch (most recent failure wins). EXTENDED `apply-batch.js` (~+90 LOC): every bus emit pairs with a `_logEntry(...)` write — lifecycle entries (`started/done/aborted/error`) carry batch metadata (`total/succeeded/failed/elapsedMs/error`); per-leg entries (`ok/failed`) enrich from `_state.legs[legIdx]` so the audit row carries `origin/dest/depTime/pricePct/service/direction/waveLabel` even after batch state is reset. `_legMetaForIdx(legIdx, seq)` resolves leg metadata by index with a seq-based fallback. Defensive — `add` failures are caught + warned but never block the pipeline. EXTENDED `preview-panel.js` (~+125 LOC): added a `retryQueue` sub-state, `_loadRetryQueue` (called on `ctx:ready`, after every `auto-apply:done`, and after `_dismissPersistedQueue`), `_renderPersistedRetryBanner` (red-tinted banner with `Retry N` + `Dismiss` buttons), `_retryPersistedQueue` (materialises queue entries back into the form-driver leg shape and dispatches via `_applyAll(legs, {source: "retry-persisted"})` — no confirmation modal, this IS the retry), `_dismissPersistedQueue` (calls `AesAfpAutoApplyLog.dismissRetryQueue` then re-renders). `_renderFooter` switches between in-flight / just-finished / persisted-retry / default-tip modes — the persisted retry banner takes precedence over the default tip but yields to in-flight or just-finished. **Manifest fix**: Track 6's slice 6b commit dropped `apply-batch.js` from the AFP content-script block when adding `schedule-diff.js`; this slice restores it AND adds `auto-apply-log.js` ahead of it (`apply-batch.js` calls `_logEntry` which uses the singleton). Final auto-scheduler load order: `grid-state.js → objective.js → allocator.js → schedule-diff.js → slot-optimizer.js → auto-apply-log.js → apply-batch.js → preview-panel.js`. **Track 5 final-deliverable**: end-to-end auto-scheduler flow from "Auto-build week" through "Apply all" through abort/retry now ships behind the `autoScheduler.enabled === true && tier === "apply-on-confirm"` tier gate (defaults disabled / preview-only). The no-programmatic-submit invariant is held: every per-leg POST goes through `AesAfpFormDriver.fillAndSubmit` clicking AS's own green button — the batch pipeline is a sequencing layer over the existing single-leg path, not a new submit path.

**Slice 5d — live progress UI + abort.** Extended `preview-panel.js` (~+410 LOC) — added an `apply` sub-state (snapshot of the in-flight batch), five `auto-apply:*` bus subscriptions (`start` / `progress` / `done` / `aborted` / `error`), and a complete rewrite of `_renderFooter` to switch between three modes: live-progress widget, post-batch summary banner, and the existing default tip. `_renderApplyProgress` paints the in-flight widget — top row with `Applying leg X of N…`, last-result symbol (✓/✗) hover-titled with the per-leg error, ok/failed counters, live `~Xm remaining` ETA, and an Abort button (crimson); progress bar (linear-gradient blue, 200ms width transition); status line carrying the phase label, batchId, and elapsed time (red on `phase: error`). A 1Hz ticker (`_startApplyTicker` / `_stopApplyTicker`) re-renders every second so elapsed/ETA advance smoothly between leg-done events. `_renderApplyResultBanner` paints the post-batch banner — `Done/Aborted/Errored: N ok · M failed · Tm elapsed`, a "M failed — retry" button (only when `failed > 0`) routed to `_retryFailed()`, and a Dismiss button. `_liveSecondsPerLeg` divides elapsed wall-clock by completed leg count once the first leg lands, clamped to `[3s, 20s]`; falls back to slice-5b's `ESTIMATED_SECONDS_PER_LEG = 7` constant before any leg completes. `_abortApply` calls `AesAfpAutoApplyBatch.abort()` (defensive against module-not-loaded). **No appliedLegs rollback needed** — apply-batch.js writes the draft store ONLY on `phase: leg-done` with `ok: true`, so legs that hadn't been confirmed by AS were never written; legs that DID complete before abort stay marked applied (because they really did POST to AS). `_retryFailed` pulls failed seqs from `_state.apply.results.filter(!ok)`, looks them up in the current `lastBuild.flights` (toasts "No matching legs in the current Build — re-run Auto-build first." when the build was regenerated), materialises them with current overlay edits, emits `auto-apply:retry-requested` on the bus + dispatches via `_applyAll(legs, {source: "retry-failed"})` (skips the confirmation modal — the user already acknowledged the original batch). All bus handlers guard on `batchId` mismatch so stale events from a previous batch don't corrupt the active state. Public surface gained `abortApply()`, `retryFailed()`, and an `applyState` getter (defensive copy of `_state.apply`) so the diagnostics console can interrogate progress without poking internals. Phase-1 limitation: progress UI lives in-memory only — page reload restarts blank (the hidden tab keeps running in background.js but the page-side widget doesn't survive); persisting apply state to `chrome.storage.local` for crash-recovery is a Phase-2 enhancement.

**Slice 5c — batch fillAndSubmit pipeline.** Extended `background.js` (~+170 LOC) with a per-aircraft batch submit pipeline that opens ONE hidden tab and iterates legs serially, broadcasting progress back to the originating tab. New constants `AFP_BATCH_FILL_TIMEOUT_MS = 30s`, `AFP_BATCH_RELOAD_TIMEOUT_MS = 30s`, `AFP_BATCH_INTER_LEG_DELAY_MS = 500ms` (AS rate-limits Wicket form posts), `AFP_BATCH_TOTAL_TIMEOUT_MS = 20 min` hard cap. New `_afpBatchState` map keyed by `batchId` for abort plumbing. `_afpRunBatchSubmit(req, sender)` — open hidden tab → wait for load → for each leg: send `aes:afp:fill-and-submit` with `leg._batch = {idx, total, batchId, aircraftId}` metadata, wait for AS reload, broadcast progress, sleep 500ms (skipped after the last leg). Reload-timeout recovery: closes the (possibly wedged) hidden tab, recreates a fresh one, waits for initial load, continues to next leg — stops a single bad leg from poisoning the batch. `_afpEnqueueBatch` reuses the existing `_afpSubmitQueues` map so a batch can't race a single-leg Apply on the same aircraft. `_afpBroadcastBatchProgress` uses `chrome.tabs.sendMessage` to the originating tab when known, else `chrome.runtime.sendMessage`; both swallow `lastError` (a navigated/closed origin tab is OK; the batch keeps running in its own hidden tab). Two new `chrome.runtime.onMessage` handlers: `aes:afp:apply-batch` enqueues + responds with the final outcome; `aes:afp:apply-batch:abort` sets the abort flag AND closes the hidden tab so the in-flight `_waitForTabComplete` / `_sendTabMessageWithTimeout` reject promptly. NEW `apply-batch.js` (~330 LOC, registered after slot-optimizer.js + before preview-panel.js so the singleton exists by the time slice 5b's modal calls into it) — vanilla IIFE attaching `window.AesAfpAutoApplyBatch` with `start(payload)` / `abort()` / `state` getter. `start()` re-checks the tier gate via `AesAfpSettings.load()` (slice 5b is the load-bearing gate; this is a backstop), attaches the progress listener BEFORE dispatch, emits `auto-apply:start` on the bus, sends the `aes:afp:apply-batch` runtime message, returns a Promise resolving on completion. `abort()` sends the abort message + emits `auto-apply:aborted`. Per-leg success mirrored into `AesAfpActiveDraftStore.appliedLegs` via `setApplied(server, aircraftId, seq, ts)` so the Fleet Hub overlay tab repaints applied legs in green within ~200ms. Bus events emitted: `auto-apply:start` / `:progress` (one per phase: queued, tab-opened, tab-loaded, leg-start, leg-done, aborted, timeout, done, error) / `:done` / `:aborted` / `:error`. EXTENDED `form-driver.js` (~+25 LOC): `fillAndSubmit(leg)` accepts `leg._batch` metadata for diagnostics overlay hints; defensive top-level `{batch: [...]}` shape rejected with a clear error pointing the caller at the `aes:afp:apply-batch` background message (the page reloads between legs so the form-driver can't carry batch state in-page). `?aes-debug` smoke (4 assertions): public surface + initial state + empty-legs bail. **No-programmatic-submit invariant held**: every per-leg submit still goes through `AesAfpFormDriver.fillAndSubmit` clicking AS's own green button — the SOLE entry point in the codebase that triggers AS form submission, documented in form-driver.js header. The batch pipeline is a sequencing layer over the existing single-leg path, not a new submit path.

**Slice 5b — Apply-all CTA + confirmation modal.** Extended `preview-panel.js` (~+330 LOC) — replaced slice 5a's placeholder hint string with a real Apply-all button next to the Auto-build CTA, plus a new `_openConfirmModal` flow + supporting helpers (`_materialiseLegs`, `_updateModalCounts`, `_closeConfirmModal`, `_applyAll`, `_toast`, `_fmtDuration`). Public surface gained `openConfirmModal()` + `applyAll(legs, opts)` so the diagnostics console can drive the same flow without DOM clicks. Tier gate: button rendered always (so users discover it) but disabled with a per-blocker tooltip unless `lastBuild` is non-null AND `flights.length > 0` AND `autoScheduler.enabled === true` AND `autoScheduler.tier === "apply-on-confirm"` AND `flights.length <= maxLegsPerApply` (default 28). When dormant, an inline hint string under the CTA names the two settings keys to flip. Modal anatomy: header (title + ×), body (subtitle with up-front estimate `legs × ESTIMATED_SECONDS_PER_LEG = 7s` formatted via `_fmtDuration`, alternating-row checkbox list per leg with index/direction/origin→dest/depTime/price%/edit-tag, required I-understand checkbox), footer (counter + Cancel + Apply). Apply turns crimson when armed (selection > 0 AND ack ticked); disabled gray otherwise. Esc / × / dim-background / Cancel all close cleanly. `_materialiseLegs` is the canonical adapter from Track 3's `Build.flights[]` shape (with `depTimeLocal`) to the `{seq, origin, destination, depTime, pricePct, service, …}` shape `AesAfpFormDriver.fill` consumes, merging `draft.perLegEdits[seq]` overlays and falling back to `defaultPricePct` / `defaultService` for missing fields. Bus contract: `auto-apply:requested {ctx, legs, requestedAt, source}` emitted unconditionally on every Apply click, even when slice 5c isn't loaded — gives slice 5e (audit log) a single subscription point. Apply hands off to `AesAfpAutoApplyBatch.start(payload)` when present (slice 5c); otherwise toasts "Apply-batch pipeline not loaded yet (slice 5c)" so the click doesn't appear to silently fail. `RouteAssistantToast` calls are defended via `_toast` (toast-host.js loads earlier in the manifest but IIFE timing isn't strict). **No AS POST in this slice** — invariant held; the actual fill+submit is slice 5c's responsibility. Manifest unchanged (file is already registered from 5a).

**Slice 5a — preview panel render.** New `AesAfpAutoSchedulerPreview` IIFE (`render`, `runAutoBuild`, `lastBuild`) mounts into a new `auto-preview` slot added to `host.js:WIDE_SLOT_NAMES` between `tools` and `candidates`. Six-section root: heading, summary cells (Legs · Weekly hours vs ceiling · Score · Maintenance forecast · Built-when), CTA row (`Auto-build week` / `Re-run auto-build`), status line, Gantt preview via `RouteAssistantWaveOverlay.renderGantt` (same call shape wave-applier uses, so any future renderer improvements come for free), per-leg overlay list with inline editor that mutates `AesAfpActiveDraftStore.perLegEdits[seq]` via `setEdit(server, aircraftId, seq, patch)`, and a footer hint pointing at the tier gate. Maintenance forecast cell color-codes Track 2's `forecastRatio7d`: <100% red / <105% amber / ≥105% green. Edits ALWAYS round-trip through `AesAfpActiveDraftStore` so the Fleet Hub overlay tab repaints via `chrome.storage.onChanged` (same listener wave-applier uses). Validation: origin/dest IATA `^[A-Z]{3}$`, time `HH:MM`, price 1..500; invalid values flag the input red without writing. Bus subscriptions: `ctx:ready` (load settings + draft + budget; render root; attach storage listener), `spec:resolved` (recompute budget), `candidates:updated` (re-enable Build button), `auto-schedule:built` (adopt build payload), `maintenance:scraped` / `wear:updated` (recompute budget). `runAutoBuild()` calls `AesAfpAutoScheduler.run({persist: true})` — Track 3 is a pure compute (no AS POST), so this slice doesn't touch the no-programmatic-submit invariant. **Tier gate posture:** the "Apply all" CTA is a placeholder hint string this slice; slice 5b replaces it with the real button gated behind `settings.aircraftFlightPlan.autoScheduler.enabled === true && .tier === "apply-on-confirm"` (defaults `false` / `"preview-only"`). `?aes-debug` smoke (3 assertions) covers public surface + `_ago` formatter. Manifest registration: `preview-panel.js` sits AFTER the four auto-scheduler files (grid-state, objective, allocator, slot-optimizer) it depends on, BEFORE `wave-applier.js` + `content_aircraftFlightPlan.js`.

**Slice 4e — save tweaked variant + full pipeline entry point.** New `SlotOptimizer.optimize({aircraftId, presetId, candidates?, spec?, ctx?, budget?})` ties slices 4a → 4d into a single async call: load demand profile → propose adjustments → filter feasibility → score the BASELINE (so we can report scoreDelta = winner − baseline) → score each survivor → save the winner via `SchedulePresets.createTweaked`. Returns `{savedPresetId, beforeWaves, afterWaves, scoreDelta, baselineScore, winnerScore, proposalSource, proposalDelta, runs}` on success or `{skipped: true, reason}` on any of the bail paths (tier-gate off, no candidates, no proposals, no feasible survivors, no winner, save failed). Tier-gated: `optimize()` returns `{skipped: true, reason: "autoScheduler disabled"}` without persisting anything when `settings.aircraftFlightPlan.autoScheduler.enabled !== true` — Phase-1 ships disabled per the plan. New `SchedulePresets.createTweaked({base, waves, tweakedFor, nameSuffix?})` piggybacks on `create()` (no fork in the CRUD path) but stamps three provenance fields onto the new record: `tweakedFrom` (base preset id), `tweakedAt` (`Date.now()`), `tweakedFor` (aircraftId). Hub copied from base; `factors` deep-cloned; default name suffix ` · auto-tweaked`. The `wave-applier.js:_renderToolbar` preset picker patch is 4 lines: skip records whose name starts with `__aes-auto-tmp-` (slice-4d scratch presets — defensive against orphans from a crashed run), and prepend `🔧 ` to records carrying a `tweakedFrom` field. Verified via Node stub: tier-gate honoured across three permutations (no settings → "autoScheduler disabled", `enabled: false` → same, `enabled: true` no presets → "SchedulePresets unavailable"); `createTweaked` round-trip persists all three provenance fields plus a copied hub, and the new record appears in the post-save `load()` list. **Open question**: live-page verification of the plan's MCO mistuned-preset spec (waves at 03:00 + 14:00 → tweaked toward 09:00 + 17:00) requires the AFP page + cached FlightsFromStore data + the optimizer toggled on; not run from this agent session — see `CLAUDE/handover-fragments/AUTO-4e.md` for the manual checklist. Note that without per-route hour buckets in the FlightsFrom scrape (TODO from 4a), the demand profile is flat and the shift heuristic only fires on `weeklyFlights` gaps — the spec's "shift toward 09:00/17:00" effect won't materialise until the scrape enriches.

### Track 6 — Existing-schedule diff + wipe pipeline — slices 6a (+ followup), 6b (+ followup) shipped this session
**Surfaces in:** per-aircraft Flight Plan page (`/app/fleets/aircraft/<id>/0`) — pure modules, no UI yet (Track 6's UI surfaces land in slice 6d). **Files:** `modules/aircraft-flight-plan/host.js` (rewrote `readVisualFlightPlan` + collapse-helper followup), `modules/aircraft-flight-plan/auto-scheduler/schedule-diff.js` (NEW), `manifest.json`, `CLAUDE/handover-fragments/AFP-VFP-parser.md` (NEW), `CLAUDE/handover-fragments/AUTO-6a.md` (NEW), `CLAUDE/handover-fragments/AUTO-6b.md` (NEW), and the populated `CLAUDE/...:21944:0?13 MULTIDAYROUTES.html` capture moved into the references folder.

Track 6 makes the auto-scheduler safe for aircraft that already have flights: read the Visual Flight Plan, diff against the proposed Build, present the diff to the user, then (if confirmed) delete and rebuild via Track 5's batch pipeline. Slices 6a + 6b ship the read+compare layer; 6c–6e (delete pipeline, confirmation modal, transactional wipe) are blocked on Track 5's batch pipeline.

- **Slice 6a — VFP parser hardening (+ followup multi-day collapse).** Rewrote `host.js:readVisualFlightPlan` (Slice A's stub shipped against an empty-VFP capture and emitted one entry per *every* block). New parser filters to `.block.flight`, derives `depTimeLocal`/`arrTimeLocal` from CSS `margin-left`/`width` percentages (1% = 14.4 min — the AS positioning is the source of truth, not the unreliable `<span class="start">` text which is HHMM for long bars and just-minutes for short bars), resolves origin from the previous `.block.location > .outbound[title]` IATA and destination from the next `.block.location > .inbound[title]` IATA, captures the AS flight id by parsing `numbers/(\d+)` out of the per-flight overlay anchor, and emits a structured leg with synthetic `seq` (1-based, ordered by `(dayIdx, depTime)`). **Followup change (same file, only `readVisualFlightPlan` + new helper):** added `_collapseDayCrossPairs(legs)` post-pass that pairs `block flight started` legs (one half of a long-haul) with `block flight ended` legs sharing the same `flightId` on `(dayIdx + 1) % 7` — Sun→Mon wrap included — and merges them into ONE logical leg with `crossesMidnight: true`, `arrTimeLocal` from the ended half, `durationMin` summed across both halves, and `destination` pulled from the ended half's same-day next `.inbound[title]`. Validated against the populated 21944 MULTIDAYROUTES capture (CLAUDE/...:21944:0?13 MULTIDAYROUTES.html — 6 day-cross pairs across Mon-Sun): 20 raw flight bars collapse to **14 logical legs** (6 cross-midnight + 8 same-day). Same-day-only 13536 capture (14 flights) and empty-blocks 6968 captures stay unchanged. The retired `spansIntoNext` / `spansFromPrev` flags are stripped from output; only `crossesMidnight` survives. Pathological unpaired halves (a `started` with no matching `ended` or vice versa) emit `console.warn("[AES afp-6a] unpaired day-cross …")` and stay in the output as same-day legs — safe failure mode. 24-assertion Node stub run all green covering empty/null input, same-day legs unchanged, simple paired collapse, multiple pairs, Sun→Mon wrap, unpaired warnings, and the full 14/20 collapse count. Confidence audit in `AFP-VFP-parser.md`: HIGH for flightId/flightLink/flightCode/depTimeLocal/arrTimeLocal/origin/destination after the followup; UNCERTAIN for full flight number (the `.code` span carries only the suffix; airline-prefix lookup deferred to slice 6c). Touched ONLY `readVisualFlightPlan` and added one new helper in host.js — `WIDE_SLOT_NAMES`, `findNewFlightForm`, `findFormTabs`, mount, plus the parallel-track `readSchedule`/`AesAfpVfpReader` integration all untouched. No new manifest entry (host.js was already loaded).
- **Slice 6b — schedule diff engine (+ followup configurable tolerance).** New `AesAfpScheduleDiff.compare(currentLegs, proposedLegs, opts?)` returns `{keep:[{currentSeq, proposedSeq, deltaMin}], delete:[currentLeg], add:[proposedLeg], moveTime:[]}`. "keep" match rule: same origin + destination (case-sensitive 3-char IATA) + `timeDeltaMin(depTimeLocal) ≤ tolerance` (default 15), where `timeDeltaMin` is the shorter-around-midnight distance (so 23:55 vs 00:05 → 10 min not 1430 min). When more than one proposed leg matches a current leg's `(O, D, depTime±tol)` window, the smallest absolute time delta wins. Each proposed leg can be claimed at most once per `compare()` call; greedy on the current side. **Phase-1 simplification (permanent, retired open question):** when O/D matches but depTime delta is OUTSIDE the window, the pair gets split into delete+add — `moveTime` is always `[]` (kept in the return shape so a future slice can flip the heuristic without breaking callers). Reasoning: AS's edit-flight Wicket POST is more fragile than the delete-then-create path; reusing Track 5's apply pipeline via delete+add costs one extra POST per moved leg but lets 6c–6e ship without a separate edit-flight reverse-engineering session. **No day-mask expansion**: this slice compares legs as-passed; callers wanting per-day apples-to-apples comparison must pre-expand `dayMask` (slice 6d's confirmation modal is the right place). Safe-failure: non-array inputs become `[]`; legs missing/malformed `origin`/`destination`/`depTimeLocal` go to `delete` (current side) or `add` (proposed side). **Followup change:** `compare` accepts a 3rd `opts` arg with shape `{toleranceMin?: number}`. New `_resolveTolerance(opts)` picks the active value by precedence — caller wins (finite ≥ 0), then `window.AesAfpSettings.cached?.()...autoScheduler.diff.toleranceMin` (sync read, only consulted when a future slice registers `cached()` for the apply pipeline to pre-warm), falling back to the `TOLERANCE_MIN` constant (15). Defensive non-finite/negative values at any layer fall through to the next so a misconfigured setting never breaks the diff. Settings shape `settings.aircraftFlightPlan.autoScheduler.diff.toleranceMin: 15` declared as a working-tree edit to `settings-extension.js` (defaults block + `_mergeAircraftFlightPlan` deep-merge via `numFieldNonNeg` + `save` deep-merge so a partial save doesn't blow away sibling knobs); the file itself is still untracked at this followup, so the addition lands when whoever owns slice F next commits it. Until then `_resolveTolerance` falls through to `TOLERANCE_MIN` (identical 15), so the diff is bit-for-bit compatible with slice 6b for callers not passing the opt arg. UI to expose the slider is deferred to slice 6d's confirmation modal — declared field gives 6d a stable target. Public surface also exposes `timeDeltaMin(a, b)`, `isMatchable(leg)`, and the read-only `TOLERANCE_MIN: 15` constant for callers + tests. Verified via 34-assertion `?aes-debug` smoke block — 30 from slice 6b (empty/empty, identical lists, ±14 within window, ±30 outside, different-dest, lowercase-IATA rejected, ambiguous-multiple closest-wins, duplicate-current single-claim, midnight wrap) plus 4 from the followup (`{toleranceMin: 5}` rejects ±10min, `{toleranceMin: 60}` keeps ±30min, `{toleranceMin: 0}` only keeps exact-time, `{toleranceMin: -1}` falls back to default 15) — all green under both browser load and a Node stub run (`global.window = {location:{search:"?aes-debug"}}` → 34/34). Manifest registration: `schedule-diff.js` sits AFTER Track 3's `allocator.js`, BEFORE Track 4's `slot-optimizer.js` per the slice spec.

### Track 1 — AFP form-driver hardening + selector audit + diagnostics overlay — shipped this session
**Surfaces in:** per-aircraft Flight Plan page (`/app/fleets/aircraft/<id>/0`), gated on the `?aes-debug` query flag for the overlay slice. **Files:** `modules/aircraft-flight-plan/form-driver.js`, `modules/aircraft-flight-plan/host.js`, `modules/aircraft-flight-plan/diagnostics.js`, `modules/aircraft-flight-plan/SELECTORS.md` (NEW).

Fixes the long-standing "I clicked a candidate but the AS dropdowns didn't change" bug. Root cause: AS uses **select2 v3** (the origin/destination selects carry the `select2-offscreen` class — a v3-only marker; v4 uses `select2-hidden-accessible`); v3's `val` setter requires `triggerChange=true` as the third argument or the visible chip stays on "Choose One" while `<option selected>` is silently correct underneath. Our previous code called `$sel.select2("val", value)` with no third arg inside a try/catch that swallowed the v3-vs-v4 signature mismatch, so the chip never repainted. Submit-from-this-state was a footgun.

**Slice 1a — select2 v3/v4 commit fix.** New `_detectSelect2Version()` helper inspects `$.fn.select2.amd` (v4) vs `$.fn.select2.defaults.matcher` (v3). `_commitSelect` now branches on the detected version: v3 → `$sel.select2("val", value, true)` (third arg = triggerChange, repaints chip); v4 → `$sel.val(value).trigger("change")`; unknown → both. Errors no longer silently swallow — they route to `_diag("commit-select-error", ...)` for visibility through the diagnostics overlay (slice 1c). The native `dispatchEvent("change")` fallback survives for the no-jQuery case. The airport-id concern from the planning prompt is verified-correct without code change: `_findOptByIata` matches against the option's visible label `"City, Region (IATA)"` via `\b<IATA>\b`, then `_commitSelect` reads `opt.value` (the airport id, what AS Wicket expects on POST). No semantics change there.

**Slice 1b — "New Flight Number" tab activation.** The form's submit button only exists in the New tab's panel — when the user is on the "Existing Flight Number" tab, `findNewFlightForm()` returns null and form-driver bails. New `host.js:findFormTabs()` locates both tab anchors (text-match on "New Flight Number" / "Existing Flight Number" since Wicket ids reshuffle every render; href substring `'newFlightNumber'` / `'toggle~new'` is the fallback) and reports `activeTab` as `"new" | "existing" | "unknown"`. Exposed on `window.AesAfp.getFormTabs`. Form-driver's new `_ensureNewTabActive()` clicks the New anchor when the active tab is "existing" and polls `findNewFlightForm()` every 100ms up to 1.5s for the panel swap; timeout records `tab-activate-timeout` and falls through to the existing form-not-found error path. `fill(leg)` is now `async` and awaits this gate; `fillAndSubmit` likewise. Both internal callers (`bus.on("candidate:selected")` and the toolbar "Fill latest pick" button) are fire-and-forget so the async conversion is invisible to them. The single external caller (`content_aircraftFlightPlan.js`'s `aes:afp:fill-and-submit` handler) was already promise-based via `.then(...)`, so the API surface didn't move.

**Slice 1c — diagnostics overlay.** Extends the existing `diagnostics.js` (which previously logged bus events to `console.groupCollapsed` only) with a fixed-position floating panel rendered when `?aes-debug` is set on the URL or `localStorage["aes-afp-debug"] === "1"`. Five sections: header (with × that sets the LS flag to "0" and removes the panel), live form-state table (six rows — name, value, chip text — polled every 500ms; reads chip text from `.select2-chosen` for v3 selects and from `option.text` for native ones; surfaces `"form not in DOM (active tab: ...)"` when the New tab isn't active), last-fill JSON (memoed from `candidate:selected` + `form:filled` bus events), recent-events tail (last 8 from the existing 200-cap ring), and three buttons — `[Force re-sync]` re-emits the memoed `candidate:selected` payload through the bus (exercises the full path including form-driver's listener), `[Run self-test]` walks the SELECTORS table from slice 1d, `[Clear log]` empties the events ring. New public surface on `window.AesAfpDiagnostics`: `record(kind, payload)` (consumed by form-driver's `_diag` shim, which is a no-op when diagnostics isn't loaded — zero production cost), `runSelfTest()`, `forceResync()`, `clearLog()`, `memo`, `SELECTORS`. `record` accepts both 2-arg `(kind, payload)` and the existing 3-arg `(kind, name, payload)` shapes; the wrapBus internals continue using the 3-arg form.

**Slice 1d — SELECTORS.md + self-test.** New `modules/aircraft-flight-plan/SELECTORS.md` (first per-module selector reference doc — convention going forward for modules with select2 / Wicket exposure). Twelve selectors documented across four scopes: form root, six selects (origin/destination select2 v3, four native), reverse anchor, two tabs, hidden Wicket fields. Each entry pairs primary + fallback selectors plus the version-specific notes (e.g., select2-offscreen is v3, option values for origin/destination are airport ids not IATA). The runtime mirror is the `SELECTORS` table in `diagnostics.js` — `runSelfTest()` walks it, querying with the right scope (form-rooted via `findNewFlightForm()`, tabs via `findFormTabs()`'s purpose-keyed dispatch since the tab anchors aren't pure CSS-selectable, page-level via `document.querySelector`) and returns `{purpose, selector, ok, count, note}` per row. Overlay's "Run self-test" button renders pass/fail inline.

**Slice 1e — validation pass.** Manual checklist (no automated test — the bug is purely visual select2 chip repaint):
1. Open `/app/fleets/aircraft/<id>/0?aes-debug` — the diagnostics overlay appears top-right.
2. Click "Run self-test" — every row green on the New tab.
3. Click 5 distinct candidate rows (or apply 3+ wave legs); each click should repaint all six form chips with the new values. The overlay's live form-state table reflects each commit within 500ms.
4. Switch to the "Existing Flight Number" tab and click a candidate — form-driver auto-flips to the New tab within ~300ms and fills normally; overlay logs `tab-activate` + `tab-activate-ok`.
5. Click "Force re-sync" — the last `candidate:selected` re-runs through the bus and the form re-fills.

**Verification.** Internal smoke (no live AS access from this session): the diff is structurally consistent (form-driver / host / diagnostics co-evolved; SELECTORS.md mirrors the runtime table). Live verification is the user's manual pass per the checklist above; any residual gaps go into a follow-up fragment in this section.

**Deferred (out of scope for this track).** Submit button state isn't surfaced in the overlay (would need an extra row reading `f.submitBtn.disabled` plus a hover-test for cursor: not-allowed) — easy to add if the user wants it. The overlay isn't draggable; if it covers something the user needs, dismiss with × and re-enable via `?aes-debug=1`. Selectors for adjacent surfaces (route-assistant panel, schedule-management) aren't documented yet — recommend mirroring the SELECTORS.md template per module as a future hygiene pass.

### AFP Schedule Control (Fleet Hub) — Tier 1 dry-run shipped this session
**Surfaces in:** Fleet management page (`/app/fleets*`) — new **D** action chip per aircraft row alongside R/S/P. **Files:** `modules/aircraft-flight-plan-dashboard/` (NEW directory, 9 files), `modules/fleet-hub/inline-table.js`, `modules/fleet-hub/host.js`, `manifest.json`.

First slice of the AFP "schedule control" surface that the user wired this session. Goal: compose a wave plan + create flight numbers via proxy GET → POST WITHOUT navigating into each aircraft's Flight Plan page, plus a deep-link affordance that preserves the `?<flightNumberId>&aes-debug` URL pattern. **This slice ships dry-run only** — `AesAfpFnApplier` mirrors `RouteAssistantPricingApplier`'s `dryRunOnly:true` + `applyEnabled:false` double-gate convention, both gates hard-coded `true`/`false` respectively; `apply()` always returns `{status: "dry-run", ...}`. Tier 2 will introduce the live POST + verify + undo path; Tier 3 the bulk multi-aircraft loop.

**New module — `modules/aircraft-flight-plan-dashboard/`.** Nine files, all isolated from the existing AFP slices to preserve the AFP form-driver "never POSTs" invariant (HANDOVER §10): (1) `flight-number-apply-log.js` — global ring `aircraftFlightPlan:fnApplyLog` (cap 200) + per-aircraft ring `aircraftFlightPlan:fnApplyLog:<server>:<aircraftId>` (cap 20); 5-min dedup on `(fingerprint+status)` mirrors `pricing-apply-log.js`. (2) `proxy-page-fetcher.js` — `fetchAircraftFormContext(aircraftId)` GETs `/app/fleets/aircraft/<id>/0` with `credentials: "include"`, parses via the applier's static helper, 60-sec in-memory cache. (3) `flight-number-applier.js` — the gold-standard mirror of `pricing-applier.js`. `parseFormContext(html)` lifts `host.js:findNewFlightForm()` onto a static helper that walks a DOMParser doc; `buildBody({formContext, leg})` composes the URL-encoded body via `_findOptByIata` (option text \b<IATA>\b, same regex as `form-driver.js:_findOptByIata`) + value matchers for hours/minutes/price/service; `apply()` short-circuits to dry-run after preflight + body composition. (4) `candidate-pipeline.js` — wraps `AesAfpRouteCandidates.compute` + `AesAfpWaveApplier.buildFromCandidates` for off-page invocation. Both wrapped APIs are headless-safe (route-candidates' `bus.emit("candidates:updated")` is guarded by `if (window.AesAfp && AesAfp.bus)` at line 152; wave-applier's `buildFromCandidates` calls `RouteAssistantWaveOverlay.buildSchedule` directly with no slot dependency). (5) `deep-link.js` — `AesAfpDashboardDeepLink.buildAircraftUrl(server, aircraftId, flightNumberId)` constructs the `?<fnId>&aes-debug` form. (6) `settings-extension.js` — Tier 1 hard-coded gates; `save()` is a no-op + warn so storage editing can't unlock writes. (7) `fleet-roster-view.js` — thin adapter over `FleetHubAircraftAggregator.enrich()` with a 30-sec cache. (8) `panel.js` — fixed-position overlay (z 99999) opening as a 560px modal at top-right; sections: header (reg/equipment/loc + "↗ Open in AS" deep-link + ×), preset picker + Generate, status line (hub · destinations · already-scheduled · candidates · build legs), build preview table with per-leg "Dry-run" buttons that inline-expand the would-be POST body, "Recent applies (last 10)" expander reading from the per-aircraft ring, gate footer ("🔒 Tier 1 — dry-run only"). (9) `host.js` — singleton on `/app/fleets*`, lazily wires the dependency graph on first D click and opens the panel.

**Modified files.** `modules/fleet-hub/inline-table.js` — added `D` to `EVENT` + `MARKER` static maps, added the per-row chip in `_actionsCell`, added the click branch in `_onClick`. `modules/fleet-hub/host.js` — added `_dashboardHost` lazy field, added the `E.D` listener in `_bindTableListeners`, added `_onActionD(detail)` that lazy-instantiates `AesAfpDashboardHost` and calls `openFor(detail)`. `manifest.json` — appended 13 dependency scripts + 9 new dashboard scripts to the existing `/app/fleets*` `content_scripts` entry. **De-duplication checked** per HANDOVER §10 (the AFP-zero-tab invariant): cross-checked against the existing `/app/fleets*` block — `state-store`, `active-draft-store`, `submit-bridge`, `range-buckets`, `presets-store`, `schedule-store`, `schedule-builder` are NOT re-listed. AFP `host.js` is loaded here (it's idempotent — line 32 `if (window.AesAfp) return`; `mount()` bails on missing `.as-page-aircraft`); we DO NOT load `content_aircraftFlightPlan.js` (which would call `mount()`), and we DO NOT load AFP `form-driver.js` (which has its own toolbar render path that would silently disable on missing slots).

**Storage delta.**
```
aircraftFlightPlan:fnApplyLog                       # global ring (cap 200)
aircraftFlightPlan:fnApplyLog:<server>:<aircraftId> # per-aircraft ring (cap 20)
aircraftFlightPlanDashboard:settings                # T1 reads ignore stored values; gates locked
```

**Verification.** Manual smoke test on `/app/fleets*`: D chip renders alongside R/S/P; click D → modal opens with header + preset dropdown; "↗ Open in AS" link constructs `https://<server>/app/fleets/aircraft/<id>/0?aes-debug`; Generate runs the proxy GET (verify in DevTools Network — single GET, no POST), candidate pipeline produces a build, per-leg "Dry-run" buttons append a `dry-run` entry to `aircraftFlightPlan:fnApplyLog:<server>:<aircraftId>` and inline-expand the body preview. **Critical T1 invariant:** ZERO POSTs in the Network tab during any interaction with the modal. Reload `/app/fleets*` → D chip and modal flow survive Wicket re-renders.

**Deferred (out of scope for this slice).** Tier 2 (live single-leg POST + verify + undo behind explicit double-gate flip), Tier 3 (bulk multi-aircraft + cooldowns + circuit breaker), Tier 4 (silent-auto via `chrome.alarms`), delete/replace existing flight numbers (create-only is the user-locked scope).

### Sitewide brutalist skin + QOL — shipped this session
**Surfaces in:** every AS page (`/app/*`, `/action/*`). **Files:** `modules/site-skin/bootstrap.js` (NEW), `modules/site-skin/table-polish.js` (NEW), `modules/site-skin/keyboard-shortcuts.js` (NEW), `modules/site-skin/density-toggle.js` (NEW), `modules/site-skin/click-to-copy.js` (NEW), `modules/site-skin/breadcrumb.js` (NEW), `css/skin/skin-global.css` (NEW), `css/skin/skin-finance.css` / `skin-fleet.css` / `skin-schedule.css` / `skin-info.css` / `skin-ops.css` (NEW), `manifest.json`, `options.html`, `options.js`, `modules/aes-menu.js`.

Extends the brutalist treatment from AES-injected surfaces (Route Assistant, AES menu, About dialog, status strip) to AirlineSim's *native* Wicket+Bootstrap GUI sitewide. Plus four QOL features that reuse the same primitives.

**Activation.** Opt-in toggle persists in `chrome.storage.sync` under `aes_skin_enabled` (default `true`) and `aes_skin_density` (default `comfortable`). The bootstrap script runs at `document_start`, reads both values synchronously into a `chrome.storage.sync.get` callback, and stamps `<html data-aes-skin="on|off" data-aes-density="comfortable|compact" data-aes-page="<kind>">` before AS paints. Body class `aes-skin` is mirrored as soon as `<body>` is present (MutationObserver fallback). `chrome.storage.onChanged` reapplies the attributes within ms of a toggle — no reload needed. The bootstrap also exposes `window.AESSiteSkin` (`isEnabled` / `getDensity` / `getPageKind` / `setEnabled` / `setDensity` / `cycleDensity` / `showShortcuts` / `hideShortcuts`) so other modules read/write through one helper instead of re-implementing storage glue.

**Override layer.** `css/skin/skin-global.css` (~600 LOC) restyles every AS Bootstrap primitive scoped to `body.aes-skin`: typography baseline, `.btn` family (incl. `.btn-primary` → rust, `.btn-success` → moss, `.btn-warning` → amber, `.btn-danger` → crimson outline-fill on hover, `.btn-info` → cobalt), `.form-control` / labels / `.input-group-addon` / `.help-block`, `.panel*` with the signature double-rule seam under `.panel-heading`, `.well`, `.alert*` (left-edge status stripe), `.navbar` / `.navbar-default` / `.nav-tabs` / `.nav-pills` / `.breadcrumb`, `.table` / `.table-striped` / `.table-hover` / `.table-bordered` (oxide thead, paper-rule rows, bone-2 stripe), `.modal-content` / `.modal-header` / `.modal-footer` / `.close` / `.modal-backdrop`, `.dropdown-menu` (folded in from former `content.css` legacy block), `.badge` / `.label*`, `.pagination`, `.progress*`, `.popover` / `.tooltip-inner`, `.list-group`, AS's own `.as-panel` / `.as-fieldset` / `.as-table-well`. Density compact-mode reduces padding on tables/forms/buttons via `html[data-aes-density="compact"]` selectors. Five per-page polish files (`skin-finance.css`, `skin-fleet.css`, `skin-schedule.css`, `skin-info.css`, `skin-ops.css`) scope to `body.aes-skin[data-aes-page="…"]` for narrow tweaks (right-aligned currency, mono regs, schedule tab quirks). Every rule excludes `#aes-route-assistant` so the RA panel's dark theme is undisturbed. Zero hardcoded hex/px — pure tokens from `design-tokens.css`.

**QOL #1 — table polish.** `modules/site-skin/table-polish.js` (~95 LOC). Scans every `<table>` on `DOMContentLoaded` and after each AS Wicket AJAX rerender (debounced MutationObserver, 200 ms). Tables with ≥8 rows (and not inside an AES-injected panel or RA root) gain `.aes-skin-sticky` so CSS pins `<thead th>` via `position:sticky`. Tables with ≥12 rows additionally get a brutalist filter input above them (`.aes-skin-filter-wrap` — mono caps "FILTER" label + search input + "N ROWS" count badge). Typing hides non-matching rows via `requestAnimationFrame`-batched textContent substring match. Filtered count updates live.

**QOL #2 — keyboard shortcuts + density toggle.** `modules/site-skin/keyboard-shortcuts.js` (~140 LOC). Vim-style two-key sequences with 800 ms prefix timeout: `g d` dashboard, `g f` fleets, `g s` scheduling-for-current-hub, `g a` accounting, `g l` leasing, `g i` inventory-for-current-hub, `g m` markets-for-current-hub, `g o` ops, `g e` enterprise-info-for-current-id, `g x` settings. `/` focuses the first visible filter/search/text input. `?` opens a centred brutalist help popover (`.aes-skin-help` — uses the `.aes-modal__close` primitive for parity). `Esc` closes the help; otherwise listeners are no-ops while the user is typing in a field. `Ctrl/Meta/Alt` chords pass through so browser shortcuts aren't hijacked. `Shift+D` (separate file `density-toggle.js`, ~25 LOC) cycles `comfortable ↔ compact` via `AESSiteSkin.cycleDensity()`.

**QOL #3 — click-to-copy.** `modules/site-skin/click-to-copy.js` (~90 LOC). Single delegated `mouseover`/`click` listener on `document.body`. A cell becomes `.aes-skin-copyable` (cursor:copy + dashed rust outline on hover) when its trimmed text matches: currency (`-?(?:[A-Z]{1,3}\$|\$|€|£)?\s?-?\d{...}`), 3-letter uppercase IATA, or aircraft registration (`X-XXXX` style — fleet pages only). Click → `navigator.clipboard.writeText` (with `execCommand` fallback) → 1.4 s success toast (`.aes-toast--success` lazily mounted on a sub-host so it doesn't compete with RA's own toast host). Skips cells inside `<a>`/`<button>`/inputs/`#aes-route-assistant`/`.aes-panel`.

**QOL #4 — breadcrumb strip.** `modules/site-skin/breadcrumb.js` (~70 LOC). Parses `location.pathname` against a small map and renders `nav.aes-skin-breadcrumb` (`position:sticky; top:0; z-index:5` so it sits below `.modal-backdrop` at 1040) as the first child of `<body>`. Format: UPPERCASE display caps with `paper-rule` slash separators, last crumb in rust as the leaf. Page kinds mapped: finance (accounting/leasing/capital/assets/cashflow), fleet (fleets list + aircraft detail with reg parsed from title), scheduling-`<HUB>`, inventory-`<HUB>`, markets-`<HUB>`, aircraft-market, info/airport-`<id>`, info/enterprise-`<id>`, ops/stations, dashboard, settings, flight-info, staff. Skipped on the scheduling page family because the Route Assistant panel already provides chrome there.

**AES menu wiring.** `modules/aes-menu.js`'s `#createMenuItem` factory gained two new prop types: `onClick` (function — bound to the inner anchor's click) and `stateLabel` (function — appends a live mono-caps badge that gets re-stamped via `chrome.storage.onChanged` listener attached to the menu). Three new top items added above the existing "Community" header: `Brutalist Skin [ON|OFF]`, `Density [COMFORT|COMPACT]`, `Shortcuts` (opens the same `?` help popover via `AESSiteSkin.showShortcuts()`).

**Options page.** `options.html` gained a brutalist settings panel above the data display: a single skin checkbox + comfortable/compact density radio. `options.js`'s new `wireSiteSkinSettings()` reads the same `chrome.storage.sync` keys, writes through on change, and listens for changes to keep the UI in sync if the user toggles via the AES menu while the options tab is open.

**Manifest topology.** Two global content_scripts entries on `app/*` + `action/*`:
- Entry A (`run_at: "document_start"`) — all six skin CSS files + the existing `design-tokens.css` / `components.css` / `content.css` + `bootstrap.js`. Loading at document_start prevents flash of un-skinned UI.
- Entry B (default `document_idle`) — jquery, helpers, design-tokens.js, aes-menu.js, about-dialog.js (existing), then the five new QOL JS modules (keyboard-shortcuts, density-toggle, table-polish, click-to-copy, breadcrumb).

**What's untouched.** Route Assistant panel (`#aes-route-assistant` + `data-aes-theme="dark"`) — its own dark surface and primitives stay. AES-injected panels using `.aes-panel` / `.aes-modal` / `.aes-toast` — those primitives in `components.css` are untouched. Existing accounting/finance AES panels — pass through unchanged. Wicket form behaviour — every override is style-only; no AS classes renamed/removed.

**Verification.** Manual smoke test (no automated harness in this extension): reload extension, visit each AS page family (dashboard, fleet, finance×5, scheduling, inventory, markets, info, ops, settings), confirm panel chrome / tables / forms / modals render brutalist; toggle skin off in options and confirm reversion within ~50 ms; long table sticky header + filter; `g f` navigates; `Shift+D` cycles density; click an AS$ figure → toast confirms copy; press `?` → help popover. RA scheduling page should still render its dark panel without regression.

### Cohesion pass — Aircraft Flight Plan Assistant wire-up + Tier 3 ↔ back-test loop — shipped this session
**Surfaces in:** per-aircraft Flight Plan page (`/app/fleets/aircraft/<id>/0`) + Route Assistant Auto-Pricing apply modal. **Files:** `manifest.json`, `modules/route-assistant/panel.js`.

Closed two integration loops left open by the prior sessions' work. No new features — purely "make the latest deliverables actually reach the user."

**Loop 1 — AFP wire-up.** The eight slice files in `modules/aircraft-flight-plan/` (host, spec-resolver, route-candidates, form-driver, wave-applier, audit-log, settings-extension, state-store) were committed but the manifest only registered Slice A (`host.js`) for `/app/fleets/aircraft/*/0*`. Slices B-F never loaded; none of the `RouteAssistant*` stores they read defensively loaded either. The AFP `content_scripts` entry now declares the full dependency block: 11 RA modules (settings-store, fleet-store, demand-store, score, distance-resolver, type-specs-store, route-overrides-store, route-note-store, watchlist-store, wave-overlay, toast-host) + flightsfrom data-store + aircraft-type-specs + 7 AFP slice files (settings-extension before host so AesAfpSettings is available, then host so the bus exists, then audit-log/spec-resolver/route-candidates/form-driver/wave-applier which all subscribe to the bus at load time), then the `content_aircraftFlightPlan.js` entry script. **Critical de-duplication:** `state-store.js` and the four `schedule-management/*` modules used by wave-applier are NOT included in the AFP block because the user's `/app/fleets*` content_scripts entry already loads them on this URL — listing them in both entries would re-execute the `class` declarations and throw. Cross-checked via Python set-diff on the three matching entries (generic `/app/*`, fleets-star, AFP-zero-tab): zero file overlap, 40 distinct files inject on the AFP URL. AFP module is now reachable end-to-end on the per-aircraft Flight Plan page.

**Loop 2 — Tier 3 dry-run apply → back-test store.** The Tier 3.1 apply path defined a `trigger: "tier3-apply"` value in `sandbox-backtest-store.js`'s entry schema but never invoked `RouteAssistantSandboxBacktestStore.log(...)`. Added the call inside `_openPricingApplyModal`'s `renderResult` closure (panel.js ~13420). Three changes: (1) the modal launch site (`_buildOrsSandboxScenarioCard` → "Apply this scenario as price…" button) now passes `sandboxProjected` (full projected envelope) and `sandboxModelParams` (the projection's model params snapshot) alongside the existing `sandboxScenario` + `projectedDelta`; (2) `_openPricingApplyModal` captures both as closure variables; (3) `renderResult` logs a back-test entry gated on **(a)** `sandboxProjected` being present (only sandbox-launched applies — manual row-context-menu applies have no projection to compare against) AND **(b)** `result.status` being `verified` or `posted` (real writes — dry-run is skipped because the back-fill loop would otherwise associate the projected share with a marketShare snapshot whose prices never actually changed, corrupting slice 3c's bias/RMSE metric). Entry shape mirrors the calibrate-T entry (ts, trigger, scenario, modelParams, projected: {share, paxPerWeek, revenuePerWeek, profitPerWeek}). Back-fill via `_runBulkMarketScrape` → `backfillManyFromMarketShares` is automatic (the trigger string is irrelevant to back-fill; only ts + missing observed.share matter). Slice 3c's "Model fit" row in the settings drawer immediately starts including Tier 3 entries alongside calibrate-T entries once Tier 3.2 ships and real applies start flowing. **Today**, while both gates are blocked, no entries flow yet — the wire is laid for the next slice.

**Toast cross-page continuity.** AFP's audit-log slice (Slice F) emits toasts via `RouteAssistantToast.show()`; the manifest expansion above includes `toast-host.js` so the toast container mounts on the AFP page. The N2 notification bell stays panel-scoped by design (HANDOVER §10 — "N2 popover is not a toast itself … Outside-click + Escape teardown is local"). Cross-page behaviour: AFP toasts flow through `RouteAssistantToast._history` (in-memory, ephemeral) and become visible via the bell on the user's NEXT visit to the RA panel. Closing the AFP tab clears that history slice.

**VFP parser scope.** Validated `host.js`'s outer Visual Flight Plan selector chain (`.as-panel.visual-flight-plan .vfp.vfp-main .day .blocks`) against the captured `/app/fleets/aircraft/6968/0` snapshot — 7 `.day` elements with `.blocks` children, exactly matching `host.js:175`. The captured sample has empty `.blocks` so the per-flight-bar inner shape (`depTimeLocal`, `destination`, `flightNumber`) is still untested against populated data; the existing TODO at `host.js:167-172` documents this and the slice-C/F consumers handle null fields gracefully. Future session: re-validate against a populated capture.

### ORS Sandbox slice 3 (a · b · c) + slice 6d — shipped this session
**Surfaces in:** Route Assistant panel ORS Sandbox results card (`/app/com/scheduling*`) + settings drawer + scenario card. **Files:** `modules/route-assistant/sandbox-backtest-store.js` (NEW), `modules/route-assistant/panel.js`, `manifest.json`.

Three trust+audit slices that close the slice-3 roadmap, plus the slice-6d ergonomics bonus. Together they make Tier 3 Auto-Pricing write-back legible — the user can see whether projections are trustworthy before committing them. 6d ships alongside because it's pure ergonomics (~30 LOC) and the slider reset compounds naturally with the new confidence pill.

**3a — Confidence pill on the Outcome card.** Every projection now shows a small coloured pill above the Outcome table summarising three signals into a high/medium/low level: (1) per-route T-calibration freshness (calibrated <30d → +1, 30–90d → 0, uncalibrated or >90d → −1), (2) ORS data age across cached classes (<7d / 7–30d / >30d), (3) connection-count sample size summed across Y/C/F (≥10 / 5–9 / <5). Aggregate score determines the pill colour; hover for the per-signal tooltip. Pure read of already-loaded data — no fetches, no storage, no model touch. New `_orsSandboxConfidence(route, result)` + `_buildOrsSandboxConfidencePill(confidence)` instance methods next to `_clampedClasses`. ~50 LOC.

**3b — Back-test logging store.** Each Calibrate-T click logs a back-test entry capturing the projection (share / pax / rev / profit + scenario + modelParams) plus the observed share that drove the calibration. Cap 50 entries per route via shift(). Subsequent markets-page bulk scrapes back-fill `observed.share` for entries logged within an 8-day window before the scrape, so calibrations that don't have an immediate observation still gather one when fresh marketShare lands. Tier 3 apply hook can add a second log call with one line — pre-staged via the `trigger: "tier3-apply"` value in the entry shape. New module `modules/route-assistant/sandbox-backtest-store.js` (~150 LOC) mirrors `route-overrides-store.js` pattern: `static PREFIX`, `_pairKey`, `get` / `getMany` / `loadAll` / `log` / `remove` / `backfillManyFromMarketShares` / `_normaliseEntry` / `_normaliseScenario`. Pair key directional. Hooks: `_calibrateOrsSandboxT` re-projects with the new T explicitly so the logged projection matches the calibrated state, then `RouteAssistantSandboxBacktestStore.log(...)`. `_runBulkMarketScrape` calls `RouteAssistantSandboxBacktestStore.backfillManyFromMarketShares(pairs, msByPair, ourEnterpriseId)` after `_applyCachedDemand` — one combined `bulkLoadCache({families: ["marketShare"]})` read so back-fill costs ≈ one extra storage round trip per bulk run. Manifest entry registered between `ors-model.js` and `toast-host.js`.

**3c — Model-fit summary in the settings drawer.** The ORS Sandbox expander gains a new info row above the modelParams inputs: `Model fit: bias +X.Xpp, RMSE Y.Ypp · N routes · M observations · [Refresh]`. Aggregates back-test entries across every route via `RouteAssistantSandboxBacktestStore.loadAll()`; only routes with ≥3 back-filled observations contribute (avoids single-observation noise). Reports bias = mean signed error (positive ⇒ projections under-estimate share) and RMSE = root-mean-square error, both in percentage points. Insufficient-data fallback message when fewer than 3 observations across the whole user. Refresh button reloads + recomputes (cheap — only reads chrome.storage.local). New `_computeOrsSandboxModelFit(allBacktests)` + `_renderOrsSandboxModelFitRow(host)` instance methods near `_clampedClasses`. ~80 LOC.

**6d — Reset-per-slider.** Every Y/C/F price slider, the cargo slider, the frequency input, and the comfort selector now have a small ↺ button next to the readout. Click → restore the route's actual current value (1.0× for price/cargo multipliers, `route.currentFrequency` for freq, 0 for comfort) and dispatch the control's native input/change event so the existing onChange recompute fires. Local helper `mkResetBtn(title, onClick)` defined at the top of `_buildOrsSandboxScenarioCard`. Recovery for "I've explored too far" without leaving the sandbox. ~30 LOC across the four control sites.

### Auto-Pricing Tier 3.1 — shipped this session
**Surfaces in:** Route Assistant panel — Auto-Pricing settings expander · row context menu · ORS Sandbox card · new bulk-apply modal. **Files:** `modules/route-assistant/pricing-applier.js` (NEW, ~530 LOC), `modules/route-assistant/pricing-apply-log.js` (NEW, ~245 LOC), extensions to `modules/route-assistant/panel.js` (~1100 LOC) and `modules/route-assistant/settings-store.js` (`pricing.apply` block + `_mergePricing` deep-merge), `manifest.json`.

The first slice of Auto-Pricing **write-back** to AS. Slice 3.1 is **dry-run only** — the full pipeline (GET handshake → preflight → form-body construction → audit log) runs on every Apply, but no POST hits AS. Tier 3.2 (next session) flips one default to enable real writes; 3.3 adds bulk Apply + silent-auto.

**Form target.** `POST /app/com/markets/<HUB><DEST>?<wicket>-1.-pair-pair~panel-settings-settings~form` with body fields `classes:prices:0:newPrice` (Y), `:1:` (C), `:2:` (F), `:3:` (Cargo), the five General Settings selects (round-tripped at their current values on price-only applies), the four scope checkboxes (`settings:airportPair` / `settings:flightNumbers` / `settings:returnAirportPair` / `settings:returnFlightNumbers`), and the disambiguating submit-button name (default `submit-prices`). Mirrors `ors-scraper.js`'s wicket handshake exactly — fresh GET per Apply (no session reuse) so PageExpiredException can't silently corrupt.

**Two-gate model.** Real writes need BOTH `settings.routeAssistant.pricing.apply.enabled = true` AND `apply.dryRunOnly = false`. Slice 3.1 ships `apply.enabled = false` AND `apply.dryRunOnly = true` — flipping just one doesn't unlock anything. Belt-and-braces: each gate has a separate semantic meaning in 3.2+ (kill switch vs slice gate). The applier instance constructor enforces both — even if a caller passes `{dryRun: false}`, the instance's `dryRunOnly` overrides.

**`RouteAssistantPricingApplier`** (pricing-applier.js).
- `apply(hub, dest, prices, opts)` — async; orchestrates GET → `parseFormContext` → `preflight` → `buildBody` → POST (skipped in dry-run) → response parse + verify. Returns `{status: "dry-run"|"verified"|"posted"|"failed"|"aborted", logId, prevPrices, newPrices, preflight, bodyPreview, ...}`.
- `static parseFormContext(html)` — extracts wicket session, form action path, hidden inputs, current Y/C/F/Cargo prices, defaults, slider ranges (per-class min/max from inline `slider({…})` script regex), and General Settings select values. Returns null when the pricing form can't be located (page format change, not logged in). Trusts the snapshot's `name=` attribute over the static `FIELD_NAMES` map so an AS reorder of the table doesn't misalign indices.
- `static buildBody({formContext, prices, settings, scope, submitButton})` — assembles `URLSearchParams`. Missing classes round-trip at their current value (partial-update safe). Submit button name included as a body field per Wicket convention.
- `static preflight({formContext, prices, warnAboveDeltaPct, cooldownMinPerRoute, lastApplyAt})` — pure-function; returns `{blockers, warnings, deltas, percentDeltas}`. Blockers (disable Apply): `noFormContext`, `noPrices`, `negativePrice`, `outOfSliderRange`, `cooldownActive`. Warnings (informational): `largeDelta` (≥ `warnAboveDeltaPct`).
- `static fingerprint(hub, dest, prices, scope)` — deterministic hash for idempotency dedup (5-min window) by the apply log.
- `_verify(hub, dest)` — re-fetch + parse `currentPrices` for post-write verification (Tier 3.2 entry; ready in 3.1).

**`RouteAssistantPricingApplyLog`** (pricing-apply-log.js).
- Two stores, both written atomically per apply: global timeline `routeAssistant:pricingApplyLog` (cap 200, dedup-merged on identical fingerprint within 5 min) + per-route ring `routeAssistant:pricingApplyLog:<HUB>-<DEST>` (cap 20, no dedup).
- Records: `{id, ts, hub, dest, status, source, scope, prevPrices, newPrices, requestedPrices, verifiedPrices, verifyAt, httpStatus, fingerprint, reason, sandboxScenario, projectedDelta, preflight: {blockers, warnings, deltas, percentDeltas}, error, warning, bodyPreview, dryRun, count}`. Auto-cleanups: function-references stripped, oversized strings truncated.
- API: `add(record, {dedupWindowMs})`, `update(id, patch)`, `getRecent(n)`, `getForRoute(hub, dest, n)`, `getLastSuccessAt(hub, dest)` (for cooldown), `getLastSuccessMap(pairs)` (bulk), `prune(newLimit)`, `clear()`.

**Three apply UX entry points.**
- **Row context menu (primary single-route path)** — third item "Apply price…" added next to the existing override editor + ORS Sandbox drill-in. Opens the per-route preview modal pre-filled with cached `ownPricing.prices`.
- **Sandbox card "Apply this scenario as price…"** — bridge from the simulation card to the write modal. Reads the resolved per-class scenario prices from `_orsSandboxResult.perClass.<cls>.scenarioPrice` (fallback: `current × multiplier`) and seeds the modal inputs. Carries the projected delta envelope (paxPerWeek, profitPerWeek, share, rating) into the apply log so a future back-test can join projected vs observed.
- **Bulk apply modal (3.3 entry point, 3.1 lists routes only)** — opens from the Auto-Pricing expander's "Open bulk apply…" CTA. Table of every visible route with cached pricing; per-row "Open…" button opens the single-route modal. 3.1 lists routes only; 3.3 turns this into a multi-row preview with Apply across selection.

**Per-route apply modal** (`_openPricingApplyModal`). Per-class price grid (Y/C/F/Cargo) with current value + live Δ% colored by `warnAboveDeltaPct` / `requireConfirmAboveDeltaPct` thresholds; scope checkboxes (4); optional reason textarea (≤ 240 chars, persisted to log); sandbox-projection mini-card when launched from the sandbox; collapsible "What gets posted" body preview; preflight host that fills after Dry-run preview with blockers/warnings or "preflight passed". Two action buttons — Dry-run preview (always enabled) + Apply (disabled in 3.1; tooltip explains the gate). Toast on result.

**Auto-Pricing expander Tier 3 sub-section** (`_renderTier3ApplyBlock`). Purple-tinted block under the rose Live-route-data block. Header shows current stage badge (3.1 = amber dry-run, 3.2-disabled = grey, 3.2-enabled = green). Surfaces: rationale text · Apply-enabled toggle · default-scope row (4 checkboxes) · "Open bulk apply…" CTA · live "Recent applies" preview list (last 10 entries, click a row → `_scrollToRouteRow` to that route). Preview re-fetches after every apply via `_refreshAllOpenTier3LogPreviews` so users see entries appear in real time.

**Settings.** New `pricing.apply` sub-block: `enabled` (kill switch), `dryRunOnly` (slice gate), `defaultScope: {airportPair, flightNumbers, returnAirportPair, returnFlightNumbers}`, `roundPolicy`, `cooldownMinPerRoute` (default 60), `warnAboveDeltaPct` (5), `requireConfirmAboveDeltaPct` (15), `pricingApplyLogLimit` (200), `perRouteApplyLogLimit` (20), `submitButton`, `showRecentApplies`, `recentApplyPreviewCount`. Pre-staged for 3.3: `silentAutoMaxPerDay/Hour`, `silentAutoMinDeltaPct`. New `_mergePricing` deep-merge so partial saves don't blow away `defaultScope` siblings.

**Verification against real HTML.** Sample `/Users/jihwan/Desktop/CLAUDE/https-:free1.airlinesim.aero:app:com:markets:JFKATL?869.html` confirmed: wicket session `869`, form action path ends `…-pair-pair~panel-settings-settings~form`, all 4 price input names match (`classes:prices:0..3:newPrice`), all 3 submit buttons present (`p::submit` / `submit-prices` / `submit-settings`), 4 slider ranges parsed from inline scripts (`1..296` / `1..640` / `1..1130` / `1..214` for Y/C/F/Cargo), hidden-fields div empty (matches the ORS form pattern).

**3.2 next.** Flip `apply.dryRunOnly` default to false; modal's Apply button activates when both gates clear; receipt verify post-POST; `_undoableSave` integration so a misclicked write is one toast click away from being reverted (re-applies prevPrices via the same applier path); per-route + global cooldown enforced from `apply-log.getLastSuccessAt`; rate-limit/circuit-breaker on consecutive 429/503 mirrors the ORS pattern.

**3.3 next.** Bulk apply modal turns into a selection table with Apply across N routes; silent-auto loop behind `silentAutoEnabled` with hard caps (`silentAutoMaxPerDay`, `silentAutoMaxPerHour`, `silentAutoMinDeltaPct`); confirmation modal on first activation.

### ORS Sandbox slice 2c — shipped a prior session
**Surfaces in:** Route Assistant panel ORS Sandbox card (`/app/com/scheduling*`) + settings drawer. **Files:** `modules/route-assistant/rating-observation-store.js` (NEW), `modules/route-assistant/rating-alpha-store.js` (NEW), `modules/route-assistant/ors-scraper.js`, `modules/route-assistant/demand-derivator.js`, `modules/route-assistant/ors-model.js`, `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`, `manifest.json`.

Replaces the global `α_price = 8` (rating points lost per +100% price change) with **a per-route, per-class value derived from observations logged on every ORS scrape**, plus a manual override slot. Closes the last open slice-2 item; ORS Sandbox slice 2 is now fully shipped.

**Observation log.** New `routeAssistant:ratingObservations:<HUB>-<DEST>` directional store. After every `RouteAssistantOrsScraper.scrape()` saves (line 616), `_logRatingObservation` reads the matching markets-page `ownPricing` snapshot and writes `{at, prices:{Y,C,F}, ratings:{Y,C,F}, ownConnections:{Y,C,F}, competitorConnections:{Y,C,F}, comfortLevel, pricingScrapedAt, orsScrapedAt}`. Inside the scraper (NOT the panel) so bulk syncs log too — `bulkScrape` at `ors-scraper.js:809` loops calling `scrape()`. Append-only with FIFO cap 50 / 90d age prune on every read; honors `settings.routeAssistant.orsSandbox.ratingObservations.autoLogOnScrape` (default true, stable across versions).

**Regression.** `RouteAssistantDemandDerivator.derive(...)` extended to accept an observations record. New `_ratingPriceElasticity(observations, cls, notes, opts)` runs per class with this filter pipeline: (1) drop pairs where price/rating non-finite or non-positive, (2) drop where pricing/ORS scrape gap > 24h, (3) drop where comfortLevel changed vs prior obs, (4) drop where ownConnections delta ≥ 1 (own-frequency confound), (5) drop where competitor connection count jumped > 20%, (6) need ≥ minObs (default 4), (7) need range(priceDev%) ≥ 8% lever arm, (8) need ≥ 2 distinct priceDev% buckets at 1% rounding, (9) OLS via existing `_linregSlope`, (10) negate ONCE → magnitude, (11) reject if magnitude < 0 (non-monotone), (12) clip to `[0, 50]`. Backwards-compat: legacy 4-arg call `derive(h, i, o, opts)` still works (heuristic detects `opts` shape vs observation record shape).

**Cascade resolver.** Per-route per-class α resolution order in `_recomputeOrsSandbox`: **override > derived (≥minObs) > siblingDerived > fleetMedian > global default**. Sibling cascade only fires from classes whose source is already "derived" on this route (one hop, no cascade-chains). Fleet-median computed in `RouteAssistantPanel._computeFleetMedianAlpha(rows)` once per `_applyCachedDemand` pass; skipped when fewer than 5 routes contributed. Two-pass resolver in `_recomputeOrsSandbox`: first pass resolves override/derived; second pass cascades unresolved classes.

**Sign convention.** α stored everywhere as a **positive magnitude** in `[0, 50]`. The OLS slope is naturally negative on well-behaved routes; negated ONCE inside `_ratingPriceElasticity`. The model formula at `ors-model.js:466` keeps its existing `base − α × priceRatio` shape (no `Math.abs` at the consumer). Panel reads with `Number.isFinite`, never `||`, so a manual override of `0` (route's rating doesn't respond to price for that class) is honored verbatim.

**UI.** New collapsed `<details>` expander INSIDE the Outcome card via `_buildOrsSandboxAlphaExpander(result, route)` — three rows (Y/C/F), each shows the resolved α + source label and lets the user pin a manual value. Save wraps in `_undoableSave` so a misclick is one click of Undo away. Settings drawer's `_renderOrsSandboxSection` gains an "Auto-log rating observations on every ORS scrape" toggle + total observation count + "Reset all observations" button (destructive, confirms before wiping). Model emits per-class source notes ("class Y: α=6.2 (derived from 12 observations)" etc.) into the existing notes footer.

**Why slice 2c, why now.** Tier 3 Auto-Pricing write-back depends on accurate share/profit projections; slice 2 closed three of four model-precision gaps but α stayed global. Slice 2c lets the model self-tune per route as the user runs ORS scrapes during normal play — gated by data accumulation, not session work. Cold-start UX kept sensible by the cascade fallback; no surprise behaviour change on day one.

### ORS Sandbox slice 2 (a · b · d · e) — shipped a prior session
**Surfaces in:** Route Assistant panel ORS Sandbox card (`/app/com/scheduling*`). **Files:** `modules/route-assistant/ors-model.js`, `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`.

Four model-precision slices, all from the slice-2 roadmap. Slice 2c shipped this session — see directly above.

**2b — Independent per-class price control.** The single Y-anchored `priceMultiplier` was replaced with `priceMultipliers: {Y, C, F}`. Three sliders render in the scenario card (one per cabin class with a cached observed fare); routes with only Y cached collapse back to a single slider — visually identical to slice 1. The C/F preview row is gone (sliders show new prices directly). New `RouteAssistantOrsModel._normaliseScenario` is the canonicaliser: accepts both the slice 1 shape (single `priceMultiplier`) and the slice 2 shape, always returns the slice 2 shape. The pax-pool elasticity now reads the **Y** multiplier only — pax pool is Y-anchored per the demand-derivator's series. Saved scenarios under the legacy shape auto-migrate at `_mergeOrsSandbox` read time via the new `_coerceScenarioMultipliers` helper that clamps each class to `[0.30, 3.00]`.

**2a — Frequency synthesis.** When `newFreq > currentFreq`, the model now synthesises additional own-connection rows before per-class ranking instead of greying out rank/share. Implementation: `_synthesizeOwnConnections(classRec, baseFreq, requestedExtras, notes, cls)` clones a template (preferring own + nonstop, else highest-rated own); each clone keeps the template's rating, totalDuration, totalPrice, sets `bookable: true`, deep-clones legs with `isOurs: true` forced, distinct synthetic flightCode (`synth-<cls>-<i>`). **No timestamps** are needed — connection schemas in cache have none and the model's rank/softmax doesn't read them. Saturation is capped at +ceil(0.5 × baseFreq) via `_capSynthesisCount` so adding 30 synth rows to a 5-row list can't push share to ~1.0 in a way the user can't sanity-check; the cap also respects `MAX_FOR_SOFTMAX`. `newFreq < baseFreq` keeps the slice-1 LF-dilute behaviour (you can't tell which connections to drop without timestamps); a note surfaces this asymmetry. Notes also call out synth count, saturation cap, and "no own connection to clone" skip.

**2d — Cargo branch.** `perClass.CARGO` is now populated when `route.orsByClass.CARGO.connections[]` is cached, alongside Y/C/F. New `_projectCargo(arg)` mirrors `_projectClass` but simpler — no comfort lift, no rating shift (the `cargoMultiplier` scales yield only). A new cargo slider appears in the scenario card when CARGO is cached; it scales `cargoYieldPerKgKm` via the existing profit-estimator `override.cargoYieldPerKgKm` hook (no estimator changes needed — verified at `profit-estimator.js:200` where `effectiveCargoYield × cargoLoadFactor × distance` folds into revenue). The cargo demand pool also shifts via `cargoElasticity` when `cargoMultiplier !== 1` so `useRealDemandForLF` users see demand drop proportionally. Output envelope adds `baseline.cargoPerWeek` / `projected.cargoPerWeek` / `delta.cargoPerWeek` (separate from `paxPerWeek` so pax aggregates stay clean); the results card renders a Cargo/wk row when present. `_assembleOrsSandboxRoute` now propagates `cargoElasticity` from the demand-derivator output. Notes: "cargo multiplier scales yield only — no rating shift modelled" when `cargoMultiplier !== 1`; "no CARGO connection list cached — cargo profit unchanged by share" when only the pool is cached.

**2e — Visual rating-clamp annotation.** The Outcome card's `renderRow` now accepts an `annotation` argument; the Rating row passes one built by `_clampedClasses(result)` that scans `result.perClass.<cls>.priceRatio` (matches the model's `RATING_CLAMP_LOW/HIGH = 0.5/1.5`). When any class clamped, the projected Rating cell shows a `*` and an amber colour with a tooltip listing which classes hit the floor/ceiling — no more reading the notes footer to know that a rating projection was clipped.

### ORS Sandbox slice 1.5 — shipped this session
**Surfaces in:** Route Assistant panel ORS Sandbox card (`/app/com/scheduling*`). **Files:** `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`.

Two correctness/ergonomics fixes on top of slice 1 — small but they close gaps the user would have hit on their second route.

**Per-route scenario state.** Slice 1's `lastScenario` was a single global record — switching from JFK→LAX to JFK→ORD inherited the prior route's slider positions (almost always wrong). Replaced with `lastScenarioByRoute: {[hub-dest]: scenario}`. The `_mergeOrsSandbox` deep-merge validates each entry's shape (numeric `priceMultiplier`, numeric-or-null `frequency`, numeric `comfortDelta`) and silently drops malformed records. **One-shot legacy migration:** when settings load and a stored config has a global `lastScenario` plus `lastRouteIata`, settings-store surfaces it as `_legacyLastScenario` (a transient field, never re-persisted); the panel reads it as a fallback exactly when the user reopens the route they had open pre-upgrade. First save through `_recomputeOrsSandbox` writes per-route and `delete`s both legacy keys, so the migration completes silently on the first slider drag.

**Calibration freshness.** Markets drift; a 30-day-old T is worth re-running. `perRouteTemperatureCalibratedAt: {[hub-dest]: ms}` sibling map captures the timestamp on every Calibrate-T click. The sandbox's T banner now reads "Using calibrated T = 18.4 for this route · global T = 25 · calibrated 12d ago [reset]" with the age coloured grey (<30d), amber (≥30d, "consider recalibrating"), or red (≥90d, "recalibrate"). The settings drawer status line shows "(N stale ≥30d)" so users see calibration decay across all routes without opening each one. Reset-all and per-route reset both clear the timestamp alongside the T.

**Why it's slice 1.5 not slice 2:** these fix slice 1's stated UX limitations (per-route scenario was implicit-but-wrong; calibration drift was silent). Slice 2 is the model precision work (cargo, frequency synthesis, independent class pricing) — see "ORS Sandbox roadmap" below.

### Polish bundle — shipped this session
**Surfaces across:** route-assistant panel + multi-class ORS settings + helpers. **Files:** `.gitignore`, `helpers.js`, `modules/route-assistant/{panel,settings-store,inventory-page-scraper,ors-scraper}.js`.

Cross-cutting fixes after a beta-readiness audit. None change behaviour visibly to a normal user; all close silent footguns.

| Fix | File | Why |
|---|---|---|
| Scratch HTML/txt fixtures gitignored | `.gitignore` | Five untracked dev-reference files (`USA Stations.html`, `created station after.html`, `html - for smaller countries without reigions.html`, `station alabama.html`, `aircraft.txt`) would have packaged into the .crx (~218 KB plus a leaked spec note). |
| `escapeHtml` hoisted to helpers.js | `helpers.js`, `panel.js` | `wave-overlay.js` (manifest line 133) calls `escapeHtml` but it lived in `panel.js:8087` (loads after). Worked at runtime only because methods aren't called at parse time. Helpers.js is in the first content-script block — every page guarantees it's available. |
| `_mergeOrs` renormalises classWeights | `settings-store.js:_mergeOrs` | A stored partial `{ECONOMY: 1.00}` merged with defaults `{0.75, 0.20, 0.05}` summed to 1.25; composite ORS scorer didn't renormalise. New `_renormaliseWeights` helper clamps non-negative entries and divides through sum. |
| `_diffConfig` tolerant of reordered set arrays | `panel.js:_diffConfig` | Export-import roundtrip flagged `classesToScrape: ["ECONOMY","BUSINESS","FIRST"]` vs `["FIRST","BUSINESS","ECONOMY"]` as changed. Added `_DIFF_SET_PATH_KEYS` whitelist (`classesToScrape` only currently) + `_arrayEqualsAsSet` helper; ordered arrays still diff strictly via `_arraysEqual`. |
| Legacy `defaultPayload` dropped on save | `settings-store.js:_mergeOrs` | Lazy-rename to `primaryClass` (slice-2-multi-class shape) was reading the legacy field but never deleting it; round-tripped forever. `delete out.defaultPayload` after the rename. |
| `parseIntSafe` decimal-aware | `inventory-page-scraper.js` | Old impl stripped `.` and `,` indiscriminately — `"$1,234.56"` → 123456 (100× too big). Latent (`avgFare` not consumed downstream) but a footgun. New impl detects the rightmost separator with a 1–2 digit tail as a decimal point; thousand separators stripped. 12/12 smoke-tests pass. |
| Route-note popover unsaved-state UX | `panel.js:_openRouteNotePopover` | Cancel/outside-click/Escape now confirms before discarding unsaved typing. Save is disabled while textarea is empty (only Delete can remove an existing note). Save with unchanged text early-returns instead of writing a fresh `updatedAt`. |
| Restored ors-scraper comments | `ors-scraper.js` | Two helpful inline comments (`PageExpiredException` watch + `safetyCap` rationale) were dropped during the multi-class refactor. Restored. |

### Q4 saved table views + Q6 retirement planner + Q10 opening checklist — shipped this session
**Surfaces in:** controls bar (Q4 view dropdown), 🛩 panel-header button (Q6 modal), right-click on NEW-status rows (Q10 popover). **Files:** `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`.

Operational planning slice — three independent items shipped together. All read-only against AS; Q4 is the foundation primitive.

**Q4 saved table views.** New `settings.routeAssistant.savedViews: []`. Each entry: `{id, name, filters, viewMode, sortField, sortDir, compactView, createdAt}`. Quick-switch dropdown + Save-current button (`+`) + Delete prompt (`🗑`) rendered in the controls bar via `_renderSavedViewsControl(host)`. Selecting a view writes its snapshot back into settings (deep-merge for `filters.statuses` so a save predating a new status type still loads cleanly) and re-renders. Save-current snapshots the current state via `window.prompt` for the name. Delete via numeric prompt; toast comes with one-click Undo via the existing toast `action` slot.
- **Snapshot covers**: filters block, view mode (pax/cargo/all), sort field + direction, compact view.
- **Snapshot does NOT cover**: search query, Q1 quick-filter chip toggles, watchlist starring. Intentionally ephemeral — saving them would surprise users when applying an old view.
- **Composes with future column-customization slices**: when U7 / U8 ship, `columnOrder` and `columnVisibility` extend the snapshot shape and the existing apply path picks them up automatically.

**Q6 aircraft retirement planner.** New 🛩 button in panel header opens `_openRetirementPlanner()`. Modal lists every owned aircraft sorted by age desc with retirement timeline. `MAX_LIFE_YEARS = 25` (per AS rules). Header strip with a numeric "highlight aircraft retiring within N months" input (default 24); summary chip shows count within threshold. Per-row table:
- Reg. · Equipment · Age (yr) · Until retire (yr) · Status · "Find replacement →" link
- Rows past life: red background + ⌛ pill
- Rows within threshold: yellow background + ⏳ pill
- "Find replacement" opens the dashboard's Used Aircraft Scanner in a new tab.

Reads from `this.fleet.aircraft` (already populated by `RouteAssistantFleetStore`). No new I/O.

**Q10 route opening checklist.** New right-click menu entry "Opening checklist…" rendered conditionally when `row.status === "NEW"`. Opens `_openRouteOpeningChecklist(row)` modal with 7 prerequisites:
1. Distance resolved (from `row.distanceKm`)
2. AS demand seeded (from `row.paxScore`)
3. Aircraft selected (from `_fleetContext()`)
4. Service profile set (from `serviceConfigMap`)
5. Markets scraped (from `row.ownPricing` / `row.marketSharePeriod`)
6. ORS connection list cached (from `row.orsByClass`)
7. Carriers list cached (from `row.carriers`)

Each item shows ✓/✗ + a one-line hint + a "fix this →" button when ✗ that opens the relevant AS page or fires a toast pointing at the right Settings expander. Header summary: "X / 7 ready". Pure derivations from existing row decorations + cached records — no fresh I/O, no new storage keys.

### Q3 override expiration dates — shipped this session
**Surfaces in:** override editor + Dest cell. **Files:** `modules/route-assistant/route-overrides-store.js` (extended), `modules/route-assistant/panel.js` (extended), `modules/route-assistant/aggregator.js` (extended).

"Test this LF for 4 weeks then revert" — overrides now carry an optional `expiresAt` unix-ms timestamp. The full override editor gains an "Expires in N days" input (empty = never expires, default behaviour preserved). Once expired, the record stays in storage with a ⌛ marker on the Dest cell so the user can see + clear it; the aggregator passes `null` to the estimator so $/flt/$/wk reflect the demand-driven defaults.

**Store extension (`route-overrides-store.js`):**
- `_clean` accepts optional numeric `expiresAt` (any positive timestamp; past values are valid — the store doesn't reject them since callers handle expiry).
- `_hasAnyValue` extended so a save with only `expiresAt` doesn't accidentally collapse to a `remove()`.
- New static helpers: `isExpired(record, nowMs)` (true when `expiresAt ≤ now`) and `daysUntilExpiry(record, nowMs)` (negative = already expired, in days).
- File header documents the new field; key shape gains the optional `expiresAt`.

**Override editor (`_openOverrideEditor`):**
- New "Expires in" row below "Note" — number input (0–3650 days) + live-updating hint text. Empty = "days · empty = never expires"; positive value = "expires <localized date>"; pre-filled with current days-remaining when an unexpired `expiresAt` is in the record (or the localized expiry date when already expired, in red).
- Save translates the days input into `expiresAt = Date.now() + days × 86400000`. Empty / 0 / non-numeric → null (clears expiry). Editing an existing override RESETS the clock at save time — the user re-confirms intent.

**Cross-save preservation:** every other save path now carries `expiresAt` through unchanged so the user's TTL doesn't get wiped on incidental yield/LF tweaks elsewhere:
- Profit-modifier popover Save: passes `expiresAt: existing.expiresAt` (the popover doesn't expose an expiry input).
- Profit-modifier popover Clear → Undo restore: re-saves prev with prev's `expiresAt`.
- Q2 bulk override `_applyBulkOverride.mergeRecord`: prev's `expiresAt` carried through; bulk modal doesn't expose an expiry input either (would clutter the bulk UI).
- Q2 Undo restore: prev's `expiresAt` re-saved.

**Aggregator skip-applying:**
- The estimator call site at `aggregator.js:259` now gates the `override` parameter through `RouteAssistantRouteOverridesStore.isExpired(row.override) ? null : row.override`. The estimator falls back to demand-driven LF / configured base yield when expired — exactly the same path it would take if no override existed.
- The override RECORD remains attached to `row.override` so the UI can render the ⌛ marker and the editor can still display the expired values for review.

**Row decoration in `_renderRows`:**
- After `_detectStatusTransitions`, a new walker decorates every row with `_expired` (boolean), `_expiresSoon` (boolean, true when 0 < days ≤ 7), and `_expiresDays` (number, can be negative). Pure derivation from `row.override.expiresAt` against `Date.now()`.

**Visual marker** in the Dest cell render closure:
- Existing 📌 pin stays (route has any override).
- ⌛ glyph (grey, "Override expired Nd ago — falling back to defaults") appended when `row._expired`.
- ⏳ glyph (yellow, "Override expires in N days …") appended when `row._expiresSoon`. Tooltip nudges the user to right-click to renew or clear.
- No new icon when override is fresh (no expiry, or expiry > 7 days away) — keeps the cell clean for the common case.

**Limits:**
- Q9 bulk frequency template (also blocked on Tier 3 write-back) doesn't have any UI for setting expiry — same reason as Q2: the bulk modal is too cluttered. Per-route editor is the only place to set/edit expiry.
- The `expiresSoon` threshold is hardcoded at 7 days. If users want a different threshold the natural place is `settings.routeAssistant.overrides.expiresSoonDays` — defer to a follow-on slice if requested.
- Already-expired records aren't auto-deleted. They stay in storage until the user explicitly clears via the override editor's "Clear" button or via Q2 with all fields ticked + applied (which writes a fresh record). Auto-cleanup-on-mount is a follow-on if the user reports storage growth.

### Q8 status-change history per route — shipped this session
**Surfaces in:** St cell tooltip on every row of the Route Assistant table. **Files:** `modules/route-assistant/status-history-store.js` (NEW), `modules/route-assistant/panel.js` (extended), `manifest.json`.

Operational memory for long-running games. Was this route always OVER, or did it pop into OVER yesterday after a frequency increase? Hover the St cell and the answer is right there: `OVER since 12d · was OK before · 4 transitions tracked`.

**Detection.** A new `_detectStatusTransitions(rows)` step in `_renderRows` (after scoring + watchlist + alert evaluation, before sort) walks every scored row and:
1. Looks up the route's history in `this.statusHistoryMap`.
2. If the latest entry's `to` matches `row.status`, no-op (dedup).
3. Otherwise builds a new transition `{from: <last.to | null>, to: row.status, at: Date.now()}` and:
   - Updates the in-memory map immediately so the St cell tooltip reflects the new transition without waiting for the storage round-trip.
   - Fires `RouteAssistantStatusHistoryStore.appendTransition(...)` as fire-and-forget.
4. Decorates `row._statusHistory = record` regardless of whether a new transition was appended this render. The St render closure reads `row._statusHistory` directly — keeps the closure free of `this`-dependence.

**Store** at `routeAssistant:statusHistory:<HUB>-<DEST>` → `{hub, dest, transitions: [{from, to, at}, …], updatedAt}`. Append-only with capacity capped at `MAX_TRANSITIONS = 20` per route — so a long-running game doesn't accumulate unbounded storage. Pair key is **directional** (matches override/note stores). Static API: `get / getMany / appendTransition / latestTransition / remove`. CRUD pattern mirrors `route-note-store.js`.

**St cell tooltip extension.** The status column's render closure (originally just `td.title = "${status}: ${description}"`) now composes a multi-line tooltip:
```
OVER: route flying over capacity vs scored seats
OVER since 12d · was OK before
4 transitions tracked
Recent: OK→OVER (12d), UNDER→OK (35d), NEW→UNDER (61d)
```
Uses the existing `_formatRelativeTime` helper with the `" ago"` suffix stripped. Maxes the recent-history footer at the 5 newest entries.

**Bulk-load on refresh.** `refresh()` calls `RouteAssistantStatusHistoryStore.getMany(pairs)` once for every visible destIata; the result lands in `this.statusHistoryMap` keyed by `<HUB>-<DEST>` uppercased. Renders read from the in-memory map — no per-render storage round-trip.

**Limits / out of scope:**
- The first-ever observation of a route appends `{from: null, to: <status>, at: now}` — the tooltip displays this as "first observed" since there's no prior status. This means every route starts accumulating history on first sight.
- Status-history storage growth is bounded at 20 transitions × every route × every hub. At scale (50 hubs × 200 routes per hub × 20 transitions × ~40 bytes) ≈ 8 MB, well within `unlimitedStorage` permission.
- No alert-trigger integration yet — Q+ alert rules don't read `_statusHistory` (they check `_diff` only). Adding "alert me when status changes to OVER" is a follow-on slice.

### Cluster A — Bulk operations (U5 + U12 + Q2 + Q5) — shipped this session
**Surfaces in:** Route Assistant table (`/app/com/scheduling*`). **Files:** `modules/route-assistant/panel.js`.

Foundation primitive (multi-select) + 3 follow-on bulk workflows. Read-only against AS — every mutation routes through `_undoableSave` so any bulk write is one click of Undo away.

**U5 multi-select rows.** Constructor adds `_selectedRoutes = new Set()` (destIata uppercased) + `_selectAnchorDest` (for shift+click range). Built inline in `_buildTable`:
- New checkbox column at index 0 (NOT in `RouteAssistantPanel.COLUMNS` — schema for sort/group/scoring/freeze stays clean).
- Header has a select-all checkbox with three states (checked / indeterminate / unchecked) recomputed each render against the visible row set.
- Body checkboxes wired to `_handleRowSelectClick(row, ev)`: plain click toggles + sets anchor; shift+click ranges from anchor to clicked in current visible order; ⌘/ctrl+click = plain (checkboxes already discrete).
- Selection survives sort changes (anchor + set are tracked by destIata).
- Filter changes drop hidden routes via the intersection at end of `_renderRows`; fires a 4-second info toast `"3 routes hidden by filter — selection updated"` when ≥1 drops.
- Selected rows get a 3px purple left-edge band (`border-left: 3px solid #a78bfa`).
- Right-click menu (`_openRowContextMenu`) gains a conditional `"Add <DEST> to selection (N)"` / `"Remove <DEST> from selection (N)"` entry when ≥1 selected.

**U12 selection footer.** `_renderSelectionFooter(visibleRows)` mounted into `tableHost` after `_buildTable` runs. Sticky-bottom (`position: sticky; bottom: 0; z-index: 3`) so it pins to the visible edge during long-table scroll. Skipped entirely in Wave View / ORS Sandbox / heatmap modes (selection state is preserved in memory, footer just doesn't render). Layout: count chip · Σprofit/wk · avg score · avg ourPaxShare · Edit-overrides button · Compare button (enabled only at exactly 2 selected) · Clear button. All metrics reduce from `_selectedRoutes ∩ scoredRows` in one pass.

**Q2 bulk override editor.** `_openBulkOverrideEditor(selectedRows)` opens a modal with: chip list of selected routes (× per-chip removes from selection) + 4 input rows. Each input row is `[apply-this-checkbox] [label] [number-input]` — the input is disabled + muted when "apply this" is off, so users can't accidentally write a value they didn't intend to apply. Placeholder text: blank (no override), the value (all routes agree), or `"varies"` (routes have different existing values). Confirm dialog above N=20; progress toast above N=50.
- Apply path goes through `_applyBulkOverride(selectedRows, fieldsToApply)`:
  1. Capture every prev override into `prevByPair` before any save.
  2. For each selected route, build merged record = prev fields preserved EXCEPT for the user's checked fields. Critical: `RouteOverridesStore.save` is full-replace (route-overrides-store.js:80–87), so we MUST merge with prev or we lose untouched fields per route.
  3. `Promise.all(routes.map(...))` parallel writes — ~10× wall-time savings at N=50.
  4. Update `overrideMap` + `row.override` per-route.
  5. Single `_recomputeProfit()` at the end (not per-route — major perf win).
  6. Wrap in `_undoableSave` with batched restore that re-saves prev records in parallel (or `remove(...)` when prev was null).

**Q5 compare two routes modal.** `_openCompareModal(selectedRows)` triggered from the U12 footer's Compare button (visible only when `_selectedRoutes.size === 2`). 3-column table (route A · route B · Δ) for ~15 fields. Direction-aware Δ coloring: green when A's value moves the score in the better direction relative to B (per-field `dir: "higher" | "lower" | null`); red opposite; neutral grey when one side is null OR direction is undefined. `"—"` for missing fields with a one-line legend at the top. Per-route Open-in-AS buttons in footer.

### Cluster B — Decision-support overlays (U11 + per-hub topRoutes + Q13) — shipped this session
**Surfaces in:** Route Assistant table hover (U11) + new 🗺 panel mode (Q13). **Files:** `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`.

**U11 hover row preview card.** Singleton floating card reused across rows. `mouseenter` schedules a 200ms dwell via `_scheduleRowPreview(row, anchorEl)`; mouseleave starts a 150ms grace dismiss via `_scheduleRowPreviewDismiss()`. Cursor entering the card itself cancels the dismiss timer so users can hover the card to copy values.
- Card contents: title (HUB→DEST + city) · status pills (★ starred / 🛠 override / 📝 note / status / aircraft fit) · per-flight breakdown table (pax revenue · cargo revenue · fuel · crew · maintenance · other fixed · profit/flight · profit/week, color-coded green/red on profit lines) · key stats line (score · pool · RM% · ORS gap · share %) · footer (distance · weekly flights · competitors) · inline route note when present.
- Position: prefer right of row; fall back to left; below if neither fits. Viewport-clamped vertically so the card never escapes.

**Per-hub topRoutes refactor (foundation for Q13).** `_publishTopRoutes(visible)` now writes TWO keys per render:
- `routeAssistant:topRoutes` — single global key, current hub. **Legacy contract preserved** so the Used Aircraft Scanner consumer (which reads this for J slice 4 route-fit) keeps working unchanged.
- `routeAssistant:topRoutes:<HUB>` — NEW per-hub key. Each panel mount writes its own hub's row here. Q13 reads from all of them keyed by `recentHubs` to build the matrix.
Slim record now also includes `profitPerWeek` + `ourPaxShare` (used as Q13 metric options alongside score). New static method `RouteAssistantPanel.loadAllHubTopRoutes(hubs)` returns `Map<HUB, blob>` for the heatmap renderer.

**Q13 yield heatmap matrix view.** New 🗺 button in panel header next to 🧪 Sandbox. `settings.routeAssistant.heatmap.{enabled, metric: "score" | "profit" | "share"}`. `_toggleHeatmap` persists; `_renderHeatmap(sorted)` branches in `_renderRows` after Wave View + ORS Sandbox checks (mutual exclusion — wave wins, sandbox second, heatmap third).
- Y axis: current hub first + every other recent hub (deduped).
- X axis: union of all hubs' top-50 destinations, sorted by current hub's score desc.
- Cells: colored chip showing the user-selected metric. Color scale spans the full visible range — three-stop gradient (deep blue → muted amber → vibrant green) via `_heatmapColor(t)`. Click any cell → opens `/app/com/scheduling/<HUB><DEST>` in a new tab.
- Empty cells (`·` in muted color) when a destination isn't in that hub's cached topRoutes (visit the hub once to populate).
- Sticky first column (hub label) + sticky header row (dest IATAs) so the grid stays navigable on wide hub-rich data.
- Header strip: title + "<N> recent hubs" subtitle + Metric selector (Score / Profit / week / Pax share %).
- Empty-state banner when fewer than 2 hubs visited or no cached topRoutes records exist.

### N2 Notification center + Q1 chip bar + Q15 hub quick-swap + progress-toast propagation — shipped this session
**Surfaces in:** Route Assistant panel header (🔔 button), controls bar (chips + hub strip), corner toasts during bulk syncs. **Files:** `modules/route-assistant/toast-host.js` (extended), `modules/route-assistant/panel.js` (extended), `modules/route-assistant/settings-store.js` (pre-staged by user this session).

A coherent foundation cluster on top of the toast primitive. Each item independently shippable; together they turn "powerful but dense" into "fast to operate."

**Q1 — Quick-filter chips bar.** Pill toggles for content-shape filters: ★ Watch, 💸 Loss, 🛠 Override, 📝 Note, Δ Changed. Settings pre-staged in `filters.{watchlistOnly, lossMakers, hasOverride, hasNote, onlyChanged}`. `_renderQuickFilterChips(host)` renders the strip into the controls bar after the search input. Each chip flips its setting field, persists, and re-renders. Active chips fill with their accent color (gold / red / purple / blue / green); inactive are outline-only. Composes with existing filters (status, minScore, fleetFlyableOnly, search).

**Q15 — Hub keyboard quick-swap.** `settings.recentHubs` (capped at 5, deduped case-insensitively, most-recent-first) tracks every hub the panel has mounted on. `_trackRecentHub(iata)` called from `refresh()` pushes the current hub. `_renderRecentHubsBar(host)` renders chips in the controls bar — each chip shows `<HUB><sub>N</sub>` with a digit that mirrors the Alt+N shortcut. Click chip → navigates to `/app/com/scheduling/<HUB>` in the same tab. `_attachHubShortcuts()` (called from constructor) wires Alt+1..5; `_detachHubShortcuts()` (called from `dispose`) cleans up. Renders nothing when only one hub has been visited (zero clutter on first install).

**Progress-toast propagation** — wired `RouteAssistantToast.progress` into 5 more bulk-sync sites:
- `_runBulkPriceScrape` (single-phase)
- `_runBulkCarrierScrape` (single-phase)
- `_runBulkEnterpriseMetaSync` (single-phase, dynamic todo count)
- `_runRefreshContractualPartners` (single-phase, by enterprise id)
- `_runBulkMarketScrape` (3-phase: families → historic → inventory; phaseLabel changes per phase, single rolling bar across the union)
- `_runBulkDemandSync` (2-phase: historic + inventory)
Each follows the same shape: open `progressHandle` on entry, pump `update(...)` from inside `onProgress`, call `complete(...)` with terminal type (success / warn / error) at the end. Bulk-sync UX is now uniform across all 6 (ORS already done in the previous slice).

**N2 — Notification center.**
- `RouteAssistantToast` now keeps a session-scoped `_history` array (capped at 50) of every toast fired. Each entry: `{id, message, type, timestamp, action: {label, fn} | null}`. The `action.fn` is kept by reference — history is in-memory only, NOT persisted to chrome.storage (functions aren't serialisable). Closing the tab clears it.
- `RouteAssistantToast.getHistory()` / `clearHistory()` accessors.
- New 🔔 panel-header button between settings ⚙ and config ⇅. Click opens a popover anchored under the bell.
- Popover: header (🔔 title · count · Clear button) + scrollable body (reverse-chronological list, each row = colored type-dot · message · "3m ago" · "↻ Undo" hint when re-runnable) + footer hint.
- Click any row with an action → re-fires `action.fn()`. Errors caught + reflected via `RouteAssistantToast.error`. Closing the popover triggers automatic outside-click + Escape teardown.
- New module-scoped helper `_formatRelativeTime(ts)` — "just now" / "12s ago" / "3m ago" / "1h ago" / "2d ago" / localised date for ≥ 7 days.

### Active prompts (Q+, foundation extension) — shipped this session
**Surfaces in:** Route Assistant settings drawer + floating toasts. **Files:** `modules/route-assistant/alert-rules-store.js` (NEW), `modules/route-assistant/alert-evaluator.js` (NEW), extensions to `panel.js` + `toast-host.js`, `manifest.json`.

Turns the panel from passive data into active prompts. Declarative per-row rules — "fire a notification when RM% jumps > 5pp on a starred route" or "alert me when the ORS rating gap drops below 10" — that evaluate on every panel mount against the diff-decorated rows and fire toasts for triggers.

**`RouteAssistantAlertRulesStore`** — persistent rules store at `routeAssistant:alertRules` → `{rules: [<RuleRecord>], updatedAt}`. Each rule:
```js
{
  id, label, field,
  operator:      "increased_by" | "decreased_by" | "above" | "below",
  threshold:     <number>,
  scope:         "watchlist" | "all",
  severity:      "info" | "warn" | "error",
  enabled:       <bool>,
  cooldownHours: <number, default 24>,
  lastFiredByRoute: {<HUB>-<DEST>: <ms epoch>},
  createdAt, updatedAt
}
```
CRUD via `add` / `update` / `remove` / `recordFired` / `load` / `saveAll`. Records normalised on every read so malformed rules don't crash the panel.

**`RouteAssistantAlertEvaluator`** — pure-function `evaluate(rules, rows, ctx)` returning the list of triggered alerts. No side effects, no DOM. Reads `row._diff.<field>` for `increased_by` / `decreased_by` operators, or `row[<field>]` for `above` / `below`. Watchlist scope short-circuits to rows where `row._starred`. Per-rule per-route cooldown suppresses repeats within `rule.cooldownHours`. `availableFields()` exposes the catalog of 11 valid alert targets (score, demand, competition, ORS, RM tightness) to the settings UI dropdown.

**Panel wiring:**
- Constructor: `_alertRules = []` + `_alertFiredThisMount = new Set()`. Fired-set prevents the same rule+route from re-toasting on intra-mount re-renders.
- `refresh()` reloads rules via `_loadAlertRules()` and resets the fired set.
- `_renderRows` calls `_evaluateAndFireAlerts(this.scoredRows)` AFTER `_decorateRowsWithDiffs` and AFTER `_starred` decoration. Each triggered alert fires `RouteAssistantToast.show(message, {type, duration: 8000, action: {label: "View", fn: () => _scrollToRouteRow(dest)}})` and persists `recordFired(ruleId, routeKey)` for cross-mount cooldown.
- `_scrollToRouteRow(destIata)` finds the matching row in the current table, smooth-scrolls into view, flashes amber background for 2s.

**Settings UI (`_renderAlertRulesSection`)** — amber-tinted expander mounted below the ORS Sandbox section. Lists existing rules with enable toggle + severity dot + scope chip + cooldown chip + delete (×, undoable via `_undoableSave`). Add-rule row: field dropdown (from `availableFields()`) + operator dropdown + threshold input + scope dropdown + severity dropdown + Add button.

### N1 progress toasts — shipped this session
**Surfaces in:** floating toasts during long-running bulk syncs. **Files:** `modules/route-assistant/toast-host.js` (extended), `panel.js`.

Extends the toast primitive with three new methods:
- `RouteAssistantToast.progress(message, opts)` — creates a STICKY toast (no auto-dismiss) with an inline progress bar. Returns `{id, dismiss(), update(patch), complete(patch)}`.
- `RouteAssistantToast.update(id, {message?, progressPct?, progressLabel?})` — updates the message + bar fill + secondary label in place.
- `RouteAssistantToast.complete(id, {type?, message?, duration?})` — converts the sticky toast to auto-dismissing with terminal type (success / warn / error). Bar fills to 100%, restyles, auto-dismisses after `duration` (default 3.5s).

**Wired into `_runBulkOrsScrape`** as the canonical example. The existing `onProgress` callback now also pumps `progressHandle.update(...)` with `progressPct = 100 × done / total` and `progressLabel = "12 / 87 routes"`. On completion: `progressHandle.complete({type: "success" | "warn" | "error", message: ...})`. User can tab away from the panel during a 5+min scrape and watch the bottom-right corner instead of the inline status line.

**Future wirings (~5 LOC each):** `_runBulkMarketScrape`, `_runBulkDemandSync`, `_runBulkCarrierScrape`, `_runBulkEnterpriseMetaSync`, `_runRefreshContractualPartners`. Each already has an `onProgress` callback — adding the progress toast handle is mechanical.

### Q11 single-step undo (foundation extension) — shipped this session
**Surfaces in:** Route Assistant panel save dialogs (`/app/com/scheduling*`). **Files:** `modules/route-assistant/panel.js`.

A panel-level helper `_undoableSave({label, perform, restore, afterRestore, type, durationMs, actionLabel})` that wraps any save call. Caller captures the previous state first, hands the helper a `perform` closure that runs the save and a `restore` closure that re-applies the captured state. On success, the helper fires a `RouteAssistantToast.show(...)` with an Undo button wired to `restore`. Default duration 6000ms (longer than usual to give time to react). On failure the helper fires an error toast and re-throws so the caller can keep its own error path.

**Wired into 4 sites this session:**
- Profit-modifier popover Save → "Override saved for JFK→LAX [Undo]"
- Profit-modifier popover Clear → "Override cleared for JFK→LAX [Undo]"
- Route-note popover Save → "Note saved for JFK→LAX [Undo]"
- Route-note popover Delete → "Note deleted for JFK→LAX [Undo]"

Each site captures the prev `row.override` / `routeNoteMap.get(pairKey)` record into a closure, runs the save, then exposes a restore that handles both the "had a previous record → re-save it" and the "previously empty → remove the new save" paths. After Undo: a 2.5s "Reverted" info toast.

**Future call sites** (~15 minutes each to wire): full override editor (line ~8964), service-config popover Save / Clear (line ~8809 / 8740), batch override editor (line ~5100), settings export/import Apply, ORS Sandbox Calibrate-T (already has banner — could add Undo for the per-route T overwrite).

**Why it's foundation:** every store-save call site now has a stock pattern for "make this reversible." Future stores (alert thresholds, saved table views, strategy presets, etc.) will adopt it for free. Cumulative QoL — the user's anxiety-cost of clicking Save drops to near-zero because the worst-case is one click of Undo away.

### Foundation QoL primitives — shipped this session
**Surfaces in:** Route Assistant panel (`/app/com/scheduling*`). **Files:** `modules/route-assistant/toast-host.js` (NEW, ~167 LOC), extensions to `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`, `css/content.css`, `manifest.json`.

A small foundation slice — primitives that future features can hook into, not iteration on existing ones:

- **U14 — Toast notifications.** New `RouteAssistantToast` global with `show(message, opts)` / `dismiss(id)` / `clearAll()` / `info|success|warn|error` shortcuts. Singleton DOM container at the bottom-right of the viewport, slide-in animation, auto-dismiss timers (default 3.5s; pass `0` for persistent), optional action button (e.g. Undo). Stack capped at 6; oldest dismissed when exceeded. Re-using an `id` replaces the existing toast in place (good for spammed save calls).
  - **Distinct from the existing `_noteToast`** — `_noteToast` is a single inline status line under the panel header (still in use at 17 sites). `RouteAssistantToast` is for *floating transient* notifications with optional one-click actions. Both kept; future features pick the right one for the situation.
  - **Wired into 3 sites this slice (validation, not exhaustive):**
    - Watchlist toggle — toast with one-click Undo. ⭐ Starred / Unstarred routes get a 3.5s confirmation that lets the user reverse without the round trip back to the destination cell.
    - Route note save / delete — success toast confirming the write took.
    - ORS Sandbox Calibrate-T — success toast mirrors the in-card banner so the user sees the result even if they're scrolled away from the sandbox card.
  - **Future features that will reuse the primitive:** Q11 single-step undo (every store-save call site), bulk-sync completion notifications, alert thresholds (Q+), import-config success / failure, calibrate-flagged batch result.

- **Q12 — Persistent free-text search.** New 🔍 input on the controls bar — first control so it's the most discoverable. Filters table rows by IATA / city-name / route-note-text (case-insensitive substring). Persists to `settings.routeAssistant.searchQuery` with a 200ms debounce (typing stays fluid; one storage write + one re-render per pause). Native `<input type="search">` element so the X clear-button is free. Composes with existing filters (status / minScore / fleetFlyableOnly / search query all AND together).

- **U15 — Group-header hover highlight.** Every `<th>` and `<td>` in the route table now carries a `data-group="<group>"` attribute. Mouseenter on a group header (the row above the column headers) toggles a CSS class `aes-grp-hover` on every cell with the matching `data-group`, lifting the column-band's brightness via inset box-shadow. CSS rule lives in `css/content.css` (only addition this slice). Pure CSS would have needed `:has()` + universal sibling combinators that don't quite exist; JS toggle is ~15 LOC, trivial.

- **U1 — Sticky table header.** Already shipped in an earlier session — verified during this work. The thead's `position: sticky` plus the per-column `top: 22px` for the column-header row + `top: 0` for the group-header row keep both rows glued during vertical scroll. Frozen leading columns (Score + Dest) via `position: sticky; left: …px` were also already in place. **Removed from the QoL backlog as already-done; documented here so a future session doesn't re-implement.**

**Why these four together:** they're foundation primitives, not iterations on the just-shipped ORS Sandbox. The toast system is the bigger one — every future feature that writes state can call `RouteAssistantToast.show(...)` instead of inventing its own confirmation UI. The search bar compounds with every existing filter. Group-hover is a cheap discoverability win. Slice cost: ~280 LOC.

### ORS Sandbox (Letter I — slice 1) — shipped this session
**Surfaces in:** Route Assistant panel header (`/app/com/scheduling*`) — new 🧪 toggle next to 📊 Wave View. **Files:** `modules/route-assistant/ors-model.js` (NEW, ~510 LOC), extensions to `modules/route-assistant/panel.js` (~1380 LOC), `modules/route-assistant/settings-store.js`, `manifest.json`.

A new panel mode that replaces the table with a per-route pricing simulator. Pick a route → drag price / frequency / comfort sliders → projected rank, market share, pax/wk, revenue/wk, profit/wk update live. Read-only against AS — no writes.

**Pipeline (closed-form, transparent, every constant settings-tunable):**
1. **Rating projection (per cabin class).** `newRating = clamp(baseRating − α_price × (newPriceY − observedPriceY)/observedPriceY + α_comfort × comfortDelta, [0.5×base, 1.5×base])`. Linear-in-percent (NOT log-ratio) — defensible 0–30% deviation, surfaces a clamp note outside that. Defaults `α_price = 8` rating points per ±100% price change, `α_comfort = 5` per service-level step. Mutates the rating only on "every-leg-ours" connections; mixed-ownership multi-leg connections (interline) keep their fixed rating + a `mixedOwnership` note.
2. **Rank projection.** Re-sort the cached `byClass.<class>.connections[]` (top 50) by mutated rating descending; mirror `RouteAssistantOrsScraper.computeRanks` (`ors-scraper.js:486–528`) to derive `rankAny / rankFirstLegOurs / rankAllOurs / rankNonstop / rankBookable`.
3. **Share projection (numeric-stable softmax).** `weights = exp((ratings − maxRating) / T)`; `ourShare = sum(weights[i] for i where allOurs) / sum(weights)`. Default `T = 25`. Per-route override `settings.routeAssistant.orsSandbox.perRouteTemperature[<HUB>-<DEST>]` populated by a "Calibrate T from this route's actual share" button — bisection over `[1, 200]`.
4. **Pax/week.** `paxDemandPool × ourShare`, with the pool first scaled by `(newPriceY/observedPriceY)^paxElasticity` when both are present (uses the demand-derivator's clipped slope). Falls back to share-only when pool is null.
5. **Revenue/profit.** Calls `RouteAssistantProfitEstimator.estimate({…, override: {paxLF: projectedPax/(seats×freq), yieldPerKm: newPriceY/distance}, …})` so the existing fuel/crew/maintenance/age-penalty math is reused unchanged.

**UI:**
- Header strip: title · `<HUB>→<DEST>` route label · "↗ Open route in AS" link · "Pick another" button. When no route is picked, body shows a route-picker dropdown sourced from `this.scoredRows.filter(r => r.orsByClass)`.
- Two-column body: left "Scenario" card with three controls (Y price multiplier slider 0.30×–3.00× clamped to AS slider range + C/F preview labels that scale proportionally; frequency number input with "/wk · LF effect only" hint; comfort 5-stop selector −2 budget … +2 premium); right "Outcome" card — three columns (Base · Projected · Δ) for [rating, rank.nonstop‖any, share, pax/wk, rev/wk, profit/wk] with cell-level tooltips explaining each metric.
- Calibrate-T affordance: button under the controls; on success a banner reads "Using calibrated T = 18.4 for this route · global T = 25  [reset]". Refuses calibration if marketShare data is missing, the user's enterprise isn't in the leaderboard, or the freshness window is > 7 days.
- Notes footer: collapsible list of every clamp / fallback / data-gap surfaced by `RouteAssistantOrsModel.project(...).notes[]` — clamped rating, mixed-ownership rows, no own connection, no demand pool, etc.

**Drill-in entry points:**
- 🧪 button in panel header — toggles `settings.routeAssistant.orsSandbox.enabled`. Mutually exclusive with 📊 Wave View; Wave wins when both flags are on (Wave's render branch fires first in `_renderRows`).
- Right-click any row → 2-item menu — "Modify yield / LF…" (default, current behaviour) + "Open in ORS Sandbox 🧪" (sets `_orsSandboxRoute`, enables sandbox, re-renders).

**Frequency lever** dilutes LF only — does NOT synthesise additional connection slots. When freq exceeds current scheduled count, the rank/share/rating projections gray out (suppressed via `projected = baseline`) with caption "frequency above current; rank/share unchanged in slice 1 (LF affected only)" in the notes footer.

**Mode mutual exclusion:** the `_renderRows` branch order is Wave View → ORS Sandbox → table. Both modes can be enabled simultaneously in settings, but only one renders.

**Settings drawer expander** (📐 _renderOrsSandboxSection_ mounted below `_renderDemandDepthSection`): three numeric inputs for the global model params + Reset-all-per-route-Ts button (with count) + Reset-defaults button + a "How it works" footnote.

**Limits / out of scope (slice 1):**
- Pax-only. Cargo numbers pass through the estimator unchanged. `byClass.CARGO` rarely scraped anyway (most users only enable Y/C/F in `ors.classesToScrape`).
- Y-anchored multi-class price slider; C and F prices scale proportionally via the cached `ownPricing.prices.{Y,C,F}` ratios.
- Read-only. No "Save scenario as override" CTA; no Tier 3 write-back. Both deferred to slice 2.
- No multi-route batch sweep; no sensitivity sweep sparkline (live slider drag covers the same affordance interactively).

### Route Assistant — production
**Mounts on:** `/app/com/scheduling*`. **File:** `modules/route-assistant/panel.js` (~2700 lines).

A side-panel overlay that scores destinations from the current hub on demand × competition × distance × profit. Every Phase from 1 → 2.7 has shipped:

- **Phase 1** — basic table: destinations from flightsfrom + AS station demand + own-schedule cross-check. NEW/OK/UNDER/OVER status flags, sortable scored table.
- **Phase 1.5** — distance enrichment via three-tier resolver (`distance-resolver.js`): AS scheduling-page header → AS airport coords + great-circle → flightsfrom detail page. Persistent cache.
- **Phase 2** — aircraft-fit + profit estimator (`profit-estimator.js`). Falloff zone for over-range routes, OOR status flag. Per-route override modal (LF / yield) right-click on a row.
- **Phase 2.5** — yield modulation by demand, cargo revenue, crew/maintenance/fixed costs, age penalty.
- **Phase 2.6** — distance cache invalidation, per-route LF/yield overrides.
- **Phase 2.7** — fuel-price automation. Scrapes ASc$/l from `/action/portal/index`; per-type fuel burn via `RouteAssistantFuelBurn.estimate(spec, overrides)` (heuristic loadProxy × effFactor calibrated from forum data, ±15%); user can override per type.

Long form: `MANUAL.md §2–§15`.

### Used Aircraft Scanner — production (slices 1 + 2 of J shipped)
**Mounts in:** dashboard panel at `/app/enterprise/dashboard*`. **Files:** `modules/used-aircraft-scanner/`, `content_marketScan.js` (child-tab worker).

Concurrent child-tab scan of the AS used aircraft market across a preset list of types. Family + type filter automation; type-spec enrichment via `/action/enterprise/aircraftsType?id=`; sortable results table with scoring + filtering; route-range filter; CSV export; session resume across panel reloads; "Open offer" deep-link via Wicket-state replay.

**Slice 1 (earlier session) — GUI overhaul:**
- `family-grid-panel.js` — `MarketScanFamilyGrid` drill-down picker (chips strip, category filter, search, custom-add input, expandable per-family disclosures).
- `type-family-map.js` — `AS_FAMILY_CATEGORY` map, `AS_CATEGORY_ORDER`, `TypeFamilyMap.category()` / `categoryColor()` / `familyList()`.
- `results-table.js` — Family column + 4px color rail on the leftmost cell.

**Slice 2 (this session) — smarter deal-scoring:**
- New `modules/used-aircraft-scanner/deal-metrics.js` — `MarketScanDealMetrics` pure-function helpers: `pricePerSeat`, `seatKmYearCost`, `daysToBreakEven`, `fleetSynergy`, `routeFit`, `maintenanceTrajectory`, plus `decorate(row, ctx)` that runs all six and writes named scalars/labels onto the row.
- `results-table.js` — six new columns (`$/seat`, `$/seat·km/yr`, `BE (days)`, `Maint.` pill, `Fleet` badge, `Route-fit (HUB)`); four new scoring fields (`pricePerSeat`, `seatKmYearCost`, `breakEvenDays` lower-is-better; `routeFitCount` higher-is-better); maint pill is render-only (numeric `maintRank` for sort) and fleet badge is binary; new `setContext({fleetByType, economics, topRoutes, topRoutesHub})` plumbed before `_draw()`; CSV path emits the new columns.
- `presets-store.js` — added the four new scoring entries with `pricePerSeat` enabled by default; deep-merge ensures existing user blobs gain the new keys without resetting tuned siblings.
- `content_dashboard.js` — `loadDealContext(server)` loads RA fleet (synergy), RA economics (break-even math), and `routeAssistant:topRoutes` (route-fit count). Best-effort: any failure leaves the metric as null and the cell as em-dash.
- `modules/route-assistant/panel.js` — new `_publishTopRoutes(visible)` writes a slim snapshot (top-50 visible scored rows: destIata, destName, distanceKm, score, status, paxScore, cargoScore) to a single global key `routeAssistant:topRoutes` on every render. Single key (no per-hub keys); the scanner reads the most-recent published hub.
- `manifest.json` — registered `deal-metrics.js`, `modules/route-assistant/settings-store.js`, `modules/route-assistant/fleet-store.js` in the dashboard content-script block.

### Auto-Pricing — Tiers 1 + 2a + 2b shipped, Tier 3 open
**Surfaces in:** Route Assistant panel (`/app/com/scheduling*`). **Files:** `modules/route-assistant/schedule-page-scraper.js`, `modules/route-assistant/markets-page-scraper.js`, `modules/route-assistant/ors-scraper.js`, `content_scheduling.js`, `content_markets.js`, `modules/route-assistant/panel.js`.

Four-tier rollout, user-confirmed:

- **Tier 1 — visibility (shipped).** Scrapes `/app/com/scheduling/<HUB><DEST>` for live route data: assigned aircraft (registration + typeId + name), departure time, **per-day flight counts** (`dailyFlights[7]` Mon→Sun), weekly total (`weeklyFlights`), cruise speed. Live-capture on visit + bulk-sync CTA in the "Live route data" expander. Three columns under the rose-tinted `pricing` group: **Eq**, **Dep**, **Wk** (e.g. `12 2222211` = 12/wk, 2x Mon–Fri + 1x weekends). Multi-daily fully captured.
- **Tier 2a — Markets page (shipped this session).** New scraper for `/app/com/markets/<HUB><DEST>` covering competitor flights (every flight on the route with price + availability + status), your own pricing form snapshot (Y/C/F/Cargo current + default + slider ranges), market-share leaderboard (pax + cargo, with previous-period link), and 25-week historic capacity/price chart data. Storage **split across 4 directional key families** (`competitors`, `ownPricing`, `marketShare`, `historic`) so each can have its own freshness window. Live-capture on `/app/com/markets/*` visit + bulk-sync CTA in the teal-tinted "Market Analysis" expander. Four columns under the new `markets` group: **Mkt%** (your pax share), **Cmp#** (competitor count), **Cmp$** (median competitor Y price + delta), **Drft** (pricing drift vs. AS defaults).
- **Tier 2b — ORS rank (multi-class, shipped this session).** Scraper at `modules/route-assistant/ors-scraper.js` for `/app/info/ors`. Per-route GET → POST handshake (Wicket session can't be shared) — repeated **per cabin class** (default Y/C/F). Walks every result page, parses each connection's overall rating + per-leg flight code/ID/aircraft/price/status, identifies "our" connections via the user-schedule cache flight-number set + carrier-prefix fallback. Storage shape: `{byClass: {ECONOMY, BUSINESS, FIRST}, classesScraped, ourFlightIds, ourCarrierPrefixes}` — each class record carries the per-class rank flavors + ratings + connection list. Per-class failure isolation: empty results (e.g. no F cabin on a regional route) ARE valid; only HTTP 429/503/timeout propagates to the circuit breaker. **Composite blending:** `_applyCachedOrs` computes the headline metric on read using user's `combineMethod` (`capacityWeighted` default + `weighted`/`min`/`max`/`avg`) + class weights — composite re-projects without re-scraping when settings change. **Weight presets** in `RouteAssistantSettings.ORS_WEIGHT_PRESETS`: Capacity-weighted (auto from picked aircraft seat config; fallback to Standard), Standard 75/20/5, All Economy, Two-class 60/40, Business-heavy 40/50/10, Premium long-haul 30/40/30, Custom. Concurrency=2, stagger=1500ms, **circuit breaker** halts after 3 consecutive 429/503 and disables sync for 10 min. **Pace:** ~10 routes/min for 3 classes (down from ~30 routes/min single-class). Drill-in drawer (▾) opens with **Y/C/F/All tabs**. Amber "ORS Rank" expander: Classes multi-checkbox, Combine method dropdown, Weight preset dropdown with description hint, Window/Ground/Primary controls, More-options disclosure with per-column toggles + custom weight inputs (sum normalised to 1.0) + show-per-class-columns toggle + carrier-prefix override + min display threshold. Sync button label dynamically reflects pace estimate. Columns under `ors` group: **ORS** (composite), **RkNS** (composite nonstop rank), **Gap** (composite rating gap), **OrsC#** (distinct first-leg competitors), plus **Y / C / F** per-class metric columns (non-Compact view + `showPerClassColumns` only).
- **Tier 3 — apply / write-back (open).** HIGH risk. Confirmed autonomy posture: one-click + batch-confirm by default; silent auto-apply behind a separate explicit setting. Target endpoint: the markets-page pricing form (`POST` to `/app/com/markets/<HUB><DEST>?<wicket>-pair~form` with `classes:prices:N:newPrice` body fields).
- **Tier 4 — yield feedback.** Shipped this session as Roadmap G slice 1; see "Yield Feedback" below. Auto-Pricing Tier 4 is now folded into that loop.

**Naming note:** scrapes the **scheduling page** for live operational data (no prices on that page yet). File `modules/route-assistant/schedule-page-scraper.js`, class `RouteAssistantSchedulePageScraper`. Storage prefix `routeAssistant:ticketPrice:` is preserved for backwards compat — don't rename.

### Yield Feedback (Roadmap G) — slices 1–4 (sessions 1+2) shipped this session
**Surfaces in:** Route Assistant panel (`/app/com/scheduling*`). **Files:** `modules/route-assistant/yield-history-store.js`, `modules/route-assistant/yield-snapshot.js`, plus extensions to `aggregator.js`, `panel.js`, `settings-store.js`, `content_aircraftFlights.js`.

Closed-loop estimator-vs-actuals: takes the live-route-data cache (which lists assigned tails per route) and joins with each tail's `<server>aircraftFlights<id>` profit record (written by `content_aircraftFlights.js`). Profits are attributed per route by frequency-weighted (default), distance-weighted, equal split, or **per-flight (exact)** (slice 4); persisted as `routeAssistant:yieldHistory:<HUB>-<DEST>` time series.

**Slice 1 (earlier in session):**
- `RouteAssistantYieldHistoryStore` — directional cache with bulk load + append + history-limit prune.
- `RouteAssistantYieldSnapshot` — pure attribution engine. Reads aircraftFlights + ticketPrice records, returns per-route snapshots.
- `aggregator.js` — `buildRouteRows` accepts `yieldHistoryMap`; rows carry `actualProfitPerFlight/Week`, `actualFrequency`, `actualSnapshots`, `actualVariancePct`, `actualContributingTails`.
- `panel.js` — "Yield feedback (actuals)" expander, `actuals` column group with `Act $/flt` and `Δ%`, ASCII sparkline tooltip, **Calibrate from actuals** button in the per-route override editor.

**Slice 2 — delta-mode + auto-snapshot + Calibrate-flagged:**
- New `routeAssistant:yieldBaselines` global key — single blob of per-tail `{profit, profitFlights}` snapshots from the last run. Each `takeSnapshot` updates it for every observed tail.
- `yield-snapshot.js _buildRegProfitMap(aircraftRecs, baselines)` — when in `deltaMode`, computes per-tail $/flt as `(profit − prevProfit) / (profitFlights − prevProfitFlights)`. Tails without a prior baseline silently fall back to cumulative; result reports `tailsUsingDelta`.
- `panel.js` — yield-feedback expander gains **Delta mode**, **Auto on mount** checkboxes and a **Calibrate flagged (N)** button that opens a modal listing every Δ%-flagged route + the calibrated yield, batch-saving overrides on confirm. Status column appends a `V+` / `V−` pill when |Δ%| ≥ warn threshold. Diag block reports the snapshot mode + delta tail coverage.
- `settings-store.js` — `yieldFeedback` block gains `deltaMode`. `autoSnapshotOnMount` now wired to `_maybeAutoSnapshot()` after `mount()`'s refresh completes.

**Slice 3 — cargo-yield calibration:**
- `derivedYieldFromActuals(row)` refactored from `number | null` → `{side: "pax"|"cargo", value, alt: {side, value} | null} | null`. Inverts the estimator's `paxRevenue + cargoRevenue` equation for whichever side is solvable, holding the other at its current revenue contribution. Auto-picks the dominant side by current revenue share; carries the alt for mixed routes.
- Per-route override editor's Calibrate button branches: pre-fills `cyldInput` when `side === "cargo"`, `yldInput` when pax; surfaces the alt-side value in the status note for hand-paste; button label switches between "Calibrate (cargo) from actuals" and "Calibrate from actuals".
- Calibrate-flagged modal gains a **Side** column with a per-row P/C dropdown (disabled when only one side is solvable). On flip, Old/New cells refresh live from `primary` vs `alt`. Save loop branches on the chosen side and writes either `yieldPerKm` or `cargoYieldPerKgKm`. Note text reflects the side actually written: `"calibrated 2026-04-26 (pax)"` or `"… (cargo)"`. Cargo-only routes (e.g. freighters) now appear in the flagged list and produce valid calibrations.

**Slice 4 — per-flight attribution (sessions 1+2 shipped, session 3 parked):**
- `content_aircraftFlights.js` extended to capture per-flight FN linkage from the aircraft history page DOM (FN ID from `/app/com/numbers/<id>` href, origin/destination IATAs from rows 3/5, dep UTC from row 4 title) and persist a slim envelope array onto `<server>aircraftFlights<id>.flights[]`: `[{flightId, flightNumber, flightNumberId, status, originIata, destinationIata, depUtc}]`.
- **Pre-existing bug fixed:** the legacy `flight.id = parseInt(url.match(/\d+/)[0])` picked up the "1" in `free1` hostname instead of the actual flight ID after `?id=`. Replaced with `url.match(/[?&]id=(\d+)/)[1]`. Affects every join into `<server>flightInfo<flightId>` — was silently broken, fixed inline because per-flight attribution depends on the join.
- `yield-snapshot.js _collectPerFlightProfits(all, server, hubFilter, aircraftRecs)` — walks every tail's `flights[]`, joins each finished/inflight envelope with `<server>flightInfo<flightId>.money.CM5.Total`, aggregates per `{originIata, destinationIata}` route key. Multi-leg FNs naturally handled because each leg's envelope carries its own route.
- `takeSnapshot` accepts `attributionMode: "per-flight"` (added alongside `frequency`/`distance`/`equal`). When set, each route is attributed exactly from its measured flights; routes with no measured flights fall back to frequency-weighted per-tail attribution. Result includes `attributionMode`, `routesWithPerFlight`, `routesFellBack`, `perFlightAvailable`.
- `panel.js` — attribution dropdown gains **per-flight (exact)** as the first option. Diag block surfaces per-flight stats and a fallback warning when any route fell back. Console summary log includes per-flight stats.
- **Session 3 parked:** extending `content_flightInfo.js` to extract origin/dest/FN inline at parse time. Currently per-flight attribution requires both an aircraft history visit AND a flight-detail visit per measured flight. Session 3 would let the flight-detail visit alone suffice — tighter the path on fresh worlds. Needs a `/action/info/flight?id=<id>` HTML sample.

**Limits:**
- Snapshots only see routes whose ticket-price record has been scraped (run "Sync route data" first), and tails the user has visited via `/app/fleets/aircraft/<id>/1`.
- Per-flight mode additionally requires `<server>flightInfo<id>` records for each measured flight (visited via `/action/info/flight?id=<id>` or the "Extract finished flight profit" bulk button).
- v1 stores cumulative averages by default; flip Delta mode for true periodic yield (requires at least one prior snapshot to anchor).
- Cargo and pax sides cannot both be calibrated from a single snapshot — one variable per equation. Run two snapshots if the user wants to dial both.

### Service-profile + per-class auto-detect — shipped this session
**Surfaces in:** Service columns (Seats/wk · Mix · Svc) on the Route Assistant panel. **Files:** `modules/route-assistant/service-profile-scraper.js` (NEW), `modules/route-assistant/markets-page-scraper.js` (extended), `modules/route-assistant/service-config-store.js` (extended), `modules/route-assistant/aggregator.js` (extended), `content_fleetManagement.js` (extended), `modules/route-assistant/panel.js` (extended).

Replaces the manual class-mix / service-level inputs with values scraped from AS:

- **Per-tail Y/C/F seat counts** — `content_fleetManagement.js` now extracts the "Y/C/F" column on `/app/fleets` (e.g. "217/30/0") into `seatsY/seatsC/seatsF` on each tail's fleet record. Aggregator uses these as the second-priority class-mix source (after per-route override, before settings default).
- **Per-class fares (Y/C/F/Cargo)** — `markets-page-scraper.js` `_parseOwnPricing` reads the Pricing fieldset on `/app/com/markets/<HUB><DEST>`. The aggregator's `_applyServiceProjection` now resolves per-class effective yield as: route override → `ownPricing[cls] / distance` (scraped) → defaults. Per-class `yieldSource` exposed on the breakdown.
- **Service-profile assignment** — same scraper grabs the `serviceProfileId` from the General Settings fieldset's `<select>`. New `RouteAssistantServiceProfileScraper` fetches `/action/enterprise/serviceProfiles` (list) + `/action/enterprise/serviceProfile?id=<id>` (per-profile catering levels per class). The detail parser robustly walks any 2-letter category prefix + class letter naming convention (`dry/drc/drf` for drinks, `mdy/mdc/mdf` for entrees, etc.) so AS adding new categories doesn't break it. Aggregate `classScore: {Y, C, F}` ∈ [0..1] is normalised across whichever categories the page exposes.
- **Service-config popover** — gains a green "Auto-detected" banner above the inputs listing what AS knows (mix from assigned tail, AS fares per class, AS profile name + per-class quality scores).
- **Settings → Service profiles** — new "AS service profiles auto-detect" sub-block with **Refresh AS service profiles** CTA + cache count + last-sync timestamp.
- **Mix column** — color-codes the source (purple = route override, green = from assigned tail, sky-blue = settings default).

**Cache layout:**
- `routeAssistant:serviceProfilesList` → `{profiles: [{id, name, minDistanceKm, isDefault}], scrapedAt}`
- `routeAssistant:serviceProfile:<id>` → `{id, name, categories: {drinks: {Y, C, F}, ...}, categoryByPrefix, classScore, scrapedAt}`
- markets-page records (`routeAssistant:markets:ownPricing:<HUB>-<DEST>`) gain `generalSettings.serviceProfileId` (number) alongside the existing `serviceProfile` (label string).

**Limits:** the coarse Budget/Standard/Premium service-level model is preserved for now — the scraped profile name + quality score is informational (surfaced in Svc tooltip + popover banner). A follow-up slice will let users *score* a route by its profile's classScore directly. Per-pax cost has no AS source — still inherited from settings or manual override.

### Station Automation — production (earlier session)
**Mounts on:** `/app/info/airports/<id>` + `/app/ops/stations*`. **Files:** `content_stationOpen.js`, `modules/station-automation/`.

Auto-opens stations across queued countries. Worker runs on the airport detail page to click "Open Station", then on the stations-ops page to reconcile success. Untouched this session.

### FlightsFrom integration — production (earlier session)
**Mounts on:** `flightsfrom.com/*`. **Files:** `content_flightsFrom.js`, `modules/flightsfrom/`.

Scrapes per-IATA destination listings (weekly flights, airline counts, distance via detail-page tier-3). Cache: `flightsFrom:<IATA>`.

This session: Letter F shipped on top — `modules/route-assistant/carriers-scraper.js` adds a per-pair detail-page scraper that the Route Assistant panel calls directly via `fetch(...)` (cross-origin, covered by host_permissions, same pattern as distance-resolver tier-3). Output enriches the panel's Cmp column with a colored intensity badge + per-carrier hover tooltip.

### Contractual partners (F slice 3) — shipped this session
**Surfaces in:** Cmp popover on the Route Assistant panel (`/app/com/scheduling*`). **Files:** `modules/route-assistant/contractual-partners-scraper.js` (NEW), extensions to `modules/route-assistant/settings-store.js` and `modules/route-assistant/panel.js`.

Reads the user's own enterprise's "Contractual partners" tab (`/app/info/enterprises/<your_id>?tab=1`) and surfaces partner glyphs in the per-row carrier popover that opens from the `Cmp` column. The popover already lists every competitor on a route (F slice 2 work); F slice 3 adds:
- ⇄ (green) glyph next to enterprises in the user's INTERLINING set
- ✦ (purple) glyph next to enterprises in the ALLIANCE set (off by default; user opt-in via the toggle)

Source: a single fetch per own enterprise yields the entire partner list (alliance + interlining + lessor + any future relation types) in a `<table class="partners">` markup. Captures relation tags verbatim so unknown future tag values still land in the cache. Single key per own-enterprise, non-directional cache (`routeAssistant:contractualPartners:<enterpriseId>`), 30-day TTL. Multi-enterprise (canopy-style) users can list multiple comma-separated IDs; the popover unions every partner set.

UI: a new purple "Contractual partners" sub-panel in the Carriers expander hosts the own-enterprise-IDs text input, the Refresh CTA, the glyph toggles, and a hint for finding your enterprise ID in AS. The data layer (`_partnersByEnterpriseId`) is rebuilt by `_applyCachedContractualPartners()` on init and after every refresh.

### Diff against last visit (Daily-driver QoL slice 1) — shipped this session
**Surfaces in:** every numeric Route Assistant column. **Files:** `modules/route-assistant/panel.js`.

Each numeric column now carries a small grey ▲/▼ badge showing the change since the user's previous visit to this hub. Tracked fields: `score`, `paxScore`, `cargoScore`, `weeklyFlights`, `airlineCount` / `competitorCount` (Cmp), `paxDemandPool`, `cargoDemandPool`, `ourPaxShare` (Mkt%), `orsRatingGapToTop` (Gap), `rmTightness` (RM%).

**Data flow:**
- New cache `routeAssistant:lastSnapshot:<HUB>` — per-hub key, overwritten on every render with the full `this.scoredRows` set.
- `refresh()` loads the snapshot once per `(mount, hub)` pair into `this._diffPrevSnapshot`. Hub change in the scheduler dropdown invalidates and re-loads (`_diffBaselineHub` mismatch). Within a single mount the in-memory baseline survives across re-renders, so deltas stay anchored to "what the user saw last visit" instead of re-baselining on every storage event.
- `_renderRows` decorates `this.scoredRows` with `_diff.<field>` scalars before sort/draw, via `_decorateRowsWithDiffs`.
- `_drawTable` writes the new snapshot at the end (after `_publishTopRoutes`) via `_writeDiffSnapshot`.
- Each tracked column's render closure ends in `_appendDiffBadge(td, "<field>", row)`. Cmp passes an array fallback `["competitorCount", "airlineCount"]` so the badge tracks whichever source produced the displayed count.

**Display:** badge format varies by field type — pax/cargo pools render compact (`▲1.2k`), `ourPaxShare` and `rmTightness` render in percentage points (`▲5pp`), score / counts / gap render rounded integers (`▼3`). Hover any badge for the exact delta + the previous mount's timestamp.

### Watchlist / starred routes (Daily-driver QoL slice 2) — shipped this session
**Surfaces in:** the destination cell of every Route Assistant row. **Files:** `modules/route-assistant/watchlist-store.js` (already existed from a previous session, now wired in), `modules/route-assistant/panel.js`, `modules/route-assistant/settings-store.js`, `manifest.json`.

The destination cell now starts with a clickable ☆ glyph. Click it to star the route; the glyph flips to ★ (gold) and the row floats to the top of the table regardless of the current sort. Star is a *primary* sort key — within both the starred group and the unstarred group the user's chosen sort field+direction is honoured. Toggle is debounced at the storage level only; the UI flips immediately and persistence runs async.

**Watch triggers** — when a starred row's `_diff` (vs. the previous mount's snapshot) shows a "worse" change in any of the tracked fields, a small red `•` appears next to the star with a multi-line tooltip listing every fired trigger and its delta. Tracked fields + worsening directions are defined in the new top-level `RA_WATCH_TRIGGERS` array next to `RA_DIFF_TRACKED`:

| Field | Worse direction | Meaning |
|---|---|---|
| `paxScore` | decreases | Pax demand fell |
| `cargoScore` | decreases | Cargo demand fell |
| `airlineCount` | increases | Real-world competitor entered |
| `competitorCount` | increases | AS competitor entered |
| `rmTightness` | increases | RM tightness rose (less headroom) |
| `orsRatingGapToTop` | decreases | ORS gap-to-top shrunk |

**Settings** — new `settings.routeAssistant.watchlist` block: `floatStarredToTop` (default `true`) gates the float-to-top sort behaviour without affecting the star itself; `showAlertBadges` (default `true`) hides the red dot if the user finds it noisy. Storage of the starred set is unaffected by both toggles — the data persists either way.

**Storage** — single global key `routeAssistant:watchlist` → `{pairs: ["<HUB>-<DEST>", ...], updatedAt}`. NOT per-hub: a multi-hub user can star routes across hubs and the panel filters by current hub at render time. Pair key is **directional** `<HUB>-<DEST>` (uppercased), matching the orientation of the panel's other directional stores.

**Caveats:** filters (status / minScore / fleetFlyable) STILL apply to starred rows — a starred route hidden by a filter doesn't surface. Documented as a known choice in §10. No cross-tab `chrome.storage.onChanged` listener for the watchlist key in v1; refresh covers the multi-tab case.

### Settings export / import (Daily-driver QoL slice 3) — shipped this session
**Surfaces in:** the Route Assistant panel header. **Files:** `modules/route-assistant/panel.js`.

A new **⇅** button between settings (⚙) and minimise (_) opens a small popup menu with two options: "Export config → file" and "Import config ← file".

**Export** writes both `settings.routeAssistant` and `settings.usedAircraftScanner` blobs into a single JSON envelope:
```json
{
  "format": "aes-config",
  "version": "0.6.9-beta",
  "exportedAt": "2026-04-26T...",
  "server": "free1",
  "routeAssistant": {…},
  "usedAircraftScanner": {…}
}
```
Filename pattern: `aes-config-<server>-<YYYY-MM-DD>.json`. Triggered via `Blob` + temporary `<a download>` click.

**Import** opens the system file picker, validates `format === "aes-config"` (refuses anything else), recursively diffs the incoming blobs against current settings via `_diffConfig`, and shows a modal listing every added/removed/changed leaf path with from→to values. Apply writes both blobs back through `RouteAssistantSettings.save()` + `UsedAircraftPresets.save()`, which deep-merge against current defaults — older exports missing newer keys still load cleanly. Cancel/Escape/click-outside dismisses without writing.

**Diff display** — added paths show in green, removed in red, changed in amber. Object-vs-non-object transitions are reported as `changed`; arrays are compared by JSON equality (no element-by-element diff, to keep the modal tractable when the user has a 50-preset list).

**Use cases:** migrate config between AS worlds (different server prefixes), share tuned configs with friends, back up settings before a major experiment, single-click recovery from a botched edit.

### Route notes (Daily-driver QoL slice 4) — shipped this session
**Surfaces in:** the Dest cell of every Route Assistant row. **Files:** `modules/route-assistant/route-note-store.js` (NEW), `modules/route-assistant/aggregator.js`, `modules/route-assistant/panel.js`, `manifest.json`.

A free-text per-route note. Captures context that doesn't fit any of the structured fields: "tried 2x daily, dropped — too much C-class capacity", "watching for AA entry", "served by partner under interline". A 📝 glyph appears in the icon row on every Dest cell next to 📅 📦 🛫. Subdued grey when the route has no note; full color when it does. Click opens a small popover with a textarea (5 rows, 500 char max), char counter, and Save / Delete / Cancel buttons. Saved note text appears as the cell-wide tooltip on hover, and the icon's title shows the first 80 chars.

**Note on spec deviation:** the original backlog entry said the glyph would only render "when present". I widened it to always-visible (subdued when empty) so the affordance to *add* a note is discoverable; clicking 📝 on an empty route opens the popover with a blank textarea. The "when present" reading required a separate way to add new notes (right-click menu, keyboard shortcut, etc.) that didn't fit the existing UX.

**Storage:**
- New cache `routeAssistant:routeNote:<HUB>-<DEST>` → `{hub, dest, text, createdAt, updatedAt}`. **Directional** pair key (matches `routeAssistant:override:`, `routeAssistant:yieldHistory:` etc. — outbound vs inbound notes are uncommon but cheap to support and the orientation matches every other per-route store).
- Empty / whitespace-only saves remove the key entirely (no tombstones).
- Bulk-loaded once per `refresh()` into `this.routeNoteMap` via `RouteAssistantRouteNoteStore.getMany`; passed into `buildRouteRows` and attached as `row.routeNote` (full record) + `row.routeNoteText` (string for quick truthy checks in render closures).

**Popover lifecycle** — `_openRouteNotePopover` / `_closeRouteNotePopover` mirror the existing `_openProfitModifierPopover` pattern: outside-click + Escape teardown, deferred listener attachment to avoid self-dismissal, viewport-clamped positioning. Save mutates `row.routeNote` + `row.routeNoteText` in place + updates `this.routeNoteMap` + calls `this._renderRows()` so the icon flips between subdued/full color without a full data reload. Storage listener is intentionally NOT wired to the new key — saves go through the panel directly, so a chrome.storage.onChanged hook would just trigger redundant `refresh()` calls (which would reload fleet, distances, schedule etc. just for a text edit).

### Schedule Management — production (earlier session)
**Files:** `modules/schedule-management/`.

Wave templates, range-bucket aircraft-fit checks, schedule builder. Used by Route Assistant for fleet-aware fit/profit + (this session) the Wave View overlay.

### Wave View — Roadmap H slice 1, shipped this session
**Surfaces in:** Route Assistant panel header (`/app/com/scheduling*`) — new 📊 toggle next to Compact. **Files:** `modules/route-assistant/wave-overlay.js` (NEW), extensions to `modules/route-assistant/panel.js` and `modules/route-assistant/settings-store.js`, `manifest.json`.

A new panel mode that takes the top-N scored rows + the user's selected `SchedulePresets` record and runs them through the existing `ScheduleBuilder` (`modules/schedule-management/schedule-builder.js`) to produce a Gantt-style wave timeline. Slice 1 is **read-only** against `ScheduleStore` — the build runs in-memory only.

**Render layout:**
- Header strip: preset picker (sourced from `settings.scheduleManagement.presets`) · Top-N input (5..100, default 20) · Aircraft display (read-only mirror of `this.selectedSpec`) · ⟳ Re-run · 📅 Edit presets → (deep-link to dashboard).
- Hour ruler at top (cropped to `preset.factors.slotWindow` when configured, else 0–24h).
- One swim-lane per wave with: a 140px label column (wave name + S/M/L composition + arrival/departure windows) and a relative-positioned strip with subtle shaded bands for arrivalWindow + departureWindow.
- Flight bars positioned by `depTimeLocal` (% of lane width). Outbound = blue (top half), inbound = green (bottom half). Range-bucket leaves a coloured left edge: shortHaul lighter blue, longHaul darker. Hover → tooltip with full details. Click → opens `/app/com/scheduling/<HUB><DEST>` in a new tab.
- Warnings panel (range/slot violations from `evaluateFlights()`) and unplaced strip (routes that didn't bucket-fit any wave) below.

**Reuse:** `ScheduleFactors` (kmToNm, parseHHMM, formatHHMM, bucketize), `ScheduleBuilder` (validatePreset → assignRoutes → evaluateFlights), `SchedulePresets.load()`. No new storage keys — toggle + topN + lastPresetId persist under `settings.routeAssistant.{waveView,waveOverlay}`.

**Module split:** `RouteAssistantWaveOverlay` exposes three pure-function helpers — `buildRoutesFromScoredRows(scoredRows, opts)`, `buildSchedule(preset, scoredRows, ctx)`, `renderGantt(host, build, opts)`. The renderer takes `onFlightClick` so the panel wires the navigation handler without coupling overlay code to the panel instance.

**Out of scope for slice 1** (deferred to slices 2–N): drag-edit slots, auto-optimise (LP/heuristic), connection-graph computation, interline-store, slot-store, multi-hub network view, save-schedule CTA, competitor schedule overlay.

---

## 2 · This session's changes

| Feature | File | Type |
|---|---|---|
| Auto-Pricing Tier 3.2 | `modules/route-assistant/settings-store.js` | extended — `pricing.apply.dryRunOnly` default flipped `true → false`; new fields `circuitBreakerThreshold: 3`, `circuitBreakerCooldownMs: 600000`, `circuitBreakerTrippedAt: null`, `circuitBreakerHaltReason: null`. Existing users keep persisted dryRunOnly via `_mergePricing` deep-merge. |
| Auto-Pricing Tier 3.2 | `modules/route-assistant/pricing-applier.js` | extended (~+90 LOC) — constructor accepts breaker fields + `onBreakerTrip` / `onBreakerReset` callbacks; new Step-0 cooldown gate at the head of `apply()` short-circuits to `code: "breakerCooldown"` aborted record when within cooldown window (skipped in dry-run); GET + POST paths route 429/503 through new `_handleRateLimit(envelope, where, status)` which increments `_consecutiveErrors`, fires `onBreakerTrip` at threshold, writes a `code: "rateLimit"` failure log entry; `_completeAsVerified` + `_completeAsPostedUnverified` call new `_resetBreakerCounter()` which fires `onBreakerReset` so the panel can null trippedAt in settings. |
| Auto-Pricing Tier 3.2 | `modules/route-assistant/panel.js` | extended (~+190 LOC) — Tier 3 expander gains "Dry-run only" checkbox + breaker-tripped red banner with Reset button; `_persistPricingBreakerTrip` / `_persistPricingBreakerReset` helpers round-trip trip state through `RouteAssistantSettings.save`; `_getPricingApplier` threads breaker fields + callbacks into the applier; `_openPricingApplyModal` awaits `fetchLastApplyAt() → log.getLastSuccessAt(hub, dest)` and threads it into `collectArgs(false, lastApplyAt)` for the per-route cooldown gate; `renderResult(result, applierUsed)` adds `rateLimit` / `breakerCooldown` toast branches and a 6-second `RouteAssistantToast.success({action: Undo})` on `verified` / `posted` that re-runs `applier.apply(hub, dest, prevPrices, {source: "undo"})`; modal copy + button titles de-tiered (no more "Tier 3.1" / "Tier 3.2" strings). |
| Tech-debt sweep (folded into 3.2) | `modules/route-assistant/schedule-page-scraper.js` | renamed from `ticket-price-scraper.js` — class `RouteAssistantTicketPriceScraper → RouteAssistantSchedulePageScraper`; storage prefix `routeAssistant:ticketPrice:` left untouched per §10 stable-key invariant. |
| Tech-debt sweep (folded into 3.2) | `modules/used-aircraft-scanner/type-family-map.js` | extended — `familyList(overrides)` folds `block.typeFamilyOverrides` into the matching family card; truly unknown labels still land in Custom. Closes the open Family-card grid override-walk debt. |
| Tech-debt sweep (folded into 3.2) | `modules/used-aircraft-scanner/deal-metrics.js` | extended — new `BLOCK_HOURS_BY_CATEGORY` map (commuter/turboprop/regional 8h, narrowbody 12h, widebody 14h) + `_blockHoursFor(row)` resolver via `row.familyCategory`; `breakEven` reads through the resolver. `DAILY_BLOCK_HOURS = 10` survives as legacy fallback. |
| Tech-debt sweep (folded into 3.2) | `content_dashboard.js` | extended — `displayUsedAircraftScanner()` installs a singleton `chrome.storage.onChanged` listener (250ms debounced) on `routeAssistant:topRoutes` + per-hub keys; re-pulls `loadDealContext(server)` and re-renders results. Closes the topRoutes-freshness debt. Previous listener removed on each remount via `window._aesTopRoutesListener`. |
| Tech-debt sweep (folded into 3.2) | `manifest.json` | extended — `ticket-price-scraper.js → schedule-page-scraper.js` in the `/app/com/scheduling*` content-script block. |
| ORS Sandbox slice 2c | `modules/route-assistant/rating-observation-store.js` | NEW (~165 LOC) — append-only obs log keyed `routeAssistant:ratingObservations:<HUB>-<DEST>` (directional). FIFO cap 50; 90d age prune on every read. Public API: `get / getMany / add / clear / clearAll / count`. `add` is read-modify-write under one storage call; race-tolerant under bulk-sync concurrency=2 + stagger. Empty records pruned to skip storage tombstones. |
| ORS Sandbox slice 2c | `modules/route-assistant/rating-alpha-store.js` | NEW (~110 LOC) — per-route per-class manual α overrides at `routeAssistant:ratingAlpha:<HUB>-<DEST>` (directional). Sibling to `route-overrides-store` (kept SEPARATE because the existing `_clean` uses full-replace semantics — five other override callers don't pass α and would silently erase it). Stores positive magnitude in `[0, 50]`; `0` preserved verbatim ("rating doesn't respond to price"). |
| ORS Sandbox slice 2c | `modules/route-assistant/ors-scraper.js` | extended — new `_logRatingObservation(server, hub, dest, savedRecord)` static + a try/catch'd call after `saveRecord` (line 616). Reads matching `routeAssistant:markets:ownPricing:<HUB>-<DEST>`, builds the per-class observation with prices/ratings/connection counts/comfortLevel/timestamps, calls `RatingObservationStore.add`. Honors `settings.routeAssistant.orsSandbox.ratingObservations.autoLogOnScrape`. Inside the scraper (NOT panel) so `bulkScrape` covers all paths. |
| ORS Sandbox slice 2c | `modules/route-assistant/demand-derivator.js` | extended — `derive(historic, inventory, ownPricing, observations, opts)` accepts a 5th positional arg with backwards-compat heuristic for legacy 4-arg callers. New `_ratingPriceElasticity(observations, cls, notes, opts)` runs the per-class confounder-filter pipeline (>24h pricing/ORS gap; comfort change; ownConnections delta ≥ 1; competitor count churn > 20%) + range-of-priceDev gate (≥8% lever arm) + distinct-bucket gate (≥2 buckets at 1% rounding) + OLS via existing `_linregSlope` + slope→magnitude negation + `[0, 50]` clip + non-monotone reject. New `_priceDevRange` and `_distinctBuckets` helpers. Output adds `ratingPriceElasticityByClass:{Y,C,F}`, `ratingObservationCounts:{Y,C,F}`, `ratingDerivationNotes:[]`. |
| ORS Sandbox slice 2c | `modules/route-assistant/ors-model.js` | extended — `_projectClass` reads `params.ratingPriceElasticityByClass[cls]` via `Number.isFinite` (NOT `||`, so `0` is honored), falls back to `params.ratingPriceElasticity`. Per-class result envelope adds `alphaUsed` for the panel. `project()` reads `input.modelParams.alphaSourceByClass`, pushes per-class source notes ("override" / "derived" / "siblingDerived" / "fleetMedian" / "global"), and surfaces `ratingPriceElasticityByClass` + `alphaSourceByClass` in the output's `modelParams`. |
| ORS Sandbox slice 2c | `modules/route-assistant/panel.js` | extended (~280 LOC) — `_applyCachedDemand` bulk-loads observations + α-overrides via `getMany`, threads observations into derivator, lifts new fields onto rows. New `RouteAssistantPanel._computeFleetMedianAlpha(rows)` static; new module-scoped `_median` helper. `_assembleOrsSandboxRoute` lifts per-route fields. `_recomputeOrsSandbox` runs the **two-pass cascade resolver** (override > derived ≥minObs > siblingDerived > fleetMedian > global) before passing `modelParams.{ratingPriceElasticityByClass, alphaSourceByClass}` to `RouteAssistantOrsModel.project`. New `_buildOrsSandboxAlphaExpander(result, route)` — collapsed per-class override `<details>` mounted inside the Outcome card; Save wraps in `_undoableSave`. Settings drawer's `_renderOrsSandboxSection` adds an auto-log toggle + observation count display + "Reset all observations" destructive button. |
| ORS Sandbox slice 2c | `modules/route-assistant/settings-store.js` | extended — `orsSandbox.ratingObservations` defaults block: `autoLogOnScrape: true` (STABLE across versions; flag don't-touch comment), `minObservationsForDerivation: 4`, `maxAgeDays: 90`, `priceDevRangeGate: 0.08`, `distinctBucketsRequired: 2`, `allowSiblingClassFallback: true`, `allowFleetMedianFallback: true`. `_mergeOrsSandbox` adds shallow-merge for the nested block. |
| ORS Sandbox slice 2c | `manifest.json` | extended — registered `rating-observation-store.js` + `rating-alpha-store.js` immediately after `route-note-store.js` in the `/app/com/scheduling*` content-script block (before `panel.js`). |
| Polish bundle | `.gitignore` | extended (5 scratch HTML/txt fixtures excluded so they can't ship in the .crx) |
| Polish bundle | `helpers.js` | extended (`escapeHtml` hoisted out of `panel.js` so wave-overlay.js no longer relies on panel.js parse order) |
| Polish bundle | `modules/route-assistant/panel.js` | extended — `_diffConfig` set-tolerant for `classesToScrape`-style multi-selects (`_DIFF_SET_PATH_KEYS` + `_arraysEqual` + `_arrayEqualsAsSet`); `_openRouteNotePopover` confirms-on-discard + Save-disabled-when-empty + skip-noop-write |
| Polish bundle | `modules/route-assistant/settings-store.js` | extended — `_mergeOrs` renormalises `classWeights` to sum 1.0 + drops legacy `defaultPayload` after lazy-rename; new `_renormaliseWeights` static |
| Polish bundle | `modules/route-assistant/inventory-page-scraper.js` | extended — `parseIntSafe` decimal-aware (rightmost separator with 1–2 digit tail = decimal point; thousand separators stripped). 12/12 smoke-tests. |
| Polish bundle | `modules/route-assistant/ors-scraper.js` | extended — restored two inline comments (`PageExpiredException` watch + `safetyCap` rationale) dropped during the multi-class refactor |
| ORS Sandbox slice 1.5 | `modules/route-assistant/settings-store.js` | extended — `lastScenarioByRoute: {[hub-dest]: scenario}` replaces global `lastScenario`; `perRouteTemperatureCalibratedAt: {[hub-dest]: ms}` sibling map; `_mergeOrsSandbox` validates each entry's shape, drops orphan stamps, surfaces legacy `lastScenario` once via `_legacyLastScenario` for one-shot migration |
| ORS Sandbox slice 1.5 | `modules/route-assistant/panel.js` | extended — `_renderOrsSandbox` reads `lastScenarioByRoute[routeKey]` with `_legacyLastScenario` fallback; `_recomputeOrsSandbox` writes per-route + deletes legacy keys on first save; `_refreshOrsSandboxTBanner` shows "calibrated Xd ago" with grey/amber/red threshold colouring (30d / 90d) and clears both T + stamp on reset; `_calibrateOrsSandboxT` writes `Date.now()` to the stamp map; `_renderOrsSandboxSection` status line shows "(N stale ≥30d)" count; Reset-all-Ts clears both maps |
| ORS Sandbox slice 2 (a/b/d/e) | `modules/route-assistant/ors-model.js` | extended — scenario shape now `{priceMultipliers:{Y,C,F}, cargoMultiplier, frequency, comfortDelta}`; new `_normaliseScenario` canonicaliser (accepts legacy single `priceMultiplier`); per-class price multipliers thread through `_projectClass`; pax-pool elasticity reads Y multiplier only; new `_synthesizeOwnConnections` + `_capSynthesisCount` for slice 2a (clones own-only template, deep-clones legs with `isOurs:true`, distinct synth flightCode, capped at +ceil(0.5×currentFreq) and `MAX_FOR_SOFTMAX − existingTotal`); new `CARGO_PAYLOAD` constant + `_projectCargo` mirror of `_projectClass` (no comfort lift, no rating shift); cargo branch populates `perClass.CARGO`, shifts `cargoDemandPool` via `cargoElasticity` when `cargoMultiplier !== 1`, threads `override.cargoYieldPerKgKm` into projected `_estimate`; output envelope adds `baseline.cargoPerWeek` / `projected.cargoPerWeek` / `delta.cargoPerWeek`; slice 1 freq-suppression block removed |
| ORS Sandbox slice 2 (a/b/d/e) | `modules/route-assistant/panel.js` | extended — `_renderOrsSandbox` calls `RouteAssistantOrsModel._normaliseScenario` for both saved + legacy fallbacks; `_buildOrsSandboxScenarioCard` replaces single Y slider + C/F preview with a per-class loop (renders only classes with cached fares — single-class collapse mirrors slice 1) via new `_buildOrsSandboxPriceSliderRow`; cargo slider (green accent) appears when `orsByClass.CARGO` is cached or `cargoDemandPool > 0`; `_updateOrsSandboxCFPreview` removed; `_recomputeOrsSandbox` persists `priceMultipliers` + `cargoMultiplier` via `_normaliseScenario`; `_buildOrsSandboxResultsCard.renderRow` accepts an annotation arg; new `_clampedClasses(result)` helper drives the rating-row asterisk + tooltip when `priceRatio ≥ ±0.5`; new Cargo/wk row in the outcome table when `cargoPerWeek` is non-null; freq slider hint reflects synthesis behaviour; `_assembleOrsSandboxRoute` propagates `cargoElasticity` |
| ORS Sandbox slice 2 (a/b/d/e) | `modules/route-assistant/settings-store.js` | extended — `_mergeOrsSandbox` validator accepts both legacy `priceMultiplier` numeric and slice 2 `priceMultipliers: {Y,C,F}` object; new `_coerceScenarioMultipliers` (clamps each class to `[0.30, 3.00]`, fans legacy single value to all three classes, returns null on malformed → record dropped); new `_coerceCargoMultiplier` (clamps to `[0.30, 3.00]`, defaults to 1.0); persisted entries always written under slice 2 shape |
| Used Aircraft Scanner | `modules/used-aircraft-scanner/family-grid-panel.js` | NEW (slice 1) |
| Used Aircraft Scanner | `modules/used-aircraft-scanner/type-family-map.js` | extended (slice 1) |
| Used Aircraft Scanner | `modules/used-aircraft-scanner/results-table.js` | extended (slice 1 + 2) |
| Used Aircraft Scanner | `modules/used-aircraft-scanner/deal-metrics.js` | NEW (slice 2 — pure-function metrics) |
| Used Aircraft Scanner | `modules/used-aircraft-scanner/presets-store.js` | extended (slice 2 — 4 new scoring fields) |
| Used Aircraft Scanner | `content_dashboard.js` | extended (slice 1 + 2; `loadDealContext` helper) |
| Used Aircraft Scanner / Route Assistant cross-link | `modules/route-assistant/panel.js` | extended (slice 2 — `_publishTopRoutes`) |
| Used Aircraft Scanner | `manifest.json` | extended (registered family-grid-panel.js, deal-metrics.js, RA fleet-store.js + settings-store.js in dashboard block) |
| Auto-Pricing | `modules/route-assistant/schedule-page-scraper.js` | NEW (misnamed — scrapes scheduling page, not prices) |
| Auto-Pricing | `modules/route-assistant/settings-store.js` | extended (`pricing` block added) |
| Auto-Pricing | `modules/route-assistant/panel.js` | extended (Eq/Dep/Wk columns, expander, _applyCachedPrices) |
| Auto-Pricing | `content_scheduling.js` | extended (live-capture on /app/com/scheduling/<HUB><DEST>) |
| Auto-Pricing | `manifest.json` | extended (registered schedule-page-scraper.js) |
| Yield Feedback (G slice 1) | `modules/route-assistant/yield-history-store.js` | NEW (Roadmap G slice 1) |
| Yield Feedback (G slice 1) | `modules/route-assistant/yield-snapshot.js` | NEW (Roadmap G slice 1) |
| Yield Feedback (G slice 1) | `modules/route-assistant/aggregator.js` | extended (yieldHistoryMap input, applyYieldHistory, actuals projection) |
| Yield Feedback (G slice 1) | `modules/route-assistant/settings-store.js` | extended (`yieldFeedback` block) |
| Yield Feedback (G slice 1) | `modules/route-assistant/panel.js` | extended (snapshot expander, Actuals column group, sparkline tooltip, calibrate button) |
| Yield Feedback (G slice 1) | `manifest.json` | extended (registered yield-history-store.js + yield-snapshot.js) |
| Yield Feedback (G slice 2) | `modules/route-assistant/yield-snapshot.js` | extended — `BASELINE_KEY = "routeAssistant:yieldBaselines"`, `_buildRegProfitMap(aircraftRecs, baselines)` delta-aware, `_saveBaselines` persists current cumulative numbers post-run; result includes `mode`, `tailsUsingDelta` |
| Yield Feedback (G slice 2) | `modules/route-assistant/aggregator.js` | extended — row carries `actualSnapshotMode` for tooltip |
| Yield Feedback (G slice 2) | `modules/route-assistant/settings-store.js` | extended — `yieldFeedback.deltaMode` |
| Yield Feedback (G slice 2) | `modules/route-assistant/panel.js` | extended — Delta mode + Auto on mount checkboxes; **Calibrate flagged (N)** button + `_flaggedRoutesForCalibration` + `_openCalibrateFlaggedModal`; V+/V− pill on the status column; `_maybeAutoSnapshot()` after `mount()` refresh; diag block + summary log surface snapshot mode |
| Yield Feedback (G slice 3) | `modules/route-assistant/panel.js` | extended — `derivedYieldFromActuals` refactored to `{side, value, alt}`; pax + cargo solvers w/ dominant-side auto-pick; per-route override editor calibrate branches; Calibrate-flagged modal gains per-row Side dropdown w/ live Old/New refresh + branched save |
| Yield Feedback (G slice 4 sessions 1+2) | `content_aircraftFlights.js` | extended — `getFlights()` extracts `flightNumberId`, `originIata`, `destinationIata`, `depUtc` per row; saved cache gains `flights[]` envelope. Fixed pre-existing flightId regex bug (`?id=` query-string match instead of leading `\d+`). |
| Yield Feedback (G slice 4 sessions 1+2) | `modules/route-assistant/yield-snapshot.js` | extended — `_collectPerFlightProfits(all, server, hubFilter, aircraftRecs)` joins envelope × flightInfo per `{originIata, destinationIata}` route; `attributionMode: "per-flight"` short-circuit in `takeSnapshot`; `_weeklyFreqOf(rec)` helper; result includes `attributionMode`, `routesWithPerFlight`, `routesFellBack`, `perFlightAvailable` |
| Yield Feedback (G slice 4 sessions 1+2) | `modules/route-assistant/panel.js` | extended — attribution dropdown adds `per-flight (exact)` as first option; diag block + summary log report per-flight coverage |
| Carriers (F) | `modules/route-assistant/carriers-scraper.js` | NEW (per-pair flightsfrom scraper + bulkScrape + competitive-intensity classifier) |
| Carriers (F) | `modules/route-assistant/settings-store.js` | extended (`carriers` block — showCarrierIntensity, concurrency, staggerMs, lastBulkScrapeAt, carriersMaxAgeDays) |
| Carriers (F) | `modules/route-assistant/panel.js` | extended (`_applyCachedCarriers`, `_renderCarriersSection` expander, `_runBulkCarrierScrape`, Cmp column → colored pill + hover tooltip via `formatCarriersTooltip`) |
| Carriers (F) | `manifest.json` | extended (registered carriers-scraper.js in scheduling block) |
| Carriers (F slice 2) | `modules/route-assistant/enterprise-meta-scraper.js` | NEW (per-enterprise `/app/info/enterprises/<id>` scraper for banner + avatar + name + IATA; bulk + cache mirror of carriers-scraper) |
| Carriers (F slice 2) | `modules/route-assistant/panel.js` | extended (`_applyCachedEnterpriseMeta`, `_runBulkEnterpriseMetaSync`, `_openCarrierPopover` / `_closeCarrierPopover` / `_positionCarrierPopover` / `_buildCarrierRow`, Cmp column rewired to open the rich popover when AS market-share data is present, `_initialBadge` helper) |
| Carriers (F slice 2) | `modules/route-assistant/settings-store.js` | extended (`carriers` block — added enterpriseMetaConcurrency, enterpriseMetaStaggerMs, enterpriseMetaMaxAgeDays, lastEnterpriseMetaSyncAt) |
| Carriers (F slice 2) | `manifest.json` | extended (registered enterprise-meta-scraper.js in scheduling block) |
| Tabbed RA view | `modules/route-assistant/panel.js` | extended (tabBar above tableHost, `_renderTabBar`, `_currentViewMode`, `RouteAssistantPanel._columnModes`, mode-filtered `_activeColumns` + scoring blend + settings drawer) |
| Tabbed RA view | `modules/route-assistant/settings-store.js` | extended (`viewMode: "all"\|"pax"\|"cargo"` top-level field with defaults + load merge) |
| Tabbed RA view | `modules/route-assistant/panel.js` SCORING_FIELDS | each entry tagged with `modes` array (paxScore = ["all","pax"], cargoScore = ["all","cargo"], everything else = all three) |
| Demand depth (K) | `modules/route-assistant/markets-page-scraper.js` | extended — per-payload historic fetch via `?payload=` query param, `byPayload` cache shape with backwards-compat read (`_migrateHistoricRecord`), `_mergeHistoric` to fold partial scrapes into the union, new `scrapeHistoricPayload` + `bulkScrapeHistoric` orchestration |
| Demand depth (K) | `modules/route-assistant/inventory-page-scraper.js` | NEW — direct fetch of `/app/com/inventory/<HUB><DEST>`, multi-strategy parser for per-class RM buckets + forward departures, circuit breaker on consecutive 429/503 |
| Demand depth (K) | `modules/route-assistant/demand-derivator.js` | NEW — pure functions: pool / avgPrice / elasticity (log-log regression, clipped) / rmTightness. PAX series prefers explicit byPayload.PAX; falls back to summed Y+C+F |
| Demand depth (K) | `modules/route-assistant/aggregator.js` | extended — passes `paxDemandPool` / `cargoDemandPool` / `useRealDemandForLF` from fleet context to estimator |
| Demand depth (K) | `modules/route-assistant/profit-estimator.js` | extended — opt-in `useRealDemandForLF` path: when set + pool present + freq>0, LF = clamp(pool / (seats×freq), [LFmin, LFmax]). Existing paxScore-interpolated path kept as fallback |
| Demand depth (K) | `modules/route-assistant/settings-store.js` | extended — new `demandDepth` block (showDemandColumns, classCoverage, useRealDemandForLF, concurrency 3 / stagger 1200ms, historicWindowPeriods 12, lastBulkScrapeAt, historicMaxAgeDays null, inventoryMaxAgeDays 3) |
| Demand depth (K) | `modules/route-assistant/panel.js` | extended — 5 new SCORING_FIELDS (paxDemandPool/cargoDemandPool/paxElasticity/cargoElasticity/rmTightness) with mode-tags; new "demand" column group (Pool / C-Pool / Elast / C-Elast / Avg$ / RM%); `_columnModes` extended with pax*/cargo* prefix derivation; `_applyCachedDemand`, `_runBulkDemandSync`, `_renderDemandDepthSection` (slate-tinted expander with confirm gate on `useRealDemandForLF` toggle); fleet context carries the toggle into the estimator |
| Demand depth (K) | `manifest.json` | extended — registered `inventory-page-scraper.js` + `demand-derivator.js` in scheduling block |
| Markets (Tier 2a) | `modules/route-assistant/markets-page-scraper.js` | NEW (per-route scraper writing 4 split key families: competitors, ownPricing, marketShare, historic) |
| Markets (Tier 2a) | `content_markets.js` | NEW (live-capture on `/app/com/markets/<HUB><DEST>`) |
| Markets (Tier 2a) | `modules/route-assistant/settings-store.js` | extended (`marketAnalysis` block — showColumns, concurrency, staggerMs, per-family TTLs, defaultPayloadChart) |
| Markets (Tier 2a) | `modules/route-assistant/panel.js` | extended (`_applyCachedMarkets`, `_renderMarketAnalysisSection` expander, `_runBulkMarketScrape`, `markets` column group + 4 cols Mkt%/Cmp#/Cmp$/Drft) |
| Markets (Tier 2a) | `manifest.json` | extended (registered markets-page-scraper.js in scheduling block + new content-script block for `/app/com/markets/*`) |
| ORS (Tier 2b) | `modules/route-assistant/ors-scraper.js` | NEW (per-route GET→POST handshake, walks all result pages, computes 5 rank flavors + 4 ratings, circuit-breaker, full connections cached) |
| ORS (Tier 2b) | `modules/route-assistant/settings-store.js` | extended (`ors` block — every form input + every rank flavor exposed; primaryColumn dropdown drives the headline panel column) |
| ORS (Tier 2b) | `modules/route-assistant/panel.js` | extended (`_applyCachedOrs`, `_renderOrsRankSection` expander with all controls, `_runBulkOrsScrape` w/ circuit-breaker UI, `_openOrsConnectionsDrawer` drill-in modal, `ors` column group + 4 cols ORS/RkNS/Gap/OrsC#) |
| ORS (Tier 2b) | `manifest.json` | extended (registered ors-scraper.js in scheduling block) |
| Diff against last visit | `modules/route-assistant/panel.js` | extended (constructor `_diffPrevSnapshot` + `_diffBaselineLoaded` + `_diffBaselineHub`; `refresh()` per-(mount,hub) baseline load via `_loadDiffSnapshot`; `_renderRows` decorates `this.scoredRows` via `_decorateRowsWithDiffs`; `_drawTable` writes via `_writeDiffSnapshot`; module helpers `RA_DIFF_TRACKED` / `_loadDiffSnapshot` / `_writeDiffSnapshot` / `_decorateRowsWithDiffs` / `_formatDiffNumber` / `_appendDiffBadge`; 11 column render closures append the badge: score / paxScore / cargoScore / weeklyFlights / Cmp / Mkt% / Pool / C-Pool / Gap / RM%) |
| Docs | `HANDOVER.md` | rewrite (this file) |
| J slice 3 + 4 v3 | `modules/used-aircraft-scanner/deal-metrics.js` | extended (routeFit() reads `weeklyFlights`; `frequencyLimited` counter + label; `decorate()` passes `weeklyDemandPerScorePoint`; new row fields surfaced) |
| J slice 3 + 4 v3 | `modules/used-aircraft-scanner/results-table.js` | extended (route-fit tooltip lists all 3 gates; new `_formatPricePerSeatTooltip`/`_formatMaintTooltip`/`_formatFleetTooltip` wired into the column definitions; redundant inner-pill `title` attrs removed) |
| J slice 4 v3 | `modules/used-aircraft-scanner/presets-store.js` | extended (`routeFit.weeklyDemandPerScorePoint = 100` default; deep-merge auto-fills for older settings blobs) |
| F slice 3 | `modules/route-assistant/contractual-partners-scraper.js` | NEW (per-enterprise `?tab=1` scraper; `table.partners` parser keeps relations verbatim; bulk + cache mirror of `enterprise-meta-scraper`) |
| F slice 3 | `modules/route-assistant/settings-store.js` | extended (`carriers` block — `myEnterpriseIds`, `partnersConcurrency`, `partnersStaggerMs`, `partnersMaxAgeDays`, `lastPartnersSyncAt`, `showInterliningGlyph`, `showAllianceGlyph`) |
| F slice 3 | `modules/route-assistant/panel.js` | extended (`_applyCachedContractualPartners`, `_runRefreshContractualPartners`, ⇄/✦ glyphs in `_buildCarrierRow` via new `nameRow` wrapper, "Contractual partners" sub-panel in the Carriers expander with own-IDs input + Refresh CTA + glyph toggles) |
| F slice 3 | `manifest.json` | extended (registered `contractual-partners-scraper.js` in the scheduling block) |
| Watchlist (Daily-driver QoL slice 2) | `modules/route-assistant/watchlist-store.js` | wired in (file existed from a prior session but was never registered or referenced — no code change to the store itself) |
| Watchlist (Daily-driver QoL slice 2) | `modules/route-assistant/settings-store.js` | extended (new `watchlist` block — `floatStarredToTop`, `showAlertBadges`) |
| Watchlist (Daily-driver QoL slice 2) | `modules/route-assistant/panel.js` | extended (constructor `_watchlist` Set; `refresh()` loads via `RouteAssistantWatchlistStore.load()`; `_renderRows` decorates `r._starred`; `_sortRows` floats starred rows; `_syncRenderContext` exposes `_showWatchTriggers`; `_toggleWatchlist(hub, dest)` instance method; destIata render closure prepends `_buildWatchlistStar(row, hub)`; new top-level `RA_WATCH_TRIGGERS` + `_evaluateWatchTriggers(row)` + `_buildWatchlistStar(row, hub)`) |
| Watchlist (Daily-driver QoL slice 2) | `manifest.json` | extended (registered `watchlist-store.js` in the scheduling block, immediately before `panel.js`) |
| Settings export / import (Daily-driver QoL slice 3) | `modules/route-assistant/panel.js` | extended (`_exportConfig` / `_openImportConfig` / `_showImportDiffModal` / `_openConfigMenu` instance methods; new "⇅" header button between settings/minimise; module helpers `AES_CONFIG_FORMAT` constant + `_diffConfig` recursive object diff + `_formatDiffValue`; both `RouteAssistantSettings.save` and `UsedAircraftPresets.save` reused for write-back so per-store deep-merge handles missing keys gracefully) |
| Route notes (Daily-driver QoL slice 4) | `modules/route-assistant/route-note-store.js` | NEW (mirrors route-overrides-store CRUD pattern; PREFIX/MAX_TEXT_LEN constants; directional pair key) |
| Route notes (Daily-driver QoL slice 4) | `modules/route-assistant/aggregator.js` | extended (destructure `routeNoteMap`, attach `row.routeNote` + `row.routeNoteText`) |
| Route notes (Daily-driver QoL slice 4) | `modules/route-assistant/panel.js` | extended (constructor `routeNoteMap` + `_routeNotePopover*` state; `refresh()` loads via `RouteAssistantRouteNoteStore.getMany`; `dispose()` calls `_closeRouteNotePopover`; row click delegation routes `[data-routenote-trigger='1']` to the popover; Dest column render closure appends 📝 icon (subdued/full + tooltip + cell-wide title); new `_openRouteNotePopover` / `_closeRouteNotePopover` instance methods modeled on `_openProfitModifierPopover`) |
| Route notes (Daily-driver QoL slice 4) | `manifest.json` | extended (registered `route-note-store.js` next to `route-overrides-store.js` in the scheduling content-script block) |
| Wave View (H slice 1) | `modules/route-assistant/wave-overlay.js` | NEW (RouteAssistantWaveOverlay — `buildRoutesFromScoredRows`, `buildSchedule`, `renderGantt`; pure-function bridge from RA scoredRows → ScheduleBuilder → Gantt) |
| Wave View (H slice 1) | `modules/route-assistant/panel.js` | extended (constructor `_waveBuild` / `_wavePresets` / `_waveBuildHub`; new `📊` header button + `_toggleWaveView` instance method; `_syncRenderContext` mirrors button opacity/title; `_renderRows` branches on `settings.waveView` after scoring + sort + watchlist decoration; new `_renderWaveOverlay(sorted)` + `_buildWaveHeader(preset, presets, topN)` instance methods) |
| Wave View (H slice 1) | `modules/route-assistant/settings-store.js` | extended (top-level `waveView: false` toggle + `waveOverlay` block — `lastPresetId`, `topN`, `showWarnings`, `showUnplaced`; deep-merged in `load()`) |
| Wave View (H slice 1) | `manifest.json` | extended (registered the schedule-management deps `range-buckets.js` / `schedule-store.js` / `presets-store.js` / `schedule-builder.js` in the scheduling block alongside the new `wave-overlay.js`, all loaded before `panel.js`) |
| ORS Sandbox (Letter I slice 1) | `modules/route-assistant/ors-model.js` | NEW (~510 LOC) — `RouteAssistantOrsModel` with public API `project({route, scenario, modelParams, economics, useRealDemandForLF})` + `calibrateTemperature({allRatings, ourIndices, observedShare})` + `findOurInLeaderboard(marketSharePax, ourEnterpriseId)`. Closed-form rating shift + numeric-stable softmax + per-class projection. Reuses `RouteAssistantProfitEstimator.estimate` with `override.paxLF` / `override.yieldPerKm` so revenue/cost math is shared. Pure functions, no DOM, no I/O. |
| ORS Sandbox (Letter I slice 1) | `modules/route-assistant/panel.js` | extended (~1380 LOC) — constructor `_orsSandboxRoute` / `_orsSandboxResult` / `_orsSandboxRaf` state; new `🧪` header button + `_toggleOrsSandbox` instance method; `_syncRenderContext` mirrors button opacity/title; `_renderRows` branches on `settings.orsSandbox.enabled` AFTER the Wave View branch (Wave wins when both on); new `_renderOrsSandbox` + `_buildOrsSandboxHeader` + `_buildOrsSandboxRoutePicker` + `_buildOrsSandboxNoOrsBanner` + `_buildOrsSandboxScenarioCard` + `_buildOrsSandboxResultsCard` + `_buildOrsSandboxNotes` + `_assembleOrsSandboxRoute` + `_recomputeOrsSandbox` (rAF-coalesced) + `_calibrateOrsSandboxT` + `_refreshOrsSandboxTBanner` + `_updateOrsSandboxCFPreview` + `_mkOrsSandboxRow` + `_formatOrsSandboxValue` instance methods; `_renderOrsSandboxSection` settings expander mounted below `_renderDemandDepthSection`; `contextmenu` row handler now opens a 2-item menu via new `_openRowContextMenu` (instance method) — first item preserves the existing override editor, second item routes to `_openInOrsSandbox`. |
| ORS Sandbox (Letter I slice 1) | `modules/route-assistant/settings-store.js` | extended (~25 LOC) — `orsSandbox` block in `_defaults()` (enabled / lastRouteIata / lastScenario / modelParams / perRouteTemperature) + new `_mergeOrsSandbox` deep-merge in `load()` so partial saves don't wipe user-tuned siblings. |
| ORS Sandbox (Letter I slice 1) | `manifest.json` | extended — registered `modules/route-assistant/ors-model.js` in the scheduling content-script block immediately before `panel.js` (after `wave-overlay.js`). |
| Foundation QoL — U14 toast primitive | `modules/route-assistant/toast-host.js` | NEW (~167 LOC) — `RouteAssistantToast` global with `show / dismiss / clearAll / info / success / warn / error`. Singleton bottom-right host, stack of up to 6, slide-in animation, auto-dismiss timer, optional action button. Reusable by every future save/sync/import flow. |
| Foundation QoL — U14 toast wiring | `modules/route-assistant/panel.js` | extended (3 sites, ~30 LOC) — `_toggleWatchlist` shows a Star/Unstar toast with one-click Undo + an error-toast on persistence failure; `_exportConfig` shows a success toast naming the downloaded file; `_calibrateOrsSandboxT` shows a success toast mirroring the in-card banner. |
| Foundation QoL — Q12 free-text search | `modules/route-assistant/panel.js` | extended (~50 LOC) — `_renderControls` prepends a 🔍 `<input type="search">` to the controls bar; `_applyFilters` extended to skip rows whose IATA / city / routeNoteText doesn't contain the query (case-insensitive). Debounced 200ms storage write + re-render. |
| Foundation QoL — Q12 settings | `modules/route-assistant/settings-store.js` | extended — top-level `searchQuery: ""` in `_defaults()` + load-merge accepting any string. |
| Foundation QoL — U15 group-header hover | `modules/route-assistant/panel.js` | extended (~30 LOC) — group `<th>` + column `<th>` + body `<td>` each get a `data-group` attribute; mouseenter on a group header toggles a CSS class on every cell with the matching attribute. |
| Foundation QoL — U15 CSS | `css/content.css` | extended (~15 LOC) — `.aes-grp-hover` rule lifts cell brightness via `box-shadow: inset 0 0 0 999px rgba(255,255,255,0.045)`. |
| Foundation QoL — U14 manifest | `manifest.json` | extended — registered `modules/route-assistant/toast-host.js` in the scheduling content-script block immediately before `panel.js`. |
| Q11 — single-step undo helper | `modules/route-assistant/panel.js` | extended (~50 LOC) — `_undoableSave({label, perform, restore, afterRestore, type, durationMs, actionLabel})` instance method. Composes with `RouteAssistantToast.show` to fire a 6s success toast with an Undo button wired to `restore`. Caller captures prev state in a closure; helper is store-agnostic. |
| Q11 — wiring | `modules/route-assistant/panel.js` | extended (~120 LOC, 4 sites) — profit-modifier popover Save + Clear and route-note popover Save + Delete now route through `_undoableSave`. Each captures the prev override/note record into a closure that the Undo button restores via the same store API. |
| Calibration timestamp | `modules/route-assistant/panel.js` | extended — `_calibrateOrsSandboxT` now writes `settings.routeAssistant.orsSandbox.perRouteTemperatureCalibratedAt[<HUB>-<DEST>]` alongside the per-route T so the sandbox results card can flag stale calibrations (markets drift; 30+ day-old calibrations should be re-run when new marketShare lands). Field was added to settings-store defaults this session by the user. |
| Active prompts (Q+) — store | `modules/route-assistant/alert-rules-store.js` | NEW (~167 LOC) — `RouteAssistantAlertRulesStore` with `add / update / remove / recordFired / load / saveAll`. Persists rules at `routeAssistant:alertRules`. Records normalised on every read; malformed rules dropped silently. Per-rule per-route cooldown via `lastFiredByRoute` + `cooldownHours`. |
| Active prompts (Q+) — evaluator | `modules/route-assistant/alert-evaluator.js` | NEW (~162 LOC) — pure-function `RouteAssistantAlertEvaluator.evaluate(rules, rows, ctx)`. Reads `row._diff.<field>` for `increased_by`/`decreased_by` operators, `row[<field>]` for `above`/`below`. Watchlist scope short-circuits via `row._starred`. `availableFields()` exposes 11 alert targets to the settings UI. |
| Active prompts (Q+) — panel wiring | `modules/route-assistant/panel.js` | extended (~150 LOC) — constructor `_alertRules` + `_alertFiredThisMount`; `refresh()` calls `_loadAlertRules()`; `_renderRows` calls `_evaluateAndFireAlerts(scoredRows)` after diff + starred decoration; new `_loadAlertRules`, `_evaluateAndFireAlerts`, `_scrollToRouteRow` instance methods; `_renderAlertRulesSection` settings expander mounted below ORS Sandbox section; `_buildAlertRuleRow` helper for list rendering. |
| Active prompts (Q+) — manifest | `manifest.json` | extended — registered `alert-rules-store.js` + `alert-evaluator.js` in the scheduling content-script block before `panel.js`. |
| N1 progress toasts | `modules/route-assistant/toast-host.js` | extended (~115 LOC) — new `RouteAssistantToast.progress(message, opts)` returning a `{id, dismiss, update, complete}` handle; `RouteAssistantToast.update(id, {message, progressPct, progressLabel})` mutates the existing toast in place; `RouteAssistantToast.complete(id, {type, message, duration})` converts sticky → auto-dismissing with terminal type. |
| N1 progress toasts wiring | `modules/route-assistant/panel.js` | extended (~30 LOC) — `_runBulkOrsScrape` opens a progress handle on entry, pumps `update(...)` from inside `onProgress`, calls `complete(...)` on finish (success / halted / failed). Other bulk-sync sites can adopt the same pattern in ~5 LOC each. |
| Q1 chip bar | `modules/route-assistant/panel.js` | extended (~80 LOC) — new `_renderQuickFilterChips(host)` instance method called from `_renderControls` after `_renderRecentHubsBar`. Iterates `[★ Watch, 💸 Loss, 🛠 Override, 📝 Note, Δ Changed]`; each chip toggles `settings.filters[<field>]`, persists, re-renders. Active state fills with the chip's accent color. |
| Q1 filter wiring | `modules/route-assistant/panel.js` | extended (~12 LOC inside `_applyFilters`) — `watchlistOnly` consults `this._watchlist`; `lossMakers` checks `r.profitPerWeek < 0`; `hasOverride`/`hasNote` short-circuit on `r.override` / `r.routeNoteText`. `onlyChanged` already lives at the post-decoration filter call site in `_renderRows`. |
| Q15 hub strip | `modules/route-assistant/panel.js` | extended (~90 LOC; partially user-pre-staged) — `_trackRecentHub(iata)` pushes the current hub onto `settings.recentHubs` (cap 5, dedupe case-insensitively); called from `refresh()`. `_renderRecentHubsBar(host)` renders the chip strip in the controls bar — empty when only one hub visited. `_attachHubShortcuts` / `_detachHubShortcuts` wire Alt+1..5 keyboard navigation. Skipped when focus is in an input/textarea/contentEditable. |
| Progress toast propagation | `modules/route-assistant/panel.js` | extended (~150 LOC across 5 bulk-sync sites) — `_runBulkPriceScrape`, `_runBulkCarrierScrape`, `_runBulkEnterpriseMetaSync`, `_runRefreshContractualPartners`, `_runBulkMarketScrape` (3-phase rolling bar), `_runBulkDemandSync` (2-phase rolling bar). All 6 bulk-sync sites now have uniform progress toasts (ORS already done in the previous slice). |
| N2 notification center — toast extension | `modules/route-assistant/toast-host.js` | extended (~40 LOC) — `_history` array (cap 50) with `MAX_HISTORY` constant; every `show()` call pushes a slim record. Accessors `getHistory()` / `clearHistory()`. History is in-memory only; closing the tab clears it. |
| N2 notification center — panel UI | `modules/route-assistant/panel.js` | extended (~150 LOC) — new 🔔 header button + `_openNotificationCenter(anchor)` instance method (popover with header + scrollable body + footer hint) + `_buildNotificationRow(entry)` helper. Outside-click + Escape teardown. Click any row with an action re-fires `action.fn()`. Module-scoped `_formatRelativeTime(ts)` helper. |
| Cluster A — U5 multi-select | `modules/route-assistant/panel.js` | extended (~140 LOC) — constructor `_selectedRoutes` + `_selectAnchorDest`. `_buildTable` checkbox column (header select-all with indeterminate state + body checkboxes per row) + 3px purple left-edge band on selected rows. `_handleRowSelectClick(row, ev)` for plain/shift/ctrl click handling. End of `_renderRows`: visible-IATA intersection + drop-toast wired in once. `_openRowContextMenu` extended with conditional `Add/Remove from selection (N)` entry. |
| Cluster A — U12 footer | `modules/route-assistant/panel.js` | extended (~100 LOC) — `_renderSelectionFooter(visibleRows)` mounted into `tableHost` AFTER `_buildTable` runs. Sticky-bottom (`position: sticky; bottom: 0; z-index: 3`). Skipped in Wave View / ORS Sandbox / heatmap modes. Count + Σprofit/wk + avg score + avg ourPaxShare reduce in one pass. Action buttons: Edit overrides… (Q2) + Compare (Q5, enabled at exactly 2 selected) + Clear. |
| Cluster A — Q2 bulk override | `modules/route-assistant/panel.js` | extended (~280 LOC) — `_openBulkOverrideEditor(selectedRows)` modal with chip list + per-field "apply this" tri-state inputs + Cancel/Apply. `_applyBulkOverride(selectedRows, fieldsToApply)` captures every prev override into `prevByPair`, merges per-route preserving untouched fields (CRITICAL: `RouteOverridesStore.save` is full-replace), `Promise.all` parallel writes, single `_recomputeProfit()` at end, wraps in `_undoableSave` with batched restore. Confirm above N=20; progress toast above N=50. |
| Cluster A — Q5 compare modal | `modules/route-assistant/panel.js` | extended (~160 LOC) — `_openCompareModal(selectedRows)` 3-column table for ~15 fields with direction-aware Δ coloring. Module-scoped `_formatCompareValue(v, fmt)` helper. Per-route Open-in-AS buttons in footer. |
| Cluster B — U11 hover preview | `modules/route-assistant/panel.js` | extended (~190 LOC) — constructor `_rowPreviewEl` + `_rowPreviewTimer` + `_rowPreviewLeaveTimer`. `_scheduleRowPreview(row, anchorEl)` 200ms dwell. `_scheduleRowPreviewDismiss()` 150ms grace. `_showRowPreview(row, anchorEl)` builds the singleton card with title + pills + breakdown table + stats. `_hideRowPreview()` cleans up. `mouseenter`/`mouseleave` wired on each `<tr>` in `_buildTable`. Card itself cancels dismiss on cursor enter. Module-scoped `_statusColor(s)` + `_fitColor(f)` helpers. |
| Cluster B — Per-hub topRoutes | `modules/route-assistant/panel.js` | extended (~30 LOC) — `_publishTopRoutes` now writes BOTH the legacy `routeAssistant:topRoutes` key (single global, current hub) AND a NEW `routeAssistant:topRoutes:<HUB>` per-hub key. Slim record gains `profitPerWeek` + `ourPaxShare` for Q13 metric options. New static `RouteAssistantPanel.loadAllHubTopRoutes(hubs)` returns Map<HUB, blob>. |
| Cluster B — Q13 yield heatmap | `modules/route-assistant/panel.js` | extended (~210 LOC) — new 🗺 header button + `_toggleHeatmap` method + `_renderHeatmap(sorted)` render branch (after Wave View / ORS Sandbox in `_renderRows`). `_buildHeatmapHeader(cfg)` with metric selector. Sticky-corner table with hub Y-axis + dest X-axis (sorted by current hub's score desc). Click cell → opens hub-dest scheduling page. Module-scoped `_heatmapColor(t)` for the three-stop gradient. |
| Cluster B — heatmap settings | `modules/route-assistant/settings-store.js` | extended (~10 LOC) — `heatmap: {enabled: false, metric: "score"}` block in `_defaults()` + load-merge entry. |
| Q8 — status-history store | `modules/route-assistant/status-history-store.js` | NEW (~129 LOC) — `RouteAssistantStatusHistoryStore` with `get / getMany / appendTransition / latestTransition / remove`. Per-route key `routeAssistant:statusHistory:<HUB>-<DEST>` (directional). Append-only with dedup-on-no-change inside `appendTransition`. Cap at 20 transitions per route — older trimmed via `splice`. |
| Q8 — panel detection + tooltip | `modules/route-assistant/panel.js` | extended (~80 LOC) — `refresh()` bulk-loads via `getMany` into `this.statusHistoryMap`. New `_detectStatusTransitions(rows)` step in `_renderRows` after `_evaluateAndFireAlerts`: walks each scored row, decorates `row._statusHistory = record`, appends a new transition (in-memory + fire-and-forget storage write) when status differs from the latest entry. St column render closure extended to compose a multi-line tooltip with status rule + "since X · was Y before" + N-transitions-tracked + recent-5 history. |
| Q8 — manifest | `manifest.json` | extended — registered `modules/route-assistant/status-history-store.js` in the scheduling content-script block alongside `route-note-store.js`. |
| Q3 — store | `modules/route-assistant/route-overrides-store.js` | extended (~30 LOC) — `_clean` accepts optional `expiresAt`; `_hasAnyValue` updated; new static helpers `isExpired(record, nowMs)` + `daysUntilExpiry(record, nowMs)`. File header documents the new field. |
| Q3 — editor | `modules/route-assistant/panel.js` | extended (~50 LOC in `_openOverrideEditor`) — new "Expires in" days input + live hint; pre-fills days-remaining when an unexpired `expiresAt` exists, or the date in red when expired. Save translates days → `Date.now() + days × 86400000`; empty/0/NaN → null. |
| Q3 — propagation | `modules/route-assistant/panel.js` | extended (~12 LOC) — `_openProfitModifierPopover` Save + Clear/Undo paths and `_applyBulkOverride` mergeRecord + Undo restore now carry `expiresAt` through unchanged. The popover/Q2 modal don't expose an expiry input — TTL only mutable via the full override editor. |
| Q3 — skip applying | `modules/route-assistant/aggregator.js` | extended (~6 LOC) — the estimator call gates `override` through `RouteAssistantRouteOverridesStore.isExpired(row.override) ? null : row.override`. Record stays on the row so the UI can still render the expired marker. |
| Q3 — row decoration + Dest icon | `modules/route-assistant/panel.js` | extended (~30 LOC) — `_renderRows` walker after `_detectStatusTransitions` decorates `row._expired` / `_expiresSoon` / `_expiresDays`. Dest cell render closure appends ⌛ (expired, grey) or ⏳ (expires soon, yellow) next to the existing 📌 override pin. |
| Q4 — saved views settings | `modules/route-assistant/settings-store.js` | extended (~6 LOC) — `savedViews: []` block in `_defaults()` + load-merge entry that filters malformed records. |
| Q4 — controls bar UI | `modules/route-assistant/panel.js` | extended (~160 LOC) — `_renderSavedViewsControl(host)` rendered in the controls bar after the Q1 chips. New `_saveCurrentView()` (window.prompt → snapshot), `_applySavedView(view)` (writes back to settings + `_render`), `_deleteSavedViewPrompt()` (numeric prompt + Undo via existing toast action slot). |
| Q6 — retirement planner button + modal | `modules/route-assistant/panel.js` | extended (~165 LOC) — new 🛩 header button + `_openRetirementPlanner()` instance method. Modal sorts aircraft by age desc, color-codes past-life (red) + retiring-within-threshold (yellow) rows, threshold input drives live re-render, per-row "Find replacement →" link to the dashboard's Used Aircraft Scanner. `MAX_LIFE_YEARS = 25` is module-local; tweak at one site if AS ever changes. |
| Q10 — opening checklist menu + popover | `modules/route-assistant/panel.js` | extended (~150 LOC) — `_openRowContextMenu` gains a conditional "Opening checklist…" entry when `row.status === "NEW"`. New `_openRouteOpeningChecklist(row)` instance method renders a 7-item ✓/✗ list (distance / demand / aircraft / service profile / markets / ORS / carriers). Each ✗ row gets a "fix this →" button that opens the relevant AS page or fires a Settings-pointer toast. Pure derivations from cached row state — no new I/O. |

Plan files written this session:
- `~/.claude/plans/abstract-popping-manatee.md` — used for slice 1 of J + earlier handoff plan.
- `~/.claude/plans/ticket-price-tier-1.md` — Auto-Pricing Tier 1 plan.
- `~/.claude/plans/let-s-start-with-1-a-quizzical-lynx.md` — Tier 2a (markets) + 2b (ORS) plan.
- `~/.claude/plans/enumerated-toasting-sonnet.md` — J slice 3 + J slice 4 v3 + F slice 3 plan (this session).
- `~/.claude/plans/kind-snuggling-chipmunk.md` — Watchlist / starred routes plan (this session).
- `~/.claude/plans/glittery-dreaming-noodle.md` — Wave View (Roadmap H slice 1) plan (this session).

---

## 3 · Project map

```
AES.v0.6.9-beta/
├── manifest.json                              MV3 manifest. Content-script blocks per AS URL pattern.
├── background.js                              MV3 service worker (minimal).
├── helpers.js                                 AS namespace: formatCurrency, cleanInteger, …
├── HANDOVER.md                                THIS file.
├── MANUAL.md                                  Long-form reference (~875 lines).
│
├── content_dashboard.js                       Dashboard panel mount. ~3700 lines. Hosts the Used Aircraft Scanner UI.
├── content_scheduling.js                      Mounts RouteAssistantPanel on /app/com/scheduling*; live-captures route data on /app/com/scheduling/<HUB><DEST>.
├── content_marketScan.js                      Used-aircraft-scanner CHILD TAB worker (runs on /app/aircraft/market*).
├── content_flightsFrom.js                     flightsfrom.com scraper (runs on www.flightsfrom.com).
├── content_stationOpen.js                     Station-opening automation.
├── content_aircraftFlights.js                 Per-aircraft flight-history extractor.
├── content_aircraftFlightPlan.js              Aircraft Flight Plan Assistant entry — calls window.AesAfp.mount() on load. Companion to modules/aircraft-flight-plan/.
├── content_fleetHub.js                        Fleet Hub entrypoint (overlay on /app/fleets*).
├── content_fligthSchedule.js                  Own-schedule extractor (note original typo).
├── content_inventory.js                       Inventory-page utilities.
├── content_personelManagement.js              Personnel staffing helpers.
├── content_enterpriceOverview.js              Enterprise overview helpers.
├── content_flightInfo.js                      Flight-detail data extractor.
├── content_settings.js                        AS settings-page helpers.
├── content_fleetManagement.js                 Fleet-page helpers.
│
├── modules/
│   ├── aes-menu.js                            Top-bar AES dropdown (every page).
│   ├── about-dialog.js                        About modal.
│   ├── aircraft-type-specs.js                 AESAircraftTypeSpecs.fetchById(typeId) — shared aircraft type-spec parser.
│   │
│   ├── used-aircraft-scanner/
│   │   ├── type-family-map.js                 AS_TYPE_TO_FAMILY map + AS_FAMILY_CATEGORY + helpers.
│   │   ├── presets-store.js                   UsedAircraftPresets settings CRUD.
│   │   ├── scan-session-store.js              MarketScanSession — per-scan storage.
│   │   ├── deal-metrics.js                    MarketScanDealMetrics — slice-2 metrics ($/seat, $/seat·km/yr, BE-days, fleet-synergy, route-fit, maint pill). NEW this session.
│   │   ├── results-table.js                   MarketScanResultsTable — sortable HTML + CSV. Family column + color rail + slice-2 deal columns.
│   │   ├── scan-controller.js                 ScanController — concurrent child-tab orchestrator.
│   │   └── family-grid-panel.js               MarketScanFamilyGrid — type picker.
│   │
│   ├── route-assistant/
│   │   ├── settings-store.js                  RouteAssistantSettings.load/save. Includes economics + pricing blocks.
│   │   ├── demand-store.js                    RouteAssistantDemandStore — per-IATA AS pax/cargo demand cache.
│   │   ├── country-resolver.js                IATA → countryId via DemandStore (cache-only).
│   │   ├── parallel-scanner.js                Concurrent CountryScraper runner; seedAllCountries().
│   │   ├── distance-resolver.js               Three-tier hub→dest distance with persistent cache.
│   │   ├── route-overrides-store.js           Per-route LF/yield user overrides.
│   │   ├── fleet-store.js                     User's fleet inventory cache.
│   │   ├── type-specs-store.js                AS aircraft-type spec cache.
│   │   ├── fuel-price-scraper.js              World fuel price scrape (table → SVG fallback).
│   │   ├── fuel-burn-estimator.js             Per-type fuel-burn heuristic + override.
│   │   ├── profit-estimator.js                Pure-function profit math.
│   │   ├── score.js                           Pure weighted-average normaliser.
│   │   ├── aggregator.js                      buildRouteRows + applyFleetContext.
│   │   ├── schedule-page-scraper.js           Scheduling-page scraper for live route data. (Storage prefix retains legacy `ticketPrice:` for compat.)
│   │   ├── carriers-scraper.js                Per-pair flightsfrom scraper for the full carrier list. NEW (Letter F). Bulk-scrape + competitive-intensity classifier.
│   │   ├── enterprise-meta-scraper.js         Per-enterprise /app/info/enterprises/<id> scraper for banner + avatar + name + IATA. NEW (F slice 2). Non-directional cache; powers the Cmp popover's rich rendering.
│   │   ├── contractual-partners-scraper.js    Per-(your-own)-enterprise /app/info/enterprises/<id>?tab=1 scraper for the contractual partners table (alliance + interlining + lessor). NEW (F slice 3). Cache keyed by your-own enterprise id; the popover cross-references competitor ids against the union of partner sets.
│   │   ├── inventory-page-scraper.js          Per-route /app/com/inventory/<HUB><DEST> scraper for RM buckets + forward departures. NEW this session (Letter K). Circuit breaker on consecutive 429/503.
│   │   ├── demand-derivator.js                Pure functions for pool / elasticity / RM tightness from cached markets historic + inventory + ownPricing. NEW this session (Letter K).
│   │   ├── markets-page-scraper.js            Markets-page scraper for /app/com/markets/<HUB><DEST>. NEW this session (Tier 2a). Writes 4 split key families: competitors, ownPricing, marketShare, historic. bulkLoadCache(pairs, {families}) reads any subset in one combined chrome.storage.local.get.
│   │   ├── ors-scraper.js                     ORS rank scraper for /app/info/ors. NEW this session (Tier 2b). Per-route GET → POST handshake (Wicket can't share sessions). Walks all result pages, computes 5 rank flavors + 4 ratings, stores full connection list. Circuit-breaker on 3× consecutive 429/503.
│   │   ├── wave-overlay.js                    Wave View Gantt renderer (H slice 1). Pure-function bridge from RA scoredRows → ScheduleBuilder → HTML/CSS swimlane Gantt. NEW this session.
│   │   └── panel.js                           RouteAssistantPanel — UI. ~4200 lines after this session.
│   │
│   ├── flightsfrom/
│   │   ├── data-store.js                      FlightsFromStore.
│   │   └── scan-controller.js                 FlightsFromController (child-tab orchestrator).
│   │
│   ├── station-automation/
│   │   ├── storage.js                         StationAutomation run-state storage.
│   │   └── country-scraper.js                 CountryScraper — country/county directory walker.
│   │
│   ├── schedule-management/
│   │   ├── range-buckets.js                   ScheduleFactors — distance/time math.
│   │   ├── presets-store.js                   SchedulePresets.
│   │   ├── schedule-store.js                  Built schedule cache.
│   │   ├── schedule-builder.js                ScheduleBuilder — preset → wave-aware flight records.
│   │   ├── open-stations-modal.js             Open stations modal (dashboard + fleet hub).
│   │   └── schedule-panel.js                  Dashboard schedule UI.
│   │
│   ├── aircraft-flight-plan/                  Aircraft Flight Plan Assistant — six-slice module on /app/fleets/aircraft/<id>/0.
│   │   ├── host.js                            Slice A — publishes window.AesAfp (ctx, bus, slot, mount, getCurrentSchedule, getNewFlightForm).
│   │   ├── settings-extension.js              AesAfpSettings.load/save — top-level settings.aircraftFlightPlan block.
│   │   ├── state-store.js                     AesAfpStateStore — per-aircraft state (also loaded via /app/fleets* for Fleet Hub).
│   │   ├── audit-log.js                       Slice F — global timeline + per-aircraft ring; subscribes to bus events; emits toasts via RouteAssistantToast.
│   │   ├── spec-resolver.js                   Slice B — typeId resolution cascade (fleet → page-scan → cache → fetchById).
│   │   ├── route-candidates.js                Slice C — scored candidate list (FlightsFromStore + DemandStore + RA Score) with chip filters.
│   │   ├── form-driver.js                     Slice D — pre-fills the New Flight Number form. SAFETY: never POSTs, never clicks submit.
│   │   └── wave-applier.js                    Slice E — wraps RouteAssistantWaveOverlay.buildSchedule + renderGantt; per-leg Apply emits candidate:selected.
│   │
│   ├── fleet-hub/                             Fleet Hub overlay on /app/fleets*.
│   │   ├── host.js                            Hub mount + lifecycle.
│   │   ├── aircraft-aggregator.js             Per-aircraft data collation across cached AFP state + RA stores.
│   │   ├── inline-table.js                    Inline aircraft table.
│   │   ├── schedule-row.js                    Per-row schedule renderer.
│   │   └── summary-strip.js                   Top summary widgets.
│   │
│   ├── aircraft-flights/                      Per-aircraft history panel modules.
│   ├── inventory/                             Inventory-page validation.
│   └── data-models/                           Shared data shapes.
│
├── css/content.css                            Cross-feature styles.
├── images/                                    Logo assets.
├── popup.html / popup.js / options.html / options.js
└── js/jquery-3.4.1.min.js                     Vendored jQuery.
```

---

## 4 · chrome.storage.local keys

| Key pattern | Writer | Shape |
|---|---|---|
| `settings` | extension-wide | `{routeAssistant: {…}, usedAircraftScanner: {…}, …}` — single shared blob, deep-merged on load. |
| `flightsFrom:<IATA>` | flightsfrom scraper | `{iata, scrapedAt, airportName, routes: [{destIata, destName, weeklyFlights, seatsPerWeek, distanceKm?, airlines, aircraft, detailUrl}]}` |
| `flightsFrom:<IATA>:status` | flightsfrom scraper | `{iata, scanId, status, error?, progress: {phase, routeCount?}}` |
| `routeAssistant:demand:<IATA>` | DemandStore | `{iata, name?, airportId?, countryId?, paxScore, cargoScore, scrapedAt, lat?, lon?}` |
| `routeAssistant:distance:<MIN>-<MAX>` | DistanceResolver | `{distanceKm, source: "as-scheduling"\|"as-coords"\|"ff-detail", resolvedAt}`. **Pair key alphabetically sorted** (symmetric). |
| `routeAssistant:override:<HUB>-<DEST>` | RouteOverridesStore | `{hub, dest, paxLF?, cargoLF?, yieldPerKm?, cargoYieldPerKgKm?, note?, expiresAt?, createdAt, updatedAt}`. Directional. **Q3 expiresAt** — optional unix-ms timestamp; when present and in the past, callers (aggregator) skip applying the numeric fields, but the record stays in storage so the UI can render the expired marker and the user can review/clear. Records WITHOUT `expiresAt` never expire. |
| `routeAssistant:fuelPriceIndex` | fuel-price-scraper | `{value, unit: "ASc$/l"\|"index", date, scrapedAt, source: "table"\|"svg", sourceUrl, history?}` |
| `routeAssistant:fuelBurnOverride:<typeId>` | fuel-burn-estimator | `{typeId, cycleL, perKmL, source: "manual", updatedAt}` |
| `routeAssistant:typeSpec:<typeId>` | type-specs-store | `{typeId, typeName, seats, range, speed, cargoCapacity, paxSatisfaction?, …}` |
| `routeAssistant:ticketPrice:<HUB>-<DEST>` | ticket-price-scraper (this session) | `{hub, dest, scrapedAt, source: "live"\|"fetch", flights[], primaryAircraftType, primaryAircraftTypeId, primaryAircraftReg, departureTime, weeklyFlights, dailyFlights[7], daysPerWeek, cruiseSpeedKmh, ourPrice: null, ourYield: null, orsRank: null, fareClasses: null}`. **Directional** (price/freq differ by direction). Tier 2 fields stay null until those scrapers ship. |
| `routeAssistant:yieldHistory:<HUB>-<DEST>` | yield-snapshot.js (this session) | `{hub, dest, snapshots: [{timestamp, profitPerFlight, profitPerWeek, frequency, aircraftTypeNames, aircraftRegistrations, contributingTails, totalKnownTails, attributionMode}], lastSnapshotAt}`. **Directional**. Snapshots pruned to `historyLimit` (default 12) on each append. |
| `routeAssistant:topRoutes` | route-assistant/panel.js (`_publishTopRoutes`) | `{hub, server, scrapedAt, count, rows: [{destIata, destName, distanceKm, score, status, paxScore, cargoScore, weeklyFlights, profitPerWeek, ourPaxShare}]}`. **Single global key** — overwritten on every panel render with up to 50 visible scored rows for the **current hub only**. Consumer: Used Aircraft Scanner deal-metrics (route-fit count). |
| `routeAssistant:topRoutes:<HUB>` | route-assistant/panel.js (`_publishTopRoutes`) | Same shape as `routeAssistant:topRoutes` but **per-hub** — each panel mount writes its own hub's key. Consumer: Q13 yield heatmap (`RouteAssistantPanel.loadAllHubTopRoutes(hubs)`). Populates incrementally as the user visits each hub's panel. |
| `routeAssistant:statusHistory:<HUB>-<DEST>` | status-history-store.js (Q8) | `{hub, dest, transitions: [{from, to, at}, …], updatedAt}`. **Directional** pair key. Append-only timestamped log of status transitions (NEW → OK → UNDER → OVER → OOR). First observation has `from: null`. Cap at 20 transitions per route (oldest trimmed). Consumer: St cell tooltip on the Route Assistant panel via `row._statusHistory` (decorated by `_detectStatusTransitions` on every render). |
| `routeAssistant:lastSnapshot:<HUB>` | route-assistant/panel.js (`_writeDiffSnapshot`) | `{hub, server, scrapedAt, rows: [{destIata, score, paxScore, cargoScore, weeklyFlights, airlineCount, competitorCount, paxDemandPool, cargoDemandPool, ourPaxShare, orsRatingGapToTop, rmTightness}]}`. **Per-hub key** (one record per hub the user has visited). Overwritten on every panel render from `this.scoredRows`. Read once per `(mount, hub)` pair in `refresh()` so the in-memory baseline anchors to "what the user saw last visit" rather than re-baselining on every storage event. **Row-property field names** (so `orsRatingGapToTop`, not `ratingGapToTop`). Consumer: render closures, via `_decorateRowsWithDiffs`. |
| `routeAssistant:watchlist` | watchlist-store.js (Daily-driver QoL slice 2) | `{pairs: ["<HUB>-<DEST>", ...], updatedAt}`. **Single global key**, NOT per-hub. Pair key is **directional** `<HUB>-<DEST>` uppercased. Loaded fresh per `refresh()` into the panel's `_watchlist` Set; the panel filters by current hub at render time. Consumer: `RouteAssistantPanel._buildWatchlistStar` (star glyph) + `_sortRows` (float-to-top) + `_evaluateWatchTriggers` (red dot when row's `_diff` shows worsening change). |
| `routeAssistant:routeNote:<HUB>-<DEST>` | route-note-store.js (Daily-driver QoL slice 4) | `{hub, dest, text, createdAt, updatedAt}`. **Directional** pair key, matches `routeAssistant:override:` orientation. Empty/whitespace text removes the key entirely. Loaded once per `refresh()` into `this.routeNoteMap`; consumer is the Dest column 📝 icon render + `_openRouteNotePopover`. Storage listener NOT wired — saves go through the panel which re-renders directly, so a chrome.storage.onChanged hook would just trigger redundant `refresh()` calls. |
| `routeAssistant:sandboxBacktest:<HUB>-<DEST>` | sandbox-backtest-store.js (ORS Sandbox slice 3b) | `{hub, dest, entries: [{ts, trigger: "calibrate"\|"tier3-apply", scenario, modelParams, projected: {share, paxPerWeek, revenuePerWeek, profitPerWeek}, observed?: {share, paxPerWeek?, period}, backfilledAt?}], updatedAt}`. **Directional** pair key. Cap MAX_PER_ROUTE=50 entries via `shift()`. Writers: `_calibrateOrsSandboxT` logs immediately with `observed.share` from the leaderboard; `_runBulkMarketScrape` calls `backfillManyFromMarketShares(...)` to fill in `observed` for entries logged within BACKFILL_WINDOW_MS=8d before the scrape. Consumer: `_computeOrsSandboxModelFit` aggregates bias + RMSE across every route via `loadAll()`; the settings drawer's "Model fit" row reads it. |
| `routeAssistant:carriers:<HUB>-<DEST>` | carriers-scraper.js (Letter F) | `{hub, dest, scrapedAt, source: "ff-detail", carriers: [{name, code?, weeklyFlights?, aircraftTypes?: []}], totalAirlines, totalWeeklyFlights, parserNotes?}`. **Directional**. Sourced from `flightsfrom.com/<HUB>-<DEST>` cross-origin fetch. `parserNotes` populated when no carrier markup matched (SPA didn't SSR the list). |
| `routeAssistant:enterpriseMeta:<id>` | enterprise-meta-scraper.js (this session — F slice 2) | `{enterpriseId, server, name, iata?, bannerUrl?, avatarUrl?, scrapedAt, parserNotes?}`. **NOT directional** — enterprise metadata is a property of the enterprise, not the route, so the same record serves every route that competitor appears on. Sourced from `/app/info/enterprises/<id>` cross-origin fetch (covered by `host_permissions`). 90-day default TTL; user-driven re-sync via the Carriers expander CTA. |
| `routeAssistant:contractualPartners:<enterpriseId>` | contractual-partners-scraper.js (this session — F slice 3) | `{enterpriseId, server, scrapedAt, partners: [{partnerId, partnerName, partnerIata, hqCity?, hqIata?, country?, relations: ["INTERLINING", "ALLIANCE", "LESSOR", ...]}], parserNotes?}`. **NOT directional, AND keyed by *your-own* enterprise id** (not by competitor — opposite to `enterpriseMeta`). One fetch of `/app/info/enterprises/<your_id>?tab=1` yields every partnership the enterprise has. The Cmp popover unions all your-own records into `_partnersByEnterpriseId` and renders glyphs (⇄ for INTERLINING, ✦ for ALLIANCE) next to matching competitor names. 30-day default TTL. |
| `routeAssistant:markets:competitors:<HUB>-<DEST>` | markets-page-scraper.js (this session — Tier 2a) | `{hub, dest, scrapedAt, source: "live"\|"fetch", competitors: [{flightCode, flightId, typeCode, typeId, depDateUtc, depDateLocal, depTimeUtc, depTimeLocal, arrTimeUtc, arrTimeLocal, serviceClass, availability, price, status, isOurs}]}`. **Directional**. Every competitor flight on the route with its actual prices + availability + status. |
| `routeAssistant:markets:ownPricing:<HUB>-<DEST>` | markets-page-scraper.js (this session — Tier 2a) | `{hub, dest, scrapedAt, source, prices: {Y, C, F, Cargo}, defaults: {…}, sliderRanges: {Y: [1, 296], …}, generalSettings: {originTerminal, destinationTerminal, serviceProfile, boardingPreference, cargoPreference}}`. **Directional**. Snapshot of the markets-page Pricing fieldset; Tier 3 write-back will POST against the same endpoint. |
| `routeAssistant:markets:marketShare:<HUB>-<DEST>` | markets-page-scraper.js (this session — Tier 2a) | `{hub, dest, scrapedAt, source, period: "17/2026", pax: [{rank, name, enterpriseId, sharePct, change}], cargo: [{…}]}`. **Directional**. Default `shareMaxAgeDays: 7` (weekly cadence). |
| `routeAssistant:markets:historic:<HUB>-<DEST>` | markets-page-scraper.js (Tier 2a + K extension) | **K — new shape:** `{hub, dest, scrapedAt, source, byPayload: {ECONOMY: {periods, capacities, prices}, BUSINESS: …, FIRST: …, PAX: …, CARGO: …}}`. **Directional**. Parsed from inline `lineChart(…).setData([…])` calls. Legacy single-payload records (`{periods, capacities, prices, payload}`) are auto-migrated on read by `_migrateHistoricRecord`; the next scrape persists the new shape. |
| `routeAssistant:inventory:<HUB>-<DEST>` | inventory-page-scraper.js (this session — Letter K) | `{hub, dest, scrapedAt, source: "fetch", classes: {Y, C, F, Cargo: {totalSeats, soldSeats, avgFare?}}, departures: [{date, time, totalSeats, sold}], parserNotes?}`. **Directional**. Sourced from `/app/com/inventory/<HUB><DEST>`. Multi-strategy parser; falls back gracefully when AS markup shifts. RM data is volatile — default 3-day TTL. |
| `routeAssistant:ors:<HUB>-<DEST>` | ors-scraper.js (Tier 2b — multi-class shape this session) | `{hub, dest, scrapedAt, params: {departureH, arrivalH, useGround}, ourFlightIds[], ourCarrierPrefixes[], classesScraped: ["ECONOMY","BUSINESS","FIRST"], byClass: {ECONOMY: {scrapedAt, totalConnections, rankAny, rankFirstLegOurs, rankAllOurs, rankNonstop, rankBookable, ourTopRating, ourBestNonstopRating, topCompetitorRating, ratingGapToTop, connections[]}, BUSINESS: {…same shape…}, FIRST: {…same shape…}}}`. **Directional**. Single key per route; ~30KB at 3 classes. **`byClass` is the source of truth** — composite/headline metrics are computed at read time in `_applyCachedOrs` from per-class records using the user's `combineMethod` + weights. NEVER store the composite — settings changes (weights, preset, combine method) re-project the composite without re-scraping. **Lazy migration:** legacy single-class records `{rankAny, …, connections}` are read-time-wrapped into `{byClass: {<original payload>: <legacy rank data>}, classesScraped: [<payload>]}` by `RouteAssistantOrsScraper._migrateOrsRecord`. |
| `<server><airline>schedule` | content_fligthSchedule.js | `{type:"schedule", server, airline, date: {<YYYYMMDD>: {…schedule arrays…}}}`. ORS scraper reads this to build the "our flight numbers" set for `isOurs` detection. |
| `routeAssistant:alertRules` | alert-rules-store.js (this session — Active Prompts) | `{rules: [{id, label, field, operator: "increased_by"\|"decreased_by"\|"above"\|"below", threshold, scope: "watchlist"\|"all", severity: "info"\|"warn"\|"error", enabled, cooldownHours, lastFiredByRoute: {<HUB>-<DEST>: msEpoch}, createdAt, updatedAt}], updatedAt}`. **Single global key** — rules are not per-hub; the evaluator filters at evaluation time by current hub's row set. **Stable contract** — adding new fields requires the existing fields stay shape-compatible since the store normalises on every read and would silently drop entries it can't validate. |
| `routeAssistant:pricingApplyLog` | pricing-apply-log.js (this session — Tier 3.1) | `{entries: [<ApplyRecord>], updatedAt}`. **Single global key** — capped at `apply.pricingApplyLogLimit` (default 200), newest-first. Identical-fingerprint entries within a 5-min window dedup-merge by bumping the head's `count`. Each `<ApplyRecord>` carries `{id, ts, hub, dest, status: "dry-run"\|"verified"\|"posted"\|"failed"\|"aborted", source: "manual"\|"sandbox"\|"batch"\|"silent-auto", scope, prevPrices, newPrices, requestedPrices, verifiedPrices, verifyAt, httpStatus, fingerprint, reason, sandboxScenario, projectedDelta, preflight: {blockers, warnings, deltas, percentDeltas}, error, warning, bodyPreview, dryRun, count}`. The `bodyPreview` is truncated at 1500 chars; the full body is reconstructible from the applier when needed (the audit trail prioritises readability). Both 3.2 (verified/posted) and 3.3 (silent-auto) write here. |
| `routeAssistant:pricingApplyLog:<HUB>-<DEST>` | pricing-apply-log.js (this session — Tier 3.1) | `{hub, dest, entries: [<ApplyRecord>], updatedAt}`. **Per-route ring** — capped at 20 entries per route, no dedup (every attempt for this route is worth seeing). Used by `getLastSuccessAt(hub, dest)` for cooldown enforcement (looks back through entries for the most recent `verified` OR `posted` record). Pair key directional `<HUB>-<DEST>` matching the orientation of every other per-route store. Cooldown skips `dry-run` and `failed` entries — only successful real writes start the cooldown clock. |
| `<server>aircraftFlights<aircraftId>` | content_aircraftFlights.js | per-aircraft profit/flight history. Used by Fleet Mgmt page directly; **read by yield-snapshot.js** to derive per-route actuals. Shape: `{aircraftId, server, registration, equipment, date, time, profit, profitFlights, finishedFlights, totalFlights, type:"aircraftFlights", flights: [{flightId, flightNumber, flightNumberId, status, originIata, destinationIata, depUtc}]}`. The `flights[]` envelope (G slice 4) is the per-FN linkage joined by `_collectPerFlightProfits` for exact attribution. Pre-G-slice-4 records are missing the envelope; visit the aircraft history page to populate. |
| `routeAssistant:yieldBaselines` | yield-snapshot.js | **NEW (G slice 2).** Single global blob `{<reg>: {profit, profitFlights, savedAt}}` of every observed tail's cumulative profit numbers from the most recent snapshot. Delta mode subtracts this from the next run's numbers to compute periodic per-tail $/flt. Refreshed on every snapshot regardless of mode so flipping into Delta later has a meaningful starting point. |
| `<server>marketScan:<scanId>` | scan-session-store | One scan's queue/state. |
| `<server>marketScan:<scanId>:r:<typeSlug>` | scan-controller (child tabs) | One per-type result blob. |
| `<server><airlineCode>stationAutomationRun:<runId>` | station-automation | One run's progress + results. |
| `aircraftFlightPlan:settings` | AesAfpSettings | `{enabled, defaultTopN, defaultPricePct, defaultService, showWavePreview, candidateChips: {rangeFitOnly, hideAlreadyScheduled, watchlistOnly}, lastSelectedPresetId, …}` — top-level sibling of `settings.routeAssistant`, not a child. |
| `aircraftFlightPlan:state:<server>:<aircraftId>` | AesAfpStateStore | `{currentLocationIata, currentLocationName, currentLocationAirportId, draftedPlan?, dismissedCandidates?, selectedPresetId?, lastFilledAt?, lastSeenAt}`. Per-aircraft, server-scoped. Read by Fleet Hub for the Loc column without re-scraping per aircraft. |
| `aircraftFlightPlan:auditLog` | AesAfpAuditLog | Global timeline, cap 200 newest-first. Each entry: `{id, ts, type, payload, server?, aircraftId?}` for type ∈ {candidate-clicked, form-filled, form-cleared, wave-generated, leg-applied, preset-selected, candidate-dismissed}. |
| `aircraftFlightPlan:auditLog:aircraft:<server>:<aircraftId>` | AesAfpAuditLog | Per-aircraft ring, cap 50. Same entry shape as the global timeline. Dual-write atomic with the global key. |

**Stable** — every prefix above is contract. External code reads them.

---

## 5 · Settings shape

### `settings.routeAssistant` (defaults from `RouteAssistantSettings._defaults()`)

```js
{
    scoring: {
        paxScore:      {enabled, weight, direction, min, max},
        cargoScore:    {…}, weeklyFlights: {…}, airlineCount: {…},
        profitPerWeek: {…}, fitOk: {…},
        actualProfitPerWeek: {…}    // Yield Feedback — score routes by realised $/wk
    },
    filters: {
        minScore, maxDistanceKm,
        statuses: {NEW, OK, UNDER, OVER, OOR},
        fleetFlyableOnly
    },
    flightsfromMaxAgeDays,
    distanceMaxAgeDays,
    collapsed,
    viewMode,                        // "all" | "pax" | "cargo" — tabbed table view
    waveView,                        // bool — H slice 1; replaces table with Gantt timeline
    waveOverlay: {                   // H slice 1 — Wave View config
        lastPresetId,                // SchedulePresets id of last picked preset
        topN,                        // 5..100; how many scoredRows feed the build
        showWarnings, showUnplaced
    },
    orsSandbox: {                    // Letter I slice 1 — ORS Sandbox pricing simulator
        enabled,                     // top-level mode toggle, mutually exclusive with waveView (Wave wins)
        lastRouteIata,               // restore picked route on next mount
        lastScenario: {
            priceMultiplier,         // 0.30..3.00 — Y-anchored, C/F scale proportionally
            frequency,               // null → use route's current freq
            comfortDelta             // -2 (budget) … +2 (premium)
        },
        modelParams: {
            ratingPriceElasticity,   // α_price — rating points per ±100% price (default 8)
            ratingComfortLift,       // α_comfort — rating points per service-level step (default 5)
            shareTemperature         // softmax T (default 25; numerically stable for AS rating ~80–180)
        },
        perRouteTemperature          // {<HUB>-<DEST>: T} — populated by Calibrate-T button
    },
    demandDepth: {                   // Letter K — per-class historic + RM buckets
        showDemandColumns,
        classCoverage,               // "summary" (PAX+CARGO) | "full" (5 payloads)
        useRealDemandForLF,          // opt-in: profit-estimator uses pool / weeklySeats for LF
        concurrency, staggerMs,
        historicWindowPeriods,       // last N weeks for elasticity regression
        lastBulkScrapeAt,
        historicMaxAgeDays,
        inventoryMaxAgeDays
    },
    aircraft: {mode, typeId, registration, falloffPct, showAircraftColumns},
    economics: {
        loadFactor, loadFactorMin, loadFactorMax,
        yieldPerKm, yieldDemandSensitivity,
        cargoYieldPerKgKm, cargoLoadFactor, cargoLoadFactorMin,
        cargoLoadFactorMax, cargoYieldDemandSensitivity,
        fuelCostPerHour, fuelPriceAutoEnabled, fuelPriceBaselineCost,
        fuelPriceBaselineValue, fuelPriceBaselineUnit, fuelAgePenaltyPerYear,
        crewCostPerHour, maintenanceCostPerHour, otherFixedPerFlight,
        falloffYieldMultiplier
    },
    pricing: {                    // Auto-Pricing scrape config
        showPricingColumns,       // gate the Live route data columns
        concurrency, staggerMs,
        lastBulkScrapeAt, priceMaxAgeDays,
        autonomyMode,             // off | suggest | oneClick | batch (T2/T3)
        silentAutoEnabled,        // separate gate for silent auto-apply
        targetMargin, competitorAdjust,  // T2 placeholders
        apply: {                  // Tier 3 write-back — both gates required for a real POST
            enabled,              // kill switch (default false)
            dryRunOnly,           // slice gate (default false post-3.2; users can re-arm via toggle)
            defaultScope: {airportPair, flightNumbers, returnAirportPair, returnFlightNumbers},
            roundPolicy,          // "nearest" | "down" | "up"
            cooldownMinPerRoute,  // 60 default; preflight blocks Apply via getLastSuccessAt(hub,dest)
            warnAboveDeltaPct,    // 5 default — preflight surfaces a warning at this Δ%
            requireConfirmAboveDeltaPct,  // 15 default
            pricingApplyLogLimit, perRouteApplyLogLimit,  // 200 / 20 cap rings
            submitButton,         // "submit-prices" | "p::submit" | "submit-settings"
            showRecentApplies, recentApplyPreviewCount,
            // Tier 3.2 circuit breaker — mirrors ors-scraper.js consecutive-error pattern
            circuitBreakerThreshold,    // 3 default — consecutive 429/503 trips the breaker
            circuitBreakerCooldownMs,   // 600000 (10 min) default
            circuitBreakerTrippedAt,    // ms epoch; null when armed; reset on first verified write
            circuitBreakerHaltReason,   // human-readable string for the banner
            // Pre-staged for Tier 3.3
            silentAutoMaxPerDay, silentAutoMaxPerHour, silentAutoMinDeltaPct
        }
    },
    yieldFeedback: {              // Roadmap G — actual-yields feedback loop
        showColumns,              // gate the Actuals column group (Act $/flt + Δ%)
        varianceWarnPct,          // ±X% triggers the Δ% highlight (default 25)
        attributionMode,          // "frequency" | "distance" | "equal"
        historyLimit,             // newest N snapshots kept per route (default 12)
        lastSnapshotAt,           // unix-ms; surfaces in the expander
        autoSnapshotOnMount       // off by default; manual Snapshot CTA only
    },
    carriers: {                   // Letter F — full carrier list per route
        showCarrierIntensity, concurrency, staggerMs,
        lastBulkScrapeAt, carriersMaxAgeDays
    },
    marketAnalysis: {             // Tier 2a — markets-page scrape config
        showColumns,              // gate Mkt% / Cmp# / Cmp$ / Drft cols
        concurrency, staggerMs,
        lastBulkScrapeAt,
        competitorMaxAgeDays,     // null = never expire
        shareMaxAgeDays,          // 7 default — weekly cadence
        historicMaxAgeDays,
        defaultPayloadChart       // PAX | ECONOMY | BUSINESS | FIRST | FREIGHT
    },
    watchlist: {                  // Daily-driver QoL slice 2 — starred routes display
        floatStarredToTop,        // bool, default true — gate the star-as-primary-sort behaviour
        showAlertBadges           // bool, default true — gate the red dot when row's _diff worsens
                                  // Storage of the starred set itself is RouteAssistantWatchlistStore;
                                  // these toggles only affect display, never persistence.
    },
    ors: {                        // Tier 2b — ORS rank scrape + multi-class compose
        showColumns,
        concurrency,              // 2 default — ORS = expensive AS solver
        staggerMs,                // 1500 default
        lastBulkScrapeAt, rankMaxAgeDays,
        // Multi-class scrape config (replaces the old single defaultPayload):
        classesToScrape,          // ["ECONOMY","BUSINESS","FIRST"] default; lazy-renamed from defaultPayload
        defaultDepartureH,        // 0..48
        defaultArrivalH,          // 24..72
        defaultUseGround,         // bool
        // Composite combining — how per-class metrics merge into headline:
        combineMethod,            // capacityWeighted (default) | weighted | min | max | avg
        weightPreset,             // capacityWeighted | standard | allEconomy | twoClass |
                                  // businessHeavy | premiumLongHaul | custom
                                  // (full table in RouteAssistantSettings.ORS_WEIGHT_PRESETS)
        classWeights,             // {ECONOMY, BUSINESS, FIRST} — values used when preset === custom
                                  // OR combineMethod === weighted
        primaryClass,             // ECONOMY default — drill-in default tab + headline-class label
        // Display preferences — every rank flavor surfaceable:
        primaryColumn,            // ratingGapToTop | rankAny | rankFirstLegOurs |
                                  // rankAllOurs | rankNonstop | rankBookable |
                                  // ourTopRating | ourBestNonstopRating
        showRankAnyColumn, showRankNonstopColumn,
        showRatingGapColumn, showCompetitorCountColumn,
        showPerClassColumns,      // gate the Y / C / F per-class cols (auto-off in Compact view)
        minRatingThresholdDisplay,   // hide values where ourTopRating < N
        // Carrier identification override (e.g. "FN,NY" for multi-airline users):
        airlineCarrierPrefixOverride,
        // Circuit-breaker state — bulk button disabled for cooldown after trip:
        circuitBreakerTrippedAt, circuitBreakerCooldownMs   // 600000 default = 10 min
    }
}
```

Defaults are deep-filled on load — adding new fields in a future version doesn't wipe user-tuned siblings.

### `settings.aircraftFlightPlan` (defaults from `AesAfpSettings._defaults()`)

```js
{
    enabled,                   // top-level kill switch for the AFP module
    defaultTopN,               // initial candidate-list size (5 / 10 / 25 / 50 / all — cycles via Top-N button)
    defaultPricePct,           // Slice D pre-fill default for the price-percent input (100 = AS default)
    defaultService,            // Slice D pre-fill default for the service-level select
    showWavePreview,           // collapse/expand the Slice E Gantt + per-leg Apply panel
    candidateChips: {          // Slice C chip filter state (persisted across mounts)
        rangeFitOnly,          // hide candidates the picked aircraft can't reach
        hideAlreadyScheduled,  // diff against Visual Flight Plan parser
        watchlistOnly          // intersect with RouteAssistantWatchlistStore
    },
    lastSelectedPresetId       // Slice E preset memory across mounts
}
```

Top-level sibling of `routeAssistant` and `usedAircraftScanner`, not a nested child. Loaded by every AFP page mount; deep-merged with defaults so existing-user storage survives schema additions.

### `settings.usedAircraftScanner` (defaults from `UsedAircraftPresets._defaults()`)

```js
{
    presets: [{id, name, types: string[]}],   // user-defined preset list
    typeFamilyOverrides: {<asLabel>: <familyName>},
    concurrency, staggerMs, lastScanId,
    routeFilter: {minRangeKm},
    scoring: {
        ageYears: {enabled, weight, min, max}, conditionPct: {…},
        seats: {…}, cargoCapacity: {…}, speed: {…}, range: {…},
        paxSatisfaction: {…}, nextBid: {…}, immediatePurchase: {…},
        leasingRate: {…}
    }
}
```

---

## 6 · AS endpoints used

| URL | Method | Purpose |
|---|---|---|
| `/action/info/countries` | GET | Country directory (StationAutomation, Route Assistant seed). |
| `/action/info/country?id=<id>` | GET | Country page (airports + demand). |
| `/action/info/county?id=<id>` | GET | Region page (US states, Russia oblasts; AS spelling: "county"). |
| `/action/info/airports/<airportId>` | GET | Airport detail (lat/lon for great-circle). |
| `/action/portal/index` | GET | World fuel price scrape (table → SVG fallback). |
| `/action/holding/stockexchanges` | GET | Fuel price secondary source. |
| `/action/enterprise/aircraftsType?id=<typeId>` | GET | Aircraft type specs. |
| `/app/com/scheduling/<HUB><DEST>` | GET | **Distance** (header) + **live route data** (Flight Numbers overview table + segments matrix). 6-char concatenated path. NO prices, NO ORS rank. |
| `/app/info/enterprises/<id>?tab=3` | GET | Own-schedule extract. |
| `/app/info/enterprises/<id>` | GET | Enterprise overview page — read by `enterprise-meta-scraper.js` (F slice 2) for banner + avatar + display name; also the link target from the Cmp popover's clickable enterprise names. |
| `/app/info/enterprises/<id>?tab=1` | GET | **INTEGRATED (F slice 3, this session).** Enterprise's "Contractual partners" tab. Source for `contractual-partners-scraper.js`. Lists every business relation in a `<table class="partners">` — each row carries one or more `<span class="type ALLIANCE">` / `<span class="type INTERLINING">` / `<span class="type LESSOR">` badges. Fetched per *your-own* enterprise id; the popover then cross-references competitor enterpriseIds against the union of partner sets to surface ⇄ / ✦ glyphs. |
| `https://www.flightsfrom.com/<IATA>` | GET (cross-origin) | Hub destination listing. |
| `https://www.flightsfrom.com/<HUB>-<DEST>` | GET (cross-origin) | Pair detail page — used by both distance tier-3 fallback **and** the Letter F carriers-scraper for the per-route carrier list. |
| `/app/com/markets/<HUB><DEST>` | GET | **INTEGRATED (Tier 2a, this session).** Per-route Market Analysis page. Source for: every competitor flight with prices + availability + status (`#inventory-table`), your own pricing form snapshot (Pricing fieldset), market-share leaderboard (pax + cargo), 25-week historic capacity/price charts (parsed from inline `lineChart(…).setData([…])`). Live-capture via `content_markets.js` + bulk-sync via the panel's Market Analysis expander. |
| `/app/info/ors` | POST (after GET handshake) | **INTEGRATED (Tier 2b, this session).** Online Reservation System search form. The actual sort the AS demand model runs against. Per-route GET first to harvest Wicket session + form action, then POST with `origin-group:…:origin = <full airport name>`, `destination-group:…:destination = <name>`, `payload = radio0\|radio1\|radio2\|radio3`, `departure-group:…:departure = <0-48>`, `arrival-group:…:arrival = <24-72>`, `ground:useGroundNetwork = on`. Walk pagination via `.navigation a.next` (sample: 77 connections / 3 pages for JFK→LAX). |
| `/app/com/markets/<HUB><DEST>` (POST `?<wicket>-1.-pair-pair~panel-settings-settings~form`) | POST | **WIRED (Tier 3.1, this session — dry-run only).** Pricing write-back. `RouteAssistantPricingApplier` does GET → harvest wicket session + form action + hidden inputs + slider ranges → builds body with `classes:prices:0..3:newPrice` (Y/C/F/Cargo) + 5 General Settings selects (`originTerminal-group:…`, `destinationTerminal-group:…`, `serviceProfile-group:…`, `boardingPreference-group:…`, `cargoPreference-group:…`) + 4 scope checkboxes (`settings:airportPair` / `flightNumbers` / `returnAirportPair` / `returnFlightNumbers`) + the disambiguating submit-button name (default `submit-prices`; alternatives `p::submit` and `submit-settings`). Each apply does a fresh GET (no session reuse) — Wicket page-version IDs invalidate per interaction; sharing a session returns a `PageExpiredException` HTML page that parses as zero (silent corruption). Three observed slider ranges from the JFK-ATL sample: Y `1..296`, C `1..640`, F `1..1130`, Cargo `1..214`. Tier 3.1 ships dry-run only — `applier.dryRunOnly = true` AND `applier.applyEnabled = false` both gate the POST; both have to be cleared in 3.2 to enable real writes. |
| `/action/enterprise/flightsPrices` | GET | Global pricing list. Not currently used — the markets-page per-route data is preferred per user. |
| `/app/com/inventory/<HUB><DEST>` | GET | Per-route inventory + fares. **Letter K** — read by `inventory-page-scraper.js` for per-class RM buckets + forward departures. Same-origin fetch under `host_permissions`. |
| `/app/com/markets/<HUB><DEST>?payload=NAME` | GET | **Letter K** — same markets page, query param drives which payload's historic chart renders. NAME ∈ {ECONOMY, BUSINESS, FIRST, PAX, CARGO}. Five fan-out fetches per route in `classCoverage = "full"`, two (PAX + CARGO) in `"summary"`. |

All AS reads use `fetch(url, {credentials: "include"})` + `DOMParser`. No writes to AS today.

---

## 7 · AS quirks (relearn-the-hard-way avoided)

- **Wicket session URLs.** AS pages have a numeric Wicket suffix like `?239`. Fetching without it usually still works (server redirects). The market scanner's child-tab worker uses `sessionStorage` to persist context across Wicket navigations.
- **Country/county spelling.** Region-level URLs use AS's spelling `/action/info/county` (US states, Russia oblasts). The country-scraper handles both spellings.
- **Airport detail page.** `/app/info/airports/<airportId>`. There's **no per-IATA lookup** endpoint — `/action/info/airports?searchString=` returns 404. IATA → airportId is cache-only via DemandStore.
- **Demand 0–10.** Encoded in image filenames `<n>.png` where `n` is 1-indexed (`1.png` = score 0, `11.png` = score 10). `CountryScraper._readDemandBars` subtracts 1.
- **Scheduling page URL.** `/app/com/scheduling/<HUB><DEST>` — concatenated 6-character path. Page header: e.g. `New York (JFK) – Los Angeles (LAX) 3,971 km` (distance source). The **Flight Numbers overview table** (legend "Flight Numbers") is the cleanest source for live route data — flight number, departure time, frequency-days pattern (`1234567`/`_234567`/etc.), assigned aircraft (registration link + type link). This page does **NOT** carry prices or ORS rank.
- **Multi-daily routes.** A single route can have multiple flight numbers AND multiple frequency rows per FN. The right representation is **per-day flight counts** (`dailyFlights[7]` Mon→Sun, integer count) not "days flown". `2222211` = 2x Mon–Fri + 1x weekends = 12/wk. Real-airline-style notation.
- **Server name.** `window.location.hostname.split(".")[0]` (e.g. `free1`, `tristar`). Storage keys are scoped per-server.
- **Pricing surfaces.** AS prices live on `/action/enterprise/flightsPrices` (global) and `/app/com/inventory/<HUB><DEST>` (per-route). NOT on the scheduling page.
- **ORS rank surfaces.** True ORS rank lives on `/app/info/ors` — submit the connection-search form (origin/destination/payload/window/ground) and parse the result list. Each `<tbody class="bookable\|unbookable">` is one connection; `<tr class="totals">` carries the connection-level rating (e.g., 67 for the top JFK→LAX nonstop in the sample). The markets-page market-share leaderboard is NOT the same as ORS rank — it's a per-period booking outcome, not a search-engine sort.
- **Wicket form POST.** The ORS form is Apache Wicket. Each scrape requires a fresh GET → POST handshake — page-version IDs increment per interaction; reusing a stale session returns a `PageExpiredException` HTML page that parses as zero results (silent corruption). Concurrency=2 / stagger=1500ms keeps the per-route 2-request handshake cost manageable. Circuit breaker on 3× consecutive 429/503 prevents soft-bans during long bulk runs.
- **Markets-page split storage.** One scrape of `/app/com/markets/<HUB><DEST>` writes 4 chrome.storage.local key families (`competitors`, `ownPricing`, `marketShare`, `historic`) so each can have its own freshness window. `RouteAssistantMarketsPageScraper.bulkLoadCache(pairs, {families: […]})` accepts a families array and issues one combined `chrome.storage.local.get`, so the split costs no extra round trip.
- **AS version.** Currently 6.13.x. Page format changes happen — when a parser stops matching, grep for the relevant label and update selectors.

---

## 8 · How to test cold

1. **Load.** `chrome://extensions` → Developer mode → Load unpacked → select `/Users/jihwan/Downloads/AES.v0.6.9-beta`. Or "Reload" if already loaded.
2. **Used Aircraft Scanner.** Dashboard → AES dropdown → Used Aircraft Scanner. Confirm:
   - Family-card grid renders with chips strip + category filter row + search input + custom-add row.
   - Pick a small preset or tick a few types in the narrowbody category.
   - Click **Start scan**. Child tabs open (allow popups for `*.airlinesim.aero`); queue table populates; results stream in.
   - Results table has **Family** column at index 0 (or 1 when scoring active), with a 4px color rail down the leftmost cell.
   - Click **Download CSV** — Family column present.
3. **Route Assistant — basic.** Visit `/app/com/scheduling`. Panel mounts bottom-right (~1 s). If first-time on this game world: purple "demand cache empty" banner → click **Seed all countries** (5–15 min, one-time).
4. **Auto-Pricing Tier 1 — live capture.** Open `/app/com/scheduling/JFKLAX` (or any route you fly). Open DevTools console. Within ~2 s, log:
   ```
   [AES routeAssistant] scheduling page parsed: 1 flight(s), primary=Boeing 767-300F, dep=06:05, 7/wk pattern=1111111, cruise=851km/h
   [AES priceScraper] live-captured JFK→LAX {…}
   ```
5. **Auto-Pricing Tier 1 — bulk sync.** Back on `/app/com/scheduling`, open the Route Assistant panel's **Live route data** expander. Click **Sync route data for all visible routes**. Watch progress: `Syncing route data: 12/87…`. After completion, **Eq / Dep / Wk** columns populate (Wk shows `12 2222211` style). Hover `Wk` → tooltip with per-day breakdown.
6. **Sanity-check distances.** JFK → LAX should be ~3,975 km, JFK → LHR ~5,540 km. Console logs `via as-scheduling` / `via as-coords` / `via ff-detail` per route.
7. **Console clean.** No JS errors during any flow.

If the Used Aircraft Scanner first-run logs `[AES priceScraper] no price/yield/ORS labels matched` — that's expected on the scheduling page; pricing/ORS aren't there. If `Wk` shows `—` for routes that have `Eq` and `Dep`: cache predates the multi-daily shape; click Sync to refresh.

8. **Tier 2a — Markets page live capture.** Open `/app/com/markets/JFKATL` (or any active route). DevTools console within ~2s:
   ```
   [AES marketsScraper] live-captured JFK→ATL competitors=N pricing=Y/C/F/Cargo share=4 hist=25
   ```
9. **Tier 2a — Markets bulk sync.** Back on `/app/com/scheduling`, open the new teal **Market Analysis** expander. Click **Sync market analysis for all visible routes**. Watch progress; after completion the **Mkt% / Cmp# / Cmp$ / Drft** columns populate. Drft shows "drift" when your prices diverge from defaults, "dflt" otherwise. Hover Mkt% for the period + top-5 leaderboard.
10. **Tier 2b — ORS rank.** Open the new amber **ORS Rank** expander. Choose payload (default Y) / window (default Wide 0–72h) / ground network (default on). Click **Sync ORS rank for all visible routes**. Pace ~30 routes/min. After completion, the **ORS** column shows your selected primary metric (default `ratingGapToTop`, signed; positive = winning). **RkNS / Gap / OrsC#** columns also populate. Click the ▾ on any ORS cell to drill into the cached connection list — no re-fetch.
11. **ORS — primary column swap.** Change the **Primary column** dropdown in the ORS expander to "Rank — own nonstop". Confirm the ORS column re-renders with `#1`-style values without re-scraping.
12. **ORS — circuit breaker.** Set ORS concurrency=10 in DevTools (`settings.routeAssistant.ors.concurrency = 10`) and trigger a bulk sync. After 3× consecutive 429/503, the expander shows a red banner with countdown and the bulk button disables.
13. **Two-enterprise sanity.** If you operate FLY NYON. + NYON. on the same world, confirm both prefixes appear in `routeAssistant:ors:JFK-LAX → ourCarrierPrefixes` and connections from either enterprise are highlighted in the drill-in drawer.

---

## 9 · Open work / next steps

Pulled from `MANUAL.md §16` (the master roadmap, letter-coded A–K) and the user's confirmed direction.

### Immediate
| Item | Status | Blocker |
|---|---|---|
| ~~**Auto-Pricing Tier 2a — Markets page**~~ | ✅ **Shipped this session.** See §1. Note the user actually wanted Tier 2a as the markets-page scraper (competitor flights + own pricing + market shares + historic charts), not just ORS rank — see new §1 for details. | — |
| ~~**Auto-Pricing Tier 2b — ORS rank**~~ | ✅ **Shipped this session as the ORS scraper.** True ORS rank from `/app/info/ors`, not the markets page (where the leaderboard is per-period booking outcomes, a different metric). | — |
| ~~**I — ORS-aware pricing simulation**~~ | ✅ **Slice 1 shipped this session.** Per-route pricing sandbox via 🧪 panel mode toggle. Read-only against AS — projects rank/share/pax/$ from cached ORS + markets + demand-derivator. Closed-form formula transparent + tunable in settings (α_price, α_comfort, T) + per-route T calibration. See §1 for the full pipeline; §9 backlog row I for slice 2 deferrals. | — |
| ~~**Auto-Pricing Tier 3.1 — foundation + dry-run**~~ | ✅ **Shipped this session.** New `pricing-applier.js` + `pricing-apply-log.js`; per-route apply modal + sandbox bridge + row context menu + bulk-apply listing modal; preflight + audit log + body-preview wired. Two-gate model (`apply.enabled` + `apply.dryRunOnly`) gates all real writes — 3.1 hard-gates so the full pipeline runs but no POST hits AS. See §1 "Auto-Pricing Tier 3.1". |

### Next
| Letter | Item | Notes |
|---|---|---|
| ~~**Tier 3.2**~~ | ~~Auto-Pricing — live single-route writes~~ | ✅ **Shipped this session.** `dryRunOnly` default flipped to `false`; circuit breaker (3 settings + 2 hooks + Step-0 cooldown gate); breaker-tripped banner with Reset; Undo toast on success; per-route cooldown threaded via `getLastSuccessAt`; modal copy de-tiered. See §1 "Auto-Pricing Tier 3.2". |
| **Tier 3.3** | Auto-Pricing — bulk + silent-auto | Bulk apply modal turns into a selection table with Apply across N routes; silent-auto loop behind `silentAutoEnabled` with hard caps (`silentAutoMaxPerDay`, `silentAutoMaxPerHour`, `silentAutoMinDeltaPct`); confirmation modal on first activation. |
| ~~**Tier 4**~~ | ~~Yield feedback loop~~ | ✅ **Shipped this session as Roadmap G slice 1.** Folded into the new yield-feedback expander. Slice 2 is delta-mode + auto-snapshot. |
| ~~**J slice 2**~~ | ~~Used Aircraft Scanner — smarter deal-scoring~~ | ✅ **Shipped this session.** See §1 / §2. Six new metrics; `pricePerSeat` enabled by default in scoring blend. |
| ~~**F**~~ | ~~Full carrier list per route~~ | ✅ **Shipped this session.** See §1 / §2. New `routeAssistant:carriers:<HUB>-<DEST>` cache, colored intensity pill on Cmp column, hover tooltip with per-carrier list. **Caveat:** flightsfrom.com is a SPA — when their server doesn't SSR the carrier list (page returns skeleton HTML) the cache row carries `parserNotes` and an empty `carriers: []`. A future slice can add a child-tab worker for guaranteed extraction. |
| ~~**G**~~ | ~~Yield-timeline / actual-yields feedback~~ | ✅ **Slices 1–4 (sessions 1+2) shipped this session.** See §1 "Yield Feedback". Slice 2: delta-mode (per-tail baseline subtraction), V+/V− pill on the status column, `autoSnapshotOnMount` toggle, batch **Calibrate flagged** modal that opens a confirmation dialog listing every Δ%-flagged route and writes a batch override on save. Slice 3: cargo-yield calibration — `derivedYieldFromActuals` refactored to return `{side, value, alt}`; mixed routes auto-pick the dominant side and surface the alt; per-route override editor's Calibrate button branches `yieldPerKm` vs `cargoYieldPerKgKm`; Calibrate-flagged modal gains a per-row Side dropdown. Slice 4 sessions 1+2: per-flight attribution. `content_aircraftFlights.js` now writes a slim per-flight envelope onto `<server>aircraftFlights<id>.flights[]` (`{flightId, flightNumber, flightNumberId, status, originIata, destinationIata, depUtc}`); fixed the pre-existing flightId regex bug (`url.match(/\d+/)[0]` was picking "1" from `free1` hostname). New `attributionMode: "per-flight"` walks every tail's envelope, joins with `<server>flightInfo<id>.money.CM5.Total`, aggregates per `{originIata, destinationIata}` route — exact attribution, no frequency averaging, multi-leg FNs handled naturally. Routes with no measured flights fall back to per-tail frequency. Snapshot result reports `routesWithPerFlight` / `routesFellBack`. **Slice 4 session 3 parked**: extending `content_flightInfo.js` to extract origin/dest inline (would let per-flight work without first visiting the aircraft history page) — needs a `/action/info/flight?id=<id>` HTML sample first. |
| ~~**H slice 1**~~ | ~~Wave overlay (Gantt timeline of recommended schedule)~~ | ✅ **Shipped this session.** New `modules/route-assistant/wave-overlay.js` (`RouteAssistantWaveOverlay` — pure-function `buildRoutesFromScoredRows` / `buildSchedule` / `renderGantt`). New 📊 toggle in panel header next to Compact; toggling replaces the table with HTML/CSS swimlane Gantt rendered into `tableHost`. Header strip exposes preset picker (sourced from `settings.scheduleManagement.presets`), top-N input (5–100 default 20), aircraft display, Re-run, Edit-presets deep-link. Reuses `ScheduleBuilder.assignRoutes → evaluateFlights` exactly; flight bars positioned by `depTimeLocal`, outbound/inbound stacked half-height, range-bucket coloured left edge, click → opens scheduling page in new tab. Warnings panel + unplaced strip + skipped log below. Read-only against `ScheduleStore` — slice 1 visualises only. Settings: `settings.routeAssistant.waveView` (top-level bool) + `waveOverlay.{lastPresetId, topN, showWarnings, showUnplaced}`. **Slices 2+ deferred:** drag-edit, save-schedule CTA, auto-optimise, connection-graph, interline-store, slot-store, multi-hub view, competitor schedule overlay. |
| ~~**H slice 2**~~ | ~~Wave overlay — drag-edit slots, save-schedule CTA, connection-graph~~ | ✅ **Shipped this session.** Drag-edit slots ship via `wave-overrides-store.js` + `ScheduleBuilder.assignRoutes({overrides})` (force-place + 📌 badge, GC stale entries on preset edit). Connection-graph ships via `ScheduleBuilder.computeConnections` + `wave-overlay._renderConnectionsOverlay` SVG curves with own/interline/alliance classifier sourced from F slice 3 partners cache (toggle via 🔗 Connections button). Save-schedule CTA ships as `RouteAssistantPanel._saveWaveScheduleToStore` wired to a 💾 button in the wave header strip — validates preset/airline/flights, wraps in `_undoableSave` (6s toast with Undo → `ScheduleStore.remove`). Module-level read-only invariant survives because the panel CTA is the single write gateway; see §10. |
| **H slice 3+** | Wave overlay — interlining + slot management + multi-hub + auto-optimise | Tier 3, multi-session. Open buckets: `interline-store.js` for partner-share-per-route metadata; `slot-store.js` for per-airport slot inventory + planned allocations; LP/heuristic auto-optimise replacing the greedy `assignRoutes` with connection-graph-maximising placement (probably the highest-impact item — currently the greedy fill leaves obvious connection gains on the table); multi-hub network optimisation (single-hub view today). |
| ~~**I slice 1**~~ | ~~ORS-aware pricing simulation — model + UI + per-class projection + T calibration~~ | ✅ **Slice 1 shipped a prior session.** See §1 "ORS Sandbox (Letter I — slice 1)". |
| ~~**I slice 1.5**~~ | ~~ORS Sandbox — per-route scenario state + calibration freshness~~ | ✅ **Shipped this session.** Per-route `lastScenarioByRoute` replaces the global `lastScenario`; calibration timestamps + 30d/90d staleness thresholds; one-shot `_legacyLastScenario` migration. See §1 "ORS Sandbox slice 1.5". |
| ~~**I slice 2 (a · b · d · e)**~~ | ~~ORS Sandbox — model precision (independent class pricing, frequency synthesis, cargo branch, rating-clamp annotation)~~ | ✅ **Shipped this session.** See §1 "ORS Sandbox slice 2". |
| ~~**I slice 2c**~~ | ~~ORS Sandbox — per-route rating-price elasticity~~ | ✅ **Shipped this session.** New `routeAssistant:ratingObservations:<HUB>-<DEST>` observation log auto-populated on every ORS scrape; new `routeAssistant:ratingAlpha:<HUB>-<DEST>` manual override store; demand-derivator extended with `_ratingPriceElasticity` per-class regression (range/bucket/confounder gates); `_recomputeOrsSandbox` cascade resolver `override > derived > siblingDerived > fleetMedian > global`. Per-class α expander mounted inside the Outcome card. See §1 "ORS Sandbox slice 2c". |
| **I slices 3–6** | ORS Sandbox roadmap (trust + workflow + viz + polish) | See "**ORS Sandbox roadmap (slices 2–6)**" sub-section directly below this table. Slice 3 = trust + audit (confidence pill, back-test logging, model-fit summary). Slice 4 = workflow ergonomics (sweet-spot finder, A/B compare, saved scenarios, multi-route batch). Slice 5 = visualisations (price-vs-profit sparkline, historical pax overlay, sensitivity heatmap). Slice 6 = efficiency polish. **Slice 3 makes Tier 3 (Auto-Pricing write-back) trustworthy.** |
| ~~**K**~~ | ~~Deeper per-route demand from AS market analysis~~ | ✅ **Shipped this session.** Per-class historic via `?payload=` query param (Y/C/F/PAX/CARGO), new inventory-page scraper for RM buckets, pure-function demand-derivator computing pool / elasticity / RM tightness. New "Demand depth" expander + 5 scoring fields + 6 columns. Profit estimator gains opt-in `useRealDemandForLF` path. **I (ORS sandbox) now fully unblocked.** |
| ~~**K — make it seamless**~~ | ~~Demand depth wiring follow-up~~ | ✅ **Shipped this session (latest).** Fixed the gap between "K is shipped" and "K actually delivers value to the user automatically." (1) `_runBulkMarketScrape` now folds in `bulkScrapeHistoric` + inventory `bulkScrape` after the markets families finish; one click in the Market Analysis expander populates everything the panel needs. Status text cycles through phases. (2) After the folded pass, `_applyCachedDemand` runs automatically — demand columns refresh without a separate Demand Depth sync. (3) Defaults flipped: `useRealDemandForLF: true` (graceful paxScore fallback when inputs missing), `classCoverage: "full"` (5 payloads — needed for I anyway), `historicMaxAgeDays: 14`. Existing users keep persisted prefs via deep-merge. (4) New `marketAnalysis.autoRefreshOnMount` (default true) + `staleThresholdDays` (14): on mount, when `lastBulkScrapeAt` is null/stale, the panel surfaces a banner and kicks off `_runBulkMarketScrape()` after a 5s grace period; user can hit Skip to suppress for `staleThresholdDays`. (5) Demand-depth column tooltips now combine the static preamble with row state — "No data" + `derivationNotes` + a CTA when missing; "Last derived: X day(s) ago" with STALE flag when present. Stale rows render in `#9ca3af` so the user notices. New `RouteAssistantPanel._demandTooltip(field, row)` + `_demandIsStale(row)` statics; `_syncRenderContext` mirrors `staleThresholdDays`. The standalone Demand Depth sync stays as a granular fallback. |
| ~~**J slice 3**~~ | ~~Used Aircraft Scanner — surface deal-metric tooltips with input breakdown~~ | ✅ **Shipped this session.** `daysToBreakEvenBreakdown()` and `seatKmYearCostBreakdown()` already retained intermediates and `decorate()` attached them to the row; results-table.js gained `_formatPricePerSeatTooltip`, `_formatMaintTooltip`, `_formatFleetTooltip` to fill remaining gaps so every deal column (`$/seat`, `$/seat·km/yr`, `BE (days)`, `Maint.`, `Fleet`, `Route-fit`) now shows a multi-line breakdown on hover. Inner-pill `title` attributes removed from `_renderMaintCell` / `_renderFleetCell` so the long tooltip wins. |
| ~~**J slice 4**~~ | ~~Used Aircraft Scanner — route-fit upgrade beyond range~~ | ✅ **Shipped this session as v3.** v2 had per-flight demand check (seats×LF ≥ paxScore×scale). v3 adds the weekly-frequency gate using the `weeklyFlights` field already on `topRoutes`: a regional that fits one flight's demand can still fail a route flown 14×/wk. New `frequencyLimited` counter, label format `x / y  (−A demand, −B freq)`, tunable via `routeFit.weeklyDemandPerScorePoint` (default 100, paxScore=10 → 1000 weekly seats). Routes without `weeklyFlights` skip the gate (no penalty — Tier 1 hasn't seen them yet). Tooltip extended to surface all three sequential gates. |
| ~~**F slice 2**~~ | ~~Carriers — AS-native enterprise enrichment in the Cmp tooltip~~ | ✅ **Shipped this session.** New `modules/route-assistant/enterprise-meta-scraper.js` fetches `/app/info/enterprises/<id>` per AS competitor and caches `{name, iata, bannerUrl, avatarUrl}` at `routeAssistant:enterpriseMeta:<id>` (90d TTL, single global key per id — non-directional). Cmp pill replaced its native `title=""` with a custom DOM popover (hover-open with 200ms delay, 250ms grace on leave, click-to-pin, Escape/outside-click dismiss — same pattern as `_openProfitModifierPopover`). Each row in the popover shows avatar + clickable name link to `/app/info/enterprises/<id>` + banner + share% + ▲/▼ change + rank, sorted by share. Falls back to the plain-text tooltip when `marketSharePax` is empty (routes without markets-scraper hits). New "Sync enterprise data" CTA in the Carriers expander batches the per-id fetches (default 4 concurrent / 600ms stagger). |
| ~~**F slice 3**~~ | ~~Carriers — interlining (IL) agreement indicator~~ | ✅ **Shipped this session.** Source resolved to `/app/info/enterprises/<your_id>?tab=1` ("Contractual partners" tab) — NOT `/action/enterprise/interlining` as the original guess suggested. New `modules/route-assistant/contractual-partners-scraper.js` mirrors `enterprise-meta-scraper.js`. Cache `routeAssistant:contractualPartners:<enterpriseId>` (non-directional, keyed by *your-own* enterprise id, NOT competitor). `_buildCarrierRow` reads `_partnersByEnterpriseId` and renders `⇄` (green) for INTERLINING and optionally `✦` (purple) for ALLIANCE next to the partner's name. New "Contractual partners" sub-panel in the Carriers expander with own-enterprise-IDs input + Refresh CTA + glyph toggles. Settings: `myEnterpriseIds`, `partnersMaxAgeDays` (30d default), `lastPartnersSyncAt`, `showInterliningGlyph`, `showAllianceGlyph`. Captures every relation type verbatim (LESSOR / ALLIANCE / INTERLINING / future tokens) so future relation types still surface. |

### ORS Sandbox roadmap (slices 2–6)

The ORS Sandbox (Letter I) ships precision + trust + workflow value across five slices. Slice 1 + 1.5 are done; this section is the path forward. Each slice maps to one of the user's three goals: **precise** (slices 2 + 3 — model correctness + trust), **obviously effective** (slice 4 + 5 — visible workflow value), **efficient** (slice 6 — UX/perf polish). Slice 2 unblocks Tier 3 Auto-Pricing write-back; slice 3 makes Tier 3 safe to ship.

**Why this ordering:** precision before workflow. A sweet-spot finder (4a) that runs against an inaccurate model (slice 2 not yet done) gives confidently-wrong answers and burns user trust. Slice 3's back-test logging needs slice 2's improved projections to compare against — measuring the wrong model isn't useful. Slice 5 visualisations get their data from the same scan helper slice 4a introduces, so 5 follows 4. Slice 6 is pure polish — last.

#### Slice 2 — Model precision (Tier 1)

The biggest correctness gaps. Four of five shipped this session; **2c** is the only remaining slice 2 item.

| # | What | Files | Why |
|---|---|---|---|
| ~~**2a**~~ | ~~Frequency lever with synthesised own-connections~~ | ~~`ors-model.js`~~ | ✅ Shipped — `_synthesizeOwnConnections` clones an own + nonstop template; cap = +ceil(0.5×currentFreq) ∧ MAX_FOR_SOFTMAX − existing. **No timestamps needed** — connection schemas in cache have none and rank/softmax don't read them. Frequency-down keeps slice-1 LF dilute. |
| ~~**2b**~~ | ~~Independent per-class price control~~ | ~~`panel.js`, `ors-model.js`, `settings-store.js`~~ | ✅ Shipped — scenario shape `priceMultipliers: {Y, C, F}` with one-shot legacy migration; UI collapses to a single slider when only Y is cached; pax-pool elasticity reads Y multiplier only (pool is Y-anchored per demand-derivator). |
| **2c** | Per-route price elasticity | `ors-model.js:project` (`elast` block), `demand-derivator.js` extension | **Deferred — needs accumulated data.** `route.paxElasticity` is already per-route; `params.ratingPriceElasticity` (the rating-shift constant) is global. Add `params.ratingPriceElasticityByRoute` derived from a new regression in `demand-derivator.js` that emits `ratingPriceElasticity` alongside `paxElasticity`. Per-route override via the existing route-overrides-store. |
| ~~**2d**~~ | ~~Cargo branch~~ | ~~`ors-model.js`, `panel.js`~~ | ✅ Shipped — `_projectCargo` mirror, cargo slider scales `cargoYieldPerKgKm` via `override.cargoYieldPerKgKm` hook in profit-estimator (no estimator changes). Cargo demand pool shifts via `cargoElasticity` when `cargoMultiplier !== 1`. Output adds `cargoPerWeek` to baseline/projected/delta. |
| ~~**2e**~~ | ~~Visual rating-clamp annotation~~ | ~~`panel.js:_buildOrsSandboxResultsCard`~~ | ✅ Shipped — `renderRow` accepts an annotation; `_clampedClasses(result)` reads `perClass.<cls>.priceRatio` (≥±0.5 threshold matches `RATING_CLAMP_LOW/HIGH`); projected Rating cell shows amber `*` + tooltip listing clamped classes. |

#### Slice 3 — Trust + audit (Tier 1, after slice 2)

Slice 2 makes projections better; slice 3 makes the user trust them. Without this slice, all projections look equally trustworthy and the user has no signal that calibration is working over time. ~400 LOC.

| # | What | Files | Why |
|---|---|---|---|
| ~~**3a**~~ | ~~Confidence indicators on the projection card~~ | ~~`panel.js:_buildOrsSandboxResultsCard`~~ | ✅ **Shipped this session.** Confidence pill above Outcome table aggregates T-calibration freshness + ORS data age + connection-count sample size into high/medium/low. See §1 "ORS Sandbox slice 3". |
| ~~**3b**~~ | ~~Back-test logging~~ | ~~new `modules/route-assistant/sandbox-backtest-store.js`~~ | ✅ **Shipped this session.** New per-route directional store at `routeAssistant:sandboxBacktest:<HUB>-<DEST>`; calibrate-T hook logs immediately, bulk markets scrape back-fills `observed.share` within an 8-day window. Tier 3 apply hook pre-staged via `trigger: "tier3-apply"`. See §1 + §4. |
| ~~**3c**~~ | ~~Model-fit summary~~ | ~~`panel.js:_renderOrsSandboxSection`~~ | ✅ **Shipped this session.** New "Model fit: bias / RMSE" row in the ORS Sandbox settings expander; `_computeOrsSandboxModelFit(allBacktests)` aggregates routes with ≥3 back-filled observations; Refresh button reloads. See §1 "ORS Sandbox slice 3". |

#### Slice 4 — Workflow ergonomics (Tier 2, multi-session)

Visible workflow value. Each is a self-contained sub-feature. ~700 LOC across the four.

| # | What | Files | Why |
|---|---|---|---|
| **4a** | Sweet-spot finder | `ors-model.js:scanPriceCurve` (new pure helper), `panel.js:_buildOrsSandboxScenarioCard` (button + result preview) | "Find optimal price" button — scan `priceMultiplier` from 0.7 to 1.3 in 5% steps, return the profit-maximising point. One click, no manual sliding. Result line: "Optimal: 0.92× → +12% profit/wk" that the user can click to apply. |
| **4b** | A/B scenario comparison | `panel.js:_buildOrsSandboxResultsCard` extension | Pin a scenario (📌 button), run another, render two projected columns side-by-side with their own delta-vs-baseline. State on `this._orsSandboxPinnedResult`; clearable via a clear-pin button. |
| **4c** | Saved named scenarios per route | new `modules/route-assistant/sandbox-scenarios-store.js` | Persist a scenario as `{routeKey, name, scenario}` under `routeAssistant:sandboxScenarios:<HUB>-<DEST>` (directional, mirrors `route-overrides-store`). Quick-toggle dropdown above the sliders. Cap at 5 per route. |
| **4d** | Multi-route batch projection | new `panel.js:_runSandboxBatch` + `_buildSandboxBatchCard` | Apply the current scenario to top-N visible routes simultaneously. Roll up projected vs baseline profit across all routes. Answers "what if I cut all top-20 fares 5%?" — currently unanswerable. |

#### Slice 5 — Visualisations (Tier 2)

Optional but highest "obviously effective" payoff. ~500 LOC.

| # | What | Files | Why |
|---|---|---|---|
| **5a** | Price-vs-profit sparkline | `panel.js:_buildOrsSandboxResultsCard` (new sparkline cell), `ors-model.js:scanPriceCurve` (re-used) | Inline sparkline of profit/wk across `priceMultiplier ∈ [0.7, 1.3]`. Sweet spot visible at a glance. Reuses slice 4a scan helper. |
| **5b** | Historical pax/wk overlay | `panel.js:_buildOrsSandboxResultsCard`, reads `routeAssistant:lastSnapshot:<HUB>` | Tiny 8-week trend line under the projected pax/wk number. Anchors "current" in context. Data is already cached. |
| **5c** | Sensitivity sweep heatmap | new card builder | 5×5 grid: `priceMultiplier` × `frequency`. Cell colour-coded by projected profit/wk delta. Click a cell to apply. Heavier render but answers "where in price-freq space am I?" instantly. |

#### Slice 6 — Efficiency polish (Tier 3, last)

UX/perf only — no model changes. ~200 LOC.

| # | What | Files | Why |
|---|---|---|---|
| **6a** | Memoize `_assembleOrsSandboxRoute` | `panel.js:_assembleOrsSandboxRoute` | Currently rebuilds the entire route bundle on every recompute. Memoize on `(routeKey, lastFetchAt-of-each-cache)` — if underlying caches haven't changed, return the cached bundle. 5–10× speedup on slow projections. |
| **6b** | Computing indicator | `panel.js:_recomputeOrsSandbox` | If `project()` takes >100ms (rare but possible on routes with 50+ connections), show a faint "computing…" overlay on the results card. Today the card freezes briefly. |
| **6c** | Toast batching during bulk calibrate | `panel.js:_calibrateOrsSandboxT` + future "calibrate all" button | If multiple calibrate calls fire within 2s, aggregate into one toast: "Calibrated T for 8 routes". |
| ~~**6d**~~ | ~~Reset/undo per slider~~ | ~~`panel.js:_buildOrsSandboxScenarioCard`~~ | ✅ **Shipped this session alongside slice 3.** ↺ button on each Y/C/F price slider + cargo slider + frequency input + comfort selector. Restores baseline + dispatches the control's native input/change event so the existing onChange recompute fires. See §1 "ORS Sandbox slice 3 + slice 6d". |

#### Cross-cutting dependencies

- **Tier 3 (Auto-Pricing write-back)** is gated on slice 2 (better projections) and slice 3b (back-test data to validate against). The Tier 3 row in the §9 "Next" table above is unblocked once 2+3b ship.
- **Slice 4c (saved scenarios)** is independent; could ship before slices 2/3 if user prefers a workflow-first taste. Slice 4a/b/d are cleaner after 2 because they all compound on projection accuracy.
- **Slice 6** can ship at any time — pure polish, no dependencies.

### Daily-driver QoL backlog (user-requested enumeration, end of K session)

The user asked "what else would be useful and practical?" after K shipped. The shortlist below is cheap-to-ship, high-frequency-use upgrades that would compound with everything currently in the panel. **Next slice picked: Diff against last visit.**

| Slice | What | Why it's practical | Est |
|---|---|---|---|
| ~~**Diff against last visit**~~ | ✅ **Shipped earlier this session.** New cache `routeAssistant:lastSnapshot:<HUB>` → `{hub, server, scrapedAt, rows: [{destIata, score, paxScore, cargoScore, weeklyFlights, airlineCount, competitorCount, paxDemandPool, cargoDemandPool, ourPaxShare, orsRatingGapToTop, rmTightness}]}` (row field `orsRatingGapToTop` rather than the spec's `ratingGapToTop` so the diff key matches the row property the cell reads). Baseline loaded once per (mount, hub) pair in `refresh()` into `this._diffPrevSnapshot`; rows decorated with `_diff.<field>` scalars in `_renderRows` before sort/draw; new snapshot written at end of `_drawTable` from `this.scoredRows` (overwrites storage but the in-memory baseline survives the mount, so intra-mount re-renders keep showing the same deltas). 11 numeric column render closures append a small grey ▲/▼ badge via `_appendDiffBadge`. Cmp column uses array-fallback (`["competitorCount", "airlineCount"]`) to track whichever source produced the cell value. | Turns the panel from a snapshot-stare into a change-detector — "on this hub overnight: Cmp +2 on JFK→LAX, RM% +5pp on JFK→ORD, ORS gap dropped on three routes." Replaces eye-scanning the table for what's changed. | ~175 LOC |
| ~~**Watchlist / starred routes**~~ | ✅ **Shipped this session as Daily-driver QoL slice 2.** See §1. The `RouteAssistantWatchlistStore` already existed from a prior session (`modules/route-assistant/watchlist-store.js`) but was unwired — manifest registration + panel.js wiring + `RA_WATCH_TRIGGERS` evaluation + the destIata cell `_buildWatchlistStar` glyph + `_sortRows` float-to-top + `settings.routeAssistant.watchlist.{floatStarredToTop, showAlertBadges}` finished it. Filters still apply to starred rows in v1; cross-tab `chrome.storage.onChanged` listener for the watchlist key was deferred. The "separate Watched table above the main one" optional sub-feature was deferred — float-to-top + visible star is enough to surface them without splitting the layout. | Keeps the few routes you're actively managing front-and-centre instead of having to re-find them in the score-sorted list every visit. | ~120 LOC |
| ~~**Route notes**~~ | ✅ **Shipped this session as Daily-driver QoL slice 4.** New `modules/route-assistant/route-note-store.js` (directional pair-key cache, mirrors `route-overrides-store.js`). 📝 icon added to the Dest cell's existing icon row (📅 📦 🛫 📝); always rendered (subdued grey when empty, full color when present) so adding a note is discoverable for empty routes — small deviation from the spec's "when present" wording. Click opens a popover with textarea + char counter (max 500) + Save / Delete / Cancel. Cell-wide tooltip surfaces the note text on hover; the icon's title shows the first 80 chars. Storage cache `routeAssistant:routeNote:<HUB>-<DEST>` (directional, empty saves remove the key). Popover lifecycle mirrors `_openProfitModifierPopover` (outside-click + Escape + viewport-clamped position). Save mutates row + map in place + `_renderRows` so the icon flips color without a full refresh. | Memory across long-running games — "tried 2x daily, dropped — too much C-class capacity"; "watching for AA entry"; "served by partner under interline". | ~225 LOC |
| ~~**Settings export / import**~~ | ✅ **Shipped this session as Daily-driver QoL slice 3.** New "⇅" button in the panel header opens a small popup menu with two items. Export downloads `aes-config-<server>-<date>.json` containing both `routeAssistant` and `usedAircraftScanner` blobs in a `{format: "aes-config", version, exportedAt, server, ...}` envelope. Import reads a JSON file, validates `format === "aes-config"`, recursively diffs against current settings, and shows a modal listing every added/removed/changed leaf path with from→to values; Apply writes both blobs back via their respective `save()` calls (deep-merge handles any missing keys against current defaults). Cancel/Escape/click-outside dismisses the modal. See §1 + §10 for the envelope contract. | Migrate between worlds, share configs with friends, back up tuning that took weeks. Single-click recovery if a sync goes wrong. | ~60 LOC |
| **Compare two enterprises** | Click any two enterprise pills in the Cmp popover → side-by-side diff modal of every overlap route showing each side's share + rank + change. | Useful for benchmarking specific competitors (who's eating your share?) without manually walking the leaderboard. Reuses F slice 2 popover infra. | ~180 LOC |
| ~~**Alert thresholds**~~ | ✅ **Shipped this session as "Active prompts (Q+)".** New `RouteAssistantAlertRulesStore` + pure-function `RouteAssistantAlertEvaluator` + amber settings expander. Declarative rules with operators (`increased_by` / `decreased_by` / `above` / `below`), watchlist-vs-all scope, per-rule per-route cooldown (default 24h). Triggers fire `RouteAssistantToast.warn` with a "View" action that scrolls/highlights the row. See §1 "Active prompts (Q+)". **Defer to slice 2:** desktop notifications via `chrome.notifications` (Q16, separate permission), trigger on N-day persistence (e.g. "RM% > 0.95 for 7 days running"), per-rule notification channel routing. | — | — |
| ~~**Cross-server view**~~ | **Subsumed by Letter L canopy MVP — see "Letter L (canopy) — multi-account MVP plan" sub-section below.** Slice L5 (cross-account dashboard) is the unified read-only aggregator across worlds and airlines; Slices L1–L3 do the storage refactor that makes it possible. Originally scoped at ~300 LOC for a Used-Aircraft-Scanner-dashboard tile but the storage scoping work is the same as for L, so they merge. | — | — |
| **L** (user-requested, ultimate goal — **v2 plan committed 2026-04-27**) | Multi-account federation — register, scope, federate, and coordinate across multiple AS accounts | **v2 plan now exists** (passive observation, no auto-login, local-only — but federation-rich: affiliation graph, Strategy DNA, combined-supply analytics, cross-account wave coordination) — see the "Letter L (canopy) — multi-account federation plan (v2)" sub-section below for the full 5-layer design + 13-slice phased program + storage scoping rubric + new §10 invariants. Detailed plan at `.claude/plans/zany-hopping-clover.md`. **Canopy auto-login** parts (encrypted credential vault, hidden-tab orchestrator, login automation) are explicitly deferred to "Letter L+" pending a re-evaluation of AS ToS and CAPTCHA/MFA fragility. **Original full vision (deferred auto-login parts only — analytics + scheduling pieces are now in-scope as L4–L12):** the user runs several AS accounts/enterprises (today: FLY NYON. + NYON., potentially more across game worlds). Today each requires a separate browser login + manual context switch. Goal is a single AES interface that holds saved credentials + session cookies for every account, transparently logs in/out as the user navigates, and aggregates every account's routes / fleet / yields / market shares / ORS rank into one consolidated dashboard. The user manages a whole "canopy" of routes across all profiles from one panel and never thinks about which account they're logged into. **Sub-features (auto-login pieces deferred):** (1) Account-vault store with encrypted credentials + per-account session cookies, refreshed on demand. (2) Background tab orchestrator that opens a hidden tab per account, logs in if expired, runs a scrape pass, closes the tab. (3) Aggregator layer above existing per-server caches — new schema like `aesCanopy:account:<accountId>` referencing existing `<server>` keys. (4) Cross-account columns in the Route Assistant (e.g. "Best account for this route", "Routes my OTHER airline already flies"). (5) Cross-account scheduler that flags conflicts (same hub-pair operated by multiple of your airlines = pricing self-cannibalisation). (6) Permission/safety gates — bulk write actions (Tier 3 pricing apply, station opens) confirm per account. **Risks:** AS may have ToS limits around session sharing / multi-instance use; needs to read AS rules on automation. Login automation is fragile (CAPTCHA, MFA). Heavy storage growth. Probably the largest single feature on the roadmap — Tier 4, multi-session build, ~2000+ LOC. **Prereqs:** essentially every existing feature (this depends on each scraper being canopy-aware; refactor work first — that refactor is what L1–L3 deliver). |

### Daily-driver QoL backlog — extended brainstorm (this session)

After Daily-driver QoL slices 1–3 shipped, the user asked for more practical, applicable ideas: 16 functional QoL features + 16 UI / interaction changes, all sized as drop-in slices typically under 200 LOC. Items that overlap with already-shipped (Diff against last visit, Watchlist, Settings export/import) or with the Daily-driver backlog table above (Route notes, Compare two enterprises, Alert thresholds, Cross-server view, L canopy) have been pruned out. The list captures the brainstorm verbatim so a future session can pick a cluster and ship.

#### Functional QoL (16)

| # | Slice | What | Why it's practical | Est |
|---|---|---|---|---|
| ~~Q1~~ | ~~**Quick-filter chips bar**~~ | ✅ **Shipped this session.** New `_renderQuickFilterChips(host)` renders [★ Watch, 💸 Loss, 🛠 Override, 📝 Note, Δ Changed] pill toggles in the controls bar. Settings pre-staged in `filters.{watchlistOnly, lossMakers, hasOverride, hasNote, onlyChanged}`; each chip flips its field + re-renders. Active state lights with accent color. See §1 "N2 Notification center + Q1 chip bar + …". | — | — |
| ~~Q2~~ | ~~**Bulk override editor**~~ | ✅ **Shipped this session.** New `_openBulkOverrideEditor` modal with 4 inputs, each gated by an "apply this field" checkbox. Reads prev records, merges with checked fields (preserves untouched per route), `Promise.all` parallel saves, single `_recomputeProfit()` at end, wrapped in `_undoableSave` with batched restore. Confirm above N=20; progress toast above N=50. See §1 "Cluster A — Bulk operations". | — | — |
| ~~Q3~~ | ~~**Override expiration dates**~~ | ✅ **Shipped this session.** Optional `expiresAt` on the override record + "Expires in N days" input in the full editor + ⏳ (≤7d) / ⌛ (expired) glyphs on the Dest cell. Aggregator passes null to estimator when expired (falls back to demand-driven defaults). Q2 bulk + profit-modifier popover preserve `expiresAt` so the TTL doesn't get wiped on incidental tweaks. See §1 "Q3 override expiration dates". | — | — |
| ~~Q4~~ | ~~**Saved table views**~~ | ✅ **Shipped this session.** New `settings.routeAssistant.savedViews: []` + dropdown / Save+ / Delete🗑 controls in the bar. Snapshot covers filters / sort / viewMode / compactView (search + Q1 chips intentionally ephemeral). Future column-customization (U7/U8) extends the snapshot shape automatically. See §1 "Q4 saved table views + Q6 retirement planner + Q10 opening checklist". | — | — |
| ~~Q5~~ | ~~**Compare two routes side-by-side**~~ | ✅ **Shipped this session.** New `_openCompareModal` with 3-column table (route A · route B · Δ) for ~15 key fields. Direction-aware Δ coloring (green when A's value moves the score in the better direction, red opposite, neutral grey for missing/undirected fields). One-line legend, per-route Open-in-AS buttons. Triggered from U12 footer's Compare button (visible only at exactly 2 selected). See §1 "Cluster A — Bulk operations". | — | — |
| ~~Q6~~ | ~~**Aircraft retirement planner**~~ | ✅ **Shipped this session.** New 🛩 panel-header button opens a modal with the fleet sorted by age desc; threshold input (default 24 months) highlights "retiring soon" rows; per-row "Find replacement →" link to the dashboard's Used Aircraft Scanner. `MAX_LIFE_YEARS = 25`. See §1 "Q4 saved table views + Q6 retirement planner + Q10 opening checklist". | — | — |
| Q7 | **Settings-strategy presets** | Save the entire panel config as a named profile ("Premium hub-and-spoke", "Cargo-heavy", "Cost-discipline"). One-click switching writes through the existing `RouteAssistantSettings.save` plumbing in a `preset` namespace. | A/B test strategies — flip back to a saved baseline after experimenting. | ~150 LOC |
| ~~Q8~~ | ~~**Status-change history per route**~~ | ✅ **Shipped this session.** New `RouteAssistantStatusHistoryStore` + `_detectStatusTransitions(rows)` in panel; St cell tooltip extended with "OVER since 12d · was OK before · 4 transitions tracked · Recent: …". Cap 20 transitions per route, append-only with dedup. See §1 "Q8 status-change history per route". | — | — |
| Q9 | **Bulk frequency template apply** | Multi-select + "Apply 7×/wk to selection" CTA. Reuses `schedule-management/schedule-builder` to compose the wave; tier-3-equivalent risk so behind a confirm modal. | Growing a region: 12 new routes, all daily — currently requires the schedule builder per route. | ~100 LOC |
| ~~Q10~~ | ~~**Route opening checklist**~~ | ✅ **Shipped this session.** New right-click menu entry "Opening checklist…" on NEW-status rows opens a 7-prerequisite popover (distance · demand · aircraft · service profile · markets · ORS · carriers) with ✓/✗ per item + "fix this →" for each ✗. Pure derivations from cached row state. See §1 "Q4 saved table views + Q6 retirement planner + Q10 opening checklist". | — | — |
| ~~Q11~~ | ~~**Single-step undo**~~ | ✅ **Shipped this session** as a foundation extension on top of the toast primitive. New `_undoableSave({label, perform, restore})` helper on the panel + 4 wired call sites (profit-modifier Save / Clear, route-note Save / Delete). Caller captures the prev state, helper fires a 6s success toast with an Undo button wired to `restore`. Future sites add ~10 lines per site. See §1 "Q11 single-step undo". | — | — |
| ~~Q12~~ | ~~**Persistent free-text search**~~ | ✅ **Shipped this session as part of Foundation QoL primitives.** New 🔍 input on the controls bar; filters by IATA / city / routeNoteText (case-insensitive); persists to `settings.routeAssistant.searchQuery` with 200ms debounce. See §1 "Foundation QoL primitives". | — | — |
| ~~Q13~~ | ~~**Yield heatmap matrix view**~~ | ✅ **Shipped this session.** New 🗺 panel mode toggle, hubs × destinations grid sourced from per-hub `routeAssistant:topRoutes:<HUB>` records (foundation refactor in same slice). Cells color-coded by user-selectable metric (score / profit / share); three-stop gradient blue→amber→green. Click cell → opens that hub-dest scheduling page. Empty-state banner when fewer than 2 hubs visited. See §1 "Cluster B — Decision-support overlays". | — | — |
| Q14 | **Fleet age/condition heatmap** | Dashboard widget: rows = aircraft type, columns = age bucket, cell intensity = count. Identifies fleet-cohort retirement clusters before they hit. | Catches "all my 757s retire in the same year" before the budget surprise. | ~200 LOC |
| ~~Q15~~ | ~~**Hub keyboard quick-swap**~~ | ✅ **Shipped this session.** `settings.recentHubs` (cap 5, deduped most-recent-first) tracks every hub the panel has mounted on. Click chip in controls bar → navigate to `/app/com/scheduling/<HUB>`; Alt+1..5 keyboard shortcut. Renders nothing when only one hub has been visited. Settings field was pre-staged by user this session. See §1 "N2 Notification center + …". | — | — |
| Q16 | **Long-op desktop notifications** | `chrome.notifications` API ping when a 30+-route bulk-sync (markets / ORS / carriers / demand depth) completes. | Lets users tab away during long runs. Several scrapers take 5+ minutes; today they need attention. | ~80 LOC |

#### UI / interaction (16)

| # | Slice | What | Why it's practical | Est |
|---|---|---|---|---|
| ~~U1~~ | ~~**Sticky table header**~~ | ✅ **Already shipped in an earlier session** (verified during this session's foundation slice). The thead is `position: sticky` with explicit `top` offsets so the group-header row + column-header row stack correctly during vertical scroll. Frozen leading columns (Score + Dest) also already in place. | — | — |
| U2 | **Frozen leading columns** | Score + Dest stay pinned when the user scrolls horizontally past the wide column set. CSS `position: sticky` on the leftmost cells. | The St / Dest columns are the row identity — losing them when scrolling right is a navigation tax. | ~80 LOC |
| U3 | **Collapsible column groups** | Click a group header strip (`actuals` / `markets` / `ors` / `demand` etc.) to collapse every column under it. Reuses the existing `COLUMN_GROUPS` map; persists open/closed state. | Lets users compress the 50+ columns to a workflow-relevant subset without losing them entirely. | ~100 LOC |
| U4 | **Right-click row context menu** | One menu consolidates the per-row actions (edit override, calibrate, watchlist, view history, copy IATA, open scheduling/markets). Cleans up the IATA cell's icon clutter. | Today actions are scattered as inline glyphs + ▾ carets + right-click-only modals. Single discoverable entry point. | ~120 LOC |
| ~~U5~~ | ~~**Multi-select rows**~~ | ✅ **Shipped this session as foundation primitive for Cluster A.** Checkbox column + shift+click ranges + select-all header + 3px purple left-edge tint on selected rows + filter-change drop-toast + conditional right-click menu entry. Selection ephemeral (in-memory only). See §1 "Cluster A — Bulk operations". | — | — |
| U6 | **Inline cell editing** | Double-click LF / yield / cargoYield cells to edit in place; ⏎ saves, ⎋ cancels. Skips the override modal for quick tweaks. | Power-user shortcut — a 1-cell edit currently takes 5+ clicks through the override editor. | ~150 LOC |
| U7 | **Column visibility chooser** | Gear icon opens a checklist of all columns with persistent toggles. | 50+ columns is a lot for a fresh user; lets people hide what they never use. | ~100 LOC |
| U8 | **Drag-and-drop column reorder** | `<th>` becomes draggable; column order persists per-user via `settings.routeAssistant.columnOrder`. | Different mental models prefer different column orderings; flexible layout. | ~100 LOC |
| U9 | **Color-blind-safe palette toggle** | Alternative status / Δ% / score colours that read clearly under deuteranopia and protanopia. Single setting flag, palette swap. | Accessibility — current palette uses red/green which is the most common confusion pair. | ~40 LOC |
| U10 | **Keyboard navigation** | `↑/↓` move row focus; `Space` toggles watchlist; `Enter` opens override editor; `Esc` closes the focused popover. | Power-user mouse-free workflow + accessibility. | ~120 LOC |
| ~~U11~~ | ~~**Hover row preview card**~~ | ✅ **Shipped this session.** Singleton floating card with 200ms dwell + 150ms grace dismiss. Contents: title + status pills (★ override 📝 status fit) + full $/flt breakdown table + key stats line + footer + inline note. Position prefers right-of-row, falls back to left or below; cursor entering the card cancels dismiss. See §1 "Cluster B — Decision-support overlays". | — | — |
| ~~U12~~ | ~~**Frozen footer with selection totals**~~ | ✅ **Shipped this session.** Sticky-bottom footer inside `tableHost` rendered when `_selectedRoutes.size > 0`. Layout: count chip · Σprofit/wk · avg score · avg ourPaxShare · Edit-overrides button · Compare button (enabled at exactly 2 selected) · Clear button. Skipped in Wave View / ORS Sandbox / heatmap modes. See §1 "Cluster A — Bulk operations". | — | — |
| U13 | **Mini-map scroll indicator** | A 6 px vertical strip on the table edge with status-band colours by row position. Click any band to jump to that segment. | Long lists become navigable visually — "where are the OOR routes?" in one glance. | ~100 LOC |
| ~~U14~~ | ~~**Toast notifications**~~ | ✅ **Shipped this session as part of Foundation QoL primitives.** New `RouteAssistantToast` global (singleton bottom-right host, slide-in animation, auto-dismiss, optional action button, stack of up to 6). Wired into 3 sites (watchlist toggle with Undo, route-note save, sandbox Calibrate-T). Foundation primitive — future features call `RouteAssistantToast.show(...)` instead of bespoke confirmation banners. See §1 "Foundation QoL primitives". | — | — |
| ~~U15~~ | ~~**Group-header hover highlight**~~ | ✅ **Shipped this session as part of Foundation QoL primitives.** Each `<th>` and `<td>` carries a `data-group` attribute; mouseenter on a group header toggles a `aes-grp-hover` class on every matching cell, lifting the column band's brightness via inset box-shadow (CSS rule in `css/content.css`). | — | — |
| U16 | **Settings drawer pin-open mode** | Drawer can be pinned open while interacting with the table. Resizable. Persists across mounts. | Iterative tuning workflows want to see the table react to setting tweaks live; closing the drawer to see the table is fiddly. | ~60 LOC |

**Highest-leverage cluster** if shipping a session of polish: **Q1 (filter chips) + Q11 (undo) + U1 (sticky header) + U5 (multi-select) + U11 (hover preview) + U14 (toasts)** — turns the panel from "powerful but dense" into "fast to operate." ~600 LOC, one focused session.

### Letter L (canopy) — multi-account federation plan (v2)

**Status:** v2 plan committed; no code shipped. Promoted from the Daily-driver QoL backlog row (above) to a dedicated sub-section. Subsumes the "Cross-server view" backlog row entirely. Detailed plan at `.claude/plans/zany-hopping-clover.md`. v2 expanded scope from "registry plumbing + read-only dashboard" (v1) to "registry + affiliation graph + Strategy DNA + combined-supply analytics + cross-account wave coordination."

**User decisions captured this session (2026-04-27):**
- Scope: every `(server, airline)` tuple is a registered account — fully cross-server + cross-airline.
- Sync mechanism: **passive observation only** — register on visit, no credential vault, no auto-login, no background tabs. Auto-login canopy (the original Letter L brainstorm) is explicitly **deferred** to a future "Letter L+" slice; the plan leaves all hooks needed to drop it in without re-keying data.
- Cloud sync between devices: deferred (local-only).
- **DNA scope: hybrid** — global template `aesCanopy:dna` + sparse per-account override `aesCanopy:dnaOverride:acct:<id>`; `effectiveDna(accountId) = deepMerge(template, override)` at leaf level; drift always computed against effective.
- **Affiliation precedence: user override always wins** — `userOverride: true` flag is absolute; auto-classification only fills empty slots; "Reset to detected" clears the flag.
- **Wave coordination: full editor with cross-account schedule writes** (Slices L9–L12) — Tier-3 two-gate apply pattern non-negotiable; per-account confirm; cross-account audit log + 60s undo window.
- **No kin hierarchy** — all self-tagged accounts are equal in the affiliation graph; no `parentEnterpriseId` field. AS's parent/subsidiary distinction is not modeled at the data level.

**Five conceptual layers (above the v1 storage plumbing):**

1. **Affiliation graph** — every observed enterprise classified as `self / allied / interline / codeshare / neutral / adversary`. Auto-feed from `routeAssistant:contractualPartners:<myEnterpriseId>` (existing F slice 3 cache). User override always wins. Storage: single global blob `aesCanopy:affiliations`. Joins everything downstream.
2. **Strategy DNA** — persistent playstyle profile (service-mix, network-shape, cargo emphasis, manufacturer prefs, size-mix targets, risk profile, country focus, tempo, brand stance, growth posture). Hybrid scope (template + per-account override). Drives DNA-fit scoring per account + per opportunity, default settings template for new accounts, and drift detection.
3. **Combined-supply / effective-competition** — RA panel "Canopy view" toggle adds columns: MyWk (combined kin weekly flights), Cmp* (true effective competitor count), FShare (combined kin marketShare), Cannib (self-cannibalization warning), Gap? (high-demand kin-absent route). Pure-function aggregator over `(routes, accounts, affiliations)`.
4. **Geographic awareness** — country / region as first-class organizing dimensions. Per-country demand pool, per-country kin presence, cross-country flow matrix, regulatory lens (domestic / continental / intercontinental). New "Geography" tab on the canopy dashboard. Region groupings stored at `aesCanopy:regions`.
5. **Cross-account wave coordination** — interline gateway detection, per-gateway wave audit (MCT-driven connection feasibility), wave alignment recommendations (Pareto-frontier shifts), canopy wave editor (multi-row Gantt with drag-shift + live connection counts), slot model (`aesCanopy:slots:<airport>`), interline-aware schedule preset solver (canopy weight in `schedule-builder`). Schedule write-back via Tier-3 two-gate apply mirroring `pricing-applier`.

**Why this matters:** today the extension is single-account-implicit. A user running multiple AirlineSim airlines (FLY NYON + NYON., potentially across worlds) gets cross-airline data corruption on shared hubs (one airline's `routeAssistant:override:JFK-LAX` clobbers the other's; ORS-rank readings overwrite each other since `isOurs` differs by airline; `routeAssistant:topRoutes:<HUB>` and `lastSnapshot:<HUB>` collide), no unified view across airlines, and settings tuned for one airline silently apply to the other. v2 goes further: the canopy doesn't just isolate accounts, it **federates** them — the user's airlines should reinforce each other (combined supply, gap-detection, wave coordination, cross-account interlining), not operate as parallel silos.

**Storage scoping rubric (single most important design decision):**

| Class | Examples | Scoping |
|---|---|---|
| **A · World-fact** (airline-agnostic, expensive to scrape, shareable) | `routeAssistant:demand:<IATA>`, `routeAssistant:distance:*`, `routeAssistant:typeSpec:<typeId>`, `routeAssistant:fuelPriceIndex`, `routeAssistant:fuelBurnOverride:<typeId>`, `flightsFrom:<IATA>` | **Stay global.** Every account benefits from another's scrape. |
| **B · Account-perspective observations** (depend on "ours") | `routeAssistant:ors:*`, `routeAssistant:markets:*` (4 families), `routeAssistant:ticketPrice:*`, `routeAssistant:carriers:*`, `routeAssistant:inventory:*`, `routeAssistant:yieldHistory:*`, `routeAssistant:statusHistory:*`, `routeAssistant:lastSnapshot:<HUB>`, `routeAssistant:topRoutes` + `routeAssistant:topRoutes:<HUB>`, `routeAssistant:sandboxBacktest:*`, `routeAssistant:ratingObservations:*`, `routeAssistant:pricingApplyLog` (+ per-route ring), `routeAssistant:enterpriseMeta:*`, `routeAssistant:contractualPartners:*` | **Per-account.** |
| **C · Account-private state** | `routeAssistant:override:*` (full-replace — clash is data-loss), `routeAssistant:routeNote:*`, `routeAssistant:watchlist`, `routeAssistant:alertRules`, `routeAssistant:ratingAlpha:*`, `aircraftFlightPlan:auditLog`, `aircraftFlightPlan:autoApplyLog`, `aircraftFlightPlan:fnApplyLog` | **Per-account.** |
| **D · Account-private settings** | `settings.routeAssistant`, `settings.aircraftFlightPlan`, `settings.usedAircraftScanner` | **Per-account, with a `settings:default:*` template each new account inherits.** |
| **E · Already correctly scoped — no change** | `<server><airline>aircraftFleet`, `<server><airline>schedule`, `aircraftFlightPlan:state:<server>:<aircraftId>`, `<server>aircraftFlights<aircraftId>`, `<server>marketScan:*`, `<server><airline>stationAutomationRun:*`, `aircraftFlightPlan:auditLog:aircraft:<server>:<aircraftId>` | None. |

**Naming convention:** `<existing-prefix>:acct:<accountId>:<existing-suffix>` (e.g., `routeAssistant:override:acct:abc123:JFK-LAX`). Additive — doesn't disturb existing keys; legacy keys stay live as fallback during migration. `<accountId>` = `sha1(server + ":" + airlineCode).slice(0,12)`.

**Slice plan (v2 — 13 slices across 5 phases):**

| Phase | # | Title | Scope summary | Est. LOC |
|---|---|---|---|---|
| **1 Foundation** | **L1** | Registry foundation | NEW `modules/_shared/account-registry.js` (singleton `AesAccountRegistry`, key `aesAccounts`) + `account-scoped-key.js` (`acctKey()` + `currentAccountIdSync()` helpers) + `helpers.js` touch wiring + `background.js` `account:touch` handler (single-writer for `aesAccounts`) + Accounts options-page section. No store refactors yet. | ~400 |
| | **L2** | Class C+D refactor (private state + settings) | `RouteAssistantSettings`, `AesAfpSettings`, `RouteOverridesStore` (full-replace — accountId MUST round-trip through every caller incl. Q2 bulk + Undo restore), `routeNoteStore`, `watchlistStore`, `alertRulesStore`, `ratingAlphaStore`, `statusHistoryStore`, `recentHubs`, `savedViews`. Migration shim. **Highest data-integrity stakes.** | ~900 |
| | **L3** | Class B refactor (account-perspective observations) | ORS, markets (4 families), ticketPrice, carriers, inventory, yieldHistory, sandboxBacktest, ratingObservations, pricingApplyLog (+ per-route ring), `topRoutes` writer (`_publishTopRoutes`), `lastSnapshot` writer (`_writeDiffSnapshot`), enterpriseMeta, contractualPartners, `routeAssistant:yieldBaselines`. | ~700 |
| **2 Affiliation + DNA** | **L4** | Affiliation graph + UI | `aesCanopy:affiliations` store (single global, kind ∈ `self/allied/interline/codeshare/neutral/adversary`); auto-classify from contractualPartners + registry; user-override absolute precedence with `userOverride: true` flag + "Reset to detected" affordance; new Affiliations options-page section; affiliation badges on Cmp popover (`★/⇄/✦/⚠`) + account chip popover. | ~500 |
| | **L5** | Strategy DNA — hybrid scope | `aesCanopy:dna` global template + `aesCanopy:dnaOverride:acct:<id>` sparse per-account overrides + `effectiveDna(accountId)` resolver (deep-merge at leaf level); options-page DNA wizard (~15-question first-run setup, editable inline thereafter); per-account DNA editor with inherit/override toggles per dimension; DNA-fit scorer module + DNA-fit pills on account rows + opportunity rows. | ~700 |
| **3 Cross-account analytics** | **L6** | Combined-supply view | RA panel "Canopy view" toggle; new columns MyWk / Cmp* / FShare / Cannib / Gap?; enterpriseId enrichment on competitor-row scrapes (gap to fill since markets-page roster doesn't currently include enterpriseId per row — fall back to carrier-prefix match against kin's known prefixes when enterpriseId missing). | ~600 |
| | **L7** | Cross-account dashboard | NEW `modules/canopy-dashboard/` (host + aggregator + tabs: All Routes / Fleet / Conflicts / Yields / Sync status / Geography / Cannibalization / Family Coverage). DNA-fit ranking on every list. Read-only. | ~1200 |
| | **L8** | Fleet balance + DNA-aware acquisitions | UAS gains DNA-fit overlay on candidate cards (uses `AS_TYPE_TO_FAMILY` for manufacturer dim); per-account deployment-fit ("for which account?" picker); cross-account fleet balance dashboard tab (current type/age/seat mix vs effective DNA target). | ~600 |
| **4 Cross-account scheduling** | **L9** | Interline Gateway detector + audit | Walk every kin's schedule + partners' observed schedules; gateways = airports with ≥2 my-side ops; per-gateway wave audit emits realized/potential connection counts under MCT (60 min same-op, 90 min cross-op kin, 120 min interline). Read-only dashboard tab. Audit results cached at `aesCanopy:gateway:<airport>`. | ~700 |
| | **L10** | Canopy wave editor (read-only preview) | Multi-row Gantt (kin per row); connection arrows between feasible inbound→outbound pairs; drag a wave cluster → time-shift → connection arrows recompute live. No save-back yet. Pareto-frontier recommendation panel ("Shift A's Wave 2 by +30 min → +12 connections from B, costs N intra-account connections"). | ~900 |
| | **L11** | Slot model + storage | `aesCanopy:slots:<airport>` per (kin, time-bucket); slot-pressure column on dashboard; capacity warnings in editor; slot record write-through invariant — schedule shifts re-derive slot records in the same `chrome.storage.local.set` batch. | ~400 |
| | **L12** | Wave-edit save-back + interline-aware solver | Tier-3 two-gate write path into per-account `ScheduleStore`; per-account confirm modal + cross-account audit log + 60s undo window; `schedule-builder` gains optional canopy weight `λ × Σ cross-account connection bonuses` alongside intra-account profit objective. | ~900 |
| **5 Polish** | **L13** | Migration cleanup + Letter L+ readiness | Ambiguous-data resolution modal, per-account storage breakdown in options ("Storage by account" + per-class breakdown), "delete account + data" confirmation flow, legacy-key prune helper (off by default; surfaces as "Reclaim ~Nkb"); hooks pre-staged for Letter L+ canopy auto-login (deferred). | ~400 |

Total ~8500 LOC across 13 slices. Multi-month, multi-session. **L1–L8 deliver standalone value** (registry, affiliations, DNA, combined supply, dashboard, fleet balance — a clear milestone after which the user can run the federation analytically). **L9–L12 unlock the operational coordination breakthrough.** L13 ships last.

**Migration is one-shot, idempotent, additive.** On first load, `migrateLegacy()` enumerates every `(server, airline)` tuple seen in existing `<server><airline>aircraftFleet` keys and registers them. Class B/C/D legacy keys are copied (not moved) into their `:acct:<id>:` namespaced form, owner inferred from `isOurs` markers when available, falling back to most-recently-active account. Multi-account users with pre-existing data see a **"Confirm legacy data ownership" modal** before passive migration locks in. `aesAccounts.migrationVersion = 1` marks complete. Legacy keys NEVER deleted by Slice L1–L5 — they remain as fallback for any reader not yet refactored. L6 prunes after one release cycle confirms safety.

**Letter L+ (canopy auto-login, deferred):** the original Letter L brainstorm — encrypted credential vault, hidden-tab orchestrator that auto-logs-in expired sessions, scrapes, closes — is the natural follow-on. Slice L1–L6 leave the registry shape ready for it: `aesAccounts.accounts[id]` simply gains a `credentials` field + a `sessionState`, and a future `modules/canopy-orchestrator/` reads the same per-account namespaced keys. ToS / CAPTCHA / MFA risks remain for that slice; re-evaluate when the user is ready. Don't ship without explicit user confirmation that AS rules permit it.

**New §10 invariants this work introduces (cumulative across slices):**

*Foundational (L1–L3):*
- Registry single-writer rule — only `background.js`'s `account:touch` handler writes `aesAccounts`. Content scripts send messages, never `chrome.storage.local.set` directly on this key.
- accountId resolution order — `currentAccountIdSync()` reads `window.__aesAccountId` set during `helpers.js` bootstrap; off-AS-page callers read `aesAccounts.viewingAccountId`. Stores must NEVER `await` registry lookup before their first read in a page lifecycle (race condition).
- Full-replace stores' accountId round-trip — `RouteOverridesStore`, `ratingAlphaStore`, etc. retain full-replace semantics; every caller capturing `prev` for Undo must capture `accountId` too so restore lands in the right account even if the chip switched mid-session.
- Migration idempotency — `migrationVersion` gate; rerunning is a no-op. Legacy keys are read-only fallback during slice L1–L7 — never overwritten by namespaced data.
- Class A keys stay global by design — adding `:acct:<id>:` to demand / distance / typeSpec / fuelPriceIndex would be a regression; cross-account sharing is the whole point.
- Per-aircraft AFP keys (`<server>aircraftFlights<aircraftId>`, `aircraftFlightPlan:state:<server>:<aircraftId>`) NOT re-scoped — aircraftId is unique per server. Only the AFP audit-log timeline gains `accountId`.

*Affiliation + DNA (L4–L5):*
- **Affiliation graph is the single source of "kin / partner / competitor".** The old `isOurs` flag stays for ORS scrape time but classification for combined-supply, cannibalization, gateways, and effective-competition all reads `aesCanopy:affiliations` via `AesCanopyAffiliations.classify(enterpriseId)`. Forking the rule (e.g. one site classifies via `airlinePrefix`, another via affiliation graph) creates silent disagreements that surface as inconsistent dashboard numbers.
- **Affiliation user-override precedence is absolute.** `userOverride: true` flag is non-negotiable; auto-classification only fills empty slots. "Reset to detected" clears the flag. Stale-override surfacing is visual-only — never silent overwrite.
- **All kin are equal — no parent/child hierarchy.** No `parentEnterpriseId` field. Kin display ordering = lastSeenAt; if hierarchy display is desired later it's a UI label, not a data-model change.
- **DNA hybrid scope: `effectiveDna(accountId) = deepMerge(template, override)` at leaf level.** Every consumer (DNA-fit scorer, drift detector, default-settings template) reads via the resolver. Direct reads of just template or just override are bugs because they miss the federation/diverge intent. Drift always against effective, never template.

*Cross-account analytics (L6–L8):*
- **Combined-supply functions are pure.** No DOM reads, no chrome.storage reads. Inputs are `(routes, accounts, affiliations)` triples. Tests can hit the function without mocking storage.
- **EnterpriseId enrichment on competitor rows is best-effort.** When markets-page scrape doesn't return enterpriseId per row, fall back to carrier-prefix match against kin's known prefixes. Don't silently fail; surface "competitor enterpriseId missing — using prefix match" in the row tooltip.
- **DNA-fit scoring is normalized 0–1.** Every dimension contributes 0–1; weighted-sum produces overall score. Adding new DNA dimensions extends the contributor list without changing the score's range.

*Cross-account scheduling (L9–L12):*
- **Wave-editor save-back uses Tier-3 two-gate pattern.** `apply.enabled === true` AND `apply.dryRunOnly === false`, mirroring `pricing-applier`. Per-account confirm-before-save + cross-account audit log + 60s undo. Don't add a third path that bypasses both gates.
- **Cross-account schedule write requires kin-tagged accounts.** A wave save touching A and B requires both as `self`. Allied / interline are observation-only — never write their schedules. Hard error otherwise.
- **Slot record write-through.** Whenever a kin's schedule shifts, `aesCanopy:slots:<airport>` records for affected hours must be re-derived in the same `chrome.storage.local.set` batch as the schedule write. Decoupling = silent contract break.
- **Gateway audit results are advisory.** Audit emits projected connection counts from MCT + timing math, not from observed booking. After any wave save, banner prompts re-scrape ORS to confirm. Tooltip explicitly says "projection — re-scrape ORS to confirm."

### Tech debt
- ~~**Rename `ticket-price-scraper.js`** → `schedule-page-scraper.js`~~ ✅ Done. Class is now `RouteAssistantSchedulePageScraper`; storage prefix `routeAssistant:ticketPrice:` left as-is per the §10 stable-key invariant.
- ~~**Family-card grid override-walk**~~ ✅ Done. `TypeFamilyMap.familyList(overrides)` now folds `block.typeFamilyOverrides` into the matching family card; truly unknown labels still land in Custom.
- **Existing presets with multi-daily routes** — Wk column reads legacy cache via `frequencyPattern` fallback; one Sync click re-writes to the new `dailyFlights[]` shape.
- ~~**`routeAssistant:topRoutes` freshness**~~ ✅ Done. `displayUsedAircraftScanner()` now installs a singleton `chrome.storage.onChanged` listener (250ms debounced — RA writes both global + per-hub keys per render) that re-pulls `loadDealContext(server)` and re-renders results. Previous listener is removed on each remount via `window._aesTopRoutesListener`.
- ~~**Break-even daily-hours assumption**~~ ✅ Done. `MarketScanDealMetrics._blockHoursFor(row)` now picks from `BLOCK_HOURS_BY_CATEGORY` (commuter/turboprop/regional 8h, narrowbody 12h, widebody 14h) keyed off `row.familyCategory`. Falls back to the 10h `DAILY_BLOCK_HOURS` constant when category is missing.
- **Carriers SSR fragility** — `RouteAssistantCarriersScraper` parses the initial HTML returned by `fetch(flightsfrom.com/<HUB>-<DEST>)`. When flightsfrom doesn't SSR the carrier list (or changes their markup), the record stores `parserNotes` describing what was tried and `carriers: []`. The panel falls back to the plain `airlineCount` integer in that case. A child-tab worker (parallel to `content_flightsFrom.js`) would let the SPA hydrate before extraction; defer until we observe SSR failing in practice.
- **Competitive-intensity bands** — three-band thresholds (1 / 2-3 / 4+) are intuition-tuned, not data-fit. If users want different cutoffs the boundaries should move into `settings.routeAssistant.carriers` as `{lowMax, midMax}`.
- **Enterprise-meta selector fragility** — `enterprise-meta-scraper.js` walks 4 strategies (`[class*='banner']`, `[class*='avatar' / 'logo' / 'profile']`, `og:image`, header-img) and disambiguates banner-vs-avatar by candidate width + class keywords. AS markup tweaks could break this. Records carry `parserNotes` so the failure mode surfaces in the popover footer ("Some banners/avatars missing — open Settings → Carriers → Sync enterprise data"). When the heuristic genuinely lands the wrong image in the banner slot, the user can right-click the popover row in a future slice to re-tune.
- **Tabbed view — per-tab weights not stored** — switching the tab gates which scoring fields contribute, but the *weight* for each field is shared across all three tabs. A user who wants `weeklyFlights × 1` on Pax but `× 0.5` on Cargo today can't express it. If demanded, lift `settings.routeAssistant.scoring` from a single block to `{all: {…}, pax: {…}, cargo: {…}}` with deep-fill on load.
- **Demand depth — elasticity small-N regressions** — `RouteAssistantDemandDerivator._elasticity` requires ≥ 4 valid (capacity, price) points after dropping zeros. New routes / dormant lanes will hit this floor and return null with a `derivationNotes` line. Acceptable now (the column shows em-dash); a future tweak could fall back to a global cross-route elasticity prior.
- **Demand depth — PAX double-count risk** — `_pickPaxSeries` prefers explicit `byPayload.PAX` over summing Y+C+F when all three are captured. AS's PAX historic is itself the sum of those three, so we pick one or the other deterministically. If AS ever exposes PAX as a *different* aggregate (e.g., excluding F), the demand pool would be wrong by ~5–10%. Verify against a live route before trusting.
- **Demand depth — inventory parser fragility** — `parseInventoryHtml` walks every table on the page and relies on header keywords (Economy/Business/First/Cargo, Total/Capacity/Sold/Booked). AS markup tweaks could break this. Records carry `parserNotes` describing what was tried. Real-world tuning likely needed against an actual `/app/com/inventory/<HUB><DEST>` HTML sample.

---

## 10 · Open invariants — don't break without checking

- Every `chrome.storage.local` key prefix in §4 is **stable**. Other features and saved user data read them.
- `RouteAssistantPanel.SCORING_FIELDS[i].field` ↔ `RouteAssistantPanel.COLUMNS[i].field` for any variable that's both scored and displayed.
- `RouteAssistantSettings.load()` always returns a fully-populated object (deep-merged with `_defaults()`). Never assume a freshly-loaded settings object lacks a field.
- `_pairKey` in `distance-resolver.js` is **alphabetically sorted** — distance is symmetric. `_pairKey` in `schedule-page-scraper.js` AND `yield-history-store.js` is **directional** — price/freq/profit differ by direction. Different rule per file; easy to mix up.
- `enterprise-meta-scraper.js` is **NOT keyed by route** — its cache key is `routeAssistant:enterpriseMeta:<enterpriseId>`. The same record is reused across every route the enterprise competes on. Don't add HUB/DEST to the key; you'd just blow up storage with duplicates.
- `contractual-partners-scraper.js` is keyed by `routeAssistant:contractualPartners:<enterpriseId>` where the enterpriseId is **your own**, not a competitor's. The record's *contents* tell you who that enterprise's partners are; the popover then matches partner ids against visible competitor ids. Fetching every visible competitor's `?tab=1` would (a) waste fetches and (b) miss cases where the partnership is asymmetric — only your own page reflects who YOU have agreed with. Don't repurpose the scraper for competitor-pulls without changing the consumer logic.
- F slice 3 popover glyphs read `this._partnersByEnterpriseId` which is built by `_applyCachedContractualPartners()`. That method is called from `init()` after `_applyCachedEnterpriseMeta()` and again after every refresh. Adding new ways for partners data to land must call it (or the glyphs go stale until the next mount).
- **Tabbed view (`viewMode`)** — `RouteAssistantPanel._columnModes(col)` and the SCORING_FIELDS `modes` array gate visibility per tab. The "all" tab MUST keep every field active (preserves the pre-tabbed score blend for users who don't switch tabs). New scoring/column entries that should hide on focused tabs need an explicit `modes` array; entries without one default to all three modes.
- **Demand depth — `historic.byPayload`** — DON'T fold the `byPayload` map back into a top-level `{periods, capacities, prices}` shape. The pre-K shape stored only ECONOMY; the new shape stores all five payloads under one record. `_migrateHistoricRecord` reads either shape on load, but new writes MUST use `byPayload`. Adding a top-level `payload` field again would silently break the per-class derivation in `demand-derivator.js`.
- **Demand depth — `useRealDemandForLF` defaults false** — flipping it on shifts every $/flt and $/wk number on routes that have demand-depth data. The `_renderDemandDepthSection` toggle prompts a confirm on first activation; that prompt MUST stay so users see the warning before profit numbers drift.
- **Demand depth cache key non-collision** — `routeAssistant:inventory:<HUB>-<DEST>` is **directional**. Cargo aircraft on the route still see the same key — there's no per-aircraft variant. Don't sub-key by typeId; the inventory page is route-level, not aircraft-level.
- Yield Feedback now supports four attribution modes (`frequency` / `distance` / `equal` / `per-flight`) and a separate `deltaMode` axis. **Cumulative vs delta** toggles per-tail averaging mode (subtracts the prior snapshot's cumulative profit/flights from each tail's current numbers). **Per-flight (exact)** is a different code path entirely — it ignores tail averages, joins each aircraftFlights envelope with `<server>flightInfo<flightId>.money.CM5.Total`, and aggregates by `{originIata, destinationIata}` from the envelope. Routes with no measured flights fall back to per-tail frequency-weighted, never silently fail. The baseline blob (`routeAssistant:yieldBaselines`) is updated on every snapshot regardless of the active mode so flipping into Delta mode doesn't need a warm-up pass.
- `dailyFlights` is always a 7-element array (Mon→Sun); `weeklyFlights = dailyFlights.reduce(+, 0)`; `daysPerWeek = dailyFlights.filter(>0).length`.
- The aggregator's `applyFleetContext(rows, null)` clears every fleet-derived field. Calling with `null` must reset to a clean Phase-1-style row.
- Cargo revenue defaults to OFF (`cargoYieldPerKgKm: 0`). Don't change the default; existing users would see surprise number shifts.
- Family-card grid walks `block.typeFamilyOverrides` via `TypeFamilyMap.familyList(overrides)` so override-redirected labels appear in their target family card. Scan-time controller still honors overrides too.
- Phase 1 + 1.5 + 2 + 2.5 + 2.6 + 2.7 are all in production with user-tuned settings. Don't rip features without confirming.
- `routeAssistant:topRoutes` is **slim by design** (only the fields the Used Aircraft Scanner consumes). Adding fields here grows the per-render storage write — confirm a new consumer before extending.
- `routeAssistant:lastSnapshot:<HUB>` row keys are the **row property names** (`orsRatingGapToTop`, not the spec name `ratingGapToTop`). The diff lookup uses these literally — renaming the row field without renaming the snapshot key would silently kill the badge for that column on the next mount. The list of tracked fields lives in one place: `RA_DIFF_TRACKED` at the bottom of `panel.js`. Add a column → add an entry there → add `_appendDiffBadge(td, "<field>", row)` to the render closure. Don't fork the list.
- The diff baseline is **loaded once per (mount, hub) pair**, stored in `this._diffPrevSnapshot`. Within a single mount/hub session the baseline does NOT refresh from storage — even though `_drawTable` overwrites the snapshot every render, in-memory deltas remain anchored to "first read on mount". Hub change in the dropdown invalidates and re-loads via `this._diffBaselineHub` mismatch. Don't add a `chrome.storage.onChanged` listener for the snapshot key; the design intent is mount-anchored deltas, not live ones.
- `MarketScanDealMetrics.decorate()` mutates the row it's given. `MarketScanResultsTable._enrichDeal` always passes a fresh `Object.assign({}, r)` copy — new callers must do the same to avoid stomping `this.rows`.
- `RouteAssistantCarriersScraper._pairKey` is **directional** — `<HUB>-<DEST>` matches the flightsfrom URL pattern. Different from the symmetric `_pairKey` in `distance-resolver.js`. Easy to mix up.
- `RouteAssistantPanel._showCarrierIntensity` is a static class field updated in `_syncRenderContext()` from `settings.carriers.showCarrierIntensity`. The Cmp column's `render` closure reads it at draw time — adding new closures that depend on the carriers settings should follow the same pattern (don't reach back into a panel instance).
- **Markets-page split storage.** `routeAssistant:markets:*:<HUB>-<DEST>` is **4 sibling key families**, not one blob. Always read via `RouteAssistantMarketsPageScraper.bulkLoadCache(pairs, {families: [...]})` so the read amplifies into one combined `chrome.storage.local.get`. Writers MUST go through `saveAllRecords` — direct writes risk leaving the families out of sync.
- **ORS `byClass` is the source of truth for all rank flavors and the composite.** `routeAssistant:ors:<HUB>-<DEST>.byClass` holds per-class records (`ECONOMY` / `BUSINESS` / `FIRST`), each carrying its own rank flavors + ratings + connection list. The composite/headline metrics (the values shown in the panel's ORS / RkNS / Gap / Y / C / F columns) are **computed at read time** in `_applyCachedOrs` from `byClass` using the user's `combineMethod` + class weights. **NEVER store the composite** — settings changes (preset, weights, combine method) re-project without re-scraping. Adding a new rank metric should be a pure-function read against `byClass`, never a re-scrape.
- **ORS lazy migration** — legacy single-class records (pre-this-session) keep working via `RouteAssistantOrsScraper._migrateOrsRecord` which wraps the flat `{rankAny, …, connections}` into `{byClass: {<original payload>: {…}}}` at read time. Pure read-time, never writes back. The next `scrape()` overwrites with the new shape.
- **Per-class column visibility (`orsClassY` / `orsClassC` / `orsClassF`)** is gated by BOTH `settings.ors.showPerClassColumns` AND `!settings.compactView`. Compact view always hides them regardless of the toggle. The gate logic lives in `_activeColumns` next to the existing group-level gates.
- **Composite formula skips null per-class values.** If FIRST has 0 connections (regional route, no F cabin), its metrics are null and the composite excludes it from the weighted average — the formula doesn't divide by 0 or treat null as zero. Same for any class not yet scraped.
- **ORS Wicket per-route handshake.** Each `RouteAssistantOrsScraper.scrape()` does GET → POST. Do NOT try to share a wicket session across the bulk batch — page-version IDs increment per interaction; reusing a stale session returns a `PageExpiredException` HTML page that parses as zero results (silent corruption). The 2-request handshake cost is acceptable at concurrency=2 / stagger=1500ms.
- **Tier 3 two-gate model is non-negotiable.** A real `RouteAssistantPricingApplier.apply()` POST requires BOTH `settings.routeAssistant.pricing.apply.enabled === true` AND `apply.dryRunOnly === false`. The applier instance enforces both — even if a caller passes `{dryRun: false}`, the instance's `dryRunOnly` overrides. Slice 3.1 shipped with both blocked; slice 3.2 flipped `dryRunOnly`'s default to `false` but `enabled` stays opt-in. Don't add a third path that bypasses these (e.g., a "force-apply" debug helper). The two gates have separate semantic meanings — kill switch (user's "off" intent) vs slice gate (the codebase's "ready" intent) — collapsing them would break the safety contract.
- **Tier 3.2 circuit breaker persists through settings.** The applier instance carries `_consecutiveErrors` for the session; `circuitBreakerTrippedAt` lives in `settings.routeAssistant.pricing.apply` so a tripped breaker survives panel remount + page reload. `_persistPricingBreakerTrip` writes; `_persistPricingBreakerReset` clears on the first verified/posted apply. Don't move trippedAt to in-memory only — a 429 storm should make Apply unreachable until cooldown expires across every page load. Don't move `_consecutiveErrors` to settings either — it resets on the first non-rate-limit terminal path (verified, posted, or even non-rate-limit failure) and shouldn't bleed across instances. The Step-0 cooldown gate at the head of `apply()` is dry-run-exempt: dry-run is a pure GET that's safe even while AS is rate-limiting POSTs.
- **Tier 3 per-apply Wicket handshake.** `RouteAssistantPricingApplier.apply()` MUST do its own fresh GET on every call. Don't try to cache the form context across applies — same reason as the ORS scraper, page-version IDs invalidate per interaction. Caching the cached-ownPricing snapshot for the modal's input defaults is fine; caching the form action/hidden fields for the POST is NOT.
- **Tier 3 `prevPrices` comes from the live GET, not the cache.** The applier's audit-log `prevPrices` field is the `currentPrices` parsed from the same GET that harvested the form context. Don't substitute `cachedOwn.prices` — a stale cache would make Undo restore to the wrong baseline. The cache is only used to seed modal inputs (UX nicety).
- **Tier 3 fingerprint dedup is at the apply-log layer, not the applier.** `RouteAssistantPricingApplier.fingerprint(hub, dest, prices, scope)` is deterministic; the applier always computes one and passes it to the apply log. The log dedups on identical fingerprint within a 5-min window (default `dedupWindowMs`) by merging into the head entry's `count` rather than adding a new entry. Don't move dedup into the applier — the apply pipeline always wants to run (the user might be retrying after fixing a preflight blocker; we want the new attempt visible), and the audit trail is where dedup belongs.
- **Tier 3 cooldown reads only successful entries.** `RouteAssistantPricingApplyLog.getLastSuccessAt(hub, dest)` walks the per-route ring looking for the most recent `verified` or `posted` entry. `dry-run` and `failed` entries are SKIPPED — a dry-run shouldn't gate a real apply, and a failed write shouldn't gate a retry. Cooldown enforcement is in preflight (`cooldownMinPerRoute`); changing the success-statuses set requires updating both.
- **Tier 3 submit button name MUST be in the body.** Wicket disambiguates which submit handler fires by which submit-button name appears in the body. Default `submit-prices` (apply prices only); `p::submit` (apply prices + settings); `submit-settings` (apply settings only — irrelevant to Tier 3). Skipping the submit name would cause AS to reject the form silently. Slice 3.1's `buildBody` always includes one.
- **Tier 3 General Settings round-trip.** `parseFormContext` captures the current selected `value` for each of the 5 General Settings selects. `buildBody` sends them back unchanged unless the caller overrides via `opts.settings`. Why: the markets-page form posts EVERYTHING in the panel; sending only price fields would clear terminals, service profile, boarding/cargo preference. Don't drop the round-trip; an apply that wipes those is a HIGH-severity regression.
- **Tier 3 missing-class round-trip.** When `prices` is partial (e.g., only `{Y: 150}`), `buildBody` sends every other class at its current value. Don't change this to skip the missing fields — Wicket forms are atomic; the response would clear the un-sent prices to zero or default.
- **Tier 3 apply log shape is contract.** Adding new fields is fine (the cleaner drops unknowns gracefully); renaming fields will silently break the panel's "Recent applies" preview list (it reads `e.status` / `e.prevPrices` / `e.newPrices` / `e.source` / `e.ts`). The preflight sub-record's shape (`{blockers, warnings, deltas, percentDeltas}`) is also contract — the modal renders blockers/warnings independently.
- **ORS circuit breaker is mandatory.** First HTTP 429/503 increments `_consecutiveErrors`; 3 in a row halts the bulk run, persists `settings.ors.circuitBreakerTrippedAt`, and disables the bulk button for `circuitBreakerCooldownMs` (default 10 min). Never silently retry through rate limits.
- **`expiresAt` MUST round-trip through every override save path.** `RouteOverridesStore.save` is full-replace (line ~80 of route-overrides-store.js) — fields not in the saved record are intentionally cleared. The full override editor passes `expiresAt` from its input. The profit-modifier popover, Q2 bulk modal, and all Undo restore closures explicitly carry `prev.expiresAt` through. Skipping any of these wipes the user's TTL on incidental yield/LF tweaks. New override-write call sites should follow the same pattern: `expiresAt: prev.expiresAt != null ? prev.expiresAt : null`.
- **`row._expired` / `_expiresSoon` decoration runs once per render in `_renderRows`.** The walker reads `row.override.expiresAt` against `Date.now()`. Expensive: O(rows). The Dest cell render closure consumes the flags but doesn't re-derive them — keeps the closure cheap. Don't move the decoration inside `_buildTable` or you'll re-run it on every cell render.
- **Aggregator's `isExpired` gate is the SINGLE point that suppresses applying.** The estimator gets `null` when expired; the row keeps the record so the UI can render. Don't add a second gate (e.g. in the panel) — they'd diverge. The aggregator's gate is what the profit numbers depend on; the panel decoration is purely visual.
- **`_detectStatusTransitions` runs on EVERY render, not just first mount.** A route's status can flip between renders (filter chip toggle changes scoring → status changes). The dedup inside `RouteAssistantStatusHistoryStore.appendTransition` AND the in-memory check in `_detectStatusTransitions` ensure no duplicate writes when status hasn't actually moved — but transitions DO get logged when the user's filter changes the computed status mid-mount. That's correct: the user is observing a real change in their workflow's view of the route. Don't move the call to mount-only; you'd miss filter-driven transitions.
- **`row._statusHistory` is decorated even when no transition is appended.** The St cell render closure depends on it being set for every row; the decoration runs unconditionally inside `_detectStatusTransitions`. Don't gate the decoration on "did we append?" — the closure will silently fall through to "no history" for ALL rows on subsequent renders.
- **First observation appends `{from: null, to: <status>, at: now}`.** Tooltip renders this as "first observed" rather than "was X before". Don't seed an empty transition or backfill from `routeAssistant:lastSnapshot:<HUB>` — the `lastSnapshot` is one-mount-old and may not reflect the actual prior status. Single-source-of-truth: the history accumulates from real observed transitions.
- **`MAX_TRANSITIONS = 20` is a hard cap.** Older entries are trimmed via `splice` on every append once the array exceeds 20. Don't bump the cap without re-checking storage growth at scale (50 hubs × 200 routes × 40 bytes per entry × N).
- **`_selectedRoutes` is ephemeral — NOT persisted to chrome.storage.** Selection lives in memory across sort/filter/refresh changes within a single panel mount. The drop-on-filter-intersection at the end of `_renderRows` is the single point that prunes hidden routes; don't add a per-call-site filter-aware drop or you'll fire the toast multiple times per render. Future "saved selection sets" feature should add a SEPARATE persisted store; do not bolt it onto `_selectedRoutes`.
- **`_selectAnchorDest` is tracked by destIata, NOT by index.** Sort changes reorder the visible rows; a destIata-keyed anchor stays valid through sort. If the anchor's row gets filtered out (no longer visible), the intersection-at-end-of-`_renderRows` clears the anchor too — shift+click thereafter falls back to plain toggle.
- **`RouteOverridesStore.save` is FULL-REPLACE, not merge.** Q2 bulk apply MUST read prev records first, build a merged record per-route preserving untouched fields, and pass the full record to `save()`. Skipping this loses untouched fields per route — silent data-loss bug. The `_applyBulkOverride` helper bakes this contract in; future bulk-write call sites should follow the same shape (capture prev → merge → save → wrap in `_undoableSave`).
- **Q2 parallel saves use `Promise.all`, NOT serial.** `chrome.storage.local.set` is contention-free between routes; serial saves at N=50 take ~250ms, parallel ~25ms. The Undo restore path is the same. Don't refactor to serial unless a future store gains contention-sensitive behaviour.
- **Q2 calls `_recomputeProfit()` ONCE at the end of the batch.** Per-route profit recompute would multiply render cost by N. Same for the Undo restore path. Don't move the call inside the per-route loop.
- **U11 hover card is a singleton** — only one card exists in DOM at a time. `_showRowPreview` calls `_hideRowPreview` first to clean up the previous one. Don't track multiple cards; the dwell + grace timing means a second card would only ever flash on cursor cross-overs and we'd still want one to win.
- **U11 dwell + grace timers must be cancellable.** `mouseenter` cancels `_rowPreviewLeaveTimer`; `mouseleave` cancels `_rowPreviewTimer`. Cursor enters card → cancels `_rowPreviewLeaveTimer`. Forgetting any of these creates flicker (card opens then closes mid-cursor-move). Test by hovering a row, moving cursor along the row's height, then to the card.
- **Q13 reads BOTH `routeAssistant:topRoutes` AND `routeAssistant:topRoutes:<HUB>`** — but only the per-hub key for cross-hub matrix data. The legacy single-key consumer (Used Aircraft Scanner deal-metrics) is unaffected; both keys are written on every render so the legacy contract survives. Don't drop the legacy key without auditing every consumer.
- **Q13 X-axis ordering uses the CURRENT hub's score** as primary sort. Destinations that exist in other hubs but not the current one fall to the alphabetic tail. This makes the current hub's row "look right" first; switching hubs (via Q15 chip click or browser navigation) re-renders with the new hub's score order at the next mount.
- **Q13 cell click opens a NEW TAB** (`window.open(..., "_blank")`) — does NOT mutate the current tab's panel state. Selection survives. The new tab is a separate AS scheduling page that the panel will mount fresh on.
- **Render branch order in `_renderRows` is wave → sandbox → heatmap → table.** All four can be independently toggled; the highest in the chain wins when multiple flags are on. Don't reorder; existing test verifications depend on Wave winning, then Sandbox, then heatmap.
- **`RouteAssistantAlertEvaluator.evaluate` is pure.** No DOM, no I/O, no `chrome.storage` reads. The panel passes `rules` (already loaded) and `rows` (already decorated with `_diff` + `_starred`) and gets back a list of triggered alerts. Rule cooldown is checked AGAINST the rule's own `lastFiredByRoute` map — the evaluator doesn't write back; the panel calls `RouteAssistantAlertRulesStore.recordFired(...)` after firing each toast. Don't put DOM/storage code inside `evaluate` — it's the contract that lets the panel call it on every render without performance fear.
- **`_alertFiredThisMount` is per-mount, NOT per-render.** Reset in `refresh()` and never elsewhere. The panel re-renders many times per mount (sort change, filter tweak, slider drag). The set keeps the same rule+route from re-firing during those intra-mount re-renders. The store-level cooldown (`rule.cooldownHours`) handles cross-mount dedup. Don't reset the in-memory set inside `_renderRows` or you'll spam toasts.
- **Alert rule `operator` semantics are strict.** `increased_by` / `decreased_by` read `row._diff.<field>` — null when there's no diff baseline (first mount on a hub). `above` / `below` read the raw row property. Don't mix — adding a new operator requires extending the evaluator's `_evalOnRow`, the available-fields catalog, AND the settings UI dropdown.
- **Alert rule field catalog is in `RouteAssistantAlertEvaluator.availableFields()`.** Adding a new alertable field means: (a) ensure the field is populated on `row.*` by the aggregator/applyCached path, (b) if it should be diff-trackable, add it to `RA_DIFF_TRACKED` in panel.js + the `_loadDiffSnapshot` shape, (c) append to `availableFields()`. Skipping (b) silently disables `increased_by` / `decreased_by` for that field with no error — the rule fires on raw `row[field]` only.
- **`RouteAssistantToast.progress` toasts are sticky by default (duration: 0).** Don't forget to call `complete(...)` or `dismiss(...)` when the operation finishes — orphaned progress toasts will sit forever until the user clicks ×. The bulk-scrape pattern always wraps in try/catch with a `complete({type: "error"})` in the catch + a final `complete({type: "success" | "warn"})` in the finally path. Future progress-toast call sites should follow the same shape.
- **`RouteAssistantToast._history` is in-memory only.** The `action.fn` reference is kept by reference in the history array; functions aren't serialisable so persisting to chrome.storage would lose them. The notification-center popover relies on the in-memory reference to re-fire actions. Closing the tab clears the history — that's documented in the popover footer. If you ever DO persist (e.g. for cross-tab visibility), drop the `action` field on serialise; deserialised history entries should render as click-disabled rows.
- **`_history` cap is enforced via `shift()` on every `show()`.** When `MAX_HISTORY = 50`, the oldest entry slides out as the newest comes in. Don't replace shift with splice or filter — shift is O(1) on most JS engines for sub-100 element arrays, and the array's reference identity must stay stable for the popover's iteration.
- **N2 popover is not a toast itself** — it's a separate floating panel anchored to the 🔔 button. It doesn't go through `RouteAssistantToast.show` (would create infinite recursion in the history). Outside-click + Escape teardown is local. Don't refactor it to use the toast primitive.
- **Q1 chips read `settings.filters[<field>]` directly.** `_applyFilters` consults `f.watchlistOnly` / `f.lossMakers` / `f.hasOverride` / `f.hasNote`; `_renderRows` consults `f.onlyChanged` post-decoration. Adding a new chip = (a) new field in `settings.filters` defaults, (b) one branch in `_applyFilters` (or `_renderRows` if it depends on `_diff`), (c) one row in the chips array in `_renderQuickFilterChips`. Don't fork the field name between defaults + filter + chip.
- **Q15 `recentHubs` cap is 5.** Hard cap on the array, NOT just on the chip-row render. The settings-store load-merge enforces the cap on every read so a stale value > 5 in storage gets trimmed. Don't bump the cap without re-checking the chip row's overflow behaviour (5 fits with the existing controls-bar layout; more would wrap).
- **Q15 Alt+N keyboard shortcut skips when focus is in input/textarea/contentEditable.** Test this — hub-swap eating Y-yield typing during override editing would be a major regression. The handler explicitly checks `t.tagName === "INPUT" || t.tagName === "TEXTAREA" || t.isContentEditable`. Don't drop the guard.
- **`_attachHubShortcuts` is idempotent.** Multiple constructor calls don't pile up listeners (the `if (this._hubShortcutHandler) return` guard ensures one handler is registered at a time). `_detachHubShortcuts` clears it. `dispose()` must call detach to prevent leaked listeners across panel re-mounts.
- **Progress toast pattern across all 6 bulk-sync sites.** Same shape: open `progressHandle` on entry, pump `progressHandle.update(...)` from inside `onProgress`, call `progressHandle.complete(...)` with terminal type at the end. Multi-phase scrapes (markets, demand) compose phases as a rolling bar over `pairs.length × phaseCount` total work — phase index times pairs.length offsets the local done into global done. Don't fork that pattern; future bulk-sync sites should mirror it line-by-line.
- **`RouteAssistantToast.update` is keyed by id, not by handle.** Calling `update(id, ...)` on a dismissed toast is a no-op. The handle returned from `progress()` is just a thin wrapper; if you store it across an async boundary, check `RouteAssistantToast._registry.has(id)` before spamming updates.
- **`_undoableSave` capture-then-perform contract.** The helper does NOT capture prev state itself — that's the caller's job. Read the prev value INTO A CLOSURE, then call `_undoableSave({perform: ..., restore: ...closure references prev...})`. This keeps the helper store-agnostic; each store has its own shape and the caller owns the read. If you skip the capture, the Undo will overwrite to whatever current state is at click time (which may have moved on if the user did several saves in a row). Future call sites should follow the same `const prev = store.get(...) || null` pattern.
- **`_undoableSave` toasts use `duration: 6000` by default.** Longer than the default 3.5s because Undo decisions take time. Don't shorten without confirming the user feels they had a chance to react.
- **`RouteAssistantToast` and `_noteToast` serve different jobs.** `_noteToast(msg, isError)` writes to a single inline status line under the panel header — used by long-running operations that want a persistent breadcrumb of "what's happening right now" (scan in progress, demand seeding, syncs). `RouteAssistantToast.show(msg, opts)` is for *transient floating* feedback — confirmations, undo affordances, errors, results. Both are kept; future features pick the right one for the situation. Don't replace `_noteToast` call sites with `RouteAssistantToast` blindly — the in-line status is load-bearing for some UX flows.
- **`RouteAssistantToast` calls are guarded by `typeof RouteAssistantToast !== "undefined"` at call sites in panel.js.** This is defensive: if the toast module ever fails to load (manifest order regression, a future split-build), the panel still functions without throwing. New call sites should follow the same guard pattern. Do NOT make the panel hard-depend on the toast global.
- **U15 `data-group` attribute is contract.** Every column header `<th>`, group header `<th>`, and body `<td>` MUST carry `data-group="<group>"` matching the COLUMN_GROUPS map. The hover highlight is just one consumer; future column-band features (filter chips, column-group toggle) will reuse the same selector. Don't drop the attribute when adding new column-render closures.
- **`_applyFilters` honours `settings.searchQuery` AFTER status / distance / fleet-flyable filters but BEFORE scoring.** The order matters because score normalisation re-bases against the visible row set; filtering before scoring keeps the score column meaningful (otherwise hidden routes would still influence the normalisation). Adding new filters should land in `_applyFilters` (between distance and search), not in `_drawTable`.
- **ORS Sandbox is read-only against AS and chrome.storage.local except `settings.routeAssistant.orsSandbox`.** The sandbox NEVER scrapes new ORS / markets data on its own (the user runs Sync ORS / Sync market analysis from the existing settings expanders); NEVER writes prices back to AS (Tier 3 is a separate feature); NEVER mutates `routeAssistant:override:` / `routeAssistant:routeNote:` / `routeAssistant:serviceConfig:` (the slice 2 "Save scenario as override" CTA is the future write path). The only persistence the sandbox does is its own settings block: `enabled`, `lastRouteIata`, `lastScenario`, `modelParams`, `perRouteTemperature`. Adding write-back here without a dedicated review is the highest-risk regression vector for slice 1.
- **`RouteAssistantOrsModel.project` is pure.** Inputs are immutable references (don't mutate `route.orsByClass[...].connections`); the model copies before mutating ratings. Adding side effects (storage writes, fetches, DOM access) to the model breaks the contract — the panel calls `project()` at 60Hz during slider drag (rAF-coalesced) and any I/O would tank perf. Keep it pure.
- **The model's rating-shift formula is `linear-in-percent`, NOT log-ratio.** `newRating = base − α_price × (newPrice − observedPrice) / observedPrice + α_comfort × Δcomfort`, clamped to `[0.5×base, 1.5×base]`. Outside ~30% deviation the formula is only directionally accurate; the panel surfaces this via the notes footer when clamps fire. Don't switch to log-ratio without retuning α_price defaults — the user's calibration intuition is built around the linear default.
- **Softmax share uses the `numeric-stable form`: `exp((rating − maxRating) / T)`.** Rating values regularly hit 100–200; without the max-subtraction, `exp(rating / T)` overflows at T < 10 and underflows when summed against zero ratings. Don't refactor away the max-subtraction.
- **`RouteAssistantOrsModel.MAX_FOR_SOFTMAX = 50`** — hard cap on connections fed to the softmax. Past rank #50 the contribution is <0.1% share at default T, and AS's pagination cutoff matches. Bumping this past 50 makes the rank list slower without changing share materially; lowering it below ~20 starts dropping legitimate competitors.
- **ORS Sandbox panel-mode mutual exclusion with Wave View** is enforced by branch order in `_renderRows` (Wave View checks first, then ORS Sandbox, then default table). Both flags can be `true` in settings simultaneously — Wave wins. Don't add a third mode that breaks the table-fallback path.
- **Right-click row menu split** preserves the existing override editor as the FIRST menu item (default action), with "Open in ORS Sandbox" second. Reordering would break user muscle memory built up over Phase 2.6 (the override editor has been right-click-only since the per-route LF/yield override shipped). The 2-item menu via `_openRowContextMenu` is the new gating point — don't bypass it back to a direct call.
- **Per-route T storage is opt-in, not auto-populated.** `settings.routeAssistant.orsSandbox.perRouteTemperature[<HUB>-<DEST>]` only gains an entry when the user clicks the Calibrate-T button on that route. The button refuses calibration when `marketShare` data is missing, the user's enterprise isn't in the leaderboard, or the freshness window between marketShare and ORS data is > 7 days — surfacing those failures by amber banner text rather than silently writing an inaccurate T. Adding an "auto-calibrate every visible route on mount" affordance in slice 2 must keep the same refusals.
- **`calibrateTemperature` returns the `closer endpoint` when the target share isn't bracketed in `[1, 200]`.** Observed shares that the rating-driven softmax can't reproduce (e.g. our 24% with rating-rank-#2 in a 5-connection list) hit this branch and return T=200 (the smooth boundary). The panel banner shows the value verbatim — the user reads "T=200" as "share isn't fully explained by rating differences alone." Slice 2 could surface this as an explicit warning, but slice 1's behaviour is correct and honest.
- **Wave overlay module is read-only against ScheduleStore; the panel's `_saveWaveScheduleToStore` is the SOLE write path.** `RouteAssistantWaveOverlay.buildSchedule()` runs `ScheduleBuilder.assignRoutes` + `evaluateFlights` + `computeConnections` in-memory; nothing persists. The `💾 Save schedule` CTA in the wave header strip (slice 2) is the single explicit-action gateway into `ScheduleStore.save` — it validates (preset present, airline loaded, no validation errors, ≥1 flight), constructs a fresh `ScheduleStore.newSchedule(...)` record from the cached build, mirrors `ScheduleBuilder.build()`'s warning surfacing for unplaced + shortfall, and wraps in `_undoableSave` so a misclick is one toast click from `ScheduleStore.remove`. **Do not** add silent-save anywhere else (e.g. an auto-save inside `_renderWaveOverlay`, a save embedded in `buildSchedule`, or a "save on preset change" hook); collapsing the explicit-action gate breaks the slice-1 read-only contract that other panel modes (sandbox, heatmap, table) still depend on.
- **Wave overlay reads from `this.scoredRows` AFTER the existing scoring + sort + watchlist decoration.** The early branch in `_renderRows` is intentionally placed at the bottom of the method (just before `_drawTable`) so Wave View honours filters, view mode, AND the watchlist's float-to-top behaviour without forking that logic. Don't move the branch earlier — you'd lose the watchlist promotion of pinned routes into the top-N candidate set.
- **`_pairKey` rule (recap, growing list).** Symmetric (alphabetically sorted): `distance-resolver.js`. Directional (`<HUB>-<DEST>` literal): `schedule-page-scraper.js`, `yield-history-store.js`, `route-overrides-store.js`, `carriers-scraper.js`, **`markets-page-scraper.js`** (this session, all 4 families), **`ors-scraper.js`** (this session). Different rule per file; easy to mix up.
- **`isOurs` detection** in `RouteAssistantOrsScraper.computeRanks` does **flight-number set match first, carrier-prefix fallback second**. Per user direction (option C of three offered). The flight-number set comes from the `<server><airline>schedule` cache (per-route `flightNumber: {FN1: {…}, FN2: {…}}` keys). For multi-enterprise users, both schedules contribute prefixes via `getOurCarrierPrefixes`.
- **Watchlist storage is global, not per-hub.** `routeAssistant:watchlist` holds every starred pair across every hub the user has touched. The panel filters by `this.hubIata` at render time (only pairs prefixed `<currentHub>-` decorate as starred). Don't add a per-hub key — the multi-hub design is intentional so a future "Watched routes" view can surface pinned routes across hubs in one place. Pair key is **directional** `<HUB>-<DEST>` uppercased; matches the orientation of `route-overrides`, `ticket-price`, `yield-history`.
- **Route notes are per-(hub, dest) and directional.** `routeAssistant:routeNote:<HUB>-<DEST>` matches the directional orientation of `route-overrides` etc. Empty/whitespace `text` field collapses to a `remove()` so deleted notes don't leave tombstones in storage. The Dest column 📝 icon is **always rendered** (subdued when empty) — this is a deliberate deviation from the spec's "when present" wording so users can discover the affordance for empty routes. Don't gate the glyph on `row.routeNoteText`; gate only its opacity + tooltip text.
- **Route note save is in-place, not a refresh.** `_openRouteNotePopover`'s Save handler mutates `row.routeNote` + `row.routeNoteText` directly + updates `this.routeNoteMap` + calls `this._renderRows()`. Don't call `await this.refresh()` on save — that would reload fleet, distances, schedule, demand etc. just for a text edit. The storage listener (`_attachStorageListener`) is also NOT wired to the `routeAssistant:routeNote:` prefix for the same reason; adding it would cause every save to refire `refresh()`.
- **Watchlist trigger fields piggyback on `RA_DIFF_TRACKED`.** `RA_WATCH_TRIGGERS` (in `panel.js`, immediately after `RA_DIFF_TRACKED`) lists `(field, worseDir)` pairs; every `field` MUST also appear in `RA_DIFF_TRACKED` because the trigger reads `row._diff.<field>` populated by `_decorateRowsWithDiffs`. Adding a tracked field for the diff badge is the prerequisite — the watchlist trigger is then a one-line addition. Removing a field from `RA_DIFF_TRACKED` without first removing it from `RA_WATCH_TRIGGERS` would leave the trigger inert (silent — the `Math.sign` check on a missing diff falls through). The signed direction matters: `+1` = an increase is bad, `-1` = a decrease is bad. Don't add a field whose worsening direction is ambiguous.
- **Watchlist + filters interact.** Starred rows are STILL subject to `status` / `minScore` / `fleetFlyableOnly` filters. A user who stars a route then dials minScore above the route's score will see it disappear from the table — the star isn't a filter override in v1. Documented behaviour; surface as a setting (`watchlist.bypassFilters`) only if the user asks. Float-to-top operates on rows that survived filtering, so the visual contract is "starred routes float above the rest of *what you're already showing*."
- **Watchlist toggle is optimistic.** `_toggleWatchlist` flips the in-memory Set + re-renders BEFORE awaiting `RouteAssistantWatchlistStore.toggle()` to keep the UI snappy. On storage failure the in-memory state is reverted and the panel re-renders again. Don't reorder these — awaiting storage first would block the click on a slow disk.
- **Settings export envelope is a contract.** The exported JSON's `format` field MUST equal `AES_CONFIG_FORMAT` (currently `"aes-config"`); the importer refuses anything else. If you ever change the constant, importing files exported under the old name silently fails. Keep the constant stable; if a true breaking change to the envelope shape is needed, version it via the existing `version` field and add a migration shim in `_openImportConfig` rather than renaming `format`.
- **Settings export covers two namespaces only.** `settings.routeAssistant` and `settings.usedAircraftScanner`. Other top-level `settings.*` keys (e.g. station-automation state, schedule presets) are intentionally NOT exported — they're per-server runtime state, not user-tuned configuration. Adding a new exportable namespace requires editing `_exportConfig` (envelope build), `_openImportConfig` (diff inputs), and the apply branch in `_showImportDiffModal` (write-back). All three sites need to learn about the new namespace; missing one silently drops it from import.
- **Import write-back uses store-level save() per namespace.** `RouteAssistantSettings.save(block)` and `UsedAircraftPresets.save(block)` each do a top-level `Object.assign` against current+defaults — the imported block REPLACES the persisted top-level keys, but missing nested fields are deep-filled by the next `load()` from `_defaults()`. Don't bypass these stores and write directly to `chrome.storage.local["settings"]` — you'd lose the deep-merge.
- **Rating-observation log is append-only with FIFO cap 50 / 90d age prune.** `RouteAssistantRatingObservationStore.add` reads, prunes, appends, and trims in a single read-modify-write; concurrent writes from a bulk-sync are serialized by the scraper's concurrency=2 + stagger. Don't lift the cap past 50 without re-checking storage budgets across multi-hub users.
- **`_logRatingObservation` lives inside `ors-scraper.js`, NOT `panel.js`.** Bulk syncs call `scrape()` directly (via `bulkScrape` at line 809); logging in the panel would miss them. Don't move it.
- **Per-route per-class α resolution order in `_recomputeOrsSandbox` is `override > derived (≥minObs) > siblingDerived > fleetMedian > global default`.** Don't fork this order without auditing both the model's notes-footer source labels (`ors-model.js:project()` per-class push loop) and the override-expander's "auto: X" hint text — they read the same `alphaSourceByClass` map.
- **Sibling cascade is single-hop.** `siblingDerived` only fires from classes whose source on this route is already exactly `"derived"` (not `siblingDerived`/`fleetMedian`/`global`). Otherwise small biases would amplify across hops. Don't relax this without a stability test.
- **α stored everywhere as positive magnitude in `[0, 50]`.** The `_linregSlope` output is naturally negative; negate ONCE in `_ratingPriceElasticity` before clipping. Don't introduce a `Math.abs` at the consumer — that would mask sign bugs (a non-monotone regression that should reject would silently flip and pass).
- **Read α with `Number.isFinite`, never `||`.** A user override of `0` ("rating doesn't respond to price for this class on this route") is valid and must not fall through to the cascade. Already enforced at `ors-model.js` `_projectClass` and `panel.js` `_recomputeOrsSandbox`; new readers must follow the same pattern. The store's `_clean` also preserves `0` explicitly.
- **Auto-log default `true` is stable across versions.** Flipping default-on ↔ default-off silently changes user-observable behaviour (the observation log accumulates as a side effect of normal scrapes). If a future redesign requires the flip, version it via a one-time migration that preserves the user's last explicit choice.
- **`rating-alpha-store` is a separate sibling of `route-overrides-store` BY DESIGN.** Five other callers of `RouteAssistantRouteOverridesStore.save` exist; the existing `_clean` uses full-replace semantics that would silently erase α fields on every override save that doesn't pass them. Don't merge α back into the override store without first refactoring `_clean` to merge-with-existing semantics across every other caller.
- **Observation schema extensions are append-only.** Adding new fields (e.g. `seasonIndex`, `weekday` for slice 3 confounder controls) is fine; renaming or repurposing existing fields breaks the regression for any obs older than the rename. The store's `_isValidObservation` is the ground-truth admission gate — extend it (don't replace it) when adding new mandatory fields.
- **Confounder filters fire vs the prior SURVIVING obs, not the prior raw obs.** `_ratingPriceElasticity` walks observations sorted by `at`, maintains a `prior` reference that updates only when the current obs is kept. This means a chain of three consecutive comfort-changes drops only one (the one immediately after the change), not all three. Don't reset `prior` on drop — the regression would over-prune.
- **AFP form-driver NEVER POSTs and NEVER calls `submitBtn.click()`.** Slice D's `setOrigin` / `setDestination` / `setDepartureTime` / `setPricePercent` / `setService` / `fill` / `reverse` / `clear` / `dryRun` only mutate input values + dispatch `input`/`change` events; the user submits AS's own green Submit button. `dryRun(leg)` returns the would-be POST body for the "Show what'd post" UI — it does NOT send. Reverse O/D click IS allowed because that's a same-page UI toggle, not a form submission. New write affordances need separate review and a fresh invariant entry.
- **AFP Dashboard applier (`aircraft-flight-plan-dashboard/flight-number-applier.js`) is the SEPARATE module that will eventually POST — it does NOT change the form-driver invariant above.** Tier 1 ships dry-run only (constructor hard-codes `dryRunOnly:true` + `applyEnabled:false`; `apply()` always returns `{status:"dry-run", ...}` regardless of caller args). The applier never runs on the AFP page itself — it's only loaded via the `/app/fleets*` manifest entry, where it operates by proxying a GET against the aircraft's URL, parsing the form context, and (in T2+) POSTing back to the same Wicket form-action URL the GET returned. Tier 2 will introduce the real POST + verify + undo path under the same `dryRunOnly`+`applyEnabled` double-gate as `pricing-applier.js`. The two-gate model is non-negotiable here too — don't add a force-apply path that bypasses both. Audit log for every attempt lives in `aircraftFlightPlan:fnApplyLog` (global cap 200) + `aircraftFlightPlan:fnApplyLog:<server>:<aircraftId>` (per-aircraft cap 20), 5-min dedup on `(fingerprint+status)` mirrors `pricing-apply-log.js`.
- **AFP Dashboard manifest must NOT duplicate scripts already on `/app/fleets*` (extends the existing AFP de-dup invariant).** The `/app/fleets*` block already loads `state-store`, `active-draft-store`, `submit-bridge`, the four `schedule-management/*` files, and the fleet-hub stack. The dashboard expansion appends a 13-script dependency block (route-assistant settings/demand/score/distance/type-specs/overrides/notes/watchlist/wave-overlay/toast-host + flightsfrom data-store + aircraft-type-specs + AFP host/route-candidates/wave-applier) plus the 9 new dashboard files. AFP `host.js` is loaded here intentionally (it publishes `window.AesAfp.bus` for the route-candidates / wave-applier IIFE subscribers) and is safe because (a) it's idempotent — `if (window.AesAfp) return` at line 32, (b) `mount()` bails when `.as-page-aircraft` is missing, and (c) `content_aircraftFlightPlan.js` (which would call `mount()` on `window.load`) is NOT loaded on `/app/fleets*`. **Do not** add `form-driver.js` to this entry — it would render its toolbar into a missing slot and add unnecessary surface area. Verify with set-diff before any further manifest edit touching the dashboard block.
- **AFP slice files use defensive `typeof X !== "undefined"` guards on every cross-module dependency.** `RouteAssistantWatchlistStore`, `RouteAssistantToast`, `AESAircraftTypeSpecs`, `RouteAssistantWaveOverlay`, `SchedulePresets`, `FlightsFromStore`, etc. are all checked before use. This means the module degrades gracefully if a manifest regression drops a dependency — slices render their "waiting" state instead of throwing. Don't refactor to direct imports / hard `import` statements; the slice-isolation contract relies on type-of guards.
- **AFP manifest entry MUST NOT duplicate files already loaded via `/app/fleets*`.** `state-store.js` and the four `schedule-management/*` modules (range-buckets, presets-store, schedule-store, schedule-builder) are loaded by the broader `/app/fleets*` content_scripts entry, which also matches `/app/fleets/aircraft/*/0`. Listing the same file in both entries causes Chrome to inject it twice into the same isolated world; the second top-level `class X` execution throws `Identifier X has already been declared` and the page breaks. Verify with a Python set-diff (`afp_js & fleets_js` must be empty) before any manifest edit touching the AFP block.
- **AFP slice load order: foundation → host → bus subscribers → entry.** `settings-extension.js` (no bus dep) before `host.js` (publishes `window.AesAfp.bus`) before `audit-log.js` / `spec-resolver.js` / `route-candidates.js` / `form-driver.js` / `wave-applier.js` (all `bus.on(...)` at module-level IIFE load) before `content_aircraftFlightPlan.js` (calls `AesAfp.mount()` on `window.load`). Reordering would leave the bus subscribers calling against an undefined `window.AesAfp.bus`. The defensive guard at `audit-log.js:233` softens the failure to a warn but the slice silently disables itself — verify with `window.AesAfp.bus._handlers` in DevTools after mount.
- **AFP storage key prefix is `aircraftFlightPlan:`, namespaced separately from `routeAssistant:`.** Slice F's audit-log + state-store use this prefix to avoid collisions with RA stores. Don't merge into `routeAssistant:auditLog:*` — the AFP module is intended to be runnable independent of the RA panel (Fleet Hub reads `aircraftFlightPlan:state:*` without RA loaded). Cross-module reads use the `aircraftFlightPlan:` prefix as a stable contract.
- **AFP audit-log dual-store atomicity.** Every `add()` writes BOTH the global timeline (`aircraftFlightPlan:auditLog`, cap 200) AND the per-aircraft ring (`aircraftFlightPlan:auditLog:aircraft:<server>:<id>`, cap 50) in a single `chrome.storage.local.set({key1, key2})` call. Don't split the write — a partial failure would leave the two views diverged forever. The AesAfpAuditLog code path inside `add()` is the single source; never write directly to either key from outside.
- **AFP bus is the integration contract; slices DON'T import each other.** Slice D doesn't know about Slice C; it subscribes to `candidate:selected`. Slice F doesn't know about Slice D; it subscribes to `form:filled`. Adding direct module-to-module references (e.g. `AesAfpRouteCandidates.last` in form-driver) breaks the slice-isolation contract that makes partial-load + partial-failure recovery possible. New cross-slice features should emit/subscribe via `window.AesAfp.bus.emit(name, payload)` / `bus.on(name, fn)`.
- **AFP Tier 3 back-test logging is gated on `result.status` ∈ {`verified`, `posted`} — dry-run is excluded.** The back-fill loop (`_runBulkMarketScrape` → `backfillManyFromMarketShares`) doesn't distinguish trigger types; it back-fills any entry within the 8-day window with `observed.share == null`. Dry-run entries logged would get back-filled with a marketShare snapshot whose prices never actually changed — projecting the WRONG share against the SAME observed share, corrupting slice 3c's bias/RMSE metric. The gate at `panel.js renderResult` is the single point that enforces this; future tier3-apply log call sites must mirror it.
- **AFP Tier 3 back-test logging requires `sandboxProjected` to be present.** Only the sandbox-launched apply path (`_buildOrsSandboxScenarioCard` → "Apply this scenario as price…") sets `args.sandboxProjected`; manual row-context-menu applies don't. Skipping the gate would cause `RouteAssistantSandboxBacktestStore._normaliseEntry` to reject the entry (it requires `proj.share != null` OR `proj.paxPerWeek != null`), but the rejection is silent — an upstream check at the panel layer makes the gate explicit and avoids storage round-trips for entries that would just be dropped.
- **Toast cross-page continuity is intentional but bell stays panel-scoped.** `RouteAssistantToast.show()` mounts on every page that loads `toast-host.js` (now including AFP via the manifest expansion); the toast container is a singleton document-level overlay. The N2 notification bell popover is only mounted by `RouteAssistantPanel.init()` on the RA panel pages. AFP toasts flow through `RouteAssistantToast._history` (in-memory, ephemeral, capped at 50 via shift) and become visible via the bell on the user's NEXT visit to the RA panel. Closing the AFP tab clears the history slice (in-memory only). Don't lift the bell to the AFP page — outside-click + Escape teardown is local to the panel mount.

---

## 11 · What's been said (recent context)

- Auto-Pricing autonomy: c+d (one-click + batch-confirm) by default; silent-auto behind a separate explicit setting.
- Pricing UI: separate "Live route data" expander, not folded into Economics drawer.
- Tier order: 1 → 2a → 2b → 3 → 4. No skip-ahead.
- `Wk` column convention: real-airline-style per-day pattern (`2222211`) over "days flown" so multi-daily routes are accurate.
- Tier 2a parser: write against a real HTML sample (no heuristic-and-iterate this time).
- Pricing surfaces: user said "no separate price pages" — meaning prefer the markets page and possibly inventory; defer the global `/action/enterprise/flightsPrices` until needed.
- J slice 2 chosen this session over the (blocked) Auto-Pricing Tier 2a/2b. User will follow up with HTML samples for the Auto-Pricing Tier 2 work in a separate session.
- **This session — Tier 2a + 2b.** User shared HTML samples for `/app/com/markets/JFKATL`, `/app/info/ors` (empty + populated JFK→LAX). User chose: build Tier 2a (markets) first then 2b (ORS); identify "our" connections by flight-number match preferred + carrier-prefix fallback (option C of three); fetch ALL ORS pages (not just page 1); store split per-family for markets (4 keys); two new expanders (Market Analysis + ORS Rank); **MAXIMISE OPTIONS AND CHOICE FILTERINGS** for ORS — surface every rank flavor + every form input as user-tunable settings.
- Plan agent recommended trimming ORS rank flavors to 4 + cutting filter UI in half; this was overridden per the user's explicit "MAXIMISE OPTIONS" direction. Strategy: store maximum data, filter at render time. All 5 rank flavors stored, 8 rank flavors selectable as primary column, all form inputs (payload, window, ground, prefix override, threshold) exposed.
- Letter F chosen as the next unblocked deliverable after J slice 2 — small bridge work, builds on the existing flightsfrom infra, opens up I (ORS sandbox) by knocking out one of its three prereqs.
- Roadmap G chosen for the yield-feedback work (subsumes Auto-Pricing Tier 4). v1 is cumulative-average attribution; delta-mode and auto-snapshot left for slice 2 to keep the diff focused.
- Attribution default = **frequency-weighted**: each tail's lifetime average $/flt × its weekly flights on a route. Distance-mode + equal-split exposed in the dropdown but unverified — frequency is the one to defend.
- Calibrate-from-actuals affordance lives inside the existing per-route override editor (right-click a row), not as a one-click bulk action — high-impact write deserves a confirmation step.

End of handover.
