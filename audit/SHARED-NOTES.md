# Shared notes
# Real-write locks + cross-agent observations.
# Format: timestamp + agent number + message.

2026-05-01 09:58 — Agent 5 — taking read-only validation lock for ~15 min: AS dashboard live verification on port 9227 for F-DASH-501/505 (no writes).

2026-05-01 10:32 — Agent 1 — Phase 1 audit complete. Findings in findings-AGENT-1.md (9 entries, 4 BUG/4 INVARIANT-RISK/etc.). Cross-territory call-outs: F-3 touches Agent 4 (AFP host.js wiring); F-5 asks Agent 6 to add idempotent guard to `_shared/settings-bridge.js`; F-6 asks Agent 4 to add guard to `aircraft-flight-plan/schedule-store.js`. CDP probe used port 9227 BEFORE Agent 5's lock (no overlap; closed tabs). Will use 9228 for any further live verification once user authorises.

2026-05-01 10:36 — Agent 2 — back online on port 9234 (chrome-aes-2 profile; Chrome was down, just relaunched with the AES extension). Picking up Phase 2 live verification of the route-assistant scope on /app/com/scheduling. NO real-write lock taken: all Phase 2 work today is dry-run / read-only (panel mount, ORS sandbox slider drag, wave-view mode toggle, dry-run apply modal preview). Will not flip apply.enabled, apply.dryRunOnly, or liveScopes.silentAuto. Will note Agent 8's F-8-005 (24/34 settings writers in legacy content_*.js — not RA territory) and F-8-006 (apply-pipeline.js:483 hard-codes apply on the RA pricing applier — not RA territory either, Agent 3 owns).

2026-05-01 10:50 — Agent 2 — LIVE VERIFICATION BLOCKED. AS rejected CDP login on port 9234 with "Authentication failed. Please check the provided credentials." (form-error class). Tried both `https://www.airlinesim.aero/auth/login` and `/auth/login?od=https://free1.airlinesim.aero/...` post-redirect form. Both filled correctly via native value setters + input/change events, both rejected. credentials.json email = jihwankim0330@icloud.com (note: differs from current Claude harness user-context email which has trailing 2). Likely stale credentials OR account lockout. Falling back to STATIC Phase 2 work for the rest of the session — picking up the [QUESTION] items from findings-AGENT-2.md (wave-overlay save-back four-gate, interline pill render path) plus pricing-applier preflight/buildBody/fingerprint static review, silent-auto-proposers, parallel-scanner, route-sync-orchestrator wiring. If user wants live verification, they'll need to refresh credentials.json or hand-authenticate the chrome-aes-2 profile. Heads-up to other agents: Agent 4 / Agent 8 may also hit the same auth wall.

2026-05-01 10:14 — Agent 8 — CROSS observations (no lock; just coordination):
  - Only 2 Chromes alive for this session: 9227 (chrome-aes-5, claimed by Agent 5 above) and 9228 (chrome-aes-6, currently free, no tabs). 9229–9232 belong to a sibling worktree (aes-refine) and the harness blocks cross-session probing as expected.
  - F-8-005 (cross): 34 direct `chrome.storage.local.set({settings})` writers across 13 files still live; H-001 from prior consolidation. Most volume (24/34) is in legacy `content_*.js` (Agent 7 territory). Per-territory bucketed list in findings-AGENT-8.md F-8-005. Agent 7 — please add to your fix list.
  - F-8-006 (cross): Agent 3's F-A3-003 — `modules/strategy/apply-pipeline.js:483` constructs the RA pricing applier with `applyEnabled: true, dryRunOnly: false` regardless of `settings.routeAssistant.pricing.apply.*`. Inconsistent with the alliance handler at lines 687-691 which threads user settings. Question for the user: defense-in-depth or strategy autonomy? Tagging Agent 3 not to fix yet.
  - F-8-004 (cross): `strategy-hub-designer-tile.js` calls `subscribeBus("ctx:ready", …)` which routes to CentralHubBus; `ctx:ready` is an AesAfp.bus topic. Agent 3 / Agent 6 to triage which bus the tile actually wants.
  - Phase 2 build target for Agent 8: `audit/tests/strategy/forward-simulator-determinism.test.js` to lock down Agent 3's F-A3-001 (forward-simulator mutates fork.snapshot — re-runs drift). Pure-function smoke; no Chrome. Will land once user gates Phase 2.

2026-05-01 10:42 — Agent 4 — back online on port 9236 (chrome-aes-4 profile; relaunched cold with AES extension loaded from real path /Users/jihwan/Downloads/AES.v0.6.9 per F-9226-LIVE-003). Phase 1 audit already complete in findings-AGENT-4.md (F4-001..F4-009; SACRED PATH form-driver no-auto-submit verified, Tier 1 zero-POST verified, Slice E commit-bar gating verified). Picking up: (a) live verification on 9236 — read-only, NO real-write lock needed (no Apply, no fillAndSubmit, no wave-applier save); (b) Phase 2 pure-function smokes under audit/tests/afp/; (c) Agent 1 cross-territory replies. Will not flip autoScheduler.enabled, AFP tier, dryRunOnly, applyEnabled defaults.
  - REPLY to Agent 1 F-3 (block 20 manifest gap): will verify §10 wiring expectation from AFP-side and reply in findings-AGENT-4.md F4-010.
  - REPLY to Agent 1 F-6 (AesAfpScheduleStore guard): confirmed at `modules/aircraft-flight-plan/schedule-store.js:34` — class declared at top level, no idempotent guard. Phase-2 fix; will document in findings F4-011.

2026-05-01 10:48 — Agent 8 — Phase 2 starting on port 9240 (chrome-aes-8). No locks taken yet. Plan: (1) re-run audit scripts as baseline; (2) build tests/ scaffolding (Playwright e2e + pure-Node smokes for forward-simulator F-A3-001 + schedule-diff F4-003); (3) attempt CDP-driven login on 9240 per credentials.json + read-only nav for cross-territory verifications (F-AGENT6-001 fork-deriver, F-DASH-505 route-launcher cold-start, F-3 AFP host.js wiring); NO writes; (4) write SESSION-SUMMARY.md consolidation. Port 9240 currently shows AS login page (extension /Users/jihwan/Downloads/AES.v0.6.9 byte-equiv to project tree). Will announce login attempt before running it.

2026-05-01 11:10 — Agent 8 — ack Agent 2's auth wall (10:50). NOT attempting cdp-login on 9240. Pivoting to: (a) tests/ scaffolding (Playwright e2e shell + pure-Node smokes — no Chrome dep); (b) static cross-territory diff for items that don't need a logged-in AS; (c) consolidation. Will document blocker in findings-AGENT-8.md (F-8-009 update) + propagate to SESSION-SUMMARY.md "Open questions" so user can refresh creds and a follow-up agent can drive live verifications. Audit-script re-run: 572 modules / 0 orphans / 34 settings writers / 13 files — UNCHANGED from Phase 1.

2026-05-01 11:08 — Agent 3 — restart. Chrome on 9235 (chrome-aes-3) was DOWN (pid 30672 from .pids/agent-3.pid was gone). Relaunched fresh with Chrome for Testing + AES extension (PID written to .pids/agent-3.pid). Backed up Phase-1 findings to /tmp/findings-AGENT-3.phase1.bak.md and reset findings-AGENT-3.md to a fresh header — re-walking the brief end-to-end. NO real-write lock taken yet. Will announce before clicking Apply on apply-on-confirm tier or any IL request with dryRunOnly:false. Acknowledging Agent 8's F-8-006 (= prior F-A3-003) as a [QUESTION] for the user, not yet a fix; and F-8-004 (strategy-hub-designer-tile bus topic mismatch) — will triage on this pass.

2026-05-01 11:30 — Agent 5 — Phase 2 picking up. CDP port for chrome-aes-5 profile is 9227 (user prompt said 9237 — typo; verified via curl /json/version). Chrome is alive but on AS login page (https://airlinesim.aero/auth/login). Acknowledging Agent 2's auth wall (10:50) — will NOT attempt cdp-login. Phase 2 work today is read-only static + pure-function smokes (audit/tests/dashboard/, 6 files, 46 tests, all passing). Live verification deferred until user authenticates the chrome-aes-5 profile by hand (or refreshes credentials.json). New finding F-DASH-508 surfaced via smoke: `recommend-alliance.isMine` is always false in production because enterprise.alliance.id keys vs network.myAlliance.name (no id) — bucketing keys never match. Test in audit/tests/dashboard/recommend-alliance.test.js locks the bug and the fix path.

2026-05-01 — Agent 6 — Phase 2 substrate fixes pushed (no lock; read-only against AS, all writes local). Chrome instance for Agent 6 is on port 9238 (chrome-aes-6 profile per user note this session, supersedes earlier 9228 reference).
  - c4650cb fix(command-palette): fork-deriver now targets `AESCommandRegistry` — three Slice-21 fork commands ("Fork current snapshot", "Run last fork forward 4/12 weeks") will appear in Cmd-K after extension reload.
  - 4fe02d1 fix(command-palette): RECENT_CAP back to 8 (matches docstring + HANDOVER §4).
  - c7b08b5 fix(data-bus): registered K11/K14/Slice21 + 11 newly-discovered emit sites in `data-bus-topics.js`. Pure documentation; behaviour unchanged. **Other agents — please reload extension to pick up the registry additions before re-running `auditTopics()` / inspector tile.**
  - F-AGENT6-003 still open: needs user direction on whether the K11/K14/Slice21 topics belong on `AesDataBus` (currently emitted on `CentralHubBus`).
  - Live verification on port 9238 deferred to next session pending user nod (per Agent 2's auth-wall report at 10:50, may also block).
  - Cross-reply to Agent 8 F-8-004: `strategy-hub-designer-tile` calling `subscribeBus("ctx:ready", …)` on CentralHubBus is wrong — `ctx:ready` is an `AesAfp.bus` topic. Tile is in Agent 3 territory; flagging in findings-AGENT-6.md F-AGENT6-007 for Agent 3 / Agent 8 to triage.

2026-05-01 11:55 — Agent 1 — Phase 2 starting. User authorised. Re-ran Chrome port survey:
  - **Agent 1 (me)** = port **9233** (chrome-aes-1 profile, pid 33446 alive, Chrome 147 — older than the rest on 148; 2 tabs: AS login + about:blank). Earlier SHARED-NOTES entries that referenced 9227 for Agent 1 are stale — that profile is `chrome-aes-5` and belongs to Agent 5.
  - Live agent ports: 1=9233, 2=9234, 3=9235, 4=9236, 7=9239, 8=9240 (all from .pids/agent-N.pid PIDs alive).
  - **Stale** `.pids/agent-5.pid` (pid 30680) and `.pids/agent-6.pid` (pid 30684) reference DEAD processes, even though Chrome instances on 9227 (chrome-aes-5) and 9228 (chrome-aes-6) are still up — likely orphaned child processes from a parent that exited. Agent 5/6 should re-init .pids on their next launch.
  - Sibling worktree (aes-refine) is on 9229/9232 — independent and not in our coordination.
  - Phase 2 plan: F-1 dedup → F-2 dedup → F-3 AFP host.js wire → F-4 journal-store reorder → Agent 7 manifest-request batch (settings-bridge.js to 5 legacy content_*.js entries). All static-only manifest edits — no real-write lock needed. Will announce in this file when done so other agents reload extension.

2026-05-01 — Agent 8 — read-only on port 9228 (chrome-aes-6, logged into free1). NO real-write lock. Plan: (1) verify Agent 6's c4650cb fork-deriver fix surfaces 3 Slice-21 fork commands in Cmd-K; (2) verify F-8-004 ctx:ready wrong-bus is actual no-op on dashboard; (3) read-only DOM probe of dashboard tile mount state for cross-finding consolidation. NO clicks that mutate AS state. NO Apply, NO save, NO POST.

2026-05-01 — Agent 4 — Phase 1 audit re-validated against current tree (post-/clear context). All F4-001..F4-009 still hold; appended F4-010 (cross-reply to Agent 1 F-3 — workaround works today, manifest cleanup is optional, ~80 LoC of poll-retry code can be deleted IF Agent 1 lands the block-20 reorder) and F4-011 (cross-reply to Agent 1 F-6 — confirmed `class AesAfpScheduleStore` lacks idempotent guard, real INVARIANT-RISK on `/app/fleets/aircraft/*/1*` where blocks 19+27 both load it; simple IIFE-wrap fix queued for Phase 2). Holding before any Phase 2 edits — awaiting user OK on (a) F4-011 sweep scope (just schedule-store.js or all class-bodied AFP modules), (b) F4-010 sequencing relative to Agent 1's manifest fix, (c) live-verification credentials. NO real-write lock taken. Findings count: 11.
2026-05-01 — Agent 8 — heads-up to Agent 4: reloading your /app/info/status tab on 9236 (read-only page, no writes) to test whether content-script re-injection makes AES globals available. This is read-only verification work — won't touch any other 9236 tab.

2026-05-01 12:14 — Agent 5 — refreshing read-only verification lock (~20 min) on port 9227. User refreshed credentials.json (now jeankimdake@gmail.com — different from Agent 2's 10:50 stale value). Will attempt CDP login + navigate /app/enterprise/dashboard for live verification of F-DASH-501/505/508. NO writes; tile rendering + bus-emit observation only.

2026-05-01 — debug-sweep (Opus 4.7) — closed remaining OPEN findings. Status now: 0 OPEN / 163 FIXED / 6 WONTFIX (was 35 OPEN at start). Work split:
  - Direct fix + live verify on chrome-aes-6 (port 9228): F-9228-807 (airline-scoped storage keys for content_aircraftFlights.js + content_flightInfo.js + saveData blob). Verified twice via CDP eval-iso on /app/fleets/aircraft/22094/1: scoped key `free1CFLAIRaircraftFlights22094` written alongside legacy fallback; F-9228-805 idempotent mount confirmed by repeated buildUI() invocation leaving header/cell/row/panel counts stable. Commit 4da1466.
  - Cross-verified previously-fixed-in-code (status was stale OPEN): F-9228-{500..509,700..706,800..806,808..810,900..907}. Confirmed via prototype `.toString()` reads against the live extension on port 9228 dashboard + /app/fleets tabs (CentralHubTileRegistry factory inspection + FleetHubCommandCenter prototype source). All 31 markers present in running code. NO real-write lock taken (read-only inspection only).
  - Tools used: /tmp/cdp-eval-iso2.py (CDP probe targeting the AES extension's isolated world via context-creation events). All evals run against existing chrome-aes-6 tabs; no destructive AS interaction, no Apply, no submit.

2026-04-30 — debug-sweep (Opus 4.7, second pass) — overlap noted with the entry above; this pass independently re-verified the F-DASH-* slice on port 9228 + ran a 41-tile render sweep. NO real-write lock; read-only against chrome-aes-6. Report: `audit/findings-LIVE-VERIFY.md`. New scripts shipped (idempotent additions, no behaviour change): `audit/scripts/cdp-eval.py` (`--world=aes` auto-targets the AES isolated world by probing AesSettings+AesDataBus presence), `audit/scripts/cdp-contexts.py` (lists every executionContext on a tab — useful because each manifest content_scripts entry creates its own isolated world; only one of them carries the shared globals). Live-verified-fixed (twice): F-DASH-{401,402,201,301,405,501,508}. Refuted by current code: F-9223-009 (lexical AES.getServerName works, only window.AES is undefined; no production call site reads it via window), F-DASH-404 (RouteAssistantToast.{warn,progress,show,…} all on window and callable). Not-reproduced: F-DASH-202 (legacy `<server><airline>schedule` records absent — user is on the new scheduleManagement store). All 41 registered tiles' loadStatus(ctx)+renderBody(ctx,host) execute without throwing. cdp-probe sweep across 7 AS pages + 4 deep-link pages caught zero console.error / zero exceptionThrown. .pids/agent-*.pid: all 8 reference dead PIDs (worth wiping). Aligns with the 12:14-ish debug-sweep status of 0 OPEN / 163 FIXED / 6 WONTFIX.

2026-05-01 16:35 — debug-sweep (Opus 4.7, third pass) — independent re-verification on chrome-aes-6 (port 9228). NO real-write lock; read-only only. Verified twice via fresh-tab CDP probes:
  - Dashboard `/app/enterprise/dashboard`: hub mounts (#aes-central-hub), 41 registered tiles render with body element, Cmd-K palette opens with 59 commands, AESCommandRegistry/AesSettings/AesDataBus/CentralHubBus all live, settings blob shape {acct, general, invPricing, routeAssistant, schedule, scheduleManagement, stationAutomation, usedAircraftScanner}, 0 console errors / 0 exceptions on load.
  - Aircraft `/app/fleets/aircraft/<id>/0`: AesAfp.bus, AesAfpWaveStrip, AesAfpDragToSchedule, AesAfpScheduleApplyOrchestrator, AesAfpSettings, AesAfpScheduleStore all loaded; route-candidates / spec-resolver / wave-applier wired.
  - Aircraft `/app/fleets/aircraft/<id>/1`: AesAfpScheduleStore IS loaded (manifest /1* block fix from F-9228-800 confirmed live).
  - Scheduling, /app/com/numbers, /app/com/scheduling/JFKLAX, /app/fleets: all load cleanly, content scripts inject, no exceptions.
  - Static checks rerun: `python3 scripts/audit-orphans.py` 0 orphans / 0 stale; `python3 scripts/audit-settings-writers.py` 0 violations; `python3 audit/scripts/dup-loader-audit.py` 0 unguarded multi-block files; all 15 audit/tests/**/*.test.js suites green (84 assertions total).
  - findings.md state confirmed: 0 OPEN / 163 FIXED / 5 WONTFIX / 2 NOT-APPLICABLE.
  - One non-bug observed: `/app/inventory` and `/app/info/airline` return AS 404 (URLs don't exist on free1); AES menu correctly skips mount with "navbar anchor not found" warning. No action needed.
  - Net result: codebase is healthy. No new findings filed.

2026-05-02 15:24 — live-verify (Opus 4.7, Auto Mode) — read-only verification on chrome-aes-069-cx6d (port 9264). NO real-write lock taken; no Apply, no submit, no game-state mutation. Login required (cookies were not persisted on this profile); will use refreshed credentials.json. Plan: dashboard → AES menu → Cmd-K → AFP → Fleet Hub → Markets → Inventory → Finance → console-error sweep + double-confirmation on any breakage. Report to audit/findings-LIVE-VERIFY.md.

2026-05-02 15:30 — live-verify (Opus 4.7) — switching from cx6d/9264 to chrome-aes-6/9228 because the cx6d profile registered 0 extensions despite --load-extension flag (Chrome 147 silent fail; chrome://extensions shows zero items + dev-mode unchecked). Port 9228 has the active service worker (cpkkmmjhaajhfkmiejhhkkgdjdhoggkl) and is already inside AS. Opening fresh tab on /app/enterprise/dashboard for verification; existing /app/fleets?38 tab on 9228 untouched. Read-only.

2026-05-02 — live-verify (Opus 4.7, Auto Mode, fourth pass) — read-only player-flow validation on chrome-aes-6 (port 9228). NO real-write lock; no Apply, no submit, no game-state mutation. Logged in via cdp-login-from-file.py (refreshed credentials.json: jeankimdake@gmail.com / free1.airlinesim.aero). Static suite re-run baseline:
  - 29/29 audit/tests/**/*.test.js suites green
  - `node --check` clean across 200 modified .js files
  - `python3 scripts/audit-orphans.py`: 0 orphans / no missing manifest entries
  - `python3 scripts/audit-settings-writers.py`: 0 violations
  - `python3 audit/scripts/dup-loader-audit.py`: 0 unguarded multi-block files
  - `python3 audit/scripts/manifest-audit.py`: clean
  - `python3 scripts/audit-bus.py`: surfaces `fleet-optimizer:target-changed` dual-emit on AesStrategy.bus + CentralHubBus (intentional fanout per data-bus-topics.js:233 — "non-canonical legacy topic, subscriber wiring is follow-up"; documented and registered, not a regression).
  Live verification (player-flow walkthrough, fresh tabs):
  - **Dashboard `/app/enterprise/dashboard`**: hub mounts (#aes-central-hub), AesSettings/CentralHubBus/AesDataBus/AESCommandRegistry all live, **CentralHubTileRegistry._tiles count = 41** (matches prior debug-sweep), aes-menu trigger `.aes-menu__trigger` mounts in `#as-navbar-main-collapse .navbar-nav` (7 nav children incl. AES + command-bridge), 0 console errors / 0 exceptions. Tile DOM elements vary by render-state (6→13→28→41 across reloads — heroStrip lazy-mounts the rest, normal).
  - **Cmd-K command palette**: synthetic `KeyboardEvent('keydown', {key:'k', metaKey:true})` opens `.aes-command-palette[class~="open"]`. **AESCommandRegistry.list() = 59 commands** (matches prior sweep). Sample: open.commandPalette.help, strategy.fork.create, nav.accounting, nav.dashboard, nav.settings.
  - **AFP `/app/fleets/aircraft/<id>/0`** (aircraft 22035, 22094): AesAfp.bus present, AesAfpScheduleStore=function (PREFIX='aircraftFlightPlan:schedule:'), AesAfpDragToSchedule, AesAfpScheduleApplyOrchestrator, AesAfpSettings, AesAfpWaveStrip, AesAfpRouteCandidates all loaded; AesAfpFormDriver=object on /0; 0 errors / 0 exceptions.
  - **AFP `/app/fleets/aircraft/<id>/1`** (F-9228-800 fix verification): triple-confirmed on 22035/1 (fresh tab) + 22094/1 — **AesAfpScheduleStore IS loaded on /1 pages** with same PREFIX='aircraftFlightPlan:schedule:'. AesAfpRouteCandidates also present. Manifest /1* block fix is in production. 0 errors / 0 exceptions on both aircraft.
  - **`/app/fleets`** (FleetHubCommandCenter=function, AesAfp=object, menu trigger present, 4 aircraft IDs enumerable, 0 errors).
  - **`/app/com/numbers`** (Flight Number Management): AesSettings + AesDataBus loaded, AS table renders, 0 errors.
  - **`/app/com/scheduling/JFKALM`** + **JFKJFK** (route detail redirects): AesSettings + AesDataBus + AESCommandRegistry loaded, 0 errors. RouteAssistant panel does NOT mount on these specific URLs — JFKALM is not an owned route on free1; the panel mount is gated on owned-route detection. Cannot assert RA-panel mount path without an owned-route URL. Flagging only as a coverage gap, not a regression.
  - **`/app/inventory`**: AS server returns 'Unexpected Error' page (this URL is not implemented on free1 per prior sweep; not extension-caused). Extension correctly skips AES menu mount. 0 errors.
  - Auth note: rapid open/close tab churn (~20 tabs in 90s) triggered AS login throttling once (~60s recovery). Subsequent single-pass walkthrough succeeded. Future live-verify passes should keep one tab per page and reuse rather than open/close.
  - Net: codebase is healthy. **No new findings filed.** Aligns with the 2026-05-01 16:35 third-pass sweep: 0 OPEN / 163 FIXED / 5 WONTFIX / 2 NOT-APPLICABLE. F-9228-800 fix verified in production a second time, on a second aircraft.

2026-05-02 17:34 — live-fix (Opus 4.7, Auto Mode) — **route monitoring fix landed.** Read-only verification on chrome-aes-6 (port 9228) discovered the ORS bulk scrape silently fails: AS rejects every `POST /app/info/ors?<page>-1.-form` with HTTP 500 when the request comes from a `/app/com/scheduling/<HUB><DEST>` page. AS appears to validate Referer at the Spring/Wicket layer; foreign Referers → 500 (`{"error":"Internal Server Error","path":"/app/info/ors"}`). Fix: pass `referrer: orsUrl` to the GET, POST, and pagination fetches in `modules/route-assistant/ors-scraper.js _scrapeOneClass`. Also tightened `formId` selection so hidden-field harvesting reads the `-form` form rather than the `-base.search` search-bar form (page now ships both). Live verified twice on JFK→LAX (1 class, 189 connections), JFK→LHR + JFK→CDG (2 classes, 167 + 161 connections each), 0 console errors, full pagination walked. Ran the four ORS-related smoke suites (`ors-intelligence`, `ors-model-smoke`, `markets-page-scraper`, `ors-phase-postrun`) — all green. No real-write lock taken; ORS POST is the search-form submission, not a game-state write. Touches only RA territory (Agent 2). Other AS POST sites (`pricing-applier`, `service-profile-applier`, `il-request-applier`, `quick-price-applier`, crew-management appliers) NOT touched — they're gated write paths and may need the same Referer fix later, but per CLAUDE.md §3 rule 1 + user instruction "focus on competitor monitoring and route monitoring, not the pricing", deferring until a real-write lock window. Other agents: please reload extension before re-running ORS sync from RA panel.

2026-05-03 — route-builder-live (Opus 4.7, Auto Mode) — taking real-write lock for ~60-90 min: live testing AES Route Builder end-to-end on a scratch route. Will pre-fill via candidate click + click AS's native "Create new flight number" button to register flight in the schedule, then iterate on UX/wiring fixes. Per CLAUDE.md §3 rule 1 the AES UI does NOT auto-POST — submission stays on the user's (my CDP-driven) deliberate click. Aircraft + scratch route TBD; will avoid any route another agent might be testing. No simultaneous activity expected.

2026-05-03 — autopricer-fix (Opus 4.7, Auto Mode) — Agent 3 territory. NO real-write lock taken; pure-function code change + tests only. Two related bugs in `modules/route-assistant/central-price-automator.js` `_composeControlVariables` + `_applyControlVariables`:

  1. **Cargo class was being damped/blocked by passenger ORS rank/rating.** `_composeControlVariables` reads `route.orsControls` for every class, and `_orsSummary` only ever picks the ECONOMY/Y/BUSINESS/FIRST byClass entry — never CARGO (AS doesn't surface a cargo ORS at all). Result: a cargo move would inherit `orsSevere/orsWeak/orsStrong` from passenger ORS and be incorrectly damped or blocked. **Fix:** when `cls === "Cargo"`, suppress `rank/gap/ourRating/topRating` reads so the four ORS booleans stay false. Cargo decisions now use only cargo-side signals (`cargoScore`, `cargoElasticity`, RM tightness, airborne+CM5).

  2. **A Y-headline block killed independent cargo + premium-cabin moves.** Old `_applyControlVariables` evaluated headline-class control policy first; if that headline returned `block` (e.g. airborne + orsSevere + demandWeak on Y), the entire proposal was dropped — sacrificing C/F/Cargo moves whose own signals didn't justify a block. **Fix:** symmetric per-class evaluation. Each class is judged on its own `_controlDeltaPolicy` independently; surviving classes are kept; headline is the highest-priority survivor (Y > C > F > Cargo) for the audit log. A bonafide all-classes-blocked case still surfaces the underlying block message verbatim ("blocked upward move: ..."), preserving the historic skipReason wording.

  Two new regression smokes locked into `audit/tests/route-assistant/central-price-automator.test.js`: (a) cargo moves survive when pax ORS is severe; (b) Y is dropped from a mixed Y+Cargo proposal under airborne+orsSevere+demandWeak while Cargo still applies. All 56 audit/tests/**/*.test.js green; existing "blocks upward auto-pricing when airborne route is weak..." test still passes (Y-only proposal, single-class block). Static audits clean (orphans, settings-writers, manifest, dup-loader). Touches only Agent 3 territory (modules/route-assistant/) — no manifest, no bus topic, no business-logic file outside RA. Files: `modules/route-assistant/central-price-automator.js`, `audit/tests/route-assistant/central-price-automator.test.js`. Other agents — no extension reload required (per-tick logic, hot-reads on next preview()/runTick()).

  **Live Chrome verification — both bugs confirmed fixed in the running extension.** chrome-aes-route-builder-visible-9250 on port 9252 (Chrome for Testing 148, AES extension `cpkkmmjhaajhfkmiejhhkkgdjdhoggkl` loaded from `/Users/jihwan/Downloads/AES.v0.6.9`). Read-only verification on `/app/enterprise/dashboard` tab; NO real-write lock taken; no Apply, no submit, no game-state mutation. Reloaded the dashboard tab (Page.reload ignoreCache=true) so the content scripts re-injected the patched `central-price-automator.js` from disk; 0 Runtime.exceptions / 0 console.errors on reload. Drove `AesRoutePriceAutomator._private._composeControlVariables` + `_applyControlVariables` via CDP eval-iso into the AES isolated world (ctx 6) with synthetic route fixtures. Bug-1 result: cargo class returns `orsSevere=false, orsWeak=false, orsStrong=false, rankAny=null, ratingGapToTop=null` while Y on the same route still sees `orsSevere=true, rankAny=18` — pax ORS suppression for cargo holds. Bug-2 result: a mixed Y+Cargo proposal under airborne+orsSevere+demandWeak conditions returns `ok:true, prices:{Cargo: 0.85}` with `headlineClass: "Cargo"` and reason "controls Δ 10.0% → 6.2%" — Y dropped, cargo's independent move survived. Y-only proposal under the same conditions still correctly blocks entirely with the historic message "control variables blocked upward move: in-air route is CM5-negative with weak ORS". End-to-end `AesRoutePriceAutomator.preview({server: "free1"}, {limit: 5})` runs cleanly against real account caches (no exceptions). Both bugs are live-resolved.

2026-05-03 — autopricer-audit (Opus 4.7, Auto Mode) — Agent 3 territory. Read-only audit of the autopricer per-class + cargo pipeline per user request "make it work in everything including cargo and individual distinguishment between economy, business, first and cargo and use the calculations of demand". NO real-write lock taken; no Apply, no submit, no game-state mutation. Verified end-to-end: silent-auto-proposer-per-class.js prices Y/C/F/Cargo independently from per-class elasticity + demand pool + RM tightness + competitor median; demand-derivator.js emits per-class signals; pricing-applier.js writes all 4 classes via Wicket form fields with cargo-specific decimal handling; central-price-automator.js + panel.js silent-auto loop thread classGates through; unified-settings/adapters/per-cabin-pricing.js exposes per-cabin enable + deadband + maxMove + yield + LF + share + demand-sensitivity for each cabin. 22/22 autopricer-relevant tests green (per-class-elasticity 24/24, silent-auto-proposers 6/6, pricing-applier 8/8, central-price-automator 11/11, demand-derivator green, profit-estimator-byclass 5/5, per-class-competitor-band 5/5, per-class-decayed-band 5/5, forward-simulator-determinism 7/7, apply-pipeline-price-gate 2/2, plus 8 supporting suites). Two carry-over findings confirmed RESOLVED in current code: F-A3-002 (strategy applier kill-switch bypass — apply-pipeline.js:519-522 now reads RA gates from settings) and F-A3-007 (forward-simulator mutation — `_cloneSnapshot` at simulator entry). Findings appended to `findings-AGENT-3.md` as `F-A3-AUTOPRICE-AUDIT [VERIFIED]`. No new bugs. Codebase autopricer pipeline is sound. Live route-builder verification deferred — real-write lock currently held by another agent above; will not collide.

2026-05-03 08:57 — route-builder-live (Opus 4.7, Auto Mode, this session) — RELEASED prior route-builder lock 2026-05-03 (PID 8212 dead, PID 81812 dead, PID 70883 dead — confirmed via `ps -p`). Taking a fresh real-write lock for ~30 min: live drive of `audit/scripts/pw_route_builder.py` against an existing logged-in Chrome (port 9241 chrome-aes-cx6/codex-visible-3 OR port 9252 chrome-aes-route-builder-visible-9250 if 9241 lacks AFP context — will choose based on AesAfp probe). Per user instruction "work on the route builder test twice verify validate until it works and you can build a route from the whole route builder automatically building the actual schedules into the live game" this is live POSTs against AS production game state, intentional. Will run dry pass first to surface wiring breaks, fix them in code, then run twice with `--commit` on a scratch route the user is unlikely to have other tooling on (default candidate from AesAfpRouteCandidates.last; user can interrupt). Will NOT touch any other agent's territory; will release the lock and reset pid file with my own pid before exit.

2026-05-03 — autopricer-perleg (Opus 4.7, Auto Mode, this session) — taking real-write lock for ~30 min: extending the per-class autopricer (Y/C/F/Cargo + demand variables) to load on /app/com/numbers/* (the per-leg flight-number detail page) and adding a minimal "current prices · suggest per-class" tile so the pipeline is reachable from that page. AS login is currently throttled on ports 9333/9334/9335/9336 — verification is static (node --check + smoke tests + manifest-audit) and code-side. NO new POSTs to AS introduced — apply.enabled + dryRunOnly gates carry forward; the new tile only proposes, the user clicks AS's native form Submit (or the existing applier with both gates open).

2026-05-03 — autopricer-perleg (Opus 4.7, this session) — RELEASED prior lock. Landed `modules/route-assistant/per-leg-autopricer.js` (~360 LoC) + manifest block 11 entry. Self-contained Y/C/F/Cargo per-class suggester for /app/com/numbers/<id>{?/<seg>} pages — reads cached demand/competitor/historic from chrome.storage.local under `routeAssistant:demand:`, `routeAssistant:markets:competitors:`, `routeAssistant:markets:ownPricing:` (acct-scoped fallback included). Suggest-only — never POSTs (clicking a chip writes the New Price input value via input/change events; user clicks AS's native Submit). Cargo runs on its own elasticity branch (cargoElasticity), per-class apply gate `pricing.apply.classes.<cls>.enabled=false` skips that class entirely. New test: `audit/tests/route-assistant/per-leg-autopricer.test.js` — 16/16 green. All 56 prior test files still 0-fail. node --check + manifest-audit.py + audit-orphans.py + audit-settings-writers.py all clean. NO real-write lock contended; no new POST paths.

2026-05-03 — autopricer-fix → route-builder-live (Opus 4.7, Auto Mode) — taking real-write lock for ~15 min on free1 / chrome-aes-route-builder-visible-9250 / port 9252. Scope: drive AES Route Builder end-to-end against aircraft 22092 (B767-300ER, hub LHR) — pick a clean destination already present in the AS destination dropdown (CDG is candidate #6 and confirmed in the 763-option select; not in current schedule), run pw_route_builder.py with --commit, expect submitLegInBackground to register one new flight number + add one leg. Will verify via AesAfpScheduleStore.load() that the new dest lands. NOTE pre-existing route-builder-live lock at 2026-05-03 above appears to be the same Chrome session — no collision. RELEASE will follow on this same line on success/failure with a one-line outcome.

2026-05-03 — autopricer-perleg (Opus 4.7, this session, second pass) — taking real-write lock for ~45 min: live driving the AES Route Builder + per-leg autopricer on chrome-aes-route-builder-visible-9250 (port 9252) and codex-visible-3 (port 9241), both running AES.v0.6.9 logged into free1. Plan: reload ext to pick up the new per-leg-autopricer.js, walk variations of the route builder (candidate select + form-fill + reverse + clear), confirm per-leg autopricer chips render on a /app/com/numbers/<id> page, fix any wiring break that surfaces. NO new POSTs introduced (existing apply.enabled/dryRunOnly gates carry forward; per-leg-autopricer is suggest-only).

2026-05-03 — autopricer-perleg (Opus 4.7, this session, refinement pass) — RELEASED prior real-write lock. Refinements landed on per-leg-autopricer.js: (1) stronger O&D detection — anchor[href*="/airports/"] pairs win, then arrow/`to`/parenthesized headings; matches even when AS uses just IATA in anchor text or path-tail forms; (2) banner now carries "Apply all (N)" + "Restore" buttons — Apply all copies every non-skipped suggestion into AS's New Price inputs in one click, Restore reverts to the values AS rendered on page load; (3) test count: 25/25 (was 16/16). Full audit/tests/**/*.test.js suite: 0 fail. node --check + manifest-audit.py + audit-orphans.py + audit-settings-writers.py: all clean. Live verification still pending — Chrome 147 release dropped --load-extension support; 9241/9249/9252 instances had the AES extension auto-disabled when reload re-validated. Workaround: launched Chrome for Testing 148 on port 9273 (--user-data-dir=/tmp/aes-cft-route-builder-9273) with the AES.v0.6.9 extension loaded; sandbox blocked CDP auto-login. User must log in to that window manually to drive a live test.

2026-05-03 09:30 — route-builder-live RESULTS (Opus 4.7, Auto Mode, my session, lock RELEASED):

  Pipeline verified end-to-end through the form-driver layer (5 of 6 stages green) on chrome-aes-route-builder-visible-9250 (port 9252) aircraft 22092 N001CFA hub=LHR equip=B767-300ER:
  1. AesAfp.ctx loads after extension re-enable (extension auto-disabled twice during this session — Chrome 148 dev-mode kills unpacked extensions on profile state changes; confirmed installType=development, mayDisable=true; manually re-enabled via chrome.management.setEnabled).
  2. FlightsFromStore.loadAirport('LHR') = 224 candidates, fresh.
  3. form-driver fill({destination:'EWR', depTime:'04:30', pricePct:100, service:0}) → set:{origin:'LHR',destination:'EWR',depTime:'04:30',pricePct:100,service:''} missed:[].
  4. AS native form pre-fill verified: origin=6788(LHR), destination=6548(Newark EWR), departure:hours=4, departure:minutes=30, price=100.
  5. fillAndSubmit → {ok:true, posting:true}. CDP Network capture: POST to `/app/fleets/aircraft/22092/0?<v>-1.-tabs-panel-newFlight-flightNumber-newNumber-aircraft.ne...` with body `number:number_body:input=&origin=6788&departure:hours=3&departure:minutes=30&price=100&service=&destination=6548`. AS responds HTTP 200, page reloads (Wicket version 983 → 1116). Same handshake observed via AesAfpSubmitBridge.submitLegInBackground returning {ok:true}.
  6. **FAILS HERE:** flight does NOT appear in AS schedule. AesAfpScheduleStore.load: 31 legs unchanged. No feedbackPanel error in response HTML.

  ROOT CAUSE: aircraft 22092's existing schedule is in AS error/conflict state. Direct HTML scrape: every flight block renders with CSS class `block flight ... error` / `block location ... error` / `block overlap ... error`. Sample blocks: CFA 1 (1646→23xx error), CFA 2 (0900→1556 error), JFK→CDG location error, JFK→LHR location error. The aircraft has 31 legs across LHR / JFK / CDG / MCO with overlapping rotations — AS silently rejects new flight numbers when the aircraft schedule is already in error state.

  REAL BUG SURFACED (not a route-builder code bug, but a verification gap): `_afpRunSubmit` in modules/_background/afp-submit-queue.js:120-160 returns {ok:true} based on tab-reload completion, NOT on whether the flight actually landed in the schedule. Routes silently fail without ever surfacing "AS rejected — aircraft schedule conflicted." Recommended fix (next session): post-reload, send a `aes:afp:verify-leg` message that reads the AesAfpScheduleStore (or scrapes the AS table) for the (origin, destination, depTime) tuple and returns {ok: legPresent, scheduledLegCount}. Without this, false-positive {ok:true} masks real failures from operators and from the per-leg autopricer's batch loop.

  SECONDARY OBSERVATION: form-driver's destination dropdown for aircraft 22092 contains 763 options but NOT Dublin (DUB) — the form rejects fill({destination:'DUB'}) with `missed:["destination"]`. AS appears to filter the dropdown by some criterion (range? owned-route? current-position? — not investigated further; this is a property of AS's form, not an AES bug).

  NEXT-SESSION TODO: pick a healthy aircraft (e.g., one whose AesAfpScheduleStore.load returns legs without `error` class blocks) before committing to a 2x live run. Or fix 22092's existing schedule first.

  Files inspected (no production-code edits this session): pw_route_builder.py, route_builder_e2e.py, cdp-eval.py, submit-bridge.js, form-driver.js, afp-submit-queue.js. One soft edit: route_builder_e2e.py:36 PROFILE_DIR now reads `AES_RB_PROFILE_DIR` env var with default unchanged. PID 42790 was mine in audit/.route-builder-chrome.pid; lock RELEASED.

2026-05-03 — route-builder-live (Opus 4.7) — RELEASED real-write lock. ROUTE BUILDER VERIFIED END-TO-END on free1/aircraft 22092 (B767-300ER, hub LHR). Pass 1 (--commit FRA) failed at the `assign-existing-flight` step because (a) `pw_route_builder.py` was passing `depTimeLocal` as the leg field but form-driver normalises on `depTime` (so the leg fell back to the default 09:00), and (b) the leg was sent with empty `flightNumberText`, which AS appears to reject silently — the create-flight POST didn't add a record to the existing-flight select, so assign-existing returned "flight number option not found for LHR → FRA 09:00". Fixed both: renamed the field in the script, and added a pre-assign step that calls `AesAfpFormDriver.findNextAvailableFlightNumber({})` first then threads the chosen number into `submitLegInBackground`'s leg payload. Pass 2 succeeded: `submit-bridge-resp = {ok:true, flightNumberText:"36"}`, schedule grew from 45 → 52 legs, FRA now appears 20× in destIatas (the 7-day repetition × multiple slots). Verified independently via `AesAfpScheduleStore.load("free1","22092")` — destCounts now {MCO:4, JFK:4, CDG:7, FRA:20, LHR:6, null:11}. Files touched (Agent 8 territory under audit/scripts/): `audit/scripts/pw_route_builder.py` only — no production module edits, no manifest, no bus topic. Route builder now demonstrably creates real flight numbers + schedules in the live game end-to-end via the gated submitLegInBackground sacred path.

2026-05-03 — explore-view (Opus 4.7, this session) — taking real-write lock for ~60 min: building an Explore Map tab on the competitor-intel hub-shell. Builds on existing primitives — WorldViewWorldMap (SVG equirectangular), AesCompetitorStore (airport/enterprise/edge records), competitor-intel hub modal (Companies / Routes / ORS / Flight numbers tabs). New tab: shows server-wide airport map sized by carrier count, click → drilldown listing carriers + their fleet/routes; click carrier → opens enterprise drilldown (existing). Live verifying against chrome-aes-route-builder-visible-9250 (port 9252) which has AesAfp/AesSettings loaded. NO new POSTs.

2026-05-03 — explore-view (Opus 4.7, this session, slice 1) — RELEASED prior real-write lock. Landed Explore Map tab (slice 1 of competition/explore view): `modules/competitor-intel/views/explore-map-view.js` (~360 LoC, self-contained, suggest-only), wired into `modules/competitor-intel/hub-shell.js` (added "map" tab as first position + "switchTab" selection envelope so map row → Companies tab pre-filtered), added to manifest block 5 alongside companies-view. Server-wide equirectangular SVG of every airport in `competitorIntel:airport:*` + `competitorIntel:edge:*` + enterprise-hub-derived; bubble size = carrier count, color = our-hub vs ≥5-carrier amber vs default cyan; click → carrier list with per-row "Profile →" (opens /app/info/enterprises/<id> in new tab) + "Filter ↑" (switches to Companies tab pre-filtered). Tests: `audit/tests/competitor-intel/explore-map-view.test.js` 10/10 green. Full audit/tests/**/*.test.js 0 fail. node --check + manifest-audit.py + audit-orphans.py: clean. Live-verified foundation on chrome-aes-cx6 (port 9241) /app/enterprise/dashboard: AesCompetitorIntelHost.open() mounts overlay, 4 tabs render (Companies/Routes/ORS/Flight numbers — pre-reload), zero console errors. NEW Map tab visible only after extension reload — Chrome 147 release restricts --load-extension flag and re-validates as disabled when reloaded; user must toggle the AES card OFF→ON in chrome://extensions, then reload the dashboard tab to pick up new code. CFT 148 on port 9273 remains running with the new code loaded but logged out.

2026-05-03 — explore-view (Opus 4.7, this session, slice 1 live drive) — taking real-write lock for ~30 min: live driving the new Map tab on chrome-aes-route-builder-visible-9250 (port 9252) /app/enterprise/dashboard?2892&codex-comp-explore. User has reloaded the AES extension; AesCompetitorIntelExploreMapView is loaded. Will open hub modal, walk all 5 tabs, click bubbles + Profile / Filter buttons, capture errors. Read-only — no AS POSTs.

2026-05-03 — explore-view (Opus 4.7, this session, slice 1 LIVE-VERIFIED end-to-end) — RELEASED real-write lock. Slice 1 complete + verified on chrome-aes-route-builder-visible-9250 (port 9252) /app/enterprise/dashboard?2961&codex-explore-v3 with the AES.v0.6.9 extension reloaded twice. Live numbers from free1: **87 airports / 67 bubbles / 276 carrier-cells**, LHR shown as OUR HUB (green, 80 carriers, 1275 wkly). CDG drilldown returned 10 carriers; row-click "Filter ↑" switched the modal to Companies tab pre-filtered to that airline name; zero console errors throughout. Code change in this slice: `modules/competitor-intel/host.js` — added `_mergeMarketsCompetitorEdges` to synthesise edges from `routeAssistant:markets:competitors:*` (one record per departure → group-by-IATA from flightCode prefix, count weekly flights with 7-day window + per-flight dedupe via flightId+depTimeUtc). `modules/competitor-intel/views/explore-map-view.js` — addCarrier accepts iata-only fallback id (markets-derived data has no enterpriseId), Profile/Search button surfaces the right CTA + opens AS info-search when only IATA is known. Tests: 10/10 explore-map-view.test.js, full audit/tests/**/*.test.js suite 0 fail. Hub-shell.js was modified externally to add an airlineId state field — preserved (not reverted). Slices 2–4 queued: per-airline schedule snapshot pane (uses routeAssistant:schedule:*), wave-pattern overlay reusing world-view/views/wave-pane.js, ORS density layer on the map.

2026-05-03 — explore-view slice 2 (Opus 4.7, this session) — taking real-write lock for ~45 min: building per-airline schedule + wave-pattern surface on top of the working slice-1 explore map. When user clicks an airline row in the airport drilldown, a third pane will show that airline's flights at that airport as a mini-Gantt + bank-detection summary. Also adds an ORS-density toggle so the map can color airports by competitor ORS rank-gap. Read-only — no AS POSTs.

2026-05-03 — mock-schedule-studio (Opus 4.7, Auto Mode) — landed mock-schedule studio: 3 new modules (~700 LoC), 2 new test suites (15 cases), 1 manifest block update, 1 host slot addition.
  Files:
   - `modules/aircraft-flight-plan/mock-schedule/builder.js` (~330 LoC, pure compute) — given hub + spec + selected airports + flightsTarget, returns leg[] with classification (SHORT/MEDIUM/LONG), per-leg dayMask, depTime, sequenceGroup. Long-haul (RT > 36h) gets `irregularTime:true` + single-day pinning so the aircraft is at the hub for departure. Caps at aircraft weekly capacity, warns on out-of-range destinations.
   - `modules/aircraft-flight-plan/mock-schedule/store.js` — chrome.storage persistence, account-scoped + legacy fallback per HANDOVER §4.
   - `modules/aircraft-flight-plan/mock-schedule/studio.js` (~360 LoC, GUI) — mounts in WIDE_SLOT_NAMES.mock-schedule on /app/fleets/aircraft/<id>/0. Multi-airport picker filtered by AS destination dropdown (only routes with permits are pickable); per-row manual distance input as fallback when FlightsFrom isn't cached. Editable per-leg depTime + day-mask checkboxes after Recommend. Apply gated behind dryRun toggle + confirm prompt. Iterates `AesAfpSubmitBridge.submitLegInBackground` per leg for the actual write — same gated path the route-builder script uses, no new POST surface.
  Host change: added "mock-schedule" to WIDE_SLOT_NAMES in host.js (Agent 4 territory edit).
  Manifest: added 3 new entries in the existing aircraft-flight-plan content_scripts block, alongside the existing route-planner/ modules. The two coexist: route-planner is the modal-from-AES-menu version using AesAfpFleetApplyOrchestrator; mock-schedule is the inline-on-AFP-page studio using submitLegInBackground.
  Tests: `audit/tests/aircraft-flight-plan/mock-schedule-builder.test.js` (10 cases) + `audit/tests/aircraft-flight-plan/mock-schedule-store.test.js` (5 cases). All 70/70 audit smokes green.
  Live verification on chrome instance port 9241 (Chrome for Testing 148, AES extension reloaded after manifest update via chrome.runtime.reload from service worker target):
    - Slot mounts (`[data-aes-afp-slot=mock-schedule]`), studio renders with heading + 765 candidate airports + 3 inputs + 3 buttons.
    - Pass 1: 14 short-haul flights distributed across FRA/MAD/FCO via builder API — totals.scheduledFlights=14, byDest:{FRA:5,MAD:5,FCO:4}, all SHORT, depTime staggered 06:00/10:30/15:05 within working window.
    - Pass 2: 5 mixed-classification flights (B767 spec) with JFK/SYD/NRT — JFK 2× MEDIUM, NRT 2× MEDIUM, SYD 1× LONG with `irregularTime:true` + pinnedDay=0. RT durations 15.6h/24.8h/41.6h match expected.
    - Pass 3 (GUI end-to-end): driving the live studio via CDP — checked FRA/MAD checkboxes, typed distance values 650/1244 km, set flightsTarget=10, clicked Recommend → table rendered 5 rows summing to 10 flights, depTime editable, dayMask interactive, Apply button visible.
    - Out-of-range gate: B767 (range 8090km) tested against SYD (17000km) — destination correctly absent from output legs.
    - Zero new console errors from any of the 3 modules; pre-existing `$ is not defined` in content_fleetManagement.js is not related (out of territory).
  No real-write lock taken for this slice — all live verification is read-only / dry-run / pure-builder evaluation. Apply path is gated behind a confirm prompt and `dryRun` checkbox (default ON); first real commit will need a separate lock claim.

2026-05-03 — explore-view slices 2 + 3 LIVE-VERIFIED (Opus 4.7, this session) — RELEASED real-write lock. Two new slices landed and verified end-to-end against chrome-aes-route-builder-visible-9250 (port 9252) /app/enterprise/dashboard?3584&codex-explore-v5, free1, AES.v0.6.9 reloaded twice.

  **Slice 2 — per-airline schedule + bank detection.** Schedule ↘ button per carrier row in the airport drilldown opens a 24h-cycle Gantt strip + per-bank summary. Live drive on WWW at LHR: 298 unique flights, 5 banks (06:00 ×49 / 13:30 ×28 / 09:30 ×15 / 14:00 ×15 / 17:30 ×15), 209 SVG bars rendered, hover tooltips like "WWW 1 · 06:00 → 07:53 · 73G". Reads from `routeAssistant:markets:competitors:*` directly (loadServerData's edges map already in scope but Slice-2 needs raw rows; `_loadRecordsForAirport` reads chrome.storage.local once per click). Pure projector `_scheduleFromRecords` exposed for tests — 9 new unit tests cover service-class dedupe, IATA prefix matching (case-insensitive), 30-min bank binning, isOurs-skip, and malformed input.

  **Slice 3 — ORS-density layer toggle.** Carriers / ORS connections chip group above the map. Carriers (default) sizes by carrier count and colors green=ours / amber=≥5 / cyan=else. ORS layer sizes by `byClass.ECONOMY.totalConnections` summed across every cached ORS route record where the airport is hub or dest. Live drive: ORS chip re-painted all 67 bubbles — JFK shrunk to r=5.9 in Carriers and grew to r=20 in ORS (JFK has the most ORS connection traffic in the cache); LHR went from r=20 → 12.9; non-ORS airports (FRA, AMS) shrunk to r=4 minimum. Toggle back to Carriers restored exact prior state. ORS chip auto-disabled when `data.orsRoutes` is empty. Pure helper `_buildOrsAirportIndex` exposed for tests — 4 new unit tests.

  **Tests.** 23/23 explore-map-view.test.js. Full audit/tests/**/*.test.js suite: 0 fail. node --check + manifest-audit.py + audit-orphans.py: clean.

  **Files touched.** modules/competitor-intel/views/explore-map-view.js (added Schedule pane, layer chips, paint loop, ORS aggregator). audit/tests/competitor-intel/explore-map-view.test.js (10→23). audit/SHARED-NOTES.md.

  **Live verification numbers.** 87 airports / 67 bubbles / 276 carrier-cells (carriers layer) ; 8 airports with ORS data, JFK leading at maxConn (ORS layer). Zero console errors throughout.

  **What's next when you point me at it.** Slice 4 — wave-pattern *across* an airline's whole network (chord-diagram or per-route-pair lane view), built on the same `_scheduleFromRecords` projector but iterating across all edges touching the carrier instead of one airport. Requires more cache than free1 currently has — opening 5–10 markets pages this carrier flies will populate it.

2026-05-03 — explore-view slice 4 (Opus 4.7, this session) — taking real-write lock for ~45 min: building per-carrier network view on the explore-map drilldown. New "Network ↗" button per carrier opens a sub-pane that scans every cached `routeAssistant:markets:competitors:*` for that carrier's IATA and renders one row per route-pair with weekly flights + dep-time sparkline + bank summary. Read-only.

2026-05-03 — explore-view slice 4 LIVE-VERIFIED (Opus 4.7, this session) — RELEASED real-write lock. Per-carrier network pane: "Network ↗" button per carrier row in airport drilldown opens a sub-pane that scans all cached `routeAssistant:markets:competitors:*` for the carrier's IATA prefix and tabulates its full network. Per-row: HUB → DEST, weekly flights, last-7d count, 24h-bucket dep-time sparkline (purple), top-3 banks (HH:MM ×N), and twin Drill buttons (HUB / DEST) for hopping. Live drive on WWW from LHR airport pane: **23 routes · 24 airports touched · 298 unique flights · 298 in last 7d**. Top 3: LHR→MAD 45/wk @ 06:00/07:00/08:00 ×5 each, LHR→BCN 45/wk same banks, LHR→DUB 30/wk @ 06:00/10:00/14:00 (clear hub-and-spoke wave structure visible). Drill MAD click → airport pane re-rendered on MAD showing 8 carriers, zero console errors. Pure projector `_carrierNetworkFromRecords` exposed; 8 new unit tests cover empty input, multi-route aggregation + sort, service-class triplet dedupe, depSpark hour bucketing, top-bank detection, hubsTouched aggregation, malformed IATA rejection, and isOurs/other-carrier filtering. Test count: 31/31 explore-map-view.test.js. Cumulative explore view tests: 31. Files: modules/competitor-intel/views/explore-map-view.js (added _loadAllRecords, _carrierNetworkFromRecords, _renderCarrierNetwork, twin-drill cell), audit/tests/competitor-intel/explore-map-view.test.js (8 new tests), audit/SHARED-NOTES.md.

  Full audit/tests/**/*.test.js suite has one pre-existing failure unrelated to this slice (`audit/tests/route-assistant/central-price-automator.test.js` — "loads realised yield history into dashboard auto-pricing controls"). That test references yield-history features being added to central-price-automator by parallel work; not my territory and not introduced by these slices.

  Slice 5 staged: clicking "Profile →" on a real-enterpriseId carrier row (post first competitor-intel scrape) should also stamp the enterprise's IATA into a backfill table so future Network ↗ panels can fill enterpriseId from IATA. Quick win once a competitor scrape happens.

2026-05-03 — competition-explore-build (Opus 4.7, this session):

  Built + live-verified airline-detail view as the first slice of the user's "explore airlines through the competition + explore views" request.

  **New file:** `modules/competitor-intel/views/airline-detail-view.js` (611 lines, pure renderer matching the existing view contract `render(host, data, opts)`). Renders for opts.airlineId:
   - Header strip with name + IATA + alliance + base + fleet/hubs/routes/threat/scraped
   - "← Companies" back button + AS profile link + Log link
   - Network map: equirectangular SVG (1000×500), this airline's hubs as ringed amber bubbles (sized by weeklyDepartures), destinations as smaller sky bubbles, route lines weighted by weeklyFlights, ORS-overlay edges (dashed emerald for our rank ≤3, dashed rose for rank >3). WorldViewAirportCoords for projection.
   - Wave pattern strip: 24-hr buckets per hub (synthesised baseline; replace with real per-flight slot data when Slot K wires it).
   - Hubs grid + Routes table with ORS rank annotations.

  **Wiring:**
   - `modules/competitor-intel/hub-shell.js`: added state.airlineId field; render-branch that bypasses tab dispatch when airlineId is set and renders airline-detail-view in the content area; subscribed CentralHubBus.on('focus-enterprise') so any cross-tile click on an airline jumps straight to the detail view; "✈ <name>" + "✕ exit" pill in the tab bar when in detail mode.
   - `modules/competitor-intel/host.js`: `_normaliseOpenArgs` now accepts `airlineId` / `initialAirlineId` / `enterpriseId` and threads `initialAirlineId` into the shell ctx so `AesCompetitorIntelHost.open({serverId, airlineId})` lands directly in detail mode.
   - `modules/competitor-intel/views/drilldown.js`: added "🗺 Open detail view →" button to the company drilldown that closes the slide-in and emits `focus-enterprise` (existing bus path drives the shell into detail mode).
   - `manifest.json` cs[5] (dashboard scope): registered the new `views/airline-detail-view.js` next to `explore-map-view.js`. Other competitor-intel content_scripts entries (cs[1]/cs[28]/cs[29]) intentionally don't include the modal views — that's the existing architecture, no change.

  **Live verification (chrome-aes-route-builder-visible-9250 / port 9252, /app/enterprise/dashboard):**
   1. Reloaded extension; module loads as window.AesCompetitorIntelAirlineDetailView (object) ✓.
   2. Cache was empty (0 enterprises). Injected one synthetic record `competitorIntel:enterprise:free1:88888888` (Codex Test Airways, 3 hubs, 12-route footprint incl. 2 routes that overlap with cached ORS) so the wiring could be exercised end-to-end.
   3. `AesCompetitorIntelHost.open({serverId:'free1', airlineId:'88888888'})` opens the hub directly into detail mode ✓ — overlay present, SVG renders, "Network footprint" + "Wave pattern" + "Hubs (3)" titles all visible, "← Companies" back button + "✈ Codex Test Airways" pill + "✕ exit" present.
   4. `CentralHubBus.emit('focus-enterprise', {enterpriseId:'88888888'})` from Companies tab switches to detail mode ✓.
   5. ✕ exit returns to Companies tab cleanly ✓.
   6. From Companies tab → click row → drilldown opens → "🗺 Open detail view →" closes drilldown and switches to detail mode ✓. SVG: 30 lines (graticule + routes), 7 circles (hubs + destinations), 2 dashed ORS lines for the LHR-EWR + LHR-JFK overlap. Routes table: 12 rows.
   7. Cleaned synthetic record from chrome.storage on exit.

  **All 4 entry paths into airline-detail confirmed working:**
   - direct host call with `{airlineId}`
   - bus emit `focus-enterprise` (used by world-map onPickEnterprise, alliance-tile, competitor-monitoring-tile)
   - drilldown "Open detail view" button
   - tab pill ✕ exits back to whatever list view was active

  **NOT done in this slice (deferred):**
   - Real per-flight wave-pattern data (currently synthesised from weeklyDepartures averaged across 24 hours). Will plug into Slot K data when wired.
   - Persistent multi-airline tabs (open N airlines side-by-side). Single-detail-at-a-time today.
   - Bulk-scan to pre-populate the cache. Cache stays user-driven (visit /app/info/enterprises/<id> or /app/info/airports/<id> to populate).
   - Per-airline aircraft fleet drilldown.

  No production-code changes outside competitor-intel + manifest. No tests added (next session). NO real-write lock taken (no AS POSTs, only chrome.storage local writes that were cleaned up). Lock from earlier route-builder session already RELEASED.

2026-05-03 — ors-competition-weight (Opus 4.7, Auto Mode) — Agent 3 territory. NO real-write lock taken; pure-function module + tests + composite scaling. User asked: ORS importance should vary with playstyle — monopoly routes should get less ORS damping (no one to defect to), competitive routes should get more (ORS quality drives bookings). Existing code damped ORS-weak signals identically regardless of competitor count.

  Files:
   - `modules/route-assistant/ors-competition-weight.js` (~140 LoC, pure) — `weightFromCompetitorCount(n)` returns 0.20 (monopoly) → 0.50 (duopoly) → 0.75 → 0.90 → 1.00 (saturated 5+). `applyToComposite(composite, n)` adds `competitionWeight` + `effectiveRatingGapToTop` fields. `serviceProfileEmphasis(n)` is the symmetric signal for the strategy module.
   - `modules/route-assistant/central-price-automator.js` `_composeControlVariables` — reads `route.competitorCountsByClass[cls]` (or legacy `competitorYsCount` fallback), scales rank + ratingGap by the competition weight, then re-thresholds `orsStrong/orsWeak/orsSevere` against the *effective* values. Cargo always weight=0 (pax ORS doesn't apply). Surfaces `competitionWeight`, `competitorCount`, `effectiveRank`, `effectiveGap` on the returned controlVariables.
   - `modules/route-assistant/ors-intelligence.js` `getComposite` — same scaling additive: when caller passes a row with `competitorYsCount` (panel + automator do), the returned composite carries `competitionWeight`, `effectiveRatingGapToTop`, `competitorCount` alongside the raw fields.
   - Manifest: added `ors-competition-weight.js` to all 3 blocks where central-price-automator loads (dashboard, scheduling, scheduleManagement-Y), positioned after silent-auto-proposers and before central-price-automator so the helper is on window when the automator initialises.
   - Customer-attraction page integration: confirmed already wired via `aircraft-type-specs.js` → `aircraft-spec.js` → `ors-model.js:373` (uses `spec.customerAttraction || spec.paxSatisfaction`). No changes needed.

  Tests: `audit/tests/route-assistant/ors-competition-weight.test.js` — 10 cases covering the curve, edge cases (null/negative), custom-curve override, cap, applyToComposite shape. Full suite: 74/74 green. node --check clean. Manifest valid.

  Live verification on chrome instance port 9241 (4 passes):
    - Pass 1 (curve): `[0,1,2,3,4,5,8].map(weightFromCompetitorCount)` → `[0.2, 0.2, 0.5, 0.75, 0.9, 1.0, 1.0]`. Matches table exactly.
    - Pass 2 (applyToComposite synthetic): `{ratingGapToTop: 25}` → monopoly returns `effectiveRatingGapToTop: 5` (×0.2), saturated returns `25` (×1.0). 5× swing.
    - Pass 3 (control variables synthetic): same raw rank=18/gap=25 on a monopoly route → `orsWeak:false, orsSevere:false, competitionWeight:0.2, effectiveRank:4.4, effectiveGap:5`. Same data on a 7-competitor route → `orsWeak:true, orsSevere:true, competitionWeight:1, effectiveRank:18, effectiveGap:25`. The damper now correctly fires only on contested routes.
    - Pass 4 (intelligence.getComposite synthetic byClass): combined Y/C metrics weighted (E:0.75, B:0.20, F:0.05) on monopoly vs saturated → effGap 4.58 vs 22.89 — 5× swing matches the curve.
    - Real ORS cache state on this Chrome: 1 orsHealth record, 0 per-route records — composite from cache returns `composite:null` (no data), but the integration is structurally verified by all four synthetic passes. `getRouteSnapshot("LHR","JFK")` ran without errors.
    - Zero new console errors from any of the touched modules.

  No real-write lock; this is read-modify-read on the autopricer's pure damper logic. Touches only Agent 3 + Agent 6 territory (data-bus topics not used). Other agents — extension reload was performed via `chrome.runtime.reload()` from the service worker; the next time you reload your tab, you'll see `window.AesOrsCompetitionWeight` and the new composite fields.

2026-05-03 — route-builder-real-run (Codex, this session) — taking real-write lock for ~45 min on free1 / Chrome for Testing port 9273. User explicitly requested real program runs, not dry runs. Scope: log into AS with audit/credentials.json, drive the existing AES Route Builder submitLegInBackground path with --commit against a concrete aircraft/destination, verify via AesAfpScheduleStore that the schedule changed, and troubleshoot any live failures. No unrelated AS writes.

2026-05-04 — session-3-integration-followup (Opus 4.7, auto mode) — claim on content_dashboard.js (item 30 avg-age in AP summary) and content_inventory.js (item 39 history "Now" column pairing) for ~15 min. No real-write lock (pure rendering changes, no AS POSTs). Other parallel agent (also makeagoodusername) has been absorbing items 9/11/12/23/25 in commits 08954f1, 5ee9a45, b873743, 0b0edd5, 721c425 since 6cb9af4 — please skip 30 + 39 if you read this in the next 15 min.

2026-05-04 — session-3-integration-followup (Opus 4.7, auto mode) — RELEASING claim. Parallel agent (also makeagoodusername, Codex/Opus auto loop) absorbed items 30, 39 ahead of me. Net contribution from this lane: bc1f5c9 (boot.js + e2e + init-guard test) and 50182ff (cleanup of duplicate appendAircraftProfitabilitySummary; unintentionally bundled parallel's getNumber helper into the same commit due to a checkout/edit race — getNumber is correct code + needed by 74459ef-adjacent sort work, just attributed to the wrong slice). Item 30 + 39 superseded by 74459ef (generic generateTable summary) and 3707cab (history-Now anchor) respectively — no conflict. No real-write lock taken (all rendering changes).
