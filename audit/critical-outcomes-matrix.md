# Critical outcomes matrix

Source-truth checkpoint for this stabilization pass. Static proofs were rerun from the current tree. Read-only live Chrome proofs for dashboard boot, command palette dispatch, AFP page globals, scheduling page globals, and fleets page globals were completed via CDP port 9228; Playwright, AFP dry-run apply, wave-overlay save, notification click-routing, and real AS write paths remain unexercised.

Note: the new settings-bridge smoke lives at `audit/settings-bridge.test.js` instead of `audit/tests/substrate/` because `audit/tests/` is root-owned in this checkout.

| Outcome | Entry | Expected | Observed failure | Primary files | Static proof | Live proof | Status | Notes |
|---|---|---|---|---|---|---|---|---|
| Settings persistence | `AesSettings` bridge + legacy/settings writers | Sibling areas and `settings.acct.<id>.<area>` survive concurrent saves; no direct whole-blob writers remain | Source contradicted audit notes: `modules/_shared/settings-bridge.js` had no queue and `scripts/audit-settings-writers.py` found 34 direct `set({settings: ...})` calls | `modules/_shared/settings-bridge.js`, `modules/route-assistant/settings-store.js`, `modules/aircraft-flight-plan/settings-extension.js`, `content_dashboard.js` | `python3 scripts/audit-settings-writers.py` → 0 violations; `node audit/settings-bridge.test.js` → 5 passed | Storage round-trips were not exercised live; dashboard/AFP/scheduling pages did expose `AesSettings` through the extension isolated world | PASS | Bridge now exports idempotently on `globalThis`, shares one queued write path, and supports scoped reads/writes |
| Strategy simulator | Forward simulator determinism | Re-running the same fork yields identical baselines and does not mutate `fork.snapshot` | `audit/tests/strategy/forward-simulator-determinism.test.js` failed 3/7 because `_decay()` mutated `fork.snapshot` in place | `modules/strategy/forward-simulator.js`, `audit/tests/strategy/forward-simulator-determinism.test.js` | `node audit/tests/strategy/forward-simulator-determinism.test.js` → 7 passed | Not required beyond static deterministic smoke | PASS | Working snapshot is cloned once per run via `structuredClone` with JSON fallback |
| AFP diff tolerance | Schedule diff honors configured tolerance | `compare(cur, pro, {toleranceMin})` keeps 30-minute deltas when tolerance allows, without breaking flightId precedence or locked-leg routing | `audit/tests/afp/schedule-diff.test.js` failed 1/15 because `compare()` ignored `opts.toleranceMin` | `modules/aircraft-flight-plan/auto-scheduler/schedule-diff.js`, `modules/aircraft-flight-plan/auto-scheduler/preview-panel.js`, `modules/schedule-management/schedule-panel.js`, `modules/strategy/diff-plan.js` | `node audit/tests/afp/schedule-diff.test.js` → 15 passed | Deferred until AFP live dry-run can be replayed against AS pages | PASS | Callers now thread diff options through the compare path |
| Extension boot | Dashboard mount + service worker boot | Extension loads once, duplicate `settings-bridge.js` injection does not redeclare, and background defaults seeding uses the shared bridge | Duplicate bridge injection in `manifest.json` plus missing idempotent guard made the bridge unsafe to reload | `manifest.json`, `modules/_shared/settings-bridge.js`, `modules/_background/legacy-defaults.js`, `background.js` | `node audit/settings-bridge.test.js` duplicate-eval check passed; full `node --check` sweep passed | CDP 9228 dashboard: `.aes-menu__trigger` visible, exactly one `#aes-central-hub`, 3 tiles, AES isolated world present, 0 exceptions | PASS | Playwright wrapper still not run because `@playwright/test` is not installed |
| Dashboard command surfaces | Cmd/Ctrl-K and hub tiles | Palette opens, dashboard tiles mount once, and navigation surfaces remain stable after the settings migration | No current repo-driven failure after persistence cleanup | `tests/integration/cmd-k-dispatch.spec.ts`, dashboard content/tiles | Static parse checks passed on migrated dashboard/settings files; persistence audit is clean | CDP 9228 dashboard: 59 registry commands; fork commands present; palette opens/focuses input; query `go to accounting` returns `nav.accounting`; dispatch reaches `/app/finance/accounting`; 0 exceptions | PASS | Direct CDP proof covers the read-only flow; Playwright wrapper still not run |
| Route Assistant / Schedule | Dashboard/overlay schedule settings round-trip | Route/schedule settings persist without dropping sibling blocks and overlay diff uses AFP tolerance | Legacy whole-blob writes in dashboard, RA settings, schedule overlay, and station automation could clobber unrelated settings | `content_dashboard.js`, `modules/route-assistant/settings-store.js`, `modules/schedule-management/open-stations-modal.js`, `modules/schedule-management/schedule-panel.js` | `python3 scripts/audit-settings-writers.py` → 0 violations; full `node --check` sweep passed | CDP 9228 scheduling page: AES menu and isolated world present; `AesSettings`, `AESTokens`, and `AesAfpScheduleStore` exposed; 0 exceptions | DEFERRED | Static proof and page boot proof are complete; live storage round-trip still pending |
| AFP dry-run safety | Apply-all emits zero AS POSTs in dry-run mode | No non-GET AirlineSim requests during dry-run apply | Live-only validation was not exercised in this pass | `tests/integration/afp-batch-dryrun.spec.ts`, AFP auto-scheduler stack | Static AFP diff smoke is green; no static path can prove zero POST behavior | CDP 9228 AFP page boot proof only: AFP globals exposed and 0 exceptions; dry-run Apply-all was not clicked | DEFERRED | Requires an explicit zero-POST dry-run pass before marking live-safe |
